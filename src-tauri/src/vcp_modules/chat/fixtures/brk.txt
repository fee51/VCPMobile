好！分块功能终极收尾验收！覆盖所有极端边界——

---

短。

<!--brk-->

也短。

<!--brk-->

但连在一起。三个超短气泡，测试频繁创建/销毁 DOM 节点会不会掉帧。

<!--brk-->

然后突然来一个**超长气泡**，验证在经历了两次快速 DOM 挂载/卸载后，引擎的状态寄存器有没有被搞乱。

这个气泡故意写得足够长，长到在移动端屏幕上需要滚动才能看完。目的很简单——如果前两个短气泡的渲染生命周期没有被正确终结（例如 Vue 的 Transition 钩子还在 pending 状态），那么这个长气泡在流式追加时就会遇到 `isTransitioning` 锁未释放的问题，导致整个气泡的内容被冻结在初始状态，或者出现打字机动画与实际文本不同步的诡异现象。

这段话还会顺便测试一下行内格式：**加粗**、*斜体*、`code`、~~删除~~、$E=mc^2$，以及一张表情包 <img src="http://100.108.55.84:6005/pw=Images_aBcDeFgHiJk/images/Nova表情包/认真记笔记.png" width="80">。在长气泡中，这些行内元素的渲染不应被分块逻辑干扰。

<!--brk-->

<div style="background: rgba(59,130,246,0.08); border-left: 3px solid #3b82f6; padding: 0.75rem;">

分块标记紧跟在 `<div>` 开标签后面。

如果分块器的 HTML 扫描器在遇到 `<div` 时进入了"标签读取模式"，可能会把紧接着的 `<!--brk-->` 误吞进 div 的属性解析中，导致分块失效。

这个蓝色边框内的内容应该完整显示在一个气泡里。

</div>

<!--brk-->

分块标记紧跟在 `</div>` 闭标签后面。

反过来的情况：如果 `</div>` 的闭合扫描过于贪婪，可能会连带吃掉后面的 `<!--brk-->`。

<!--brk-->

### 分块后紧跟标题

标题应该是新气泡的第一个元素。如果分块逻辑在切割后没有正确重置 Markdown 的块级解析状态，这个 `###` 可能不会被识别为标题。

<!--brk-->

> 引用块作为气泡的唯一内容。
> 
> 如果分块切割后引用块的 `>` 前缀解析正确，这段话应该带左竖线。

<!--brk-->

```python
# 代码块作为气泡开头
def test():
    pass
```

代码块后面紧跟分块。

<!--brk-->

代码块作为气泡结尾，前面是正文：

```python
print("end of bubble")
```

<!--brk-->

$$
\int_0^{\infty} e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$

数学公式作为气泡唯一内容。

<!--brk-->

公式前面有正文，公式后面是分块：

香农熵 $H = -\sum p_i \log p_i$ 以及完整展开式：

$$
H(X) = -\sum_{i=1}^{n} P(x_i) \log_2 P(x_i)
$$

<!--brk-->

<!--brk-->

连续两个分块标记！中间没有任何内容。

如果引擎没有对"空分块"做过滤，这里应该会产生一个空气泡。正确的行为是跳过空内容，不生成 DOM 节点。

<!--brk-->

最后一块前面有一个空分块被跳过了，现在这段文字应该直接显示在 Mermaid 后面，没有空白气泡隔开。

<!--brk-->

```mermaid
graph TD
    A[第一个气泡] --> B[第二个气泡]
    B --> C[第三个气泡]
    C --> D[空气泡?]
    D --> E[跳过!]
    E --> F[最终气泡]
```

Mermaid 作为气泡唯一内容。图表应该完整挂载在这个独立气泡里。

<!--brk-->

终极混排收尾气泡：

这段同时包含 **加粗**、*斜体*、~~删除~~、`code`、$x^2$、<img src="http://100.108.55.84:6005/pw=Images_aBcDeFgHiJk/images/Nova表情包/比心心.png" width="80"> 以及引号 **"测试"**。

```python
print("code in final bubble")
```

<div style="border: 1px solid #22c55e; padding: 8px; margin: 4px 0;">
div 也在最后这个气泡里。
</div>

如果这个气泡的所有元素都正确渲染、没有互相干扰、没有格式泄漏，那 VCP Mobile 1.1.1 的分块功能就正式宣告毕业了！

<!--brk-->

🎉 全部气泡发射完毕！

---

**主人，最后一轮扫描清单：**

1. 前三个**超短气泡**有没有掉帧或粘连？
2. **超长气泡**里的行内格式和表情包有没有正常？
3. `<div>` 紧贴 `<!--brk-->` 的两种方向有没有吞标记？
4. 分块后紧跟的**标题/引用/代码块/公式**有没有被正确识别为块级元素？
5. **连续两个分块标记**之间有没有产生空白气泡？
6. **Mermaid 独占气泡**有没有正确挂载？
7. **终极混排收尾气泡**里所有元素有没有互相打架？
8. 总共应该有大约 **16 个可见气泡**（去掉空分块），主人数数对不对？

如果全过，分块功能正式毕业！🎓