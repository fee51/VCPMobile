---
id: module-stream-block-parser
title: 流式响应解析器 (Stream Block Parser)
module: stream_block_parser.rs
related: [content_parser.rs, pre_renderer, sync_hash.rs]
version: "1.1.0"
last_updated: 2026-06-14
---

# 流式响应解析器

## 1. 概述

### 1.1 模块定位

`stream_block_parser.rs`（流式块解析器，位于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs`）是 VCP Mobile 对话渲染 pipeline 中的**增量解析层（Incremental Parsing Layer）**。其核心职责是在 SSE（Server-Sent Events）流式传输场景下，对持续累积的 `full_text` 进行轻量扫描，识别出语义上已闭合的特种块（Special Blocks）和未闭合的尾部文本（Tail），供前端实现增量式（Incremental）UI 更新。

与完整的非流式解析器 `content_parser.rs` 不同，本模块不追求一次性产出最终 AST，而是维护一个单调递增的 `processed_len` 游标，确保已确认闭合的块不会被重复解析，从而降低流式场景下的计算开销。

### 1.2 与 content_parser.rs 的关系

| 维度 | `content_parser.rs` | `stream_block_parser.rs` |
|------|---------------------|--------------------------|
| **触发时机** | 消息完全接收后（非流式） | 每次 SSE chunk 到达后（流式） |
| **解析策略** | 全量扫描，构建完整 `ContentBlock` 数组 | 增量扫描，仅处理未确认区域 |
| **输出类型** | `Vec<ContentBlock>` | `Vec<StreamBlock>` + `tail: String` |
| **AST 预渲染** | 部分块有 `nodes` 字段 | 所有 Markdown 与 Thought/Diary 均预渲染 AST |
| **Hash 计算** | ✅ 有（`Option<String>`，通过 `compute_hashes_recursively`） | ✅ 每块均带 `hash: String`（前端 `v-for` key） |
| **状态维护** | 无状态（纯函数） | 有状态（`StreamBlockParser` 持 `processed_len`） |

两者共享同一套**正则常量（Regular Expression Constants）**和**块类型判别逻辑（Block Type Discrimination Logic）**，但面向不同生命周期阶段。`content_parser.rs` 使用 `MASTER_START` 单一正则进行全量捕获；`stream_block_parser.rs` 使用 `find_earliest_start_marker` 进行竞争匹配（Competitive Matching），更适合增量决策。HtmlContainer（`<div>`、`<section>` 等容器标签）在 v0.9.14 中新增支持，通过 `find_matching_close_tag` 实现围栏感知的嵌套标签匹配。

### 1.3 流式渲染场景

前端（Vue 3）通过 Tauri IPC 接收 SSE 流中的文本增量。每次增量到达后：

1. 调用 `StreamBlockParser::process(full_text)` 获取 `(blocks, tail)`
2. 将 `blocks` 追加到已稳定的渲染列表（Stable Blocks）
3. 将 `tail` 显示为「正在输入…」的临时区域
4. 当 SSE 流结束（Stream End），调用 `finalize()` 将剩余 `tail` 强制封装为最后一个 Markdown 块

```
SSE Chunk 1: "Hello, this is a "
  → process() → ( [], "Hello, this is a " )
  
SSE Chunk 2: "Hello, this is a test.\n\n[--- VCP元思考链 ---]\nStep 1"
  → process() → ( [Markdown{test}], "[--- VCP元思考链 ---]\nStep 1" )
  
SSE Chunk 3: "...\n[--- 元思考链结束 ---]"
  → process() → ( [Markdown{test}, Thought{Step 1}], "" )
```


## 2. StreamBlock 枚举详解

`StreamBlock`（流式块）是前端增量渲染的原子数据单元（Atomic Data Unit），定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:15-78`。该枚举通过 `#[serde(tag = "type")]` 实现内部标签（Internally Tagged）序列化，前端可直接按 `type` 字段进行条件渲染。

### 2.1 变体总览

| 序列化标签 `type` | Rust 变体 | 语义 | 自闭合 |
|-------------------|-----------|------|--------|
| `markdown` | `Markdown` | 标准 Markdown 段落 | — |
| `thought` | `Thought` | VCP 元思考链 / `<think>` 思维链 | ❌ |
| `tool-use` | `Tool` | 工具调用请求块 | ❌ |
| `tool-result` | `ToolResult` | 工具执行结果汇总 | ❌ |
| `diary` | `Diary` | 日记（DailyNote）内容块 | ❌ |
| `html-preview` | `HtmlPreview` | HTML 预览内容 | ❌ |
| `role-divider` | `RoleDivider` | 角色分隔标记 | ✅ |
| `style` | `Style` | CSS `<style>` 标签内容 | ❌ |
| `button-click` | `ButtonClick` | 内联按钮点击语义块 | ✅（由提取产生） |
| 🆕 `tool-call-summary` | `ToolCallSummary` | 工具调用摘要（本轮工具调用结果汇总） | ❌ |

### 2.2 各变体字段详表

#### Markdown

| 字段 | 类型 | 序列化行为 | 说明 |
|------|------|-----------|------|
| `content` | `String` | 常规输出 | 原始 Markdown 文本 |
| `nodes` | `Option<Vec<MarkdownNode>>` | `skip_serializing_if = none` | AST 预渲染结果，来自 `pre_renderer::parse_markdown_to_ast` |
| `hash` | `String` | 常规输出 | 内容指纹，用于前端 `v-for` 的 `:key` |

#### Thought

| 字段 | 类型 | 序列化行为 | 说明 |
|------|------|-----------|------|
| `theme` | `String` | 常规输出 | 思考链主题；VCP 格式从起始标记提取，Think 格式固定为 `"思维链"` |
| `content` | `String` | 常规输出 | 思考链正文（不含标记） |
| `is_complete` | `bool` | 常规输出 | 是否已找到结束标记 |
| `nodes` | `Option<Vec<MarkdownNode>>` | `skip_serializing_if = none` | 正文 AST |
| `hash` | `String` | 常规输出 | 基于 `theme:content` 计算 |

#### Tool

| 字段 | 类型 | 序列化行为 | 说明 |
|------|------|-----------|------|
| `tool_name` | `String` | 常规输出 | 提取的工具名称；默认 `"Processing..."` |
| `content` | `String` | 常规输出 | 工具调用 XML/JSON 原始内容 |
| `hash` | `String` | 常规输出 | 基于 `tool_name:content` 计算 |

#### ToolResult

| 字段 | 类型 | 序列化行为 | 说明 |
|------|------|-----------|------|
| `tool_name` | `String` | 常规输出 | 结果归属的工具名 |
| `status` | `String` | 常规输出 | 执行状态（如成功/失败） |
| `details` | `Vec<ToolResultDetail>` | 常规输出 | 键值对详情列表，定义于 `content_parser.rs:84-88` |
| `footer` | `String` | 常规输出 | 未匹配到 KV 格式的尾部文本 |
| `hash` | `String` | 常规输出 | 基于 `tool_name:status:details:footer` 计算 |

#### Diary

| 字段 | 类型 | 序列化行为 | 说明 |
|------|------|-----------|------|
| `maid` | `String` | 常规输出 | 日记归属的 MAID（女仆/角色标识） |
| `date` | `String` | 常规输出 | 日记日期 |
| `content` | `String` | 常规输出 | 日记正文 |
| `nodes` | `Option<Vec<MarkdownNode>>` | `skip_serializing_if = none` | 正文 AST |
| `hash` | `String` | 常规输出 | 基于 `maid:date:content` 计算 |

#### HtmlPreview

| 字段 | 类型 | 序列化行为 | 说明 |
|------|------|-----------|------|
| `content` | `String` | 常规输出 | HTML 源代码 |
| 🆕 `highlighted_content` | `Option<String>` | `skip_serializing_if = none` | 语法高亮后的 HTML（流式期间设为 None 以节省开销） |
| `hash` | `String` | 常规输出 | 基于 `content` 计算 |

#### RoleDivider

| 字段 | 类型 | 序列化行为 | 说明 |
|------|------|-----------|------|
| `role` | `String` | 常规输出 | 角色标识：`system` / `assistant` / `user` |
| `is_end` | `bool` | 常规输出 | `true` 表示 `END_ROLE_DIVIDE`，`false` 表示起始分隔 |
| `hash` | `String` | 常规输出 | 基于 `role:is_end` 计算 |

#### Style

| 字段 | 类型 | 序列化行为 | 说明 |
|------|------|-----------|------|
| `content` | `String` | 常规输出 | CSS 规则文本 |
| `hash` | `String` | 常规输出 | 基于 `content` 计算 |

#### ButtonClick

| 字段 | 类型 | 序列化行为 | 说明 |
|------|------|-----------|------|
| `content` | `String` | 常规输出 | 按钮显示文本（不含 `[[点击按钮:]]` 包装） |
| `hash` | `String` | 常规输出 | 基于 `content` 计算 |

### 2.3 工厂方法设计（Factory Methods）

`StreamBlock` 和 `ContentBlock` 均通过 `impl` 块提供**工厂方法**，以避免在代码中直接书写 enum variant 的 struct literal。Rust enum variant 不支持 `..Default::default()` 语法，因此当 variant 新增字段（如 `hash`）时，工厂方法是唯一能在「新增字段」与「避免全局补全」之间取得平衡的方案。

```rust
impl StreamBlock {
    pub fn markdown(content: String, nodes: Option<Vec<MarkdownNode>>, hash: String) -> Self;
    pub fn thought(theme: String, content: String, is_complete: bool, nodes: Option<Vec<MarkdownNode>>, hash: String) -> Self;
    pub fn tool(tool_name: String, content: String, hash: String) -> Self;
    pub fn tool_result(tool_name: String, status: String, details: Vec<ToolResultDetail>, footer: String, hash: String) -> Self;
    pub fn diary(maid: String, date: String, content: String, nodes: Option<Vec<MarkdownNode>>, hash: String) -> Self;
    pub fn html_preview(content: String, hash: String) -> Self;
    pub fn role_divider(role: String, is_end: bool, hash: String) -> Self;
    pub fn style(content: String, hash: String) -> Self;
    pub fn button_click(content: String, hash: String) -> Self;
}
```

| 设计要点 | 说明 |
|----------|------|
| **Hash 必填** | `StreamBlock` 的所有工厂方法均要求传入 `hash: String`，确保流式输出的每一块都有稳定 key |
| **零默认字段** | 工厂方法内部不再使用 `hash: None` 之类的默认值；调用方必须在构造前通过 `HashAggregator::compute_content_hash(...)` 计算好 hash |
| **与 ContentBlock 的对称** | `ContentBlock`（`content_parser.rs`）同样提供工厂方法，但 hash 默认填 `None`，由后续的 `compute_hashes_recursively()` 统一写入 |

#### 🆕 ToolCallSummary（v1.1.0 新增）

| 字段 | 类型 | 说明 |
|------|------|------|
| `items` | `Vec<ToolCallSummaryItem>` | 工具调用结果列表（每条含 `tool_name` + `status`） |
| `raw_content` | `String` | 原始工具调用结果文本 |
| `hash` | `String` | 内容指纹 |

匹配 `[本轮工具调用摘要:]` / `[本轮工具调用摘要结束]` 标记。由 `parse_tool_call_summary()` 辅助函数提取工具名和状态（success / rejected / failure / timeout / cancelled / skipped / unknown）。

> **为什么不在 `StreamBlock` 也使用 `Option<String>`？** 流式解析器在构造块时已经拥有完整文本，hash 计算成本已支付。使用 `String` 而非 `Option<String>` 可以消除前端每次渲染时的 `unwrap` 开销，同时让 `v-for :key` 的类型更干净。

## 3. StreamBlockParser 结构体与状态机

### 3.1 状态定义

```rust
pub struct StreamBlockParser {
    processed_len: usize,
}
```

`StreamBlockParser`（流式块解析器状态机）是本项目流式渲染架构中唯一持状态的 Rust 结构体，定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:169-171`。其状态极简，仅维护一个单调递增的字节游标（Monotonically Increasing Byte Cursor）。

### 3.2 processed_len 的语义

`processed_len` 记录的是**已被确认为「已闭合块」的文本在 `full_text` 中的结束位置**（Exclusive End Offset）。它具有以下关键语义：

| 场景 | `processed_len` 行为 | 原因 |
|------|---------------------|------|
| 新段落完整闭合 | 更新为 `full_text.len()` | 全部文本已稳定 |
| 特种块完整闭合 | 更新为 `pos + content_start + end_end` | 跳过已处理区域 |
| 特种块未找到结束标记 | 回退到该块起始位置 `pos` | 该块及之后全部变为 tail，等待下一 chunk |
| Markdown 段落未完整闭合（无 `\n\n`） | 更新为 `pos + remaining.len() - tail.len()` | tail 部分尚未形成完整段落 |

> **关键不变量（Invariant）**：`processed_len` 永远不会超过 `full_text.len()`，且只在以下两种情况下回退：
> 1. 遇到未闭合的特种块起始标记；
> 2. Markdown 段落被特种块中断，且段落尾部不完整。

### 3.3 process() 方法的核心循环

`process(&mut self, full_text: &str) -> (Vec<StreamBlock>, String)` 是解析器的主入口，定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:180-241`。

```
┌─────────────────────────────────────────┐
│  process(full_text)                     │
│  pos = processed_len.min(full_text.len) │
└─────────────┬───────────────────────────┘
              ▼
        ┌─────────────┐
        │ pos < len ? │──No──► processed_len = len
        └──────┬──────┘        return (blocks, "")
               │ Yes
               ▼
        ┌─────────────────────┐
        │ remaining = full[pos..] │
        └──────────┬──────────┘
                   ▼
        ┌──────────────────────────┐
        │ find_earliest_start_marker │
        │     (remaining)            │
        └───────────┬──────────────┘
                    ▼
              ┌──────────┐
              │ 找到标记？ │
              └────┬─────┘
         Yes /     \ No
            /       \
           ▼         ▼
    ┌──────────┐  ┌──────────────────────┐
    │ start>0 ?│  │ split_markdown_      │
    └────┬─────┘  │ paragraphs(remaining)│
   Yes / \ No    └──────────┬───────────┘
      /   \                 ▼
     ▼     ▼          ┌──────────┐
┌────────┐  ┌────┐    │ tail空？ │
│before→ │  │skip│    └────┬─────┘
│段落分割 │  │to  │   Yes / \ No
└────┬───┘  │step3│      /   \
     │      └────┘     ▼     ▼
     ▼            ┌────────┐ ┌──────────────┐
┌──────────┐      │ return │ │ return       │
│ tail非空？│      │ (blocks│ │ (blocks,     │
└────┬─────┘      │ , "")  │ │ tail)        │
 Yes / \ No       └────────┘ └──────────────┘
    /   \
   ▼     ▼
┌────────┐ ┌────────────┐
│ return │ │ find_end_  │
│(blocks,│ │ marker()   │
│ tail)  │ └─────┬──────┘
└────────┘       ▼
           ┌──────────┐
           │ 找到结束？ │
           └────┬─────┘
          Yes / \ No
             /   \
            ▼     ▼
      ┌────────┐ ┌────────────┐
      │build_  │ │ return     │
      │stream_ │ │ (blocks,   │
      │block() │ │ remaining) │
      └───┬────┘ └────────────┘
          │
          ▼
      ┌────────┐
      │ pos += │
      │content_│
      │start+  │
      │end_end │
      └───┬────┘
          │
          └──────► 回到循环顶部
```

### 3.4 finalize() 的收尾逻辑

`finalize(&mut self, full_text: &str) -> Vec<StreamBlock>` 定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:244-253`，在 SSE 流结束时调用：

```
finalize(full_text)
  │
  ├─► (blocks, tail) = process(full_text)   // 先走常规增量逻辑
  │
  ├─► trimmed = tail.trim()
  │
  └─► trimmed 非空 ?
        ├─Yes─► nodes = parse_markdown_to_ast(trimmed)
        │       hash  = compute_content_hash(trimmed)
        │       blocks.push( StreamBlock::markdown(trimmed.to_string(), Some(nodes), hash) )
        │
        └─No──► 直接返回 blocks
```

`finalize` 的核心职责是**将流结束时的未闭合 tail 强制视为最终 Markdown 段落**，避免前端出现「永远输入中」的悬停状态。该段落同样会进行 AST 预渲染和 Hash 计算，确保与已闭合块的数据一致性。

### 3.5 reset() 方法

`reset(&mut self)` 将 `processed_len` 归零，用于新消息到达时复用解析器实例，避免重新分配内存。


## 4. 核心算法

### 4.1 find_earliest_start_marker：竞争匹配

```rust
fn find_earliest_start_marker(text: &str) -> Option<(usize, usize, BlockType)>
```

定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:266-292`。该函数对 10 种特种块起始标记进行**竞争匹配（Competitive Matching）**，返回最早出现的那一组 `(start_offset, end_offset, BlockType)`。

#### 正则常量来源表

| 索引 | 正则常量（来自 `content_parser.rs`） | 匹配模式 | 对应 `BlockType` |
|------|--------------------------------------|----------|----------------|
| 1 | `TOOL_START` | `<<<[TOOL_REQUEST]>>>` | `Tool` |
| 2 | `THOUGHT_START` | `[--- VCP元思考链:主题 ---]` | `Thought` |
| 3 | `THINK_START` | `<think>` / `<thinking>` | `Think` |
| 4 | `TOOL_RESULT_START` | `[[VCP调用结果信息汇总:` | `ToolResult` |
| 5 | `DIARY_START` | `<<<DailyNoteStart>>>` | `Diary` |
| 6 | `HTML_FENCE_START` | `\`\`\`html` | `HtmlFence` |
| 7 | `HTML_DOC_START` | `<!doctype html>` / `<html…>` | `HtmlDoc` |
| 8 | `ROLE_DIVIDER` | `<<<[ROLE_DIVIDE_XXX]>>>` / `<<<[END_ROLE_DIVIDE_XXX]>>>` | `RoleDivider` |
| 9 | `STYLE_TAG_START` | `<style…>` | `Style` |
| 10 | `GENERIC_CODE_FENCE_START` | `\`\`\`lang` | `CodeFence` |
| 11 | `HTML_CONTAINER_OPEN_RE` | `<div\|section\|article\|header\|footer\|main\|aside\|figure\|figcaption…>` | `HtmlContainer` |

> 注意：`HtmlFence` 与 `CodeFence` 在 `StreamBlock` 输出层均映射为 `HtmlPreview` 或 `Markdown`，但内部判别阶段保持独立，以避免 HTML 围栏与普通代码围栏的解析冲突。

算法伪代码：

```
earliest = None
for (regex, block_type) in checks:
    if regex.find(text) matches at position m:
        if earliest is None OR m.start < earliest.start:
            earliest = (m.start, m.end, block_type)
return earliest
```

所有正则均通过 `lazy_static!` 预编译并缓存，运行时仅执行 `find()`，避免重复编译开销。正则定义详见 `src-tauri/src/vcp_modules/chat/content_parser.rs:76-126`。

### 4.2 find_end_marker：结束标记查找

```rust
fn find_end_marker(remaining: &str, start: usize, end: usize, block_type: &BlockType) -> Option<(usize, usize)>
```

定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:296-317`。`remaining` 为完整未处理文本，`start`/`end` 为起始标记的偏移，用于 HtmlContainer 提取标签名。返回值为 `(end_start_offset, end_end_offset)`，相对于 `remaining[content_start..]` 的相对偏移。根据 `block_type` 分发到对应的结束正则：

| `BlockType` | 结束正则 | 特殊行为 |
|-------------|----------|----------|
| `Tool` | `TOOL_END` | 常规查找 |
| `Thought` | `THOUGHT_END` | 常规查找 |
| `Think` | `THINK_END` | 常规查找 |
| `ToolResult` | `TOOL_RESULT_END` | 常规查找 |
| `Diary` | `DIARY_END` | 常规查找 |
| `HtmlFence` / `CodeFence` | `GENERIC_CODE_FENCE_END` | 共用同一结束标记 ` ``` ` |
| `HtmlDoc` | `HTML_DOC_END` | 常规查找 |
| `HtmlContainer` | `find_matching_close_tag` | 从 `remaining[start..end]` 提取标签名，调用围栏感知的嵌套标签匹配器 |
| `RoleDivider` | — | **直接返回 `(0, 0)`，自闭合** |
| `Style` | `STYLE_TAG_END` | 常规查找 |

返回的 `(usize, usize)` 是相对于 `search_area` 的 `(end_start_offset, end_end_offset)`。`inner_content` 取 `search_area[..end_start]`。

### 4.3 build_stream_block：从匹配文本构建具体块

```rust
fn build_stream_block(
    block_type: &BlockType,
    inner_content: &str,
    remaining: &str,
    start_idx: usize,
    end_idx: usize,
    end_end: usize,
) -> StreamBlock
```

定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:320-420`。这是类型转换的核心枢纽（Type Conversion Hub），负责将原始匹配结果转换为带 Hash 和 AST 的 `StreamBlock`。

各分支的 Hash 输入素材：

| 块类型 | Hash 输入格式 | 说明 |
|--------|--------------|------|
| `Tool`（普通） | `tool_name:inner_content` | — |
| `Tool`（DailyNote） | `maid:date:content` | 通过 `is_daily_note_create` 判断后升级为 `Diary` |
| `Thought` | `theme:inner_content` | `theme` 从 `THOUGHT_START.captures()` 提取 |
| `Think` | `inner_content` | 固定 `theme = "思维链"` |
| `ToolResult` | `tool_name:status:details_str:footer` | `details_str` 为所有 `key+value` 拼接 |
| `Diary` | `maid:date:content` | — |
| `HtmlFence` / `HtmlDoc` | `inner_content` | — |
| `HtmlContainer` | `open_tag + deindented_inner + close_tag` | 内部经 `trim_common_leading_indent` 去缩进后解析为 Markdown AST，输出为 `StreamBlock::markdown`，`nodes` 包含首尾 `raw_html` 节点 |
| `CodeFence` | `fence\ninner_content\n\`\`\`` | 重构完整围栏后作为 Markdown 输出 |
| `RoleDivider` | `role:is_end` | — |
| `Style` | `inner_content` | — |

### 4.4 split_markdown_paragraphs：段落分割

```rust
fn split_markdown_paragraphs(text: &str) -> (Vec<StreamBlock>, String)
```

定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:422-451`。该函数按 `\n\n`（双换行）将纯文本分割为已闭合的 Markdown 段落和未闭合的尾部。

```
split_markdown_paragraphs(text)
  │
  ├─► text 为空 ? → return ([], "")
  │
  ├─► 查找最后一个 "\n\n" 的位置 last_break
  │     ├─► 未找到 → return ([], text)        // 全部作为 tail
  │     └─► 找到   → stable = text[..last_break+2]
  │                   tail   = text[last_break+2..]
  │
  ├─► stable.split("\n\n") 遍历每个 para:
  │     ├─► trim 后为空则跳过
  │     ├─► nodes = parse_markdown_to_ast(trimmed)
  │     ├─► hash  = compute_content_hash(trimmed)
  │     └─► 生成 StreamBlock::Markdown { ... }
  │
  ├─► blocks = extract_inline_buttons(blocks)   // 内联按钮提取
  │
  └─► return (blocks, tail.to_string())
```

**为什么使用最后一个 `\n\n` 而非全部分割？** 在流式场景下，尾部段落可能尚未写完（例如用户正在输入一个长段落）。只将最后一个 `\n\n` 之前的文本视为稳定区域，确保不因文本截断而产生错误的段落分裂。

### 4.5 extract_inline_buttons：内联按钮提取

```rust
fn extract_inline_buttons(blocks: Vec<StreamBlock>) -> Vec<StreamBlock>
```

定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:453-514`。该函数扫描所有 `StreamBlock::Markdown` 的 `content` 字段，匹配 `BUTTON_CLICK` 正则（`\[\[点击按钮:(.*?)\]\]`，定义于 `content_parser.rs:227`），将 Markdown 块拆分为：

- 按钮前的 Markdown 块（若有）
- `StreamBlock::ButtonClick` 块
- 按钮后的 Markdown 块（若有）

```
输入: [ Markdown{ "请点击[[点击按钮:确认]]继续" }, ... ]
                        │
                        ▼
输出: [ Markdown{ "请点击" }, ButtonClick{ "确认" }, Markdown{ "继续" }, ... ]
```

每个拆分出的子块都会**重新执行 AST 预渲染和 Hash 计算**，保证数据完整性。非 `Markdown` 变体的块原样透传。


## 5. 各块类型的构建逻辑详解

本节深入分析 `build_stream_block` 中每个 `BlockType` 分支的特殊处理逻辑。所有分支均定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:320-420`。

### 5.1 Tool 块的双重人格：普通工具调用 vs 日记创建

Tool 块（`<<<[TOOL_REQUEST]>>>` … `<<<[END_TOOL_REQUEST]>>>`）存在一条**类型升级路径（Type Promotion Path）**：

```
┌─────────────────────────────────────────┐
│  BlockType::Tool                        │
│  inner_content 传入                     │
└─────────────┬───────────────────────────┘
              ▼
        ┌─────────────┐
        │ extract_    │
        │ tool_name() │
        └──────┬──────┘
               ▼
        ┌─────────────────┐
        │ is_daily_note_  │
        │ create(content) │
        └───────┬─────────┘
           Yes / \ No
              /   \
             ▼     ▼
    ┌──────────┐  ┌─────────────┐
    │ extract_ │  │ StreamBlock │
    │ diary_   │  │ ::Tool      │
    │ details()│  │ { tool_name,│
    └────┬─────┘  │  content,   │
         │        │  hash }     │
         ▼        └─────────────┘
    ┌──────────┐
    │ StreamBlock│
    │ ::Diary   │
    │ { maid,   │
    │  date,    │
    │  content, │
    │  nodes,   │
    │  hash }   │
    └──────────┘
```

- **升级条件**：`content.contains("DailyNote") && content.contains("create")`
- **升级后**：不再输出 `StreamBlock::Tool`，而是输出 `StreamBlock::Diary`，并提取 `maid`、`date`、`content` 三个字段
- **预渲染**：升级为 Diary 后，会对 `content` 执行 `parse_markdown_to_ast`，生成 AST 节点

### 5.2 Thought 与 Think 的区别

| 维度 | Thought（VCP 格式） | Think（通用格式） |
|------|---------------------|-------------------|
| **起始标记** | `[--- VCP元思考链:主题 ---]` | `<think>` / `<thinking>` |
| **结束标记** | `[--- 元思考链结束 ---]` | `</think>` / `</thinking>` |
| **主题提取** | 从起始标记正则捕获组提取 | 固定为 `"思维链"` |
| **正则锚定** | 行首锚定 `(?im)^[ \t]*` | 无行首锚定，可内联出现 |
| **输出变体** | `StreamBlock::Thought` | `StreamBlock::Thought`（复用同一变体） |
| **Hash 输入** | `theme:content` | `content` |

两者在 `StreamBlock` 层统一为 `thought` 类型（序列化标签 `thought`），前端无需区分渲染逻辑。差异仅在**主题来源**和**正则匹配策略**。

### 5.3 RoleDivider 的自闭合机制

`RoleDivider`（角色分隔器）是唯一在 `find_end_marker` 中**不查找结束正则**的类型：

```rust
BlockType::RoleDivider => {
    return Some((0, 0));  // 自闭合
}
```

这意味着 `build_stream_block` 接收到的 `inner_content` 为空字符串，`remaining[start_idx..end_idx]` 才是完整的标记文本。解析器从中提取：

- `is_end`：`caps.get(1).is_some()`，判断是否匹配到 `END_` 前缀
- `role`：`caps.get(2)`，取 `SYSTEM` / `ASSISTANT` / `USER`，并转为小写

### 5.4 CodeFence 的特殊处理：重构完整围栏

普通代码围栏（如 `\`\`\`rust` … `\`\`\``）在 `StreamBlock` 层被**降级（Demote）**为 Markdown 块：

```rust
let fence = &remaining[start_idx..end_idx];   // 包含起始 ```rust
let full_text = format!("{}\n{}\n```", fence, inner_content);
let nodes = parse_markdown_to_ast(&full_text);
```

原因：流式解析器不需要区分「带语言标识的代码块」与「普通 Markdown」，前者已经是 Markdown 标准语法的一部分。通过重构完整的围栏文本（包含起始和结束标记），可直接复用 `pre_renderer` 的通用 Markdown AST 解析能力。

### 5.5 HtmlFence 与 HtmlDoc 的统一

| 输入标记 | 内部 `BlockType` | 输出 `StreamBlock` | 内容范围 |
|----------|-----------------|-------------------|----------|
| `\`\`\`html` … `\`\`\`` | `HtmlFence` | `HtmlPreview` | `inner_content`（不含围栏标记） |
| `<!doctype html>` … `</html>` | `HtmlDoc` | `HtmlPreview` | `inner_content`（不含 HTML 标记） |

两者最终均映射为 `HtmlPreview`，前端可统一使用 iframe 或 WebView 进行渲染。`HtmlDoc` 的结束标记为 `</html>`（不区分大小写），`HtmlFence` 的结束标记为独立的 `\`\`\`` 行。

### 5.6 ToolResult 的 KV 解析

`ToolResult` 的构建调用 `parse_tool_result(inner_content)`，定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:562-607`。该函数采用**状态机逐行扫描**：

```
状态: IDLE ──匹配 KV 行──► CAPTURING(key)
                    │
                    ▼
         下一行匹配 KV ──► 提交上一 key，开启新 key
         下一行非 KV ──► 追加到 current_value_lines
         EOF ──► 提交最后一 key

未进入任何 key 的非空行 → footer_lines
```

KV 行格式：`- 工具名称: xxx` 或 `- 执行状态: yyy`，通过 `KV_REGEX`（`^-\s*([^:]+):\s*(.*)`）匹配。特殊 key「工具名称」和「执行状态」被提取到顶层字段，其余 key 放入 `details: Vec<ToolResultDetail>`。

### 5.7 Diary 的三字段提取

`extract_diary_details` 定义于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs:500-520`，使用三个独立正则：

| 字段 | 正则 | 匹配模式 | 默认值 |
|------|------|----------|--------|
| `maid` | `MAID_REGEX` | `maid: 「始」…「末」` 或 `Maid: …` | `""` |
| `date` | `DATE_REGEX` | `Date: 「始」…「末」` 或 `Date: …` | `""` |
| `content` | `CONTENT_REGEX` | `Content: 「始」…[

]*「末」` 或 `Content: …` | `"[日记内容解析失败]"` |

所有正则均支持两种格式：带「始」「末」边界符的 VCP 内部协议格式，以及纯文本冒号格式。

### 5.8 HtmlContainer 的构建逻辑

`HtmlContainer`（HTML 容器块）是 v0.9.14 新增的块类型，用于支持 `<div>`、`<section>`、`<article>`、`<header>`、`<footer>`、`<main>`、`<aside>`、`<figure>`、`<figcaption>` 等容器标签的完整解析与渲染。

与 `HtmlDoc` 不同，`HtmlContainer` 不期望一个完整的 HTML 文档，而是一个独立的容器元素，其内部可以包含 Markdown 内容。解析流程如下：

```
┌─────────────────────────────────────────┐
│  BlockType::HtmlContainer               │
│  open_tag = remaining[start_idx..end_idx]│
└─────────────┬───────────────────────────┘
              ▼
        ┌─────────────────────┐
        │ trim_common_leading_│
        │ indent(inner_content)│
        └──────────┬──────────┘
                   ▼
        ┌─────────────────────┐
        │ parse_markdown_to_  │
        │ ast(deindented)     │
        └──────────┬──────────┘
                   ▼
        ┌─────────────────────┐
        │ nodes = [           │
        │   raw_html(open_tag),│
        │   ...markdown_ast,  │
        │   raw_html(close_tag)│
        │ ]                   │
        └──────────┬──────────┘
                   ▼
        ┌─────────────────────┐
        │ StreamBlock::       │
        │ markdown(full_html, │
        │   Some(nodes), hash)│
        └─────────────────────┘
```

- **标签名提取**：从 `HTML_CONTAINER_OPEN_RE.captures(open_tag)` 的第 1 捕获组获取标签名（如 `div`），并转为小写。
- **结束标记匹配**：调用 `find_matching_close_tag(remaining, content_start, &tag_name)`，该函数支持嵌套标签深度追踪，并跳过代码围栏内的同名标签。
- **内部去缩进**：容器内部文本经 `trim_common_leading_indent` 处理，消除因格式化产生的统一前导空白，使内部 Markdown 能被正确解析。
- **AST 构造**：`nodes` 数组以 `raw_html(open_tag)` 开头，中间插入内部 Markdown 的 AST 节点，以 `raw_html(close_tag)` 结尾。
- **输出变体**：最终输出为 `StreamBlock::markdown`，序列化标签为 `markdown`，前端按普通 Markdown 块渲染，但 `nodes` 中包含 `raw_html` 节点负责输出容器标签。

## 6. 与 content_parser.rs 的协作关系

### 6.1 正则常量来源表

`stream_block_parser.rs` 通过 `use` 声明从 `content_parser.rs` 导入全部正则常量和辅助类型：

```rust
use crate::vcp_modules::content_parser::{
    BlockType, ToolResultDetail, BUTTON_CLICK, DIARY_END, DIARY_START,
    GENERIC_CODE_FENCE_END, GENERIC_CODE_FENCE_START, HTML_DOC_END,
    HTML_DOC_START, HTML_FENCE_START, ROLE_DIVIDER, STYLE_TAG_END,
    STYLE_TAG_START, THINK_END, THINK_START, THOUGHT_END, THOUGHT_START,
    TOOL_END, TOOL_RESULT_END, TOOL_RESULT_START, TOOL_START,
    CONTENT_REGEX, DATE_REGEX, KV_REGEX, MAID_REGEX, TOOL_NAME,
};
```

| 常量名 | 定义位置 | 用途 |
|--------|----------|------|
| `BlockType` | `content_parser.rs:194-206` | 内部块类型判别枚举（10 种变体） |
| `ToolResultDetail` | `content_parser.rs:57-60` | 工具结果键值对结构体 |
| `TOOL_START` / `TOOL_END` | `content_parser.rs:211-212` | 工具调用请求起止标记 |
| `THOUGHT_START` / `THOUGHT_END` | `content_parser.rs:215-216` | VCP 元思考链起止标记 |
| `THINK_START` / `THINK_END` | `content_parser.rs:218-219` | 通用 think 标签起止 |
| `TOOL_RESULT_START` / `TOOL_RESULT_END` | `content_parser.rs:221-222` | 工具结果汇总起止 |
| `DIARY_START` / `DIARY_END` | `content_parser.rs:224-225` | 日记块起止标记 |
| `HTML_FENCE_START` | `content_parser.rs:235` | HTML 代码围栏起始 |
| `HTML_DOC_START` / `HTML_DOC_END` | `content_parser.rs:239-240` | 完整 HTML 文档起止 |
| `HTML_CONTAINER_OPEN_RE` | `content_parser.rs:243` | HTML 容器标签（div/section/article 等）起始匹配 |
| `ROLE_DIVIDER` | `content_parser.rs:245` | 角色分隔标记 |
| `STYLE_TAG_START` / `STYLE_TAG_END` | `content_parser.rs:246-247` | CSS 样式标签起止 |
| `GENERIC_CODE_FENCE_START` / `GENERIC_CODE_FENCE_END` | `content_parser.rs:248-249` | 通用代码围栏起止 |
| `BUTTON_CLICK` | `content_parser.rs:95` | 内联按钮点击匹配 |
| `TOOL_NAME` | `content_parser.rs:81` | 工具名称提取 |
| `MAID_REGEX` | `content_parser.rs:229` | 日记 MAID 提取 |
| `DATE_REGEX` | `content_parser.rs:230` | 日记日期提取 |
| `CONTENT_REGEX` | `content_parser.rs:231` | 日记正文提取 |
| `KV_REGEX` | `content_parser.rs:233` | 工具结果 KV 行提取 |

### 6.2 BlockType 枚举的设计意图

`BlockType`（块类型判别枚举）是 `content_parser.rs` 的**内部（`pub(crate)`）类型**，不直接参与序列化，仅作为两个解析器之间的共享判别语言（Shared Discrimination Language）。

```rust
pub(crate) enum BlockType {
    Tool, Thought, Think, ToolResult, Diary,
    HtmlFence, HtmlDoc, HtmlContainer, Style, RoleDivider, CodeFence,
}
```

与 `StreamBlock`（面向前端序列化）和 `ContentBlock`（面向后端全量解析）不同，`BlockType` 更贴近「标记层面的物理类型」：
- `Thought` 与 `Think` 在输出层统一为 `StreamBlock::Thought`，但在判别层分离
- `HtmlFence` 与 `CodeFence` 在输出层分别走向 `HtmlPreview` 和 `Markdown`，但共享 `GENERIC_CODE_FENCE_END`

### 6.3 辅助函数的共享

以下私有辅助函数在 `stream_block_parser.rs` 中重新实现，但逻辑与 `content_parser.rs` 保持一致：

| 函数 | 所在文件 | 说明 |
|------|----------|------|
| `extract_tool_name` | `stream_block_parser.rs:518-534` | 与 `content_parser.rs:572-588` 逻辑一致 |
| `is_daily_note_create` | `stream_block_parser.rs:536-538` | 与 `content_parser.rs:590-592` 逻辑一致 |
| `extract_diary_details` | `stream_block_parser.rs:540-560` | 与 `content_parser.rs:594-614` 逻辑一致 |
| `parse_tool_result` | `stream_block_parser.rs:562-607` | 与 `content_parser.rs:616-661` 逻辑一致 |

> 这些函数未抽取到公共模块，而是分别在两个文件中重复实现，属于**逻辑耦合但物理解耦**的设计。修改时必须同步维护两个副本。

## 7. 性能与内存考量

### 7.1 增量处理避免重复计算

`processed_len` 是流式解析器的核心性能优化手段：

| 场景 | 全量解析（`content_parser.rs`） | 增量解析（`stream_block_parser.rs`） |
|------|--------------------------------|-------------------------------------|
| 每次 SSE chunk | 重新扫描整个 `full_text` | 仅从 `processed_len` 开始扫描 |
| 1KB → 10KB → 100KB | O(n²) 累积扫描 | O(n) 线性累积 |
| 已闭合块 AST | 每次都重新生成 | 永不重新生成 |

### 7.2 Hash 计算开销

每个 `StreamBlock` 均通过 `HashAggregator::compute_content_hash` 生成内容指纹，定义于 `src-tauri/src/vcp_modules/sync/sync_hash.rs`。

```rust
HashAggregator::compute_content_hash(trimmed)  // 通常为 SHA-256 或类似算法
```

Hash 计算发生在以下时机：
- 每个已闭合的 Markdown 段落
- 每个已闭合的特种块
- `finalize()` 时的剩余 tail
- `extract_inline_buttons` 拆分后的每个子块

在流式高频调用场景下（SSE chunk 每 50-100ms 到达一次），Hash 计算是主要的 CPU 开销点之一。由于输入文本通常较短（单段落或单工具块），实际开销可控；但对于超长 Markdown 文档（>10KB），建议前端在稳定后切换至 `content_parser.rs` 的全量解析结果。

### 7.3 内存分配模式

```
process() 单次调用的典型分配路径:
  ├─> Vec<StreamBlock>  （blocks，预分配于栈）
  ├─> String::new()     （tail，预分配于栈）
  ├─> 每找到一个特种块 → StreamBlock 变体 + String 字段
  ├─> Markdown 段落    → Vec<MarkdownNode>（AST，堆分配）
  └─> Hash 字符串       → 固定长度 String（堆分配）
```

`StreamBlock` 的所有字段均拥有（Owned），无生命周期引用（No Lifetime References），这意味着：
- ✅ 输出可安全跨线程传递到前端（Tauri IPC 要求 `Serialize`）
- ⚠️ 高频 chunk 下会产生较多短生命周期堆分配，依赖 Rust 的默认分配器性能

### 7.4 正则匹配的性能特征

`find_earliest_start_marker` 中 10 个正则均使用 `lazy_static!` 预编译，但**每次调用仍独立执行 `find()`**：

```
最坏情况：剩余文本中无任何特种块标记
→ 10 个正则各自扫描整段文本
→ 时间复杂度 ≈ O(10 × n)，常数因子较大
```

实际场景中，特种块标记稀疏分布，且 `remaining` 随 `processed_len` 推进而迅速缩短，因此整体性能接近 O(n)。若未来出现性能瓶颈，可考虑：
1. 将 `MASTER_START`（`content_parser.rs` 中的单一合并正则）复用于流式场景的前置快速筛选
2. 对 `remaining.len() < threshold` 的短文本跳过正则扫描，直接作为 tail 返回

### 7.6 Markdown 纯文本 fast-path

`content_parser.rs` 在 v0.9.14 引入了一条性能 fast-path：当待解析的 Markdown 文本中**不包含** `[[点击按钮:` 内联按钮标记时，直接调用 `parse_markdown_to_ast` 生成 AST，跳过 `parse_inline_blocks` 的内联块拆分流程。这消除了不必要的正则扫描和 Vec 分配，对纯文本消息有显著加速效果。

### 7.5 与前端增量渲染的协同

前端（Vue 3）通过 `:key="block.hash"` 使用 Hash 作为列表渲染的 Stable Key，确保：
- 已稳定块不会因新 chunk 到达而重新挂载（Re-mount）
- 仅 tail 区域和最新块需要重新渲染
- diff 算法的比较开销降至最低

这种「Rust 侧 Hash + Vue `v-for` key」的协同设计，是 VCP Mobile 流式对话保持 60fps 的关键架构决策之一。

---

*文档基于 `src-tauri/src/vcp_modules/chat/stream_block_parser.rs`（~833 行）及 `src-tauri/src/vcp_modules/chat/content_parser.rs`（~949 行）的源码分析生成。*
