---
title: 同步协议详解
scope: 双端
related_files:
  - src-tauri/src/vcp_modules/sync/sync_service.rs
  - src-tauri/src/vcp_modules/sync/sync_pipeline/*.rs
  - src-tauri/src/vcp_modules/sync/sync_executor/*.rs
  - VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/transport/websocket.js
  - VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/transport/routes.js
---

# 同步协议详解

## 1. 引言

### 1.1 协议概述

VCP Mobile 同步协议（以下简称本协议）是连接移动端（Mobile）与桌面端（Desktop）的数据同步桥梁，采用 WebSocket + HTTP REST 双协议架构（Dual-Protocol Architecture）。该设计兼顾了实时控制信令的低延迟需求与大容量数据传输的可靠性需求：

- WebSocket 层：承担同步会话的生命周期管理、阶段控制、差异比对指令交换等轻量级控制面职责。所有控制消息以 JSON 文本帧（Text Frame）传输。
- HTTP 层：承担 Owner/Topic DTO、头像 bytes 与消息 NDJSON。附件只同步消息内元数据，不传二进制。

双协议层共享同一套认证令牌（Sync Token），但传输方式存在差异：WebSocket 采用 Query Param 传递，HTTP 采用 Header 传递。

### 1.2 同步数据类型

协议同步五类业务对象，但不再用一个过载枚举同时表达 Manifest、DTO 与删除目标：

| 类型枚举 | 英文标识 | 说明 |
|---------|---------|------|
| Agent | agent | 智能体配置与元数据 |
| Group | group | 群组配置与元数据 |
| Avatar | avatar | 头像图片文件 |
| Topic | topic | 话题（会话）配置 |
| Message | message | 消息历史记录 |

### 1.3 核心设计哲学

- LWW（Last-Write-Wins，最后写入胜出）：自身 Hash 不同时以 `updatedAt`（毫秒级 Unix 时间戳）仲裁；消息同时间再按 Hash 字典序裁决。
- 双哈希体系（Dual-Hash System）：V2 协议引入 configHash（配置指纹）与 contentHash（内容聚合指纹，即 Merkle Root）的分离设计，避免全量内容比对。contentHash 用于快速判定某 Agent/Group 旗下话题/消息是否发生深层变更，从而决定是否进入 Phase 3 消息同步。
- 分阶段流水线（Phased Pipeline）：同步过程由三个公开阶段组成；Topic 双 Hash 校验是 Phase 2 内部的 2.5 步，通过 WebSocket 的 PHASE_START / PHASE_COMPLETED / PHASE_ACK 机制驱动流转。

---

## 2. 协议数据模型

### 2.1 完整身份与专用状态

协议不再用万能 `EntityState`。Owner、Topic、Avatar 和 Message 各自使用强类型 live/deleted 状态。

| 字段 | 类型 | 序列化名 | 必填 | 说明 |
|------|------|---------|------|------|
| `ownerType+ownerId` | Owner/Avatar | — | 是 | OwnerKey；Avatar 另允许 `user/user_avatar` |
| `ownerType+ownerId+topicId` | Topic | — | 是 | TopicKey |
| 完整 TopicKey+`msgId` | Message | — | 是 | MessageKey |
| `configHash/contentHash` | Owner/Topic live | 同名 | 是 | DTO 配置与下级内容根 |
| `binaryHash` | Avatar live | 同名 | 是 | 原始头像 bytes SHA-256 |
| `messageHash` | Message live | 同名 | 是 | 完整消息同步指纹 |
| `updatedAt` | live 状态 | 同名 | 是 | LWW 时间 |
| `deletedAt` | deleted 状态 | 同名 | 是 | 墓碑时间，删除状态不携带 live Hash |

所有 Hash 必须为小写 SHA-256（聚合空根允许空字符串），时间必须为非负 JSON 安全整数。

哈希计算规则：
- Agent/Group 配置哈希：提取 DTO 中的同步字段子集（AGENT_SYNC_FIELDS / GROUP_SYNC_FIELDS），通过确定性 JSON 序列化后计算 SHA-256（compute_deterministic_hash）。
- Topic 配置哈希：同理提取 AGENT_TOPIC_SYNC_FIELDS 或 GROUP_TOPIC_SYNC_FIELDS。
- Content Hash（聚合哈希）：对子实体哈希列表按字典序排序后连接，再次计算 SHA-256（compute_merkle_root）。Agent/Group 的 contentHash 为其旗下所有 Topic 的 configHash + contentHash 的聚合；Topic 的 contentHash 为其旗下所有消息的 contentHash 的聚合。

### 2.2 Manifest

`ManifestRequest` 按 `manifestType` 分为三个互斥变体：

```rust
pub enum ManifestRequest {
    Owner { items: Vec<OwnerManifestState> },
    Topic { items: Vec<TopicManifestState>, targeted_owners: Vec<OwnerKey> },
    Avatar { items: Vec<AvatarManifestState> },
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `manifestType` | `owner/topic/avatar` | 唯一判别字段 |
| `items` | 对应状态数组 | 清单状态 |
| `targetedOwners` | OwnerKey 数组 | Topic 比较范围 |

Phase 1 严格按 Owner → drain → Avatar 两波执行；Phase 2 发送靶向 Topic Manifest。

### 2.3 判别词汇

```rust
ManifestType = owner | topic | avatar
EntityType   = owner | topic
OwnerType    = agent | group
DeleteTarget = owner | topic | avatar | message
```

每个判别字段只承担一项职责；协议外壳不再使用 `agent_topic/group_topic` 或裸 `id`。

---

## 3. 连接建立

### 3.1 WebSocket 连接流程

移动端在启动手动同步时，由 `start_manual_sync` 命令触发同步会话。核心连接流程如下：

1. 完成后端唯一会话与消息生成前检；VCPLog 是独立日志通道，不参与同步网络准入。
2. 从 settings_manager 一次读取 WebSocket 地址、HTTP 基地址、同步令牌、日志等级和预渲染开关，在创建 session 资源前统一校验 URL、协议、非空令牌及 Android loopback。
3. 生成不可变会话配置快照：清除 WS 旧 query 后附加唯一 token，并由同一份快照服务本次会话的全部重连。
4. 创建 HTTP Client、会话日志并尝试申请 Android ForegroundGuardian lease；Guardian 是最佳努力的后台保活增强，申请失败只记录警告，不阻断 Wire 同步。
5. 通过 tokio_tungstenite::connect_async 发起异步握手；每次建连最多等待 10 秒。
6. 连接失败或建连超时时使用同一组 3 个自动重试槽，退避依次为 500ms、1s、2s。

桌面端由 startWsServer（`VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/transport/websocket.js`）在指定端口（默认 5975）监听 WebSocket 连接。

### 3.2 Token 验证机制

桌面端在两个传输入口验证同一同步令牌：

WebSocket 入口（`VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/transport/websocket.js`）：

| 位置 | 字段名 | 格式 |
|------|--------|------|
| Query Param | token | ws://host:port?token=<sync_token> |

验证逻辑提取桌面端源码核心逻辑：连接时解析 URL Query 中的 token 参数，与 syncToken 比对，不匹配则返回 WebSocket Close Code 4001。

HTTP 入口只接受 `Authorization: Bearer <sync_token>`。不接受自定义 token Header 或 query token。WebSocket query token 与 HTTP Bearer 是两条明确的连接边界。

### 3.3 版本校验（VERSION_CHECK / VERSION_ACK）

连接建立后，同步会话的第一条消息必须是版本握手。该机制确保移动端 App 与桌面端插件的版本兼容性。

握手时序：

```
Mobile                                  Desktop
  |                                        |
  | ---- WS: VERSION_CHECK --------------> |
  |     { type: VERSION_CHECK,             |
  |       mobileVersion: <App Version>,    |
  |       protocolVersion: "1.4" }         |
  |                                        |
  | <--- WS: VERSION_ACK ----------------- |
  |     { type: VERSION_ACK,               |
  |       pluginVersion: "1.4.0",          |
  |       protocolVersion: "1.4" }         |
  |                                        |
```

移动端校验逻辑（`src-tauri/src/vcp_modules/sync/sync_service.rs`）：

| 常量 | 值 | 说明 |
|------|-----|------|
| WIRE_PROTOCOL_VERSION | 1.4 | 唯一兼容性真源；不与旧 Wire 混跑 |
| WS_CONNECT_TIMEOUT | 10s | 单次 WebSocket TCP/Upgrade 建连的最大等待时间 |
| VERSION_CHECK_TIMEOUT | 5s | 等待 VERSION_ACK 的超时时间 |
| WS_OPERATION_TIMEOUT | 5s | 单次 WebSocket send / close 的最大等待时间 |
| FINAL_ACK_TIMEOUT | 30s | Finalize 后等待桌面端最终完成确认的最大时间 |

校验规则：
- `protocolVersion` 必须精确匹配；`pluginVersion` 为非空诊断信息，不参与兼容仲裁。
- 缺字段、错误字段类型、畸形 JSON 或旧字段 `version` 均以 `VERSION_ACK_INVALID` 终止当前会话，不做降级。
- 若版本不一致，移动端立即断开连接，向前端报告错误，并提示用户下载最新插件。
- 若超时未收到 VERSION_ACK，视为桌面端插件过旧或不支持版本校验，同样断开连接。
- 最终 ACK 继续精确回显 `phase/sessionId/attemptId/nonce`；旧 Wire 在 VERSION_ACK 阶段直接拒绝。

---

## 4. WebSocket 消息类型完整参考

以下表格覆盖同步会话中所有 WebSocket 消息类型。方向列中 M-D 表示 Mobile-Desktop，D-M 表示 Desktop-Mobile。

### 4.1 控制面消息

| 消息名称 | 方向 | 触发时机 | Payload 关键字段 | 处理函数（Mobile） | 处理函数（Desktop） |
|---------|------|---------|-----------------|-------------------|-------------------|
| VERSION_CHECK | M-D | WS 连接建立后 | mobileVersion, protocolVersion: "1.4" | `run_sync_session` 发送 | Desktop 返回 VERSION_ACK |
| VERSION_ACK | D-M | 收到 VERSION_CHECK 后 | pluginVersion, protocolVersion: "1.4" | 按 Wire 校验 | 读取插件版本并返回 |
| PHASE_START | M-D | 各阶段开始时由移动端发送 | phase: string | run_sync_session 中通过 ws_stream.send 发送 | index.js 记录日志，返回 PHASE_ACK |
| PHASE_COMPLETED | M-D | 移动端完成阶段后发送；最终 `messages` 帧启动严格 ACK 等待 | 普通阶段：phase；最终帧：phase, sessionId, attemptId, nonce | `SyncCommand::Finalize` 发送最终帧，仅精确 ACK 可解除 30 秒 watchdog | index.js 回显最终帧身份字段 |
| PHASE_ACK | D-M | 桌面端确认收到阶段控制消息 | 普通阶段：phase；最终 ACK：phase, sessionId, attemptId, nonce | 普通 ACK 仅记录；最终 ACK 必须精确匹配当前 pending key 并仅消费一次 | index.js 回显所有身份字段 |
| SYNC_LOG_EVENT | D-M | 桌面端主动上报日志事件 | level: string, message: string | 通过 emit_sync_log 转发到前端 | 桌面端内部 logger 触发 |
| DESKTOP_PHASE_START | D-M | 桌面端报告自身阶段开始 | phase: string | 日志输出：[Desktop] Phase X started | 桌面端内部 logger 触发 |
| DESKTOP_PHASE_COMPLETE | D-M | 桌面端报告自身阶段完成 | phase: string | 日志输出：[Desktop] Phase X completed | 桌面端内部 logger 触发 |

### 4.2 清单与差异比对消息

| 消息名称 | 方向 | 触发时机 | Payload 关键字段 | 处理函数（Mobile） | 处理函数（Desktop） |
|---------|------|---------|-----------------|-------------------|-------------------|
| SYNC_MANIFEST_REQUEST | M-D | Phase 1 Owner/Avatar 或 Phase 2 Topic 波次 | `manifestType,items,targetedOwners?` | 发送强类型 Manifest | `handleSyncManifest` |
| SYNC_MANIFEST_RESULT | D-M | Manifest 仲裁后 | `manifestType,results` | 派发 Pull/Push/Delete | `handleSyncManifest` 返回 |
| SYNC_TOPIC_DIFF_REQUEST | M-D | Phase 2.5 | `topics[{完整TopicKey,configHash,contentHash}]` | 发送双 Hash | `handleSyncTopicDiff` |
| SYNC_TOPIC_DIFF_RESULT | D-M | Topic 比对后 | `changedTopics: TopicKey[]` | 进入或跳过 Phase 3 | `handleSyncTopicDiff` 返回 |
| SYNC_MESSAGE_DIFF_REQUEST | M-D | Phase 3 分批 | `topics[{完整TopicKey,contentHash,messages}]` | 按消息数与字节预算分批 | `handleSyncMessageDiff` |
| SYNC_MESSAGE_DIFF_RESULT | D-M | 消息仲裁后 | `results[{完整TopicKey,ok,pullMessageIds,pushTopic,deleteMessages}]` | 严格覆盖当前请求集合 | `handleSyncMessageDiff` 返回 |

### 4.3 实时变更通知消息

| 消息名称 | 方向 | 触发时机 | Payload 关键字段 | 处理函数（Mobile） | 处理函数（Desktop） |
|---------|------|---------|-----------------|-------------------|-------------------|
| SYNC_ENTITY_DELETE | M-D | 移动端本地实体或消息提交软删除 | `targetType`、完整身份、`deletedAt` | NotifyDelete 发送 | index.js 幂等删除 |
| SYNC_ERROR | 双向 | 任一端遇到不可恢复错误 | `error: { code, origin, stage, kind, retry, message, failedTopicIds }` | 严格解析完整 Wire 1.4 对象；原始 message 只写诊断日志 | `error-contract.js` / `sync_error.rs` 统一构造 |

### 4.4 内部控制命令

| 命令/消息 | 方向 | 触发时机 | 说明 |
|----------|------|---------|------|
| `SyncCommand::Cancel` | 内部 | `stop_sync` Tauri 命令被调用 | 关闭 WebSocket 并收口当前后端会话。同步中不提供用户取消入口。 |
| `SyncCommand::SendMessageDiff` | 内部 | 一批 Phase 3 收尾后 | 将下一批强类型 Message Diff 重新入队。 |
| `stop_sync` | Tauri Command | 终态关闭面板或开始重试 | 取消并等待旧后端会话，防止旧任务跨入下一次尝试。 |

### 4.5 消息 Payload 字段详解

#### SYNC_MANIFEST_REQUEST / RESULT

```json
{"type":"SYNC_MANIFEST_REQUEST","manifestType":"owner","items":[{"ownerType":"agent","ownerId":"agent_001","configHash":"...","contentHash":"...","updatedAt":1715000000000}]}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `manifestType` | `owner/topic/avatar` | 是 | 决定唯一状态形态 |
| `items` | 强类型数组 | 是 | live 使用专用 Hash+`updatedAt`；deleted 只使用 `deletedAt` |
| `targetedOwners` | `OwnerKey[]` | Topic 必填 | 明确 Topic 比较范围 |

Result 使用同一 `manifestType`，每项回显完整身份和 `action`。动作固定为 `PULL/PUSH/PULL_DELETE/PUSH_DELETE/SKIP`；Owner 可额外携带 `contentHashMismatch:true`。

```json
{"type":"SYNC_MANIFEST_RESULT","manifestType":"owner","results":[{"ownerType":"agent","ownerId":"agent_001","action":"SKIP","contentHashMismatch":true}]}
```

墓碑 Result 的 `deletedAt` 必须为非负安全整数；非删除动作禁止携带该字段。

#### SYNC_MESSAGE_DIFF_REQUEST / RESULT

```json
{"type":"SYNC_MESSAGE_DIFF_REQUEST","topics":[{"ownerType":"agent","ownerId":"agent_001","topicId":"topic_001","contentHash":"...","messages":{"msg_001":{"messageHash":"...","updatedAt":1700000000000},"msg_002":{"deletedAt":1700000000001}}}]}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `topics` | array | 是 | 每项含完整 TopicKey、`contentHash` 和以 msgId 为 key 的状态对象 |

Result：

```json
{"type":"SYNC_MESSAGE_DIFF_RESULT","results":[{"ownerType":"agent","ownerId":"agent_001","topicId":"topic_001","ok":true,"pullMessageIds":["msg_003"],"pushTopic":true,"deleteMessages":[]}]}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `ok` | boolean | 是 | 严格判别字段 |
| `pullMessageIds/pushTopic/deleteMessages` | array/bool/array | `ok:true` | 成功分支必须齐全且禁止 `error` |
| `error` | WireSyncError | `ok:false` | 失败分支禁止决策字段并终止 attempt |

响应必须逐 topic 精确覆盖本批请求。原始 JSON 中重复 topic key 也会在进入普通 `Value` 解析前被拒绝，不能依赖后写覆盖。

---

## 5. HTTP 端点完整参考

所有 HTTP 端点挂载在 `/api/mobile-sync`，只接受 Bearer token。完整逐字段参考见 `附录B_HTTP端点完整参考.md`。

### 5.1 实体下载端点

#### POST /entities/pull

| 属性 | 值 |
|------|-----|
| 方法 | POST |
| 用途 | 批量实体下载（Pull 批量，Phase 1/2 优化） |
| 请求格式 | `{items: EntitySelector[]}`，完整 Owner/Topic 身份 |
| 响应格式 | `{results:[{完整身份,ok,data?或error?}]}` |
| Body 限制 | 1000 项 / 10 MiB |
| 认证方式 | Bearer Header |
| 分批策略 | Owner 每批 50，Topic 每批 1000 |

### 5.2 实体上传端点

#### POST /entities/push

| 属性 | 值 |
|------|-----|
| 方法 | POST |
| 用途 | Owner/Topic 批量 Push |
| 请求格式 | `{items:[{entityType,完整身份,data}]}` |
| 响应格式 | `{results:[{完整身份,ok,error?}]}` |
| Body 限制 | 10000 项 / 10 MiB |
| 认证方式 | Bearer Header |
| 内部提交 | Owner 单配置；Topic 按父 config 分组 |
| 幂等性 | Owner 请求可携带 `x-idempotency-key` |

### 5.3 消息传输端点

#### POST /messages/pull

| 属性 | 值 |
|------|-----|
| 方法 | POST |
| 用途 | 流式批量下载消息（Phase 3 Pull 优化） |
| 请求格式 | `{topics:[{完整TopicKey,messageIds}]}` |
| 响应格式 | `kind:topic,ok:true/false` NDJSON |
| Body 限制 | 5mb |
| 认证方式 | Bearer Header |

NDJSON（Newline Delimited JSON）：每个消息对象序列化为一行 JSON，以换行符分隔。该格式允许接收方在流未完成时即开始逐行解析和处理，避免大内存缓冲。

#### POST /messages/push

| 属性 | 值 |
|------|-----|
| 方法 | POST |
| 用途 | 按 Topic 提交 live 最终视图与 `deletedMessages` |
| 请求格式 | Topic NDJSON |
| 响应格式 | Topic NDJSON，逐 Topic `ok` |
| Body 限制 | 单行 32 MiB、attempt 256 MiB |
| 认证方式 | Bearer Header |

### 5.4 头像端点

#### GET /avatars/pull

| 属性 | 值 |
|------|-----|
| 方法 | GET |
| 用途 | 下载头像图片 |
| Query 参数 | `ownerType`, `ownerId`（无默认值） |
| 响应格式 | 文件流或 404 Not Found |
| Body 限制 | 无 |
| 认证方式 | Bearer Header |
| 对应处理函数 | downloadAvatar (sync/entity.js) |

#### POST /avatars/push

| 属性 | 值 |
|------|-----|
| 方法 | POST |
| 用途 | 上传头像图片 |
| Query 参数 | `ownerType`, `ownerId` |
| 请求格式 | 原始二进制字节流 |
| 响应格式 | `{ownerType,ownerId,ok:true}` |
| Body 限制 | 20 MiB |
| 认证方式 | Bearer Header |
| 对应处理函数 | uploadAvatar (sync/entity.js) |

### 5.5 删除端点

#### 删除传播

| 属性 | 值 |
|------|-----|
| 在线低延迟 | `SYNC_ENTITY_DELETE`，`targetType` + 完整身份 + `deletedAt` |
| 离线 Owner/Topic/Avatar | Manifest 墓碑重放 |
| 离线 Message | 随 `/messages/push` 的 `deletedMessages` 重放 |

公共 HTTP 不提供独立删除端点。

### 5.6 HTTP 端点汇总表

| 路径 | 方法 | 用途 | Body 限制 | 流式 | 幂等性 |
|------|------|------|----------|------|--------|
| `/entities/pull` | POST | Owner/Topic Pull | 10 MiB | 否 | 否 |
| `/entities/push` | POST | Owner/Topic Push | 10 MiB | 否 | Owner 可用 |
| `/messages/pull` | POST | 消息 Pull | 5 MiB 请求 | NDJSON | 否 |
| `/messages/push` | POST | 消息与墓碑 Push | 256 MiB 总量 | NDJSON | Topic 级幂等 |
| `/avatars/pull` | GET | 头像 Pull | 20 MiB 响应 | 二进制 | 否 |
| `/avatars/push` | POST | 头像 Push | 20 MiB | 二进制 | 否 |

---

## 6. 差异比对算法详解

### 6.1 清单差异计算（Manifest Diff）

桌面端收到 `SYNC_MANIFEST_REQUEST` 后，由 `handleSyncManifest` 执行差异计算：

1. 按 `manifestType` 加载完整身份和专用状态。
2. 遍历 Mobile 清单：
   - Mobile 墓碑获胜时输出 `PUSH_DELETE`
   - 若本地不存在，输出 PUSH 动作（移动端需要推送给桌面端）
   - 若本地已删除但远程未删除，输出 PUSH_DELETE 动作（告知移动端该实体在桌面端已被删除）
   - 若 Avatar 的 hash 不一致，按时间戳裁决
   - 若 configHash 不一致，按时间戳裁决：远程较新则 PUSH，否则 PULL
   - Owner `contentHash` 不一致时标记 `contentHashMismatch=true`
3. 遍历本地剩余：对远程清单中未包含的本地实体：
   - 若本地已删除，输出 `PULL_DELETE`
   - 否则，输出 PULL（桌面端需要推送给移动端）

输出动作类型完整语义：

| 动作 | 方向 | 含义 | 后续操作 |
|------|------|------|---------|
| PULL | D-M | 桌面端数据较新或仅桌面端有，需推送给移动端 | 移动端调用 HTTP Pull 端点下载 |
| PUSH | M-D | 移动端数据较新或仅移动端有，需推送给桌面端 | 移动端调用 HTTP Push 端点上传 |
| PULL_DELETE | D-M | Desktop 墓碑获胜 | Mobile 幂等落墓碑 |
| PUSH_DELETE | M-D | Mobile 墓碑获胜 | 发送 `SYNC_ENTITY_DELETE`；后续清单继续重放 |
| SKIP | - | 数据一致，无需操作 | 无 |

### 6.2 消息差异计算（Message Diff）

Phase 3 的消息差异由 `handleSyncMessageDiff` 计算：

第一层：Topic 级快速路径
- 比较 Mobile `contentHash` 与 Desktop Topic 内容根
- 完全一致时返回空 `pullMessageIds/deleteMessages` 且 `pushTopic=false`
- 该路径避免了逐消息比对，在大部分话题未变更时极大降低计算量

第二层：消息级详细比对
- 对未命中快速路径的 Topic，从 Desktop `messages` 提交表读取消息 Hash、更新时间与墓碑
- 构建桌面端状态映射 `Map<msgId, {hash, updatedAt}>`
- 同 ID、不同 Hash 时按 `updatedAt` 较大者优胜；时间相同则 Hash 字典序较大者优胜
- Mobile 最终视图需要回写时设置 `pushTopic=true`

结果处理策略：
- 先应用 Desktop 消息墓碑，再调用 `/messages/pull` 拉取 Desktop 获胜消息
- Pull 成功后等待本地写队列落库；任一 Pull 或落库失败均终止 attempt，不执行 Push
- `pushTopic=true` 时调用 `/messages/push`，一次提交 live 最终视图与 `deletedMessages`

---

## 7. 并发控制与流量整形

### 7.1 消息 Pull 帧预算

`pull_messages_batch` 使用 32 MiB 的在途 NDJSON 帧预算。每个 Topic 按原始帧大小占用预算，处理完成后释放，防止多个大帧同时进入解析管线。

### 7.2 移动端数据库写入队列（DbWriteQueue）

为避免高频并发写入 SQLite 导致锁竞争，移动端引入 DbWriteQueue（`src-tauri/src/vcp_modules/persistence/db_write_queue.rs`）：

- 批量缓冲：Pull 下来的实体数据先进入内存队列，而非直接写入数据库
- 定时刷盘：队列在以下时机执行批量写入：
  1. 队列长度达到阈值
  2. 阶段切换时（PHASE_COMPLETED 触发 flush()）
  3. 同步完成时
- WAL 模式：SQLite 启用 Write-Ahead Logging，进一步降低并发写入的锁竞争

### 7.3 桌面端流式传输

Phase 3 的大批量消息传输采用 NDJSON 流式格式，避免移动端内存溢出：

- `/messages/pull`：Desktop 按 Topic 输出 NDJSON 并服从背压。
- `/messages/push`：Mobile 按 Topic 快照分页序列化，Desktop 逐 Topic 提交并返回结果。
- 单帧最大 32 MiB、单 attempt 总序列化预算 256 MiB、Topic 上限 10,000、消息上限 100,000；任一超限都会终止当前 attempt，而不是继续缓冲。

---

## 8. 同步状态机与事件系统

### 8.1 会话状态所有权

`run_sync_session` 是后端会话状态的唯一所有者，负责连接状态、attempt、阶段门和完成确认。`SyncPipeline` 只中继 Phase 2、Phase 2.5 与 Phase 3 的推进命令；Phase 3 完成后由 `SyncCommand::Finalize` 进入收尾。前端不读取后端内部状态枚举，只消费带 `sessionId` 的 Tauri 事件。

### 8.2 前端事件系统

同步模块通过 Tauri 的 Emitter 向前端 Vue 层发送多种事件，驱动 UI 状态更新：

| 事件名称 | 触发时机 | Payload 结构 |
|---------|---------|-------------|
| vcp-sync-status | 同步状态变更时 | `{ sessionId, status, error? }`；错误保留结构化 code 与有限失败 Topic |
| vcp-sync-progress | 阶段进度更新时 | `{ sessionId, phase, total, completed, successfulTopics?, totalTopics?, failedTopics?, legacyAttachmentWarnings? }` |
| vcp-sync-completed | 最终 ACK 且本地 drain 成功后 | `{ sessionId, status, summary: {successfulTopics, totalTopics, failedTopics, legacyAttachmentWarnings, failedTopicIds} }` |
| vcp-log | 仅自动重试、日志文件不可用等安全过程提示 | `{ id, level, category:"sync", audience:"operator", sessionId, message }` |

同步模块不再发射 `vcp-system-event` / `vcp-log-message`；全局 Toast 和通知中心不承担同步连接、完成或错误展示。

状态流转图：

```
[disconnected] --start_manual_sync--> [connecting]
    [connecting] --WS open--> [open]
    [open] --sync progress--> [open]
    [open] --error--> [error]
    [open] --all phases done--> [completed | completed_with_warnings]
    [error] --retry--> [connecting]
```

### 8.3 Phase3Tracker 任务追踪

Phase 3 中由于涉及大量 Topic 的并行 Push/Pull，移动端使用 Phase3Tracker（`src-tauri/src/vcp_modules/sync/sync_service.rs`）精确追踪每个 Topic 的完成状态：

| 属性 | 类型 | 说明 |
|------|------|------|
| completed | Mutex<HashSet<String>> | 已完成去重集合，防止同一 Topic 被重复计数 |
| modified | Mutex<HashSet<String>> | 数据实际发生变更的 Topic 集合，用于最终 Hash 冒泡 |
| total | AtomicUsize | 需要处理的总 Topic 数 |

mark_completed 方法采用首次标记有效策略：只有第一次调用返回 true，后续重复调用返回 false。当 completed.len() == total 时，自动触发 SyncCommand::Finalize，进入同步收尾阶段。

---

## 9. 三阶段协议时序图

本协议采用三阶段流水线；Mobile 端内部将 Topic 哈希比对记作 Phase 2.5，但它不构成第四个公开阶段。以下是完整的 27 步同步流程：

```
Mobile (Client)                              Desktop (Server)
    |                                               |
    |  [1] WS CONNECT (with ?token=...)             |
    | ------------------------------------------->  |
    |                                               |
    |  [2] WS: VERSION_CHECK                        |
    |      { mobileVersion: <App Version>,          |
    |        protocolVersion: "1.4" }              |
    | ------------------------------------------->  |
    |                                               |
    |  [3] WS: VERSION_ACK                          |
    |      { pluginVersion: "1.4.0",               |
    |        protocolVersion: "1.4" }              |
    | <-------------------------------------------  |
    |                                               |
    |  [4] WS: PHASE_START { phase: owner_metadata } |
    | ------------------------------------------->  |
    |                                               |
    |  [5] WS: SYNC_MANIFEST_REQUEST (owner)        |
    | ------------------------------------------->  |
    |                                               |
    |  [6] WS: SYNC_MANIFEST_RESULT (owner)         |
    |      { results: [PULL/PUSH/PULL_DELETE/...] } |
    | <-------------------------------------------  |
    |                                               |
    |  [7] HTTP: Owner Pull/Push/Delete tasks       |
    | ============================================> |
    |                                               |
    |  [8] Owner tasks == 0 + write_queue.flush()   |
    |                                               |
    |  [9] WS: SYNC_MANIFEST_REQUEST (avatar)       |
    | ------------------------------------------->  |
    |                                               |
    | [10] WS: Avatar diff + tasks + queue drain    |
    | <===========================================> |
    |                                               |
    | [11] WS: PHASE_COMPLETED { phase: owner_metadata } |
    | ------------------------------------------->  |
    |                                               |
    | [12] WS: PHASE_START { phase: topic_metadata } |
    | ------------------------------------------->  |
    |                                               |
    | [13] WS: SYNC_MANIFEST_REQUEST (topic)        |
    | ------------------------------------------->  |
    |                                               |
    | [14] WS: SYNC_MANIFEST_RESULT (topic)         |
    | <-------------------------------------------  |
    |                                               |
    | [15] HTTP: /entities/pull (Pull topics)       |
    | ============================================> |
    |                                               |
    | [16] WS: PHASE_COMPLETED { phase: topic_metadata } |
    | ------------------------------------------->  |
    |                                               |
    | [17] WS: SYNC_TOPIC_DIFF_REQUEST              |
    |      { hashes: { topicId: {configHash, contentHash} } } |
    | ------------------------------------------->  |
    |                                               |
    | [18] WS: SYNC_TOPIC_DIFF_RESULT               |
    |      { changedTopics: [topic_001, ...] }      |
    | <-------------------------------------------  |
    |                                               |
    | [19] WS: PHASE_START { phase: messages }      |
    | ------------------------------------------->  |
    |                                               |
    | [20] WS: SYNC_MESSAGE_DIFF_REQUEST (batch 1)  |
    | ------------------------------------------->  |
    |                                               |
    | [21] WS: SYNC_MESSAGE_DIFF_RESULT             |
    |      { results: { topicId:                  |
    |          {ok, pullMessageIds, pushTopic} } }  |
    | <-------------------------------------------  |
    |                                               |
    | [22] HTTP: /messages/pull (Pull)              |
    |      HTTP: /messages/push (Push)              |
    | ============================================> |
    |                                               |
    | [23] (若有更多批次，重复 20-22)                |
    |                                               |
    | [24] WS: PHASE_COMPLETED                       |
    |      { phase: messages, sessionId,             |
    |        attemptId, nonce }                       |
    | ------------------------------------------->  |
    |                                               |
    | [25] WS: PHASE_ACK                              |
    |      { phase: messages, sessionId,              |
    |        attemptId, nonce } (exact echo)           |
    | <-------------------------------------------  |
    |      Mobile final drain + emit completed      |
    |      WS: close（5 秒 deadline）               |
    |                                               |
```

### 9.1 阶段说明

| 阶段 | 名称 | 核心消息/HTTP | 说明 |
|------|------|--------------|------|
| Phase 1 | Owner Metadata | Owner Manifest → drain → Avatar Manifest | Owner 与 Avatar 独立仲裁 |
| Phase 2 | Topic Metadata | Topic Manifest + `targetedOwners` | 只处理变更 Owner |
| Phase 2.5 | Topic Validation | Topic Diff | 双 Hash 筛选消息变更 Topic |
| Phase 3 | Messages | Message Diff | 10000 条与 8 MiB 双预算，一批收尾后再发下一批 |

### 9.2 关键设计细节

- Phase Gate 机制（phase_gate 互斥锁）：每个 PHASE_COMPLETED 只能发送一次，防止重复触发导致流水线状态混乱（`src-tauri/src/vcp_modules/sync/sync_service.rs`）。
- 阶段推进保护：每个 reconnect attempt 拥有独立 tracker、计数器和任务集合；旧 attempt 的阶段命令会因 `attempt_id` 不匹配被丢弃。
- Phase 3 完整性：响应 Topic 集合必须与请求完全一致；Push/Pull 批错误、缺失结果或公共帧 `ok=false` 会失败当前 attempt，不能继续 `mark_completed`。
- 有界等待：所有阶段均不设置基于完成数的额外看门狗；HTTP 请求由共享 Client 限制为从连接开始到响应体读取完成最多 270 秒，Phase 3 Pull 还要求相邻 NDJSON chunk 不得空闲超过 30 秒。Finalize 后 30 秒未收到桌面端最终 ACK 会失败，所有 WS send/close 为 5 秒 deadline。
- 断线重连保护：pending_diff_batches 在 Phase 3 启动前清空，旧 attempt 的任务先 cancel + join，再开始新 attempt。

---

## 10. 幂等性机制

### 10.1 设计目的

在移动网络环境下，HTTP 请求可能因网络抖动而超时，但服务器端实际已处理完成。若客户端盲目重试，可能导致数据重复写入。幂等性机制通过唯一操作标识（Idempotency Key）确保同一操作在 TTL 窗口内只执行一次。

### 10.2 幂等 Key 生成公式

移动端对单 Owner 的 `/entities/push` 请求通过 `generate_owner_push_idempotency_key` 生成 Key：

```rust
fn generate_owner_push_idempotency_key(
    owner_type: OwnerType,
    owner_id: &str,
    config_hash: &str,
    updated_at: i64,
) -> String {
    let owner_type = owner_type.as_str();
    let version = updated_at.to_be_bytes();
    let mut hasher = Sha256::new();
    hasher.update(b"VCPMobileSync.OwnerPush.Idempotency.v1");
    for field in [
        owner_type.as_bytes(),
        owner_id.as_bytes(),
        config_hash.as_bytes(),
        version.as_slice(),
    ] {
        hasher.update((field.len() as u64).to_be_bytes());
        hasher.update(field);
    }
    finalize_sha256_hex(hasher)
}
```

字段使用固定顺序，并分别以 `u64` 大端长度作为前缀；编码逻辑留在 Owner Push 领域内，
没有重新引入公共的多切片 helper。

公式解析：

| 组成部分 | 来源 | 作用 |
|---------|------|------|
| domain | 固定 Owner Push 协议标识与版本 | 隔离其他端点或未来编码版本 |
| owner_type | `OwnerType::Agent / Group` | 防止跨类型 ID 冲突 |
| owner_id | 实体唯一标识 | 定位目标实体 |
| config_hash | 当前配置 DTO Hash | 绑定规范化配置内容 |
| updated_at | 与 DTO/Hash 匹配的持久化 Owner 版本 | 区分新的写入意图，包括 `A → B → A` |

Key 生成不读取当前时间。发送前会校验数据库中的 `config_hash` 仍与待发送 DTO 一致，
再取同一 live Owner 行的 `updated_at`。相同快照的跨分钟重试仍复用 Key；配置内容或版本
变化时生成新 Key。该版本沿用现有毫秒级 LWW 时间，并未新增严格单调 revision。

### 10.3 TTL 策略

桌面端幂等性控制实现于 `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/core/idempotency.js`：

| 属性 | 值 |
|------|-----|
| TTL 时长 | 300000ms（5 分钟） |
| 存储结构 | Map<opId, { timestamp, result }> |
| 清理周期 | 每 60 秒执行一次自动清理 |
| 清理逻辑 | 遍历 recentOperations，删除 now - timestamp > IDEMPOTENCY_TTL 的条目 |

TTL 只控制桌面端缓存保留时间，不进入 Key。缓存过期或桌面进程重启后，即使移动端再次
发送同一个稳定 Key，桌面端也会重新执行写入。

### 10.4 生效端点

目前幂等性机制仅在以下端点生效：

| 端点 | 方法 | 幂等头部 |
|------|------|---------|
| `/entities/push`（Owner） | POST | `x-idempotency-key` |

检测与返回逻辑（`VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/transport/routes.js`）：

桌面端接收到请求后，提取 x-idempotency-key 头部，调用 checkIdempotency(opId)。若判定为重复请求，直接返回上次缓存的结果，不执行业务逻辑；若为新请求，执行业务后调用 recordOperation(opId, result) 记录结果，供后续幂等命中。

---

## 11. 版本兼容性策略

### 11.1 版本协商机制

本协议采用严格版本匹配策略，不支持向前/向后兼容的松散协商。

协商流程：

1. Mobile 发送 `{ mobileVersion, protocolVersion: "1.4" }`。
2. Desktop 返回 `{ pluginVersion: "1.4.0", protocolVersion: "1.4" }`。
3. Mobile 按 `protocolVersion` 仲裁兼容性；`pluginVersion` 只作诊断。

### 11.2 版本不匹配处理

| 场景 | 移动端行为 | 桌面端行为 |
|------|-----------|-----------|
| 版本一致 | 继续同步流程 | 正常响应 |
| 版本不一致 | 断开 WS，状态置为 error，提示用户更新插件 | 不处理（版本校验由移动端主导） |
| 超时未收到 VERSION_ACK | 断开 WS，状态置为 error | 无有效响应 |

### 11.3 配对版本边界

- 当前 Wire 基线为 1.4，桌面插件包为 1.4.0。最终 `PHASE_ACK` 必须原样回显 `phase/sessionId/attemptId/nonce`。
- 不支持 1.1/1.2 混跑；升级和回滚都必须成对交付桌面插件/CDS 与 APK。
- 中央索引是桌面端的默认路由。`MobileSyncUseCentralIndex` 优先级必须为插件显式值 > 全局 Facade 值 > 默认 `true`，因此插件显式 `false` 不得被全局默认覆盖。路由只在重启后的新 session 中决定，attempt 中不做 local/central legacy 回退。
- CDS 服务 protocol 为 `3`；Mobile Wire 为 `1.4`，两者不是同一版本字段。CDS 同步接口统一位于 `/v3/sync/*`。

消息 canonical golden 仍用于校验 DTO 归一化与指纹；Wire 帧版本不写入该样本。完整错误字段、分类、传输外壳和扩展规则见 `16_Wire_1.4错误契约与排障规范.md`。

---

## 附录 A：相关文件索引

| 文件路径 | 职责 |
|---------|------|
| `src-tauri/src/vcp_modules/sync/sync_service.rs` | 移动端同步会话主控、WS 消息循环、HTTP 任务派发 |
| `src-tauri/src/vcp_modules/sync/sync_pipeline/pipeline.rs` | 阶段推进命令中继 |
| `src-tauri/src/vcp_modules/sync/sync_pipeline/phase1_metadata.rs` | Phase 1/2 清单构建封装 |
| `src-tauri/src/vcp_modules/sync/sync_pipeline/phase3_message.rs` | Phase 3 消息哈希查询与批次构建 |
| `src-tauri/src/vcp_modules/sync/sync_types.rs` | 强类型身份、Manifest、Diff 与删除帧 |
| `src-tauri/src/vcp_modules/sync/sync_executor/pull_executor.rs` | Pull 操作执行器（下载实体/消息） |
| `src-tauri/src/vcp_modules/sync/sync_executor/push_executor.rs` | Push 操作执行器（上传实体/消息、幂等 Key 生成） |
| `src-tauri/src/vcp_modules/sync/sync_executor/delete_executor.rs` | 软删除操作执行器 |
| `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/transport/websocket.js` | 桌面端 WebSocket 服务器与连接管理 |
| `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/transport/routes.js` | 桌面端 HTTP 路由注册与中间件 |
| `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/manifest.js` | 清单生成与差异比对逻辑 |
| `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/diff.js` | 批量哈希比对与消息差异计算 |
| `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/core/idempotency.js` | 幂等性控制核心 |
| `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/index.js` | 桌面端插件主入口、消息路由分发 |

---

## 附录 B：核心常量汇总

| 常量名 | 值 | 定义位置 |
|--------|-----|----------|
| WIRE_PROTOCOL_VERSION | 1.4 | sync_service.rs |
| VERSION_CHECK_TIMEOUT | 5 秒 | sync_service.rs |
| WS_OPERATION_TIMEOUT | 5 秒 | sync_service.rs |
| SYNC_HTTP_REQUEST_TIMEOUT | 270 秒（每个 HTTP 请求的总时限） | sync_service.rs |
| FINAL_ACK_TIMEOUT | 30 秒 | sync_service.rs |
| MAX_MESSAGES_PER_BATCH | 10000 | sync_service.rs |
| MAX_RETRIES | 3 | sync_service.rs |
| IDEMPOTENCY_TTL | 300000 毫秒 | core/idempotency.js |
| `/entities/*` Body Limit | 10 MiB | transport/routes.js |
| `/avatars/push` Body Limit | 20 MiB | transport/routes.js |
| Message NDJSON Line Limit | 32 MiB | 双端执行器 |

---

*本文档对应 Wire 1.4 当前生产合同。*

---

*最后更新：2026-08-26 | VCP Mobile v1.1.5*
