---
title: 冲突解决与边界情况
scope: 双端
related_files:
  - src-tauri/src/vcp_modules/sync/sync_service.rs
  - src-tauri/src/vcp_modules/sync/sync_executor/*.rs
  - VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/manifest.js
  - VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/entity.js
---

# 冲突解决与边界情况

## 1. 引言

在分布式双端同步架构中，冲突（Conflict）是不可避免的物理现实。移动端与桌面端各自维护独立的 SQLite / JSON 存储，任何一方的网络延迟、用户操作或后台任务都可能在同一时刻修改同一实体。VCP Mobile 的同步协议采用**最终一致性（Eventual Consistency）**模型，通过三阶段流水线（Phase 1 Owner Metadata → Phase 2 Topic Metadata → Phase 3 Messages）将冲突检测与解决限制在可控的粒度内。

冲突解决的核心策略是 **LWW（Last Write Wins，后写胜出）**，辅以**软删除（Soft Delete）双向传播**、**幂等性机制（Idempotency）**和**双哈希比对（Dual-Hash Validation）**作为保险层。本文档将逐层拆解这些机制在代码中的具体实现、决策矩阵与边界行为。

### 1.1 冲突的类型与检测粒度

VCP Mobile 的同步冲突按检测粒度分为两层：

| 层级 | 检测对象 | 冲突判定依据 | 仲裁位置 |
|------|---------|-------------|---------|
| 配置层（Config） | Agent / Group / Topic 元数据 | `config_hash` + `updated_at` | 桌面端 `manifest.js` |
| 内容层（Content） | Topic 下的消息集合 | `content_hash`（聚合哈希） | 桌面端 `diff.js` |

配置层冲突由 Phase 1/2 的 Manifest Diff 解决；内容层冲突由 Phase 3 的 Message Diff Batch 解决。附件 Hash 只随消息元数据和指纹传播，二进制 CAS 不参与跨端同步或仲裁。

（`src-tauri/src/vcp_modules/sync/sync_types.rs`、`VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/diff.js`）

---

## 2. LWW（Last Write Wins）策略

### 2.1 时间戳比较的完整规则

Mobile 发送 `SYNC_MANIFEST_REQUEST` 后，Desktop 对每个强类型状态执行以下比较链：

```
1. 若 remote.deletedAt 存在：
     → Desktop 未删除或无记录： action = PUSH_DELETE
     → 本地已删除：跳过
2. 若本地无记录：
     → action = PUSH（Mobile 推给 Desktop）
3. 若本地已删除但远端未删除：
     → action = PULL_DELETE（Mobile 拉取 Desktop 墓碑）
4. 否则（双方均存在且未删除）：
     a. 比较 configHash：
        - remote.updatedAt > local.updatedAt → PUSH
        - otherwise → PULL
     b. 若 manifestType 为 owner，额外比较 contentHash：
        - 不匹配则标记 `contentHashMismatch=true`
```

配置时间相等时决策为 `PULL`，即 Desktop 配置获胜；消息平局另按 `messageHash` 字典序仲裁。

（`VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/manifest.js`）

### 2.2 deleted_at 参与比较的特殊处理

`deletedAt` 在 Manifest 中优先于 live Hash；Mobile 墓碑输出 `PUSH_DELETE`，Desktop 墓碑输出 `PULL_DELETE`。

| 条件 | 桌面端决策 | 移动端行为 |
|------|-----------|-----------|
| Mobile 墓碑存在，Desktop 未删除 | `PUSH_DELETE` | Mobile 通知 Desktop 删除 |
| Desktop 墓碑存在，Mobile 未删除 | `PULL_DELETE` | Mobile 幂等落墓碑 |
| 双方 `deletedAt` 均存在 | 跳过 | 无操作 |

软删除使用时间戳标记而不是从同步状态中移除记录。Mobile 的所有更新均携带完整复合身份；Desktop 的 Legacy/CDS 后端分别把同一身份写入自己的墓碑列。

### 2.3 判定矩阵

以下矩阵以**远端状态**为行、**本地状态**为列，展示 `handleSyncManifest` 的完整决策：

| 远端＼本地 | 不存在 | 存在且未删除 | 存在且已删除 |
|-----------|--------|-------------|-------------|
| **不存在** | — | `PULL` | `PULL_DELETE` |
| **存在且未删除** | `PUSH` | 比较 `updatedAt` 与专用 Hash | `PULL_DELETE` |
| **存在且已删除** | `PUSH_DELETE` | `PUSH_DELETE` | 跳过 |

**补充说明**：
- `PUSH` 表示"移动端应向桌面端推送该实体"。
- `PULL` 表示"移动端应从桌面端拉取该实体"。
- `PULL_DELETE` 表示 Desktop 墓碑获胜，Mobile 应软删除。
- `PUSH_DELETE` 表示"移动端应通知桌面端该实体已在本地删除"。

（`VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/manifest.js`）

### 2.4 双哈希职责

Agent/Group/Topic 的 `config_hash` 与 `content_hash` 分别表示配置和内容状态。`manifest.js` 使用以下字段：

```javascript
const remoteConfig = remote.configHash;
const remoteContent = remote.contentHash;
const localConfig = local.configHash;
const localContent = local.contentHash;
```

只有 `localConfig !== remoteConfig` 时进入配置 LWW；`contentHashMismatch` 只引导 Topic Validation。
Avatar 不参与这套层级聚合，直接以自身 `binaryHash` 进入同一 LWW 裁决。

（`VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/manifest.js`）

---

## 3. 消息级 LWW

Phase 3 的每条消息状态为：

同一消息的状态二选一：live 为

```json
{ "messageHash": "sha256", "updatedAt": 1700000000000 }
```

deleted 为

```json
{ "deletedAt": 1700000000001 }
```

同 ID、不同 Hash 的 live 消息按以下顺序仲裁：

1. Mobile `updatedAt` 较新：`pushTopic=true`。
2. Desktop `updatedAt` 较新：消息 ID 加入 `pullMessageIds`。
3. 时间相同：Hash 字典序较大者胜出。

消息 Hash 不包含 `updatedAt`。Mobile 本地编辑推进 `messages.updated_at`；双向消息传输保留该值。Desktop 下载消息时以索引中的最终更新时间覆盖物理 history 的 `updatedAt`。

Desktop 摄取物理 history 时按以下顺序确定最终时间：

1. 合法的物理 `updatedAt`；
2. 新消息的 `timestamp`；
3. 已有消息 Hash 未变时保留索引时间；
4. 已有消息 Hash 已变时使用检测时间。

Phase 3 对同一 Topic 按桌面墓碑、Pull、写队列 `flush`、整 Topic Push 执行。Pull 或落库失败时终止当前 attempt，不执行 Push。

---

## 4. 软删除双向传播

### 4.1 PULL_DELETE vs PUSH_DELETE

两个删除动作按数据流方向命名：

| 动作 | 发起端 | 接收端行为 | 通信方式 |
|------|--------|-----------|---------|
| `PULL_DELETE` | Desktop Manifest | Mobile 软删除对应实体 | 通过 `SYNC_MANIFEST_RESULT` 返回 |
| `PUSH_DELETE` | Mobile Manifest | Desktop 软删除 | Mobile 发送 `SYNC_ENTITY_DELETE`；后续 Manifest 继续重放 |
| `SYNC_ENTITY_DELETE` | 移动端（用户主动删除） | 桌面端通过 WS 接收，软删除本地索引 | 实时 WebSocket |

Mobile 对两种删除动作均先落本地对应状态；只有 `PUSH_DELETE` 需要通知 Desktop：

```rust
if action == PULL_DELETE || action == PUSH_DELETE {
    // 执行软删除
    DeleteExecutor::soft_delete(target);
    if action == PUSH_DELETE {
        tx.send(SyncCommand::NotifyDelete { target });
    }
}
```

（`src-tauri/src/vcp_modules/sync/sync_service.rs`）

### 4.2 deleted_at 字段在 Diff 中的角色

`deleted_at` 在 Manifest 条目中的传递路径：

```
移动端 DB (agents/groups.deleted_at)
  → Phase1Metadata::build_owner_manifest (联合查询)
  → SYNC_MANIFEST_REQUEST (items)
  → handleSyncManifest (remote.deletedAt)
  → SYNC_MANIFEST_RESULT (action: PULL_DELETE/PUSH_DELETE)
  → 按 ownerType 分派 Agent/Group 领域删除
```

`deleted_at` 不参与 live 数据的 LWW 时间戳竞争；存在墓碑时进入删除传播链。

### 4.3 墓碑保持

当前协议没有恢复操作。普通 upsert、物理文件重新出现或重新扫描都不能清除同 ID 墓碑。用户重新创建业务对象时生成新 ID；安全期清理只处理正文和可重建缓存，不承担恢复语义。

Desktop 摄取若发现 `history.json` 重新包含已持久化墓碑的消息 ID，会保留墓碑并以现有 CAS 原子写路径移除该物理条目；Mobile Push 若把同一 ID 作为 live 再次提交，则该 Topic 失败并要求重新进行差异计算。

（`src-tauri/src/vcp_modules/sync/sync_executor/delete_executor.rs`）

### 4.4 HashAggregator 冒泡机制

软删除不仅影响被删实体本身，还会通过 HashAggregator 向上冒泡（Bubble），影响父级实体的 `content_hash`：

```
soft_delete_topic(T1)
  → 重新计算 Topic 的 content_hash
  → 按 T1 的 ownerType 重新计算所属 Agent 或 Group 的 content_hash
```

冒泡与墓碑更新在同一事务链中使用完整 Owner/Topic 身份；不会先按裸 `topic_id` 更新再补猜父级。

**设计意图**：删除操作必须立即反映在父级哈希中，否则 Phase 2.5 的双哈希比对将无法感知 Topic 数量的变化，导致下一轮同步仍然认为双方一致。

（`src-tauri/src/vcp_modules/sync/sync_executor/delete_executor.rs`、`src-tauri/src/vcp_modules/sync/sync_hash.rs`）

---

## 5. 幂等性机制

### 5.1 幂等 Key 生成公式

移动端在上传实体（Agent / Group）时，通过 `PushExecutor` 生成幂等键：

```
idempotency_key = SHA256(frame(domain, owner_type, owner_id, config_hash, updated_at))
```

Rust 实现：

```rust
fn generate_owner_push_idempotency_key(
    owner_type: OwnerType,
    owner_id: &str,
    config_hash: &str,
    updated_at: i64,
) -> String {
    let owner_type = owner_type.as_str();
    let version = updated_at.to_be_bytes();
    let mut hasher = Sha256::new();
    hasher.update(b"VCPMobileSync.OwnerPush.Idempotency.v1");
    for field in [
        owner_type.as_bytes(),
        owner_id.as_bytes(),
        config_hash.as_bytes(),
        version.as_slice(),
    ] {
        hasher.update((field.len() as u64).to_be_bytes());
        hasher.update(field);
    }
    finalize_sha256_hex(hasher)
}
```

（`src-tauri/src/vcp_modules/sync/sync_executor/push_executor.rs`）

每个字段使用 `u64` 大端长度前缀。Key 不再读取当前时间；相同配置快照跨分钟重试仍
命中同一 Key。`configHash` 绑定配置内容，持久化 `updatedAt` 区分新的写入意图，因此
正常的 `A → B → A` 会得到新 Key。`updatedAt` 仍是毫秒级 LWW 时间，不是严格单调
revision；其同值边界见 §7.3。

### 5.2 5 分钟 TTL 策略

桌面端 `core/idempotency.js` 维护内存中的操作缓存：

```javascript
const recentOperations = new Map();
const IDEMPOTENCY_TTL = 300000; // 5 分钟
```

检查流程：
1. 收到单 Owner `/entities/push` 请求，提取 `x-idempotency-key`；
2. 调用 `checkIdempotency(opId)`；
3. 若存在且未过期（< 5 分钟），直接返回上次结果，不执行写入；
4. 若不存在或已过期，执行 `uploadEntity`，成功后 `recordOperation(opId, result)`。

（`VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/core/idempotency.js`）

### 5.3 在 HTTP /entities/push 上的应用

`PushExecutor::push_agent` 的 HTTP 请求构造：

```
POST /api/mobile-sync/entities/push
Headers:
  Authorization: Bearer <token>
  x-idempotency-key: <sha256_key>
Body:
  { "items": [{ "entityType":"owner", "ownerType":"agent", "ownerId":"A1", "data": AgentSyncDTO }] }
```

**应用范围**：该幂等键覆盖单 Owner Push。Topic/Message 重试由完整身份与领域写入语义收敛。

### 5.4 清理任务与内存泄漏防护

桌面端幂等缓存基于内存 `Map`，存在两项防护机制：

1. **TTL 过期自动清理**：每 60 秒执行一次扫描，删除超过 5 分钟的记录；
2. **进程重启清空**：缓存仅存于 Node.js 堆内存，插件重启后全部失效。

（`VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/core/idempotency.js`）

---

## 6. 具象同步场景

### 场景 1：新建 Agent（首次同步）

**初始状态**：
- 桌面端：已存在 Agent `A1`，`updated_at = T1`，`config_hash = H1`
- 移动端：空数据库（首次安装）

**操作序列**：
1. 用户点击"开始同步"。
2. 移动端 `Phase1Metadata::build_owner_manifest` 联合查询本地 Agent/Group，返回空数组。
3. Mobile 发送 Owner `SYNC_MANIFEST_REQUEST`。
4. 桌面端 `handleSyncManifest`：远端无记录，本地存在 `A1`。
5. Desktop 返回 `SYNC_MANIFEST_RESULT`：`[{ ownerType:"agent",ownerId:"A1",action:"PULL" }]`。

**Diff 结果**：

| 实体 | 动作 | 理由 |
|------|------|------|
| A1 | PULL | 移动端无记录，需从桌面端拉取 |

**最终状态**：移动端通过 `PullExecutor::pull_entities_batch` 下载 Agent DTO，经 `DbWriteQueue` 写入 SQLite；写入事务同时计算配置 Hash。

实体 Pull 请求使用 `{entityType:"owner", ownerType:"agent", ownerId:"A1"}`；结果回显同一完整身份并在 `ok:true` 分支携带 Agent DTO。

**涉及代码**：
- `src-tauri/src/vcp_modules/sync/sync_pipeline/phase1_metadata.rs`
- `src-tauri/src/vcp_modules/sync/sync_executor/pull_executor.rs`
- `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/manifest.js`

### 场景 2：双向修改冲突

**初始状态**：
- 桌面端 Agent `A1`：`updated_at = 1000`，`config_hash = H_old`
- 移动端 Agent `A1`：`updated_at = 2000`，`config_hash = H_new`

**操作序列**：
1. 用户在桌面端修改 `A1`（`updated_at = 1000`）。
2. 用户在移动端修改同一 Agent（`updated_at = 2000`）。
3. 同步启动，移动端发送 Manifest。
4. Desktop 比对：`remote.updatedAt = 2000 > local.updatedAt = 1000`。

**Diff 结果**：

| 实体 | 动作 | 理由 |
|------|------|------|
| A1 | PUSH | 移动端时间戳更新，桌面端应接受移动端版本 |

**最终状态**：桌面端 `uploadEntity` 接收移动端 DTO，通过 `applyAgentDTO` 局部覆盖配置，保留桌面端特有字段（如 `topics` 数组）。移动端无需拉取。

**最终状态细节**：
桌面端 `entity.js` 中的 `handleAgentUpload` 执行局部合并：

```javascript
if (!fileReadSuccess) {
    config = createAgentConfig(id, data);  // 新建
} else {
    applyAgentDTO(config, data);           // 局部覆盖，保留 topics
}
```

**关键约束**：`applyAgentDTO` 明确不触碰 `config.topics` 数组，Topic 同步由独立 Phase 2 负责，防止配置同步与消息同步交叉污染。

**涉及代码**：
- `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/manifest.js`
- `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/entity.js`
- `src-tauri/src/vcp_modules/sync/sync_executor/push_executor.rs`

### 场景 3：时间戳碰撞仲裁

**初始状态**：
- 桌面端 Agent `A1`：`updated_at = 1000`，`config_hash = H_d`
- 移动端 Agent `A1`：`updated_at = 1000`，`config_hash = H_m`（`H_m ≠ H_d`）

**操作序列**：
1. 双端几乎同时修改，时间戳均为 `1000`。
2. 同步启动，Desktop 比对：双方 `updatedAt = 1000`。
3. 进入 `else` 分支。

**Diff 结果**：

| 实体 | 动作 | 理由 |
|------|------|------|
| A1 | PULL | 配置时间相等，Desktop 胜出 |

**最终状态**：这是实体配置仲裁，不是消息仲裁。实体配置时间相同时桌面端胜出，移动端拉取 `H_d`；消息平局使用第 3 节的 Hash 字典序规则。

**涉及代码**：
- `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/manifest.js`

### 场景 4：删除传播

**初始状态**：
- 桌面端：Agent `A1` 已软删除，`deleted_at = 5000`
- 移动端：Agent `A1` 仍存在，`deleted_at = null`

**操作序列**：
1. 用户在桌面端删除 `A1`。
2. 移动端尚未同步。
3. 同步启动，移动端发送 Manifest（含 `A1`，`deletedAt: null`）。
4. 桌面端比对：`remote.deletedAt = null`，但 `local.deletedAt = 5000`。
5. 桌面端返回 `PULL_DELETE`（Desktop 墓碑应由 Mobile 拉取）。

**Diff 结果**：

| 实体 | 动作 | 理由 |
|------|------|------|
| A1 | PULL_DELETE | Desktop 已删除，Mobile 应落同一墓碑 |

**最终状态**：
1. 移动端执行 `DeleteExecutor::soft_delete_agent(A1, deletedAt=5000)`，保留桌面端的稳定墓碑时间；
2. `PULL_DELETE` 不回发删除通知；下一轮双方都携带墓碑时直接跳过。

Mobile 墓碑获胜时才使用 `SYNC_ENTITY_DELETE`，帧中以 `targetType` 判别 Owner/Topic/Avatar/Message，并携带对应完整身份和 `deletedAt`。Desktop 接收后幂等写入墓碑；成功不返回额外业务 ACK，失败返回 `SYNC_ERROR`。

**涉及代码**：
- `src-tauri/src/vcp_modules/sync/sync_service.rs`
- `src-tauri/src/vcp_modules/sync/sync_executor/delete_executor.rs`
- `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/manifest.js`

### 场景 5：带附件的消息同步

**初始状态**：
- Topic `T1` 在桌面端有消息 `M1`，含附件 `att.jpg`（`hash = H_att`）
- 移动端 `T1` 无 `M1`

**操作序列**：
1. Phase 3 发送 `SYNC_MESSAGE_DIFF_REQUEST`，包含 T1 的消息状态。
2. 桌面端 `handleSyncMessageDiff` 比对：发现 `M1` 在移动端不存在。
3. Desktop 返回 `{ok:true,pullMessageIds:["M1"],pushTopic:false,deleteMessages:[]}`。

**Diff 结果**：

| Topic | pullMessageIds | pushTopic | 理由 |
|-------|--------|--------|------|
| T1 | [M1] | false | 移动端缺少 M1 |

**最终状态**：
1. 移动端 `PullExecutor::pull_messages_batch` 流式拉取 `M1`；
2. `process_topic_messages` 解析消息时，发现附件 `hash = H_att`；
3. 批量查询 `attachments` 表，发现本地无 `H_att`；
4. 消息和附件关系写入本地数据库，路径保持为空，状态投影为 `desktop_only`。

**附件路径的占位策略**：
`process_topic_messages` 在解析每条消息的附件时，执行批量路径查询：

```rust
let query = format!(
    "SELECT hash, internal_path FROM attachments WHERE hash IN ({})",
    placeholders
);
```

若本地已有同 Hash 文件，则填充真实路径并投影为 `ready`；否则保留空路径和 `desktop_only`。同步协议不提供附件下载或补传任务。

**涉及代码**：
- `src-tauri/src/vcp_modules/sync/sync_executor/pull_executor.rs`
- `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/diff.js`

---

## 7. 边界情况清单

### 7.1 空数据库首次连接

**现象**：移动端全新安装，首次连接桌面端。

**行为**：
- Phase 1：所有 Agent / Group / Avatar 均走 `PULL`；
- Phase 2：所有 Topic 均走 `PULL`；
- Phase 3：所有消息均走 `PULL`。

**性能保障**：
- 实体批量下载：`PullExecutor::pull_entities_batch` 一次 HTTP 请求拉取最多 50 个 Agent/Group 或 1000 个 Topic；
- 消息 Pull：`/messages/pull` 逐 Topic 返回 NDJSON，Mobile 按原始帧字节分配在途预算。

**涉及代码**：
- `src-tauri/src/vcp_modules/sync/sync_executor/pull_executor.rs`

### 7.2 同步中途网络中断

**现象**：WebSocket 在 Phase 2 或 Phase 3 中途断开。

**行为**：
1. `sync_service.rs` 外层 loop 捕获连接错误；
2. 指数退避重试：首次 500ms，每次翻倍，上限 5 秒，最多重试 3 次；
3. 若重试耗尽，发布 `vcp-sync-status` = `error`，前端提示用户；
4. 重新连接后，整个同步流水线从头开始（Manifest 重新发送）；单实体上传通过幂等键防重，批量接口按实体身份重新执行收敛写入。

**涉及代码**：
- `src-tauri/src/vcp_modules/sync/sync_service.rs`

### 7.3 TTL 内重复操作

**现象**：网络抖动导致同一配置请求重复到达，或者用户在桌面端 5 分钟缓存窗口内再次修改同一实体。

**行为**：
- 同一 `configHash + updatedAt` 配置快照生成相同 `idempotency_key`，跨分钟重试也会直接返回上次结果；
- 配置内容改变会更新 `configHash + updatedAt`；正常的 `A → B → A` 回到相同内容时，新的 `updatedAt` 会生成不同 Key并执行新写入；
- TTL 过期或桌面进程重启后，缓存记录消失，相同 Key 会重新执行。

**已知同值边界**：`updatedAt` 来自现有毫秒时钟。若多次配置变更发生在同一毫秒，或
系统时钟回拨后恰好复用旧值，第二次 A 仍可能与第一次 A 生成相同 Key。此次减负不为
这一低概率场景引入全局单调 revision 或持久 outbox；桌面缓存过期后会自然重新执行。

**其他接口**：Topic/Message 不携带该 Key，重复请求由完整身份和领域写入语义收敛。

**涉及代码**：
- `src-tauri/src/vcp_modules/sync/sync_executor/push_executor.rs`
- `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/core/idempotency.js`

### 7.4 损坏配置的保护

**现象**：桌面端 `config.json` 文件损坏（空文件、非法 JSON、数组而非对象）。

**行为**：
`uploadEntity` 在写入前执行严格校验：

```javascript
try {
    const content = await fs.readFile(configPath, "utf-8");
    config = JSON.parse(content);
    if (typeof config !== "object" || config === null || Array.isArray(config)) {
        throw new Error("Invalid config structure");
    }
    fileReadSuccess = true;
} catch (e) {
    if (e.code !== "ENOENT") {
        // 文件存在但损坏，拒绝覆盖
        throw new Error(`Cannot upload to corrupted config: ${e.message}`);
    }
}
```

**后果**：损坏配置拒绝接受同步更新，防止合法数据被空/错误结构覆盖。修复需用户手动清理桌面端对应目录。

**涉及代码**：
- `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/entity.js`

### 7.5 多设备同时在线（已知限制）

**现象**：用户拥有多台移动设备（如手机 + 平板），同时连接同一桌面端。

**当前限制**：
- 协议为**双端设计**（桌面端 ↔ 单个移动端），未实现多移动端仲裁；
- 若设备 A 和设备 B 同时修改同一实体，桌面端按"最后到达的 Manifest"处理，可能导致 A 与 B 的最终状态不一致；
- 消息级同步通过 Topic Hash 快速路径减少冲突，但无法完全消除写丢失（Write Loss）。

**缓解措施**：
- 高频率同步（连接即触发）缩小冲突窗口；
- 双哈希比对确保内容级不一致至少能被检测到（`changedTopics` 机制）；
- 建议用户避免在多台设备上同时编辑同一 Agent/Topic。

**涉及代码**：
- `src-tauri/src/vcp_modules/sync/sync_service.rs`
- `VCPChat/VCPDistributedServer/Plugin/VCPMobileSync/sync/diff.js`

### 7.6 超大消息负载分批

**现象**：某个 Topic 包含数万条消息（如长期聊天记录），单次 WS 消息可能超过 2 MB，导致移动端内存压力或 WS 帧截断。

**行为**：
移动端在 Phase 3 启动时，将 Topic 按消息数量分批次发送：

```rust
const MAX_MESSAGES_PER_BATCH: usize = 10000;

fn build_diff_batches(topic_states) -> VecDeque<Map<String, Value>> {
    // 每批最多 10000 条消息，约 1.5-2 MB JSON
    if current_msg_count > 0 && current_msg_count + msg_count > MAX_MESSAGES_PER_BATCH {
        batches.push_back(current_batch);
        current_batch = Map::new();
        current_msg_count = 0;
    }
}
```

Desktop 返回 `SYNC_MESSAGE_DIFF_RESULT` 后，Mobile 处理完当前批次才发送下一批：

```rust
let mut pending = pending_diff_batches.lock().await;
if let Some(next_batch) = pending.pop_front() {
    let msg = MessageDiffRequestFrame::new(next_batch);
    let _ = ws_stream.send(Message::Text(msg.to_string().into())).await;
}
```

**内存保护**：
- 10,000 条消息的批次上限确保单次 JSON 解析在移动端可控；
- 断线重连后，`pending_diff_batches` 会被清空，防止发送过时数据。

**涉及代码**：
- `src-tauri/src/vcp_modules/sync/sync_service.rs`

---

*当前硬切基线：VCPMobileSync `1.4.0` + Wire `1.4`。*
