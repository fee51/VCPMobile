---
id: VUE-SYNC-018
title: 同步会话前端
description: VCP Mobile 前端同步 V2 协议的会话管理、状态展示与日志浏览
version: 1.1.4
date: 2026-08-13
---

# 18. 同步会话前端

## 1. 概述

### 1.1 领域定位

`sync/` 前端领域负责将 Rust 后端同步 V2 协议的运行状态以可视化方式呈现给用户，并提供配置入口与历史日志回溯能力。它是用户与"三阶段增量同步协议"交互的唯一 UI 界面，承担以下职责：

- **会话管理**：控制同步面板的打开/关闭、状态机流转、视图切换（实时/历史）
- **状态可视化**：WebSocket 连接状态灯、同步阶段指示器、进度条、终端式日志流
- **安全拦截**：同步启动前的电量与省电模式检测，防止低电量或省电状态下的大批量 IO
- **配置入口**：同步服务器地址、Token、预渲染开关的设置面板集成
- **日志浏览与分享**：本地同步日志文件的分页浏览、关键字高亮、系统文件分享、定期清理

该领域**不**涉及：
- 同步协议本身的哈希计算、差异比对、冲突仲裁（由 Rust `sync_service.rs` 负责）
- 网络层 WebSocket/HTTP 连接的物理维护（由 Rust `vcp_client.rs` 与 `sync_service.rs` 负责）
- 数据库写入与事务管理（由 Rust `db_manager.rs` 与 `db_write_queue.rs` 负责）

### 1.2 模块构成表

| 文件 | 行数 | 职责 |
|------|------|------|
| `src/features/sync/SyncSessionView.vue` | 320 | 同步会话主视图：SlidePage 容器、状态灯、进度条、日志终端、Tab 切换、预渲染开关 |
| `src/core/stores/syncSession.ts` | 179 | 同步会话状态管理 Store：状态机、日志缓冲、进度追踪、Tauri 事件监听注册与清理 |
| `src/features/settings/components/SyncSettingsSection.vue` | 102 | 同步配置设置：服务器 URL、Token、表情包修复、全量同步入口按钮 |
| `src/features/settings/components/SyncLogBrowserCore.vue` | 180 | 历史日志浏览器：文件列表、分页内容阅读、关键字高亮、旧日志清理 |
| `src/core/stores/overlay.ts` | — | 全局覆盖层管理：将 SyncSession 纳入 `pageStack` 统一管控，处理 SlidePage 层级与关闭回退 |
| `src/core/composables/useScreenKeeper.ts` | 60 | 屏幕常亮锁：同步期间保持屏幕点亮，防止 Android 休眠导致连接中断 |
| `src/core/composables/useDataReload.ts` | 41 | 数据重载逻辑：同步完成后清理 AST 缓存、刷新 Agent/Group/Topic/消息数据 |

### 1.3 在整体架构中的位置

```
┌─────────────────────────────────────────────────────────────┐
│                      Vue 3 前端层                            │
│                                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              sync/ 前端领域 (本域)                      │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │  │
│  │  │SyncSession   │  │syncSession   │  │SyncSettings │  │  │
│  │  │   View.vue   │◄─┤   Store.ts   │◄─┤  Section.vue│  │  │
│  │  │  (视图层)     │  │  (状态中枢)   │  │  (配置入口)  │  │
│  │  └──────┬───────┘  └──────┬───────┘  └─────────────┘  │  │
│  │         │                 │                            │  │
│  │         └─────────────────┼────────────────────┐       │  │
│  │                           │                    │       │  │
│  │              ┌────────────┴────────────┐      │       │  │
│  │              ▼                         ▼      ▼       │  │
│  │  ┌─────────────────────┐  ┌────────────────────────┐  │  │
│  │  │SyncLogBrowserCore   │  │ useScreenKeeper /      │  │  │
│  │  │      .vue           │  │ useDataReload          │  │  │
│  │  │  (日志回溯)          │  │ (系统能力/数据刷新)     │  │  │
│  │  └─────────────────────┘  └────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│                           │                                  │
│         ┌─────────────────┼─────────────────┐               │
│         ▼                 ▼                 ▼               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐     │
│  │ overlayStore│  │settingsStore│  │  Tauri Events   │     │
│  │ (层级管理)   │  │ (配置持久化) │  │ (状态/进度事件) │     │
│  └─────────────┘  └─────────────┘  └─────────────────┘     │
└──────────────────────────┬──────────────────────────────────┘
                           │ IPC (Tauri Commands + Events)
┌──────────────────────────▼──────────────────────────────────┐
│                   src-tauri (Rust 核心层)                    │
│                                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                 sync/ 领域 (后端)                       │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │  │
│  │  │sync_service │  │sync_pipeline│  │sync_executor│   │  │
│  │  │  (会话管理)  │  │  (阶段控制)  │  │  (执行器)    │   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘   │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 1.4 核心设计原则

| 原则 | 说明 |
|------|------|
| **状态集中管理** | 所有同步相关运行时状态（连接状态、进度、日志）收敛于 `syncSessionStore`，视图组件纯渲染，不持有业务逻辑 |
| **事件驱动更新** | 后端通过 Tauri Event 通道向前端推送状态变更，前端无需轮询；`syncSessionStore` 负责事件的注册、过滤与清理 |
| **覆盖层统一管控** | `SyncSessionView` 纳入 `overlayStore.pageStack` 管理，复用 SlidePage 动画、Z-Index 语义化层级、返回键拦截 |
| **电量安全拦截** | 同步启动前强制检测电池电量与省电模式，低于 30% 或处于省电模式时智能拦截，避免数据损坏 |
| **屏幕常亮保障** | 同步期间通过 `useScreenKeeper` 引用计数锁保持屏幕常亮，同时启动 Android 前台服务降低后台被杀概率 |
| **日志窗口截断** | 内存中保留最近 200 条日志，DOM 仅渲染最近 100 条，防止长同步会话导致内存与渲染膨胀 |

---

## 2. 同步会话视图（SyncSessionView.vue）

### 2.1 打开方式（SlidePage）

`SyncSessionView` 是一个全屏覆盖层，通过 `SlidePage` 组件实现从右侧滑入的页面动画。

> 文件位置：`src/features/sync/SyncSessionView.vue`

**打开路径**：

```
用户点击"全量神经同步"按钮（SyncSettingsSection.vue）
           │
           ▼
   overlayStore.openSyncSession()
           │
           ▼
   ┌─────────────────────┐
   │ syncSessionStore    │
   │   .open()           │──> 重置状态、注册监听器
   └──────────┬──────────┘
              │
              ▼
   pageStack.push({ type: 'syncSession', ... })
              │
              ▼
   FeatureOverlays.vue 中 v-if="isSyncSessionOpen"
   渲染 <SyncSessionView :z-index="getPageZIndex('syncSession')" />
              │
              ▼
   SlidePage 播放滑入动画，页面呈现
```

**关闭路径**：

```
用户点击 X 或系统返回键
           │
           ▼
   overlayStore.closeSyncSession()
           │
           ▼
   await syncSessionStore.close() ──> 使 view generation 失效、清理监听器、await stop_sync
           │
           ▼
   stop_sync 已完成 cancel + join 后 pageStack.pop()，SlidePage 播放滑出动画
```

`connecting` 与 `connected` 都属于不可卸载状态：顶部关闭按钮与系统返回键共用 `canDismiss` 门禁，不能先移除页面再让后端同步留在后台。仅 `idle` / `error` / `completed` 可进入关闭流程，并且页面出栈发生在 `stop_sync` 返回之后。

**层级计算**：`SyncSessionView` 的 `z-index` 由 `overlayStore.getPageZIndex('syncSession')` 动态计算。`overlayStore` 维护 `pageStack` 数组，每个页面类型映射到基础层级 `LAYER_PAGE_BASE`（40）加上在栈中的索引偏移。当多个 SlidePage 同时打开时（如先打开设置页再打开同步面板），此机制确保后打开的页面层级更高，视觉覆盖正确。

> 详见 `docs/UI_LAYER_ARCHITECTURE.md` 中 "SlidePage 页面栈" 层级规范。

**按需加载**：`SyncSessionView` 在 `FeatureOverlays.vue` 中使用 `defineAsyncComponent` 按需异步加载：

```ts
const SyncSessionView = defineAsyncComponent(
  () => import('../features/sync/SyncSessionView.vue')
);
```

原因：同步会话属于低频操作，应用生命周期内大多数时间不会打开。异步加载将其 chunk 从主 bundle 中分离，降低应用冷启动时的 JS 解析与执行开销。

### 2.2 组件结构

`SyncSessionView` 通过 `SlidePage` 组件包裹内容。`SlidePage` 本身是一个极简的容器：

> 文件位置：`src/components/ui/SlidePage.vue`

```vue
<Transition name="slide-page">
  <div v-show="props.isOpen" class="fixed inset-0 pointer-events-auto"
       :style="{ zIndex: props.zIndex }">
    <slot />
  </div>
</Transition>
```

`SlidePage` 使用 Vue `<Transition>` 实现滑入/滑出动画：
- **进入**：`transform: translateX(100%) → 0`，持续时间 `0.35s`，缓动曲线 `cubic-bezier(0.32, 0.72, 0, 1)`
- **离开**：`transform: translateX(0) → 100%`，持续时间 `0.3s`，相同缓动曲线

这种曲线在移动端产生"弹性减速"的物理质感，与系统原生转场动画视觉对齐。

`SyncSessionView` 的 DOM 结构采用垂直三段式布局：

```
┌─────────────────────────────────────────┐
│ 顶部栏                                  │  ← 状态 + 点击页签 + 关闭
│ [● 等待] [实时同步][历史日志]       [X]   │
├─────────────────────────────────────────┤
│ 内容区域（仅挂载当前页签）                │
│ ┌───────────────────────────────────┐ │
│ │ 实时：idle 启动页 / 进度 + 日志终端       │ │
│ │ 历史：SyncLogBrowserCore.vue             │ │
│ └───────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ 底部工具栏                               │  ← 状态提示文本 + 复制日志按钮
│ 选择上方操作以继续                [复制]  │
└─────────────────────────────────────────┘
```

**关键样式决策**：
- 背景色使用 `#0a0f14`（极深蓝黑），与同步面板的"神经同步"产品隐喻保持一致
- 顶部栏通过 `env(safe-area-inset-top)` 适配 Android 刘海屏
- 页签紧跟在状态标右侧，按钮最小高度为 44px；选中项仅使用文字亮度和低透明度的 2px 蓝色 Accent Bar，不使用额外背景或圆角块
- 内容区域通过 `v-if/v-else` 仅挂载当前页签，不使用水平滚动、`KeepAlive` 或双页常驻 DOM

### 2.3 连接状态展示

状态通过左上角"状态灯"（圆形指示器）+ 文本标签 + 底部栏文案三层呈现：

| 状态值 | 状态灯样式 | 文本标签 | 底部栏文案 | 可关闭？ |
|--------|-----------|----------|-----------|---------|
| `idle` | `bg-gray-400`（灰色，静止） | `等待` | `选择上方操作以继续` | ✅ |
| `connecting` | `bg-yellow-400 animate-pulse`（黄色，脉冲） | `连接中` | `正在建立神经同步通道...` | ❌ |
| `connected` | `bg-blue-400 animate-pulse`（蓝色，脉冲） | `同步中` | `同步进行中` | ❌ |
| `completed` | `bg-green-400`（绿色，静止） | `已完成` | `同步已完成` | ✅ |
| `completed_with_warnings` | `bg-yellow-400`（黄色，静止） | `有警告` | `同步完成，部分旧版附件需处理` | ✅ |
| `error` | `bg-red-400`（红色，静止） | `失败` | `同步未完成` | ✅ |

状态灯使用 CSS 动画 `animate-pulse` 在活跃状态（`connecting`、`connected`）下产生呼吸效果，向用户传递"正在进行中"的视觉暗示。

> 状态流转由 `syncSessionStore.status` 驱动，`SyncSessionView` 通过 `computed` 属性映射为视觉样式。详见第 3.2 节。

### 2.4 同步进度指示

当 `status !== 'idle'` 时，视图展示进度条与日志终端。

**进度条组件**：

```
┌────────────────────────────────────────┐
│ ▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░ │  ← 过渡动画 width + bg-color
│ 元数据比对                    147/302  │  ← 阶段标签 + 计数器
└────────────────────────────────────────┘
```

进度条使用 `transition-all duration-500` 实现平滑宽度变化，颜色随状态变化：
- 正常运行：`bg-blue-500`
- 完成：`bg-green-500`
- 错误：`bg-red-500`

**阶段映射表**（`phaseLabel computed`）：

| 后端 phase 值 | 前端展示文本 | 所属协议阶段 |
|--------------|-------------|-------------|
| `initialization` | `初始化` | 会话握手 |
| `owner_metadata` | `元数据比对` | Phase 1 |
| `topic_metadata` | `会话主题同步` | Phase 2 |
| `messages` | `历史消息同步` | Phase 3 |

进度数据由后端通过 `vcp-sync-progress` 事件推送，结构为：
```json
{
  "phase": "messages",
  "total": 302,
  "completed": 147,
  "message": ""
}
```

---

## 3. 同步状态（syncSessionStore）

### 3.1 核心状态字段

> 文件位置：`src/core/stores/syncSession.ts`

`syncSessionStore` 使用 Pinia Composition API 风格定义（`defineStore('syncSession', () => { ... })`），全部状态为 `ref`：

| 字段 | 类型 | 初始值 | 说明 |
|------|------|--------|------|
| `isOpen` | `boolean` | `false` | 面板是否打开，驱动 SlidePage 显隐 |
| `canDismiss` | `boolean` | `true` | 是否允许关闭（同步进行中禁止关闭） |
| `status` | `'idle' \| 'connecting' \| 'connected' \| 'error' \| 'completed' \| 'completed_with_warnings'` | `'idle'` | 连接状态机当前值 |
| `activeSessionId` | `number \| null` | `null` | 当前后端 Session owner，所有事件必须匹配 |
| `summary` | `SyncSummary` | 全零 | 成功、总数、失败与旧附件计数 |
| `terminalError` | `SyncTerminalError \| null` | `null` | 类型化安全错误；原始异常不进入此字段 |
| `activeTab` | `'live' \| 'history'` | `'live'` | 当前激活的 Tab |
| `needsReload` | `boolean` | `false` | 同步完成后数据已更新，需刷新标志 |
| `logs` | `Array<{id, level, message, time}>` | `[]` | 实时日志缓冲区 |
| `progressData` | `{phase, total, completed, message}` | `{phase:'initialization',total:0,completed:0,message:''}` | 当前进度 |

**日志缓冲区管理算法**：

```ts
const pushLog = (level: string, message: string) => {
  const id = `${Date.now()}_${Math.random().toString(36).substring(2, 9)}`;
  logs.value.push({ id, level, message, time: new Date().toLocaleTimeString() });
  if (logs.value.length > 200) logs.value.shift();
};
```

- **ID 生成**：时间戳 + 随机字符串，确保同一毫秒内多条日志的 `key` 唯一性（Vue `v-for` 需要）
- **FIFO 截断**：当缓冲区超过 200 条时，从头部移除最旧的日志
- **时间格式**：`toLocaleTimeString()` 使用系统本地时区，显示为 `HH:MM:SS`

**监听器生命周期管理**：

```ts
let viewGeneration = 0;
let listenerSetup: Promise<void> | null = null;
let unlistenFns: UnlistenFn[] = [];

const open = () => {
  const generation = ++viewGeneration;
  cleanupListeners();
  listenerSetup = registerListeners(generation);
};
```

每个监听回调都校验 `viewGeneration`；异步 `listen()` 若在页面关闭或重开后才返回，会立即调用自身 `unlisten`，不得安装到新面板。`startSync()` 先等待同一 `listenerSetup`，避免后端已发首帧而前端监听尚未就绪。关闭时先推进 generation，再清理已安装监听器，所以旧面板事件不能污染新会话。

### 3.2 连接生命周期（未连接/连接中/已连接/断开/错误）

```
                    ┌─────────────┐
         ┌─────────►│    idle     │◄─────────────┐
         │          │   (初始)     │              │
         │          └──────┬──────┘              │
         │                 │ 用户点击"开始同步"   │
         │                 ▼                     │
         │          ┌─────────────┐              │
         │          │ connecting  │              │
         │          │   (连接中)   │              │
         │          └──────┬──────┘              │
         │                 │ vcp-sync-status:    │
         │                 │   status = "open"   │
         │                 ▼                     │
         │          ┌─────────────┐              │
         │          │  connected  │              │
         │          │   (同步中)   │              │
         │          └──────┬──────┘              │
         │                 │ vcp-sync-completed  │
         │                 │ 或 vcp-sync-status: │
         │                 │   status = "error"  │
         │                 ▼                     │
         │     ┌─────────────────────────┐       │
         └─────┤       completed         │       │
               │       (已完成)           ├───────┘
               └─────────────────────────┘
                          │
                          │ vcp-sync-status:
                          │   status = "error"
                          ▼
                    ┌─────────────┐
                    │    error    │
                    │   (错误)     │
                    └─────────────┘
```

**状态机转换规则**：

| 触发条件 | 源状态 | 目标状态 | 副作用 |
|----------|--------|---------|--------|
| 用户点击"开始同步" | `idle` | `connecting` | `canDismiss = false`、清空日志，等待监听注册后调用 `start_manual_sync` |
| 收到 `vcp-sync-status` {status:"open"} | `connecting` | `connected` | `canDismiss = false`（禁止关闭） |
| 收到 `vcp-sync-completed` | `connected` | `completed` | `canDismiss = true`、`needsReload = true`、释放屏幕锁、停止前台服务 |
| 收到带旧附件 warning 的完成事件 | `connected` | `completed_with_warnings` | 分开展示成功/总数/失败/旧附件 warning，可直接重新同步 |
| 收到 `vcp-sync-status` {status:"error"} | `connecting` / `connected` | `error` | `canDismiss = true`、释放屏幕锁、停止前台服务 |
| `start_manual_sync` 调用异常 | `connecting` | `error` | 解析 `SYNC_ERROR:<JSON>`；解析失败使用固定内部错误文案，绝不显示原始 IPC 异常 |
| 电量/省电模式拦截 | `connecting` | `error` | 分别显示固定原因与下一步；电量阈值为 30% |

**重试策略**：后端连接层共享最多 3 次自动重试预算，面板只显示“第 N/3 次自动重试”的用户过程提示。终态失败或警告允许用户点击“重新同步”；Store 会先 `await stop_sync` 完成 cancel + join，然后插入 attempt 分隔线并启动新 Session。旧 Session 的延迟日志因 `sessionId` 不匹配被拒绝。

### 3.3 同步进度与阶段

进度数据通过监听 `vcp-sync-progress` 事件更新：

```ts
listen('vcp-sync-progress', (event: any) => {
  progressData.value = event.payload;
}).then(fn => unlistenFns.push(fn));
```

`progressData` 绑定到进度条与阶段标签。Store 只在 Phase 首次出现和首次完成时追加一条中文过程日志，连续进度帧不会把技术消息逐条渲染到实时终端。

进度百分比计算（前端 `computed`）：
```ts
const progressPercent = computed(() => {
  if (store.progressData.total <= 0) return 0;
  return Math.min(100, Math.round(
    (store.progressData.completed / store.progressData.total) * 100
  ));
});
```

### 3.4 冲突列表管理

当前前端实现中**无独立的冲突列表 UI**。同步 V2 协议的冲突仲裁由后端通过 LWW（Last-Write-Wins）策略自动完成，前端通过以下方式感知冲突结果：

1. **过程反馈**：结构化进度事件驱动简明阶段日志；逐实体冲突与诊断细节仅写入持久日志
2. **错误状态**：无法自动仲裁时，后端发送类型化安全错误；错误码、失败实体与原始 detail 只用于历史日志和支持诊断
3. **数据刷新**：同步完成后 `needsReload = true`，用户关闭面板时触发 `performFullReload()`，前端所有缓存重新加载，冲突结果已体现在新数据中

---

## 4. 同步设置前端

### 4.1 SyncSettingsSection 组件

> 文件位置：`src/features/settings/components/SyncSettingsSection.vue`

`SyncSettingsSection` 是设置页面中的一个配置区块，负责同步相关的持久化参数输入与快捷操作入口。它接收 `settings: AppSettings` 作为 props（由父组件 `SettingsView.vue` 通过 `settingsStore.settings` 注入），采用纯展示/输入模式，不直接调用同步逻辑。

组件结构（自上而下）：

```
┌─────────────────────────────────────────┐
│ HTTP 服务 URL                           │  ← SettingsTextField, mono 等宽字体
│ [http://192.168.x.x:5974        ]       │
├─────────────────────────────────────────┤
│ WebSocket 服务 URL                      │  ← SettingsTextField, mono 等宽字体
│ [ws://192.168.x.x:5975          ]       │
├─────────────────────────────────────────┤
│ Mobile Sync Token                       │  ← SettingsTextField, is-secure 掩码输入
│ [••••••••••••••••••••            ]       │
├─────────────────────────────────────────┤
│ 分隔线                                  │
├─────────────────────────────────────────┤
│ 表情包图床密钥 (fileKey)                │  ← SettingsTextField, is-secure
│ [••••••••••••••••••••            ]       │
├─────────────────────────────────────────┤
│ 表情包修复库                            │  ← SettingsActionWithStatus
│ 从 VCP 服务器同步表情包元数据    [REFRESH]│
├─────────────────────────────────────────┤
│ 全量神经同步                            │  ← SettingsRow + SettingsActionButton
│ 打开全量神经同步面板...      [OPEN PANEL] │
└─────────────────────────────────────────┘
```

### 4.2 同步开关与服务器配置

**配置字段映射表**（`AppSettings` 接口，定义于 `src/core/stores/settings.ts`）：

| 字段 | 类型 | UI 组件 | 说明 |
|------|------|---------|------|
| `syncHttpUrl` | `string` | `SettingsTextField` | 桌面端 HTTP REST 服务地址（用于实体批量传输） |
| `syncServerUrl` | `string` | `SettingsTextField` | 桌面端 WebSocket 服务地址（用于信令与消息流） |
| `syncToken` | `string` | `SettingsTextField` (`is-secure`) | 鉴权 Token，需与桌面端 `config.env` 中的 `MOBILE_SYNC_TOKEN` 一致 |
| `fileKey` | `string` | `SettingsTextField` (`is-secure`) | 表情包图床构造密码，非同步协议核心字段 |
| `syncPrerenderEnabled` | `boolean` | `SettingsSwitch` | 预渲染同步开关（位于 `SyncSessionView` 的 idle 页） |
| `syncLogLevel` | `string` | —（当前无 UI） | 同步日志级别，默认 `"INFO"` |

**默认值来源**：`SettingsView.vue` 中 `settings` 响应式对象的初始值为：

```ts
const settings = ref<AppSettings>({
  // ... 其他字段 ...
  syncServerUrl: "",
  syncHttpUrl: "",
  syncToken: "",
  fileKey: "",
  syncLogLevel: "INFO",
});
```

所有同步相关字段初始为空字符串，用户首次使用时必须手动填入。`syncLogLevel` 默认 `"INFO"`，由 Rust 后端在初始化日志系统时读取，控制写入本地日志文件的详细程度。

所有文本输入框使用 `mono` 属性强制等宽字体，以提升 URL 与 Token 的可读性与精确感（符合项目 UI 美学宪法"技术精确感"原则）。

**持久化机制**：`SettingsTextField` 通过 `v-model` 绑定到 `settings.syncXxx` 字段，由于 `settings` 对象来自父组件的响应式引用，输入变更会由 `SettingsView.vue` 统一收集并通过 `settingsStore.saveSettings()` 批量持久化到 Rust 后端。`SyncSettingsSection` 本身不直接调用保存命令。

### 4.3 全量同步触发

用户在设置页点击 `OPEN PANEL` 按钮时，触发以下调用链：

```
SyncSettingsSection.vue
   │ @click="startManualSync"
   ▼
overlayStore.openSyncSession()
   │
   ▼
syncSessionStore.open()
   │
   ▼
FeatureOverlays.vue 渲染 SyncSessionView
   │
   ▼
用户在 SyncSessionView idle 页点击"开始同步"
   │
   ▼
syncSessionStore.startSync()
   │
   ▼
invoke('start_manual_sync')
```

> 设置页本身不直接触发同步执行，仅负责打开同步面板。这是为了避免用户在未确认同步参数的情况下误触启动，同时让同步过程的可视化集中在单一视图内。

---

## 5. 同步日志浏览器

### 5.1 SyncLogBrowserCore 组件

> 文件位置：`src/features/settings/components/SyncLogBrowserCore.vue`

`SyncLogBrowserCore` 是一个双态容器组件，嵌套在 `SyncSessionView` 的"历史日志"Tab 中，也可被其他页面复用。它提供本地同步日志文件的列表浏览与内容阅读功能。

历史页签未选中时该组件不挂载；每次进入时由自身 `onMounted(loadFiles)` 拉取最新文件列表。离开后文件内容、分页与滚动状态随组件一起释放；再次进入从最新列表开始，不保留上次阅读位置。

组件状态机：

```
┌─────────────┐      点击文件       ┌─────────────┐
│  文件列表态  │ ─────────────────► │  内容阅读态  │
│ (v-if=!currentFile)│              │(v-if=currentFile)│
│             │ ◄───────────────── │             │
└─────────────┘   点击"返回列表"    └─────────────┘
```

### 5.2 日志列表与筛选

**文件列表数据来源**：

```ts
const loadFiles = async () => {
  files.value = await invoke<LogFile[]>('list_sync_log_files');
};
```

返回的 `LogFile` 结构：

| 字段 | 类型 | 说明 |
|------|------|------|
| `filename` | `string` | 日志文件名（如 `20260813_120000_127_42_sync.log`） |
| `created_at` | `number` | Unix 时间戳（秒级） |
| `size_bytes` | `number` | 文件大小 |

列表项展示：`filename`（等宽字体截断）+ `created_at`（格式化）+ `size_bytes`（自动转换 KB/MB）。

**旧日志清理**：

```ts
const clearOldLogs = async () => {
  const result = await invoke<{removed: number; failed: number}>(
    'clear_old_sync_logs',
    { keepDays: 7 },
  );
};
```

- 清理 7 天前的日志文件
- 清理后自动刷新文件列表
- 通过项目统一确认层二次确认；Toast 分别报告成功数和未能删除数，不展示底层异常文本

### 5.3 日志详情与高亮

**内容阅读态**：

1. **加载**：调用 `read_sync_log_file(filename)` 获取完整文本内容
2. **预处理**：按 `\n` 分割并过滤空行
   ```ts
   lines.value = content.split('\n').filter(l => l.trim());
   ```
3. **分页**：每 500 行为一页，计算 `totalPages`
   ```ts
   totalPages.value = Math.max(1, Math.ceil(lines.value.length / linesPerPage));
   ```
4. **可见行计算**：
   ```ts
   const visibleLines = () => {
     const start = currentPage.value * linesPerPage;
     return lines.value.slice(start, start + linesPerPage);
   };
   ```
5. **高亮规则**（基于行内容字符串匹配）：

| 匹配条件 | 高亮颜色 | 含义 |
|----------|---------|------|
| 包含 `[INFO]` 且包含 `success` / `completed` | `text-green-400` | 成功信息 |
| 包含 `[ERROR]` | `text-red-400` | 错误信息 |
| 包含 `[WARN]` | `text-yellow-400` | 警告信息 |
| 包含 `[TRACE]` / `[DEBUG]` | `text-white/40` | 详细诊断 |
| 默认 | `text-white/70` | 普通日志 |

6. **操作**：复制按钮写入完整文本；紧邻的分享按钮调用 `prepare_sync_log_share_file`，由 Rust 校验所选文件确实位于 `sync_logs` 后直接复制到应用缓存，再通过插件 `shareFileNative()` 以 `ACTION_SEND + FileProvider content:// URI` 打开 Android 系统分享面板。日志正文不会为分享再次经过 WebView 或剪贴板，QQ、微信等接收方拿到的是保留原文件名的 `.log` 文件。两种操作都用固定中文 Toast 报告结果；文件在 Rust 写入前已统一脱敏。

> 分页设计理由：同步日志文件在首次全量同步后可能达到数 MB（数万行），一次性渲染会导致 DOM 阻塞与内存峰值。500 行/页在移动端屏幕上约为 20–30 屏滚动量，是性能与阅读连续性的平衡点。

---

## 6. 数据流时序

### 6.1 建立同步连接的时序

```
  用户                Vue 前端                    Rust 后端
   │                     │                            │
   │  点击"开始同步"      │                            │
   │────────────────────►│                            │
   │                     │ syncSessionStore.startSync()│
   │                     │                            │
   │                     │── invoke('plugin:vcp-mobile│
   │                     │      |get_battery_status') │
   │                     │◄───────────────────────────│
   │                     │ {level: 85, isPowerSaveMode:false}
   │                     │                            │
   │                     │── acquireScreenKeep() ─────┼────► 屏幕常亮
   │                     │── invoke('plugin:vcp-mobile│
   │                     │      |start_streaming_     │
   │                     │       service', ...) ──────┼────► 前台服务
   │                     │                            │
   │                     │── invoke('start_manual_    │
   │                     │      sync') ───────────────►│
   │                     │                            │
   │                     │                            │ 建立 WebSocket
   │                     │                            │ 连接桌面端
   │                     │                            │
   │                     │◄── Event: vcp-sync-status ─│
   │                     │    {status: "open"}         │
   │                     │                            │
   │                     │ status = "connected"       │
   │                     │ canDismiss = false         │
   │                     │                            │
   │                     │◄── Event: vcp-sync-progress│
   │                     │    {phase, total, completed}│
   │                     │                            │
   │  进度条更新          │                            │
   │◄────────────────────│ Store 生成去重后的阶段提示   │
   │                     │                            │
```

### 6.2 执行同步的完整时序

```
  Vue 前端              Rust 后端 (sync_service)        桌面端 (VCPChat)
    │                           │                              │
    │◄── vcp-sync-status: open ─┤                              │
    │  status=connected          │                              │
    │                            │  WebSocket 握手               │
    │                            │─────────────────────────────►│
    │                            │                              │
    │◄── vcp-sync-progress ──────┤  Phase 1: Owner Metadata     │
    │  phase=owner_metadata      │  Manifest 交换与比对          │
    │                            │◄────────────────────────────►│
    │                            │                              │
    │◄── vcp-sync-progress ──────┤  Phase 2: Topic Metadata     │
    │  phase=topic_metadata      │  话题级 Diff 与传输           │
    │                            │◄────────────────────────────►│
    │                            │                              │
    │◄── vcp-sync-progress ──────┤  Phase 3: Messages           │
    │  phase=messages            │  消息 NDJSON 流式传输         │
    │                            │◄────────────────────────────►│
    │                            │                              │
    │                            │  详细操作写入持久诊断日志      │
    │                            │                              │
    │◄── vcp-sync-completed ─────┤  全部 Phase 完成              │
    │  status=completed          │                              │
    │  needsReload=true          │                              │
    │                            │                              │
    │── invoke('stop_sync') ────►│  关闭 WebSocket               │
    │                            │─────────────────────────────►│
```

### 6.3 冲突解决时序

前端不直接参与冲突仲裁，冲突由后端 LWW 策略自动解决。实时面板只感知阶段进度与最终刷新，逐实体裁决进入持久日志：

```
  Vue 前端              Rust 后端                    桌面端
    │                        │                            │
    │◄── vcp-sync-progress ───┤  面板只更新阶段和计数          │
    │                          │  详细裁决写入 Session 日志      │
    │                          │                            │
    │                          │── LWW 仲裁逻辑 ───────────►│
    │                          │   以较大 ts 为准            │
    │                          │                            │
    │◄── vcp-sync-completed ───┤                            │
    │                          │                            │
    │  用户关闭面板            │                            │
    │────────────────────────►│                            │
    │                          │                            │
    │  performFullReload()     │                            │
    │  刷新 Agents/Groups/     │                            │
    │  Topics/Messages         │                            │
    │◄─────────────────────────┤                            │
```

> 冲突仲裁的完整规则详见 `docs/sync/11_冲突解决与边界情况.md`。

---

## 7. 与 Rust 后端的 IPC 交互

### 7.1 Tauri Commands 调用表

| 命令 | 调用方 | 输入 | 输出 | 触发时机 |
|------|--------|------|------|---------|
| `start_manual_sync` | `syncSessionStore.startSync()` | — | `u64 sessionId` / `SYNC_ERROR:<JSON>` | 用户点击"开始同步"，电量检测通过后调用 |
| `stop_sync` | `syncSessionStore.close()` / `retrySync()` | — | `void` | cancel 并 join 当前会话；重试必须等待其完成 |
| `list_sync_log_files` | `SyncLogBrowserCore.loadFiles()` | — | `Array<{filename, created_at, size_bytes}>` | 进入历史 Tab 或清理后刷新时调用 |
| `read_sync_log_file` | `SyncLogBrowserCore.openFile()` | `{filename: string}` | `string`（完整日志文本） | 用户点击某个日志文件时调用 |
| `prepare_sync_log_share_file` | `SyncLogBrowserCore.shareCurrentFile()` | `{filename: string}` | `string`（应用缓存中的受控暂存路径） | 用户点击日志详情的“分享”时调用；只接受单段文件名并再次校验 canonical path |
| `clear_old_sync_logs` | `SyncLogBrowserCore.clearOldLogs()` | `{keepDays: number}` | `{removed:number, failed:number}` | 用户点击"清理 7 天前的日志"时调用 |
| `get_sync_status` | —（当前前端未直接调用） | — | `SyncStatus` | 预留命令，可用于面板初始化时查询后端是否已有活跃会话 |

### 7.2 WebSocket 事件监听表

`syncSessionStore` 在 `open()` 时通过 `listen()` 注册 4 个 Tauri Event 监听器，在 `close()` 时统一清理（`cleanupListeners()`）。

监听注册与事件提交同时受 `viewGeneration`、`startAttempt` 和后端 `sessionId` 约束。`start_manual_sync` 返回前，后端可能已经发出当前 session 的事件；Store 在 invoke pending 窗口仅有界缓存最多 32 条带合法 sessionId 的状态/进度/完成事件，拿到 ID 后只按原顺序回放匹配项。缺 sessionId、旧 session 或其他 session 的事件全部拒绝。

| 事件名 | 来源（Rust 模块） | Payload 结构 | 前端处理逻辑 | 清理时机 |
|--------|-----------------|-------------|-------------|---------|
| `vcp-log` | `sync_service.rs`（operator notice） | `{id,level,category:"sync",audience:"operator",sessionId,message}` | 仅接收当前 Session 的安全过程提示；原始诊断事件被拒绝 | `close()` |
| `vcp-sync-progress` | `sync_service.rs` / `sync_executor.rs` | `{sessionId, phase, total, completed, message, ...summaryCounts}` | session 匹配且未终态时更新进度 | `close()` |
| `vcp-sync-status` | `sync_service.rs`（连接状态变化） | `{sessionId,status,message,error?: {code,category,message,guidance,failedTopicIds,logFile}}` | 主面板仅展示 `message + guidance + 日志可用性`；error 为单向终态 | `close()` |
| `vcp-sync-completed` | `sync_service.rs`（会话完成） | `{sessionId,status,summary}` | 设置 `completed` 或 `completed_with_warnings`，展示四项独立计数 | `close()` |

“复制诊断”只输出 Mobile/插件/wire 版本、Session、状态、计数、稳定错误码与日志文件名；不复制用户文案、原始异常、失败实体 ID、Token、Bearer、同步原始日志或绝对路径。

> 注意：同步连接、错误和完成不再发射全局 `vcp-system-event`。`useNotificationProcessor` 仍保留静默保护，防止旧后端事件产生重复 Toast；同步面板是唯一用户展示面。

### 7.3 HTTP 端点调用

前端**不直接调用**任何同步相关的 HTTP 端点。所有与桌面端的 HTTP 通信（实体批量下载、消息 NDJSON 流式传输、附件下载）均由 Rust 后端代理完成。前端仅通过 Tauri Commands 与 Events 与后端交互。

这种设计的优势：
- **安全**：Token 与服务器地址仅存于 Rust 层，不暴露给前端 JavaScript
- **一致性**：网络重试、超时与资源预算由 Rust 统一处理
- **离线缓冲**：Rust 层可在连接恢复后自动重试，前端无需关心网络瞬断

> Rust 后端调用的 HTTP 端点完整参考详见 `docs/sync/附录B_HTTP端点完整参考.md`。

---

## 8. 与后端文档的交叉引用

| 前端主题 | 对应后端文档 | 说明 |
|----------|-------------|------|
| 三阶段协议全景 | `docs/sync/01_架构总览与设计理念.md` | 理解 `owner_metadata` / `topic_metadata` / `messages` 三个 Phase 的后端实现 |
| 同步协议消息格式 | `docs/sync/04_同步协议详解.md` | WebSocket 握手、EntityState 语义、版本握手 |
| 流水线阶段控制 | `docs/sync/05_流水线与阶段控制.md` | `PipelinePhase` 状态机、Phase 1→2→3 的流转逻辑 |
| 差异计算 | `docs/sync/04_同步协议详解.md` | Phase 3 消息版本状态与执行顺序 |
| 移动端执行器 | `docs/sync/08_移动端执行器详解.md` | PullExecutor / PushExecutor / DeleteExecutor 的 HTTP 调用链 |
| 冲突仲裁规则 | `docs/sync/11_冲突解决与边界情况.md` | LWW 完整规则、三层冲突粒度、软删除双向传播 |
| 性能优化 | `docs/sync/12_性能优化与资源管理.md` | 在途帧预算、批量尺寸、NDJSON 流式 |
| 日志系统 | `docs/sync/13_日志系统与可观测性.md` | Session→Phase→Operation 三级日志结构、关键模式解读 |
| WebSocket 消息类型 | `docs/sync/附录A_WebSocket消息类型完整参考.md` | 当前 19 种 WS 消息的方向、Payload、处理函数 |
| HTTP 端点 | `docs/sync/附录B_HTTP端点完整参考.md` | REST 端点的路径、方法、认证、请求/响应格式 |
| 数据库 Schema | `docs/sync/附录C_数据库Schema对照表.md` | 移动端 SQLite 全部表结构、字段、约束 |

---

## 9. 设计决策与注意事项

### 9.1 为何使用 SlidePage 而非路由跳转？

同步会话是**临时性覆盖任务**，而非持久页面。使用 `SlidePage`（基于 `fixed inset-0` 的覆盖层）而非 Vue Router 导航的原因：

1. **上下文保持**：用户可在同步完成后立即回到之前的聊天或设置页面，无需重建页面状态
2. **动画一致性**：复用项目统一的 SlidePage 滑入/滑出动画体系，与 AgentSettings、GroupSettings 等行为一致
3. **层级管理**：通过 `overlayStore.pageStack` 统一管理 Z-Index 与返回键拦截，避免与底层页面的手势冲突

### 9.2 为何电量检测在前端而非后端？

电量检测逻辑位于 `syncSessionStore.startSync()` 中，而非 Rust `start_manual_sync` 内部：

1. **即时反馈**：前端可在用户点击后立即拦截并展示红色错误日志，无需等待 IPC 往返
2. **权限解耦**：电池状态通过 `plugin:vcp-mobile` 插件获取，前端直接调用插件命令比后端转发减少一次 IPC 层级
3. **UI 锁定**：拦截后前端立即将 `status` 置为 `error` 并解锁关闭按钮，避免用户困惑于"点击后无响应"

### 9.3 日志缓冲区的 200/100 截断策略

`syncSessionStore.logs` 内存中保留最近 200 条，但 `SyncSessionView` 的 DOM 仅渲染最近 100 条：

- **200 条内存保留**：支持用户点击"复制日志"时获取较完整的上下文，便于问题排查
- **100 条 DOM 渲染**：防止极长同步会话（如首次全量同步可能产生数千条日志）导致 Vue 响应式数组与 DOM 节点膨胀，避免低端 Android 设备卡顿或 OOM
- **折叠提示**：当内存中日志超过 100 条时，DOM 底部显示 `... N 条更早的日志已折叠（内存中保留最近 200 条）`，告知用户数据未丢失

### 9.4 同步进行中为何禁止切换 Tab？

`SyncSessionView` 中，当 `isSyncing`（即 `status === 'connecting' || status === 'connected'`）时，"历史日志"Tab 按钮被 `disabled`：

- 同步进行中日志流是实时追加的，切换 Tab 会中断用户的实时观察
- Store 的 `switchTab()` 同时保留业务门禁，即使绕过按钮的 `disabled` 也不能离开实时页
- 完成/错误后自动解锁，允许用户自由查看历史日志

### 9.5 前台服务与屏幕常亮的双重保活

同步期间同时启用两种 Android 保活机制：

| 机制 | 前端控制 | 作用 | 失败策略 |
|------|---------|------|---------|
| 屏幕常亮 | `acquireScreenKeep()` / `releaseScreenKeep()` | 防止设备休眠导致 WebSocket 断开 | 失败仅 `catch(() => {})`，不阻断同步 |
| 前台服务 | `start_streaming_service` / `stop_streaming_service` | 显示高优先级通知，降低 OEM 杀后台概率 | 失败仅 `catch(() => {})`，不阻断同步 |

两者均采用**宽容策略**：调用失败仅静默忽略。因为它们是稳定性增强手段，而非同步功能的必要依赖。这种设计避免了在某些 Android 设备（如权限受限的 OEM 系统）上因保活失败导致同步无法启动的问题。

### 9.6 屏幕常亮锁的引用计数设计

`useScreenKeeper.ts` 使用引用计数管理屏幕常亮状态：

```ts
let refCount = 0;

export function acquireScreenKeep(): void {
  if (refCount === 0) {
    setKeepScreenOn().catch(() => {});
  }
  refCount++;
}

export function releaseScreenKeep(): void {
  if (refCount > 0) {
    refCount--;
    if (refCount === 0) {
      clearKeepScreenOn().catch(() => {});
    }
  }
}
```

此设计允许多个并发操作（如同步 + 流式聊天）同时请求屏幕常亮，只有最后一个操作释放时才真正清除标志。同步会话的 `acquireScreenKeep()` 与 `releaseScreenKeep()` 配对使用，确保不会干扰其他需要常亮的功能。

### 9.7 同步完成后为何需要 `performFullReload`？

同步完成后，Rust 后端已修改 SQLite 数据库中的 Agents、Groups、Topics、Messages 等表，但前端存在多层缓存：

1. **AST HTML 缓存**：`clearHtmlCache()` 清理消息内容的预渲染 HTML，确保重新渲染时基于最新数据
2. **Agent/Group 缓存**：`assistantStore.fetchAgents()` / `fetchGroups()` 重新从后端读取配置
3. **Topic 缓存**：`topicStore.invalidateAllTopicCaches()` 使话题列表缓存失效
4. **消息数组**：若当前在活跃话题中，`chatHistoryStore.loadHistoryPaginated()` 重新加载消息

`needsReload` 标志确保用户不会在同步中途因 UI 数据陈旧而产生困惑。关闭面板时的 `confirm()` 对话框提醒用户"数据已更新"，确认后执行全量刷新。

### 9.8 Tab 切换与条件挂载

`SyncSessionView` 只由顶部按钮调用 `store.switchTab()` 切换 Tab，不再根据水平滚动位置反向改写状态。内容区域使用排他条件渲染：

```vue
<div v-if="store.activeTab === 'live'" role="tabpanel">
  <!-- 实时进度与日志 -->
</div>
<div v-else role="tabpanel">
  <SyncLogBrowserCore />
</div>
```

这个边界使两个页面的 UI 生命周期互不干扰：

- 默认实时页不挂载历史组件，不会提前读取日志文件列表
- 每次进入历史页时只执行一次 `onMounted(loadFiles)`，无需监听 `activeTab` 再补一次刷新
- 切回实时页后，Store 中的日志仍保留；日志容器重新挂载时自动滚到末尾

---

## 10. 术语速查表

| 术语 | 英文/缩写 | 定义 | 相关文件 |
|------|----------|------|----------|
| 同步会话 | Sync Session | 一次从连接到断开的完整同步生命周期，包含三个 Phase | `syncSession.ts` |
| 状态机 | State Machine | `idle → connecting → connected → completed/error` 的五状态有限状态机 | `syncSession.ts` |
| 阶段 | Phase | 同步会话内的逻辑分段：`initialization` / `owner_metadata` / `topic_metadata` / `messages` | `SyncSessionView.vue` |
| 进度事件 | Progress Event | 后端通过 `vcp-sync-progress` 推送的 `{phase, total, completed, message}` | `syncSession.ts` |
| 实时日志 | Live Log | 由结构化状态/进度生成阶段里程碑，并接收当前 Session 的少量 operator notice | `SyncSessionView.vue` |
| 历史日志 | History Log | 持久化到本地文件系统的同步日志，通过 `SyncLogBrowserCore` 浏览 | `SyncLogBrowserCore.vue` |
| 预渲染同步 | Prerender Sync | 同步时额外计算消息 AST 渲染缓存，增加耗时但提升后续聊天加载速度 | `SyncSessionView.vue` |
| 屏幕常亮锁 | Screen Keep Lock | 基于引用计数的 `FLAG_KEEP_SCREEN_ON` 管理器，防止同步期间息屏 | `useScreenKeeper.ts` |
| 前台服务 | Foreground Service | Android 机制，通过显示通知保持进程存活，降低 OEM 杀后台概率 | `syncSession.ts` |
| 电量拦截 | Battery Guard | 同步启动前检测电量 < 30% 或省电模式，智能拦截以保护数据安全 | `syncSession.ts` |
| 数据重载 | Data Reload | 同步完成后清理前端缓存并重新加载 Agents/Groups/Topics/Messages | `useDataReload.ts` |
| SlidePage | — | 项目统一的覆盖层页面组件，从右侧滑入，支持 Z-Index 动态计算 | `SlidePage.vue` |
| 三阶段协议 | Three-Phase Sync Protocol | V2 同步协议：Phase 1 Owner Metadata → Phase 2 Topic Metadata → Phase 3 Messages | 后端文档 |
| LWW | Last-Write-Wins | 以绝对时间戳 `ts` 为仲裁依据的冲突解决策略 | 后端文档 |
| NDJSON | Newline-Delimited JSON | 每行一个独立 JSON 对象，用于消息流式传输 | 后端文档 |

---

*最后更新：2026-08-11 | VCP Mobile v1.1.4*
