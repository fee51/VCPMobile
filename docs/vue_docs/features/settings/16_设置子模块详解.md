---
id: VUE-SETT-016
title: 设置子模块详解
description: VCP Mobile 前端 SettingsView 中 12 个设置子模块的详细说明与交互设计
version: 1.1.4
date: 2026-08-13
---

# 16. 设置子模块详解

## 1. 概述

### 1.1 领域定位

`features/settings/components/` 是 VCP Mobile 前端**全局设置中心**的原子 UI 单元集合。该目录下的 12 个 Vue 组件各自对应 `SettingsView` 中的一个功能子域，负责将用户输入转化为对 `settingsStore` 的写操作，或通过 Tauri IPC 触发 Rust 后端的维护/查询命令。

这些组件全部作为 **SettingsView 的子页面插槽**被引用，不独立存在于路由系统中。其设计严格遵循项目"高密度线性布局"美学宪法：列表重于卡片、灰度优先、2px 高亮侧边条反馈、内敛交互（`active:scale-95` 为主）。

设置子模块不涉及任何聊天业务逻辑，其职责边界清晰限定为：
- **配置读写**：用户身份、服务器连接、同步参数、主题等持久化配置
- **维护操作**：附件 GC、缓存清理、预渲染重建等低频但关键的后台任务
- **信息展示**：版本号、开源协议、电池优化引导、同步日志浏览

### 1.2 模块构成表

| 组件 | 文件 | 行数 | 职责 | 所属 SettingsView 子页面 |
|------|------|:----:|------|------------------------|
| `VcpCoreSettingsSection` | `VcpCoreSettingsSection.vue` | 67 | VCP Server URL / API Key 配置与连接验证 | `connection` |
| `AiLogicSettingsSection` | `AiLogicSettingsSection.vue` | 23 | AI 逻辑参数（预留占位） | — |
| `SyncSettingsSection` | `SyncSettingsSection.vue` | 102 | 同步服务器地址、Token、表情包修复 | `connection` |
| `UserProfileSection` | `UserProfileSection.vue` | 111 | 用户名、管理员账号、头像上传裁剪 | `identity` |
| `MaintenanceSection` | `MaintenanceSection.vue` | 86 | 附件 GC、WebView 缓存清理、预渲染重建入口 | `advanced` |
| `RebuildSessionView` | `RebuildSessionView.vue` | 169 | 现有预渲染缓存刷新的执行态 UI（SlidePage） | `advanced`（由 overlay 唤起） |
| `BatteryOptimizationGuide` | `BatteryOptimizationGuide.vue` | 171 | 国内 OEM 电池白名单分品牌引导 | `power` |
| `SyncLogBrowserCore` | `SyncLogBrowserCore.vue` | 180 | 同步日志文件列表、内容分页查看、清理 | — |
| `TopicSummarySection` | `TopicSummarySection.vue` | 37 | 话题总结专用模型选择 | `advanced` |
| `UpdateSection` | `UpdateSection.vue` | 135 | OTA 版本检查、下载进度、安装触发 | `about` |
| `AssistantSettingsSection` | `AssistantSettingsSection.vue` | 150 | 悬浮球开关、默认助手 Agent 选择、覆盖层权限管理 | `identity`（v1.1.3 已注释停用） |
| `AboutSection` | `AboutSection.vue` | 399 | 3D Logo 交互、版本号、开源链接、致谢 | `about` |

其中 `AiLogicSettingsSection` 在 v1.1.3 中仍为占位组件，未承载实际功能；`SyncLogBrowserCore` 目前嵌入在同步会话面板内部，未直接在 SettingsView 子页面中引用；`AssistantSettingsSection` 在 `SettingsView.vue` 中已被注释停用（`// const AssistantSettingsSection = defineAsyncComponent(...)`），v1.1.3 暂时停用划词悬浮助手功能。

### 1.3 在整体架构中的位置

```
┌─────────────────────────────────────────────────────────────┐
│                      SettingsView.vue                        │
│  (SlidePage 页面栈 z-page, LAYER_PAGE_BASE + index)         │
│                                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  设置主页    │  │  子页面层    │  │  ModelSelector      │  │
│  │  分类列表    │──│  (Transition)│  │  (model-value 绑定) │  │
│  │             │  │  绝对定位覆盖 │  │  BottomSheet z-sheet│  │
│  └─────────────┘  └──────┬──────┘  └─────────────────────┘  │
│                          │                                   │
│  ┌───────────────────────┼───────────────────────────────┐  │
│  │                       ▼                               │  │
│  │  ┌────────────┐ ┌────────────┐ ┌──────────────────┐  │  │
│  │  │UserProfile │ │VcpCore/    │ │Maintenance/      │  │  │
│  │  │  Section   │ │Sync Section│ │TopicSummary      │  │  │
│  │  │(identity)  │ │(connection)│ │  Section         │  │  │
│  │  └────────────┘ └────────────┘ └──────────────────┘  │  │
│  │                                                       │  │
│  │  ┌────────────┐ ┌────────────┐ ┌──────────────────┐  │  │
│  │  │ BatteryOpt │ │   About    │ │ RebuildSession   │  │  │
│  │  │   Guide    │ │  Section   │ │    View          │  │  │
│  │  │  (power)   │ │  (about)   │ │  (overlay 唤起)  │  │  │
│  │  └────────────┘ └────────────┘ └──────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────┘
                           │ props: settings (AppSettings 副本)
                           │ emit: save-request / open-model-selector
┌──────────────────────────▼──────────────────────────────────┐
│                   前端状态层 (Pinia Stores)                    │
│  useSettingsStore ──► 读写 settings.json (Rust 持久化)        │
│  useRebuildSessionStore ──► 重建状态机 + 进度事件监听          │
│  useOverlayStore ──► 打开/关闭 RebuildSession 页面栈项         │
│  useAssistantStore ──► 头像保存代理                            │
└──────────────────────────┬──────────────────────────────────┘
                           │ IPC (Tauri Commands / Events)
┌──────────────────────────▼──────────────────────────────────┐
│                   src-tauri (Rust 核心层)                    │
│  vcp_client.rs ──► test_vcp_connection                      │
│  maintenance_manager.rs ──► cleanup/clear_webview_cache     │
│  message_repository.rs ──► rebuild_all_pre_renders          │
│  update_manager.rs ──► check/download/install_update        │
│  sync_service.rs ──► list/read/clear sync logs              │
│  emoticon_manager.rs ──► regenerate_emoticon_library        │
│  settings_manager.rs ──► read/update_settings               │
└─────────────────────────────────────────────────────────────┘
```

### 1.4 核心设计原则

1. **设置副本模式**：`SettingsView` 维护打开基线与本地编辑副本，子组件通过 `v-model` 绑定。用户点击“返回”时对二者做 diff，再调用 `settingsStore.updateSettings(patch)`，避免频繁写盘及旧快照覆盖并发更新。
2. **操作即命令**：维护类操作（GC、缓存清理、重建）不修改设置副本，而是直接通过 `invoke` 调用 Rust Command，操作结果通过原子组件 `SettingsActionWithStatus` 回显。
3. **状态机隔离**：重建会话（`RebuildSessionView`）拥有独立的状态机（`idle | running | completed | error`），不耦合于 `settingsStore`，由 `useRebuildSessionStore` 统一管理。
4. **屏幕常亮守护**：所有可能长时间运行的维护操作（重建、GC）均通过 `withScreenKeep()` 包裹，防止 Android 息屏中断导致任务失败。
5. **前后台生命周期感知**：重建操作前后显式调用 `start_streaming_service` / `stop_streaming_service`，确保 Android 不会在后台中断长时间任务。

### 1.5 主题子页的直接组件边界

主题页不是 `features/settings/components/` 下的 Rust 设置 Section。`SettingsView` 直接挂载 `ThemePicker.vue`，并把 theme 子页设为 `overflow-hidden flex min-h-0`；`ThemePicker` 自己拥有唯一纵向滚动、横向 snap rail、主题草稿与确认。无状态 `ThemeLivePreview.vue` 只渲染候选 dark/light 局部变量。

主题确认区之后还有“气泡 / 统一 / 刊物”消息呈现第二入口。它直接调用 `themeStore.setPresentationMode` 并立即持久化，与 `settingsStore` 副本和主题草稿都不构成同一事务。

---

## 2. 核心设置模块

### 2.1 VcpCoreSettingsSection — VCP 核心配置

**职责一句话**：配置 VCP 后端 HTTP/HTTPS 接入点、WebSocket 日志通道及其鉴权密钥，并提供连接健康检查能力。

**源码位置**：`src/features/settings/components/VcpCoreSettingsSection.vue`

**Props**：

```ts
defineProps<{
  settings: AppSettings;
}>();

// Emit: save-request (触发父级先持久化当前输入)
defineEmits<{
  (e: 'save-request'): void;
}>();
```

**包含的表单字段**：

| 字段 | 组件 | 绑定字段 | 类型修饰 | 说明 |
|------|------|---------|---------|------|
| Chat 服务器 URL | `SettingsTextField` | `settings.vcpServerUrl` | — | 保存用户原始 HTTP/HTTPS 输入，不在前端改写 |
| Chat 请求入口 | 三项单选 | `settings.chatEndpointMode` | `standard` / `vcpTools` / `raw` | 标准 Chat、VCP 增强 Chat、原始完整 URL |
| API Key | `SettingsTextField` | `settings.vcpApiKey` | `isSecure` | 鉴权密钥，支持显隐切换 |
| VCP WebSocket URL | `SettingsTextField` | `settings.vcpLogUrl` | `mono` | 日志流地址，如 `ws://localhost:6005` |
| VCP WebSocket Key | `SettingsTextField` | `settings.vcpLogKey` | `isSecure` + `mono` | WebSocket 鉴权 Key |

**连接测试流程**：

1. URL 或入口模式变化时，组件调用 `preview_chat_endpoint`，展示 Rust resolver 计算的最终请求 URL；前端不实现第二套拼接算法。
2. 用户点击“检查模型发现”按钮。
3. 组件先 `emit('save-request')`，父级计算基线 diff 并通过 `update_settings` 合并当前输入。
4. 调用 `invoke('test_vcp_connection', { vcpUrl, vcpApiKey, chatEndpointMode })`。
5. Rust 侧复用中央 EndpointResolver：
   - 保留代理前缀、显式端口与 query，派生 `/v1/models`
   - Raw 任意端点无法安全推导时返回“模型发现不可用”，不误报 Chat 失败
   - 携带 `Authorization: Bearer {apiKey}` 请求头
   - 10 秒超时
6. 返回 `{ success, status, modelCount, models, modelDiscoveryAvailable }`。
7. 前端通过 `vcpPingStatus` 响应式对象驱动 `SettingsActionWithStatus` 回显结果。

**关键交互细节**：
- 测试前若 URL 为空，直接本地报错，不发起 IPC，减少无效网络开销。
- 状态文案示例：
  - `loading`: “正在检查模型列表...”
  - `success`: "连接成功！拉取到 42 个可用模型"
  - `error`: "验证失败, HTTP 状态码: 401" / 原始异常字符串
- 使用了 `status-multiline` 属性，错误信息允许换行显示。

**样式特征**：字段间使用 `space-y-5` 垂直分布，组间以 `border-t border-black/5 dark:border-white/5` 细线分隔，保持视觉呼吸感。

### 2.2 AiLogicSettingsSection — AI 逻辑参数

**职责一句话**：AI 推理全局默认参数的预留占位面板（当前版本未启用）。

**源码位置**：`src/features/settings/components/AiLogicSettingsSection.vue`

**包含的表单字段**：

| 字段 | 状态 |
|------|------|
| VCP 动态工具路由注入 | 暂未启用（右侧显示灰色小字） |
| 气泡主题 UI 规范注入 | 暂未启用（右侧显示灰色小字） |

该组件在 `v0.9.14` 中仅作占位渲染，使用 `SettingsRow` 展示两项预规划功能，右侧 action 插槽渲染 `<span class="text-xs opacity-60">暂未启用</span>`。不接受实际输入，无 IPC 交互。

> **未来规划**：该面板计划承载全局 Temperature、Max Tokens、Top-P、Presence Penalty、Frequency Penalty 等参数的默认值配置。当前 Agent 级别的这些参数仍由桌面端 `config.env` 控制，移动端尚未实现全局覆盖逻辑。保留该组件是为了 UI 架构的完整性，避免未来新增字段时破坏布局或需要重构路由。

### 2.3 SyncSettingsSection — 同步配置

**职责一句话**：配置三阶段增量同步协议（WebSocket + HTTP）的接入地址与鉴权 Token，并提供表情包修复库同步、全量神经同步面板入口。

**源码位置**：`src/features/settings/components/SyncSettingsSection.vue`

**Props**：

```ts
defineProps<{
  settings: AppSettings;
}>();
```

**包含的表单字段与操作**：

| 字段/操作 | 组件 | 绑定/触发 | 修饰符 | 说明 |
|-----------|------|----------|--------|------|
| HTTP 服务 URL | `SettingsTextField` | `settings.syncHttpUrl` | `mono` | 同步 HTTP API 端点，如 `http://192.168.x.x:5974` |
| WebSocket 服务 URL | `SettingsTextField` | `settings.syncServerUrl` | `mono` | WebSocket 实时同步通道 |
| Mobile Sync Token | `SettingsTextField` | `settings.syncToken` | `isSecure` + `mono` | 桌面端 `config.env` 中的 `SYNC_TOKEN` |
| 表情包图床密钥 | `SettingsTextField` | `settings.fileKey` | `isSecure` + `mono` | 用于构造表情包 URL 的密码参数 |
| 表情包修复库 | `SettingsActionWithStatus` | `invoke('regenerate_emoticon_library')` | — | 从服务器同步表情包元数据 |
| 全量神经同步 | `SettingsRow` + `SettingsActionButton` | `overlayStore.openSyncSession()` | — | 打开同步会话 SlidePage |

**表情包修复库流程**：

```
用户点击 REFRESH 按钮
    │
    ▼
emoticonStatus = { type: 'loading', message: '正在从远程服务器获取...' }
    │
    ▼
invoke('regenerate_emoticon_library')
    │
    └──► Rust: emoticon_manager.rs
         ├── 远程获取表情包 JSON 索引
         ├── 本地 SQLite upsert 表情元数据
         └── 返回: number (表情总数)
    │
    ▼
emoticonStatus = { type: 'success', message: '同步成功：共计 156 个表情' }
    │
    └──► 3s 后自动清空状态
```

**全量神经同步入口**：
- 不直接发起 IPC，而是调用 `overlayStore.openSyncSession()`。
- 该操作会将 `syncSession` 类型推入 `overlayStore.pageStack`，打开独立的同步会话面板。
- 同步会话面板内部包含 `SyncLogBrowserCore` 组件用于浏览历史日志。

### 2.4 UserProfileSection — 用户资料

**职责一句话**：管理本地用户身份标识（头像、用户名）及管理员接口鉴权凭据。

**源码位置**：`src/features/settings/components/UserProfileSection.vue`

**Props**：

```ts
defineProps<{
  settings: AppSettings;
}>();
```

**包含的表单字段与交互**：

| 字段 | 组件 | 绑定字段 | 修饰符 | 说明 |
|------|------|---------|--------|------|
| 头像 | `VcpAvatar` + `AvatarCropper` | — | `owner-type="user"` | 点击触发文件选择 → 裁剪器 → Blob IPC |
| 用户名 | `SettingsTextField` | `settings.userName` | — | 本地显示名称 |
| 管理员账号 | `SettingsTextField` | `settings.adminUsername` | `mono` | Basic Auth 用户名 |
| 管理员密码 | `SettingsTextField` | `settings.adminPassword` | `isSecure` + `mono` | Basic Auth 密码 |

**头像上传完整时序**：

```
用户点击头像区域
    │
    ▼
调用 pickFile("avatar")，打开 Android 系统图片选择器
    │
    ▼
Native 64 KiB 流式 staging
    ├── 读取尺寸但不分配全尺寸 Bitmap
    ├── EXIF 方向归一化
    ├── inSampleSize 采样至最长边 1120px WebP
    └── 返回受控临时路径
    │
    ▼
convertFileSrc(path) → <AvatarCropper :img="cropImg" />
    │
    ▼
用户在裁剪器中确认
    │
    ▼
onCropConfirm(blob: Blob)
    ├── isCropping = false
    ├── const arrayBuffer = await blob.arrayBuffer()
    ├── const bytes = new Uint8Array(arrayBuffer)
    ├── await assistantStore.saveAvatar(
    │     "user",           // ownerType
    │     "user_avatar",    // ownerId
    │     blob.type,        // mimeType
    │     bytes             // 业务层保持 Uint8Array；Tauri 内部转为 number[] JSON
    │   )
    └── assistantStore 内部调用 avatarStore.refreshAvatar("user", "user_avatar", hash)
```

**关键点**：
- 裁剪器输出最大 360×360、圆外透明的 PNG；Android 返回键只取消裁剪。
- 原图从不进入 JS 堆；取消、确认和组件卸载都会删除 1120px Native 工作副本，并回收裁剪器内部 Blob URL。
- 头像通过 `assistantStore.saveAvatar` 经 IPC 写入 Rust `avatar_service.rs`，以 SHA-256 为内容指纹并将二进制存入 SQLite `avatars` 表。
- 保存成功后由全局 `avatarStore` 统一失效并重读该 owner；页面关闭不会丢失刷新信号，旧异步读取也不能回灌缓存。
- 管理员账号密码用于远程获取表情包库等管理接口的 Basic Auth 鉴权（见 `SyncSettingsSection` 的 `fileKey` 和表情包修复）。
- 卡片使用 `SettingsCard variant="glass"`，在亮色主题下呈现半透明白色毛玻璃质感。

### 2.5 AssistantSettingsSection — 浮动助手设置（Dormant Asset）

> **⛔ DORMANT ASSET**：组件源码保留，但生产路由/HTML、窗口事件、Tauri/插件命令、capability 与 Android overlay permission 均已注销。以下内容只记录未来复用所需的历史实现。

**职责一句话**：管理 Android 悬浮球开关、默认助手 Agent 选择、覆盖层（Overlay）权限检测与引导。

**源码位置**：`src/features/settings/components/AssistantSettingsSection.vue`（150 行）

> **当前状态说明**：该组件未被 import 或渲染，且所调用的两条悬浮窗口插件命令已注销；未来恢复时必须重新完成权限、安全和构建审查。

**Props 与 Emit**：

```ts
defineProps<{
  settings: AppSettings;
}>();

const emit = defineEmits<{
  (e: 'save-request'): void;
}>();
```

**包含的表单字段与操作**：

| 字段/操作 | 组件 | 绑定/触发 | 说明 |
|-----------|------|----------|------|
| 启用全局悬浮球 | `SettingsSwitch` | `settings.enableAssistant` | 带 `Beta` 标签的胶囊形开关，控制浮动助手系统 |
| 助手绑定 Agent | `<select>` | `settings.assistantAgentId` | 下拉选择器，列出全部可用 Agent |

**悬浮球开关交互流程**：

```
用户点击 SettingsSwitch 开启
    │
    ▼
handleToggle(true)
    │
    ├── checkPermission()
    │     invoke("plugin:vcp-mobile|check_all_permissions")
    │     → 获取 { overlay: boolean, ... }
    │
    ├── if (!hasOverlayPermission):
    │     invoke("plugin:vcp-mobile|request_overlay_permission")
    │       → 跳转系统设置: ACTION_MANAGE_OVERLAY_PERMISSION
    │     props.settings.enableAssistant = false  (重置开关)
    │     return  (阻止继续)
    │
    ├── props.settings.enableAssistant = true
    │
    ├── invoke("plugin:vcp-mobile|toggle_floating_ball", { show: true })
    │     → Android 原生层显示悬浮球
    │
    ├── invoke("reconcile_local_server_cmd", { enable: true })
    │     → Rust 侧启动本地 axum 服务器 (127.0.0.1:14202)
    │
    └── emit("save-request")
          → SettingsView 持久化 enableAssistant 到 SQLite
```

**关闭悬浮球流程**：

```
用户点击 SettingsSwitch 关闭
    │
    ▼
handleToggle(false)
    │
    ├── props.settings.enableAssistant = false
    ├── invoke("toggle_floating_ball", { show: false })
    │     → Android 原生层移除悬浮球和助手窗口
    ├── invoke("reconcile_local_server_cmd", { enable: false })
    │     → Rust 侧优雅关闭本地服务器
    └── emit("save-request")
```

**权限丢失自愈机制**：

```typescript
const checkPermission = async () => {
  const status = await invoke<{ overlay: boolean }>(
    "plugin:vcp-mobile|check_all_permissions"
  );
  hasOverlayPermission.value = status.overlay;

  // 若权限被手动撤销但开关仍为开启，自动重置
  if (!status.overlay && props.settings.enableAssistant) {
    props.settings.enableAssistant = false;
    await invoke("plugin:vcp-mobile|toggle_floating_ball", { show: false });
  }
};
```

此逻辑在 `onMounted` 和 resume 生命周期事件中均被调用，确保权限被系统撤销后不会出现"开关已开启但悬浮球不显示"的不一致状态。

**Agent 选择器**：

```html
<select v-model="props.settings.assistantAgentId"
        class="bg-transparent text-sm font-semibold opacity-60 ...">
  <option value="">未绑定 (使用默认)</option>
  <option v-for="agent in assistantStore.agents" :key="agent.id"
          :value="agent.id">
    {{ agent.name }}
  </option>
</select>
```

- 依赖 `useAssistantStore` 获取 Agent 列表
- 选择变更时触发 `emit("save-request")`（通过 `watch` 监听 `settings.assistantAgentId`）
- `assistantAgentId` 被 `floatingAssistant` Store 的 `resolveAgentId()` 读取，用于确定浮动窗口对话的目标 Agent
- 空值（`""`）表示使用系统默认 Agent

**关联设置字段**：

| 字段 | 类型 | 持久化 | 说明 |
|------|------|:----:|------|
| `enableAssistant` | `boolean` | SQLite | 浮动助手系统总开关 |
| `assistantAgentId` | `string` | SQLite | 浮动窗口默认绑定的 Agent UUID |

**生命周期恢复**：

```typescript
// AssistantSettingsSection.vue:79
watch(() => lifecycleStore.isBackground, async (newVal) => {
  if (!newVal) {
    // 应用回到前台
    await checkPermission();
    if (props.settings.enableAssistant && hasOverlayPermission.value) {
      await assistantStore.fetchAgents();
      await invoke("plugin:vcp-mobile|toggle_floating_ball", { show: true });
      await invoke("reconcile_local_server_cmd", { enable: true });
    }
  }
});
```

Android 应用从后台恢复时，自动检查权限状态并恢复悬浮球和本地服务器。这处理了以下场景：
- 用户在系统设置中授权覆盖层权限后切回应用
- 应用被系统杀死后重新启动（前台服务保护范围外的情况）
- 设备从省电模式恢复后重新激活悬浮球

**UI 样式特征**：

- 使用 `divide-y divide-black/5 dark:divide-white/5` 分隔两个设置行
- 悬浮球开关右侧附带金色 `Beta` 标签：`bg-amber-500/15 text-amber-600 border border-amber-500/25`
- Agent 选择器仅在 `enableAssistant === true` 时渲染（条件渲染 `v-if`），未启用时隐藏以减少视觉噪音

**与 Rust 后端的联动**：

| 组件操作 | Rust Command | 效果 |
|----------|-------------|------|
| 开启悬浮球 | `plugin:vcp-mobile\|toggle_floating_ball` (show=true) | Android 原生创建悬浮球 + WebView 准备 |
| 开启悬浮球 | `reconcile_local_server_cmd` (enable=true) | Rust 启动 axum 服务器 (127.0.0.1:14202) |
| 关闭悬浮球 | `plugin:vcp-mobile\|toggle_floating_ball` (show=false) | 移除悬浮球 + 销毁 WebView |
| 关闭悬浮球 | `reconcile_local_server_cmd` (enable=false) | 优雅关闭 axum 服务器 |
| 检查权限 | `plugin:vcp-mobile\|check_all_permissions` | 获取 overlay 权限当前状态 |
| 请求权限 | `plugin:vcp-mobile\|request_overlay_permission` | 跳转系统设置页面 |

---

## 3. 系统维护模块

### 3.1 MaintenanceSection — 维护与清理

**职责一句话**：提供 WebView HTTP 缓存清理和现有预渲染缓存刷新入口。

**源码位置**：`src/features/settings/components/MaintenanceSection.vue`

**包含的操作按钮**：

| 操作 | 按钮文案 | 对应 Rust Command | 屏幕常亮 | 危险等级 |
|------|---------|------------------|----------|:--------:|
| 清理 WebView 缓存 | 立即清理 | `clear_webview_cache` | — | 🟡 |
| 刷新现有预渲染缓存 | 开始刷新 | —（打开 RebuildSessionView） | — | — |

**WebView 缓存清理**：
- 删除 `app_cache_dir/WebView/Default/HTTP Cache`。
- LocalStorage、Cookie 和应用会话状态保持不变。

**“刷新现有预渲染缓存”入口**：
- 该操作不直接调用 IPC，而是通过 `overlayStore.openRebuildSession('preRender')` 打开独占式重建页面。
- 执行期间显示已有缓存项的处理进度并阻止中途关闭。

### 3.2 RebuildSessionView — 重建会话

**职责一句话**：现有预渲染缓存刷新的独占式执行页面，通过状态机管理 idle → running → completed/error 生命周期。

**源码位置**：`src/features/settings/components/RebuildSessionView.vue`

**状态机**：

```
                    点击"开始重建"
   ┌─────┐        ┌─────────┐
   │idle │ ──────►│ running │◄────────────────────────────┐
   └─────┘        └────┬────┘                             │
                       │                                   │
         ┌─────────────┼────────────────┐                 │
         │             │                │                 │
         ▼             ▼                ▼                 │
   ┌─────────┐   ┌──────────┐    ┌─────────┐            │
   │completed│   │  error   │    │running* │            │
   └────┬────┘   └────┬─────┘    └─────────┘            │
        │             │                                  │
        ▼             ▼                                  │
   提示刷新页面    显示错误信息                        进度更新
        │                                                  │
        ▼                                                  │
   performFullReload() ────────────────────────────────────┘
   (完成后用户可重新开始)
```

**Props**：

```ts
interface Props {
  zIndex?: number;  // 由 overlayStore.getPageZIndex('rebuildSession') 传入
}
```

**各状态 UI 表现**：

| 状态 | 视觉 | 交互限制 |
|------|------|---------|
| `idle` | 播放图标 + “开始刷新”按钮 + 说明文字 | 自由操作 |
| `running` | 进度条（`current/total`）+ 底部提示“刷新进行中 — 请勿退出” | `canDismiss = false`，全局遮罩阻止触摸 |
| `completed` | 绿色文字“预渲染缓存刷新完成” + “完成”按钮 | `canDismiss = true` |
| `error` | 红色文字显示错误信息 + "关闭"按钮 | `canDismiss = true` |

**与 Rust 后端的完整交互链**：

```
startRebuild()  (useRebuildSessionStore.ts)
    │
    └──► invoke('rebuild_all_pre_renders')
           └──► Rust: message_repository.rs
                1. 统计并读取已有 render_cache 对应消息
                2. 并行重新编译 AST 二进制
                3. content_hash 仍匹配时更新 render_cache
                4. emit('render_rebuild_progress', { current, total })
                5. Android 端在命令内部启停前台服务
```

**完成后刷新机制**：
- 刷新成功后 `needsReload = true`。
- 用户点击关闭时，确认现有预渲染缓存已更新。
- 确认后调用 `useDataReload().performFullReload()`：
  1. `clearHtmlCache()` — 清理 AST HTML 渲染缓存
  2. `fetchAgents()` / `fetchGroups()` / `avatarStore.refreshMetadata()` — 刷新实体与头像轻量元数据，hash 变化时定点失效 Blob
  3. `invalidateAllTopicCaches()` — 使话题列表缓存失效
  4. `loadHistoryPaginated()` — 若在当前话题中，重新加载消息及渲染缓存

### 3.3 BatteryOptimizationGuide — 电池优化引导

**职责一句话**：针对国内 Android OEM 系统的电池白名单配置指南，按品牌自动检测并展示分步骤操作指引。

**源码位置**：`src/features/settings/components/BatteryOptimizationGuide.vue`

**品牌检测逻辑**：

```ts
const detectedBrand = computed(() => {
  const ua = navigator.userAgent.toLowerCase();
  for (const guide of guides) {
    if (guide.keywords.some((k) => ua.includes(k))) {
      return guide.brand;
    }
  }
  return null;
});
```

通过 `navigator.userAgent` 关键字匹配实现自动品牌检测，若检测失败默认选中第一个品牌（ColorOS）。

**支持的品牌与步骤**：

| 品牌 | 关键字 | 典型步骤 |
|------|--------|---------|
| OPPO / realme / 一加 (ColorOS) | coloros, oppo, realme, oneplus | 允许完全后台行为、关闭自动速冻、开启自启动、关闭睡眠待机优化 |
| 小米 / Redmi / HyperOS | miui, xiaomi, redmi, poco, hyperos | 省电策略设为无限制、开启自启动、允许显示后台弹出界面 |
| 华为 / 荣耀 (HarmonyOS) | harmony, huawei, honor, emui | 关闭自动管理、手动开启自启动/关联启动/后台活动、休眠保持网络 |
| vivo / iQOO (OriginOS) | funtouch, origin, vivo, iqoo | 允许高耗电运行、不限制后台运行、开启自启动 |
| 三星 (One UI) | samsung, one ui | 排除深度休眠、添加至用电不受限制列表 |

**UI 结构详解**：

1. **顶部预警卡片**：
   - 背景：`bg-amber-500/10 border-amber-500/20`
   - 图标：`Zap`（闪电）
   - 文案说明国内定制系统会激进冻结后台进程，强调配置必要性

2. **品牌选项卡（Segmented Control）**：
   - 容器：`bg-[var(--secondary-bg)] border border-[var(--border-color)] rounded-lg`
   - 选中态：`bg-[var(--bg-color)] shadow-sm`
   - 当前设备匹配的品牌显示 `Smartphone` 图标（蓝色）
   - 横向可滚动，`hide-scrollbar` 隐藏滚动条

3. **垂直时间线步骤引导**：
   - 左侧绝对定位连接线：`absolute left-[11px] top-2 bottom-2 w-px`
   - 节点：圆形序号，白色背景，`z-10` 浮于连接线上方
   - 步骤文案通过 `highlightStep()` 处理：
     - `→` 替换为半透明小三角 `▶`
     - `「关键操作」` 替换为高亮蓝色 pill 标签

4. **终极防御**：
   - 顶部 `ShieldCheck` + `Lock` 图标
   - 说明物理锁定多任务卡片的方法（呼出多任务 → 下拉卡片 → 🔒 图标）

---

## 4. 数据与日志模块

### 4.1 SyncLogBrowserCore — 同步日志浏览器

**职责一句话**：浏览、查看、清理 Rust 后端生成的同步日志文件（按日期轮转）。

**源码位置**：`src/features/settings/components/SyncLogBrowserCore.vue`

**内部数据结构**：

```ts
interface LogFile {
  filename: string;
  created_at: number;  // Unix timestamp (seconds)
  size_bytes: number;
}
```

**双视图架构**：

```
┌─────────────────────────────────────────┐
│              文件列表视图 (v-if="!currentFile") │
│  ┌─────────────────────────────────┐   │
│  │ 📄 sync_2026-05-27.log          │   │
│  │    5月27日 10:30 · 12.5 KB   >  │   │
│  ├─────────────────────────────────┤   │
│  │ 📄 sync_2026-05-26.log          │   │
│  │    5月26日 14:15 · 8.2 KB    >  │   │
│  └─────────────────────────────────┘   │
│  [ 🗑 清理 7 天前的日志 ]                │
└─────────────────────────────────────────┘
                    │ 点击文件
                    ▼
┌─────────────────────────────────────────┐
│              文件内容视图 (v-else)        │
│  [← 返回列表]           [📋 复制]         │
│  ─────────────────────────────────────  │
│  [INFO] sync completed ...      (green) │
│  [ERROR] connection failed ...   (red)  │
│  [WARN] retrying ...           (yellow) │
│  ...                                    │
│  ─────────────────────────────────────  │
│  [<]      2 / 5      [>]   (分页控制)    │
└─────────────────────────────────────────┘
```

**IPC 调用表**：

| 操作 | Command | 参数 | 返回类型 |
|------|---------|------|---------|
| 加载文件列表 | `list_sync_log_files` | — | `LogFile[]` |
| 读取文件内容 | `read_sync_log_file` | `{ filename: string }` | `string` |
| 清理旧日志 | `clear_old_sync_logs` | `{ keepDays: 7 }` | `{removed:number, failed:number}` |

**内容高亮规则**（基于字符串包含匹配）：

| 匹配条件 | CSS 类 |
|---------|--------|
| 含 `[INFO]` 且含 `success` | `text-green-400` |
| 含 `[ERROR]` | `text-red-400` |
| 含 `[WARN]` | `text-yellow-400` |
| 含 `[TRACE]` 或 `[DEBUG]` | `text-white/40` |
| 默认 | `text-white/70` |

**分页策略**：
- 固定每页 `linesPerPage = 500` 行
- 按 `\n` 切分后过滤空行，`Math.ceil(lines.length / 500)` 计算总页数
- 超长日志避免一次性渲染 DOM，保证滑动性能
- 分页控件在 `totalPages > 1` 时显示

**复制功能**：使用 `navigator.clipboard.writeText(fileContent)` 将已由 Rust 脱敏的完整日志内容写入剪贴板，并以固定中文 Toast 报告成功或失败。

**注意事项**：该组件当前仅针对深色场景设计（固定 `text-white/70` 等色值）。若未来需要在亮色设置页中复用，需补充 CSS 变量适配。

### 4.2 TopicSummarySection — 话题摘要

**职责一句话**：配置话题总结功能所使用的专用模型 ID。

**源码位置**：`src/features/settings/components/TopicSummarySection.vue`（37 行）

**Props 与 Emit**：

```ts
defineProps<{
  settings: AppSettings;
}>();

const emit = defineEmits<{
  (e: "open-model-selector"): void;
}>();
```

**包含的表单字段**：

| 字段 | 组件 | 绑定字段 | 修饰符 | 默认值 |
|------|------|---------|--------|--------|
| 总结专用模型 | `SettingsTextField` | `settings.topicSummaryModel` | `mono` | 输入框占位符显示 `gemini-3.1-flash-lite` |

**交互设计**：
- 文本框占据左侧主要空间（`flex-1`）
- 右侧配备一个方形选择按钮（`!p-3.5 !rounded-2xl`），图标为上下箭头
- 点击按钮触发 `emit('open-model-selector')`
- `SettingsView` 接收后打开全局 `ModelSelector` BottomSheet
- 用户选择模型后，通过 `@select` 事件回填 `settings.topicSummaryModel`

**使用场景**：当用户对某个话题请求"总结"时，系统使用该模型 ID 调用 VCP 后端生成话题摘要。与聊天主模型解耦，允许使用更轻量的模型（如 Flash 系列）降低成本。

---

## 5. 更新与关于模块

### 5.1 UpdateSection — OTA 更新检查

**职责一句话**：查询 GitHub Release 最新版本，管理下载进度与安装触发。

**源码位置**：`src/features/settings/components/UpdateSection.vue`

**状态机**（`UpdateStatus` Discriminated Union）：

```ts
type UpdateStatus =
  | { type: 'idle' }
  | { type: 'checking' }
  | { type: 'no-update' }
  | { type: 'update-available'; info: UpdateInfo }
  | { type: 'downloading'; progress: number; total: number | null }
  | { type: 'downloaded' }
  | { type: 'installing' }
  | { type: 'error'; message: string };
```

**状态流转图**：

```
                         点击"检查更新"
    ┌──────────────────────────────────────────────┐
    │                                              │
    ▼                                              │
┌─────────┐      ┌──────────┐                     │
│  idle   │─────►│ checking │                     │
└─────────┘      └────┬─────┘                     │
                      │                           │
         ┌────────────┼────────────┐              │
         ▼            ▼            ▼              │
    ┌─────────┐  ┌─────────────┐  ┌──────┐       │
    │no-update│  │update-avail │  │ error│       │
    └────┬────┘  └──────┬──────┘  └──┬───┘       │
         │              │            │           │
         │         点击"下载并安装"   │           │
         │              │            │           │
    4s后自动           ▼            │           │
    回到 idle    ┌───────────┐      │           │
                 │downloading│      │           │
                 └─────┬─────┘      │           │
                       │            │           │
                       ▼            │           │
                 ┌───────────┐      │           │
                 │installing │◄─────┘           │
                 └─────┬─────┘                  │
                       │                        │
                       ▼                        │
                 回到 idle ◄────────────────────┘
```

**核心流程详解**：

**1. 检查更新**：
```ts
const status: UpdateStatus = await updateStore.check();
```
- 状态唯一持有者是 Rust `UpdateSession`，前端 store 只做镜像：
  订阅 `vcp-update://status` 事件 + `get_update_status` 补拉
- Rust 请求 `api.github.com/repos/fee51/VCPMobile/releases/latest`
  （404 时降级 `/releases?per_page=1`）
- APK 资产按 `ends_with("arm64-v8a.apk")` 选择，`.apk.sha256` 旁车文件不会误命中
- 必须存在同名 `.sha256` 旁车文件（`sha256sum` 标准格式），缺失或畸形即报错
- 本地已有完整包且 SHA-256 匹配时直接进入 `readyToInstall`，不重复下载

**2. 下载并安装**：
```ts
await useUpdateDownloader().downloadAndInstall();
```
- `start_update_download` 不接受任何参数——URL 只来自 Rust 验证过的 GitHub API 响应
- Range 断点续传（保留 `.part`），单 chunk 停滞 30s 判死，指数退避最多 3 次
- 下载完成后流式 SHA-256 校验，不匹配则删除并可重试
- 下载期间 Rust 侧持有 ForegroundGuardian OTA 前台锁（`PRIORITY_OTA = 50`）
- 前端监听 store 进度，300ms 节流更新系统通知栏
- `install_update` 依次检查「安装未知应用」权限与签名证书连续性
  （APK 与自签名 SHA-256 比对），通过后触发 Android 系统安装器

**3. 降级策略**：
若应用内链路失败，弹窗提供「Release 页面」按钮：
- `openUrl(info.releasePageUrl)` — 浏览器打开 Release 页面手动下载

**UI 反馈矩阵**：

| 状态（`status.state`） | UI 表现 |
|------|---------|
| `checking` | 按钮 loading / "正在检查最新版本..." |
| `idle`（无更新） | success 提示当前版本号，4s 后自动消失 |
| `available` | 弹窗展示版本号（等宽字体）、APK 大小、Release Notes |
| `downloading` | 进度条 + 百分比 + 已下载/总大小，可取消下载或最小化弹窗 |
| `verifying` | 进度条满格 + "SHA-256 校验中" |
| `readyToInstall` | 设置行显示「已就绪，点击安装」 |
| `installing` | "正在拉起安装器..." |
| `failed` / 带 `error` | 错误框显示 `error.stage` 与信息；install 阶段提供「前往系统授权设置」 |

### 5.2 AboutSection — 版本与开源信息

**职责一句话**：沉浸式关于页面，包含可交互 3D Logo、版本号展示、功能介绍与外部链接。

**源码位置**：`src/features/settings/components/AboutSection.vue`

**视觉层次与 Z-Index**：

| 层级 | 元素 | Z-Index | 说明 |
|------|------|:-------:|------|
| 背景层 | `WebGLFluidBackground` | 0 | WebGL 流体特效，与主题无关 |
| 噪点层 | SVG 分形噪声胶片颗粒 | 1 | 抹除渐变色带，暗色 `mix-blend-mode: overlay`，亮色 `multiply` |
| 内容层 | 返回按钮 / 3D Logo / 操作列表 / Footer | 10+ | 通过 `z-10` / `z-20` 分层 |

**3D Logo 交互逻辑详解**：

该组件实现了完整的 3D 视差 + 动画序列系统：

**1. 跟手模式（用户触摸/鼠标移动）**：
```ts
const handleMove = (e: MouseEvent | TouchEvent) => {
  animSessionId++;  // 打断任何正在进行的序列
  isMixing.value = true;
  transitionStyle.value = 'none';  // 0 延迟，极致跟手
  
  // 计算相对中心点的偏移
  const targetRotY = ((x - centerX) / centerX) * 150;
  const targetRotX = ((centerY - y) / centerY) * 80;
  
  // 硬限幅防止万向锁
  rotation.value.y = Math.max(-180, Math.min(180, targetRotY));
  rotation.value.x = Math.max(-90, Math.min(90, targetRotX));
  
  // 更新光晕位置
  glowPos.value = { x: (x / width) * 100, y: (y / height) * 100 };
};
```

**2. 自动动画序列（按下后 800ms 启动）**：
按下触发 8 段 CSS 变换序列：
- 向右缓慢旋转翻面（0→180°Y，2s）
- 向左缓慢旋转复原（180→0°Y，2s）
- 向左旋转 75° 展现左侧（1.2s）
- 快速向右翻转 75° 展现右侧（0.6s）
- 快速复原 + 扭动（0.4s + 3 次 100ms 微扭）
- 上下翻转 360° + 落地抖动（0.8s + 4 次修正）

通过 `animSessionId` 自增实现打断机制：用户再次触摸时立即取消序列，回归跟手模式。

**操作列表**：

| 标题 | 图标 | 动作 |
|------|------|------|
| 功能介绍 | `i-carbon-chevron-right` | `notificationStore.addNotification` info Toast |
| 项目主页 | `i-carbon-logo-github` | `openUrl('https://github.com/fee51/VCPMobile')` |
| 意见反馈 | `i-carbon-debug` | `openUrl('https://github.com/fee51/VCPMobile/issues')` |

**版本号获取**：
```ts
const currentVersion = ref('');
onMounted(async () => {
  currentVersion.value = await getVersion();  // @tauri-apps/api/app
});
```

**主题深度覆盖**：AboutSection 是项目中少数使用 `<style scoped>` 进行深度选择器覆盖的组件。原因：
- `WebGLFluidBackground` 作为全屏动态背景，其颜色与主题无关
- 必须通过 `:deep()` 选择器对子组件（`SettingsCard`、`SettingsRow`、`SettingsInlineStatus`、`SettingsActionButton`）进行强制样式覆盖
- 暗色模式：卡片 `rgba(255,255,255,0.1)` 背景 + 白色文字
- 亮色模式：卡片 `rgba(255,255,255,0.65)` 毛玻璃背景 + `backdrop-filter: blur(24px)` + slate 文字

---

## 6. 设置原子组件体系

`src/components/settings/` 目录下包含 9 个原子组件，为全部设置子模块提供统一的视觉语言与交互范式。它们构成了 SettingsView 的"设计系统基板"。

### 6.1 表单输入封装

#### `SettingsTextField`

**源码位置**：`src/components/settings/SettingsTextField.vue`

**Props 完整列表**：

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `modelValue` | `string \| number \| undefined \| null` | — | 双向绑定值 |
| `label` | `string` | — | 顶部标签（10px, uppercase, font-bold, opacity-40） |
| `placeholder` | `string` | — | 占位符 |
| `type` | `string` | `'text'` | 原生 input type |
| `mono` | `boolean` | `false` | 等宽字体（`font-mono text-sm`） |
| `isSecure` | `boolean` | `false` | 安全模式：失焦时圆点遮罩 |
| `center` | `boolean` | `false` | 居中对齐 |
| `error` | `boolean` | `false` | 错误态 |
| `disabled` | `boolean` | `false` | 禁用态 |

**安全输入设计**：

```
输入框获取焦点 (onFocus)
    ├── isFocused = true
    └── 显示明文

输入框失去焦点 (onBlur)
    ├── isFocused = false
    └── 若 isMasked === true → 应用 -webkit-text-security: disc

点击右侧眼睛图标
    └── 切换 isMasked 状态
```

**CSS 遮罩实现**：
```css
.masked-input {
  -webkit-text-security: disc;
}
```
该属性在 WebKit 环境下将字符替换为圆点，但保留真实值于 DOM 中，不影响 `v-model` 绑定。

**样式特征**：
- 底色：`bg-black/5 dark:bg-white/5`
- 圆角：`rounded-2xl`
- 内边距：`p-3.5`
- 聚焦态：`focus:border-blue-500/50 focus:bg-black/10 dark:focus:bg-white/10`
- 错误态：`border-red-500/50 bg-red-500/5`，右侧显示错误图标
- 禁用态：`opacity-40 cursor-not-allowed`
- 安全输入：`pr-12` 预留眼睛图标空间

### 6.2 开关与选择器

#### `SettingsSwitch`

**源码位置**：`src/components/settings/SettingsSwitch.vue`

**Props**：

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `modelValue` | `boolean` | — | 开关状态 |
| `disabled` | `boolean` | `false` | 禁用 |
| `activeColor` | `string` | `'bg-blue-500'` | 开启态背景色 |

**实现细节**：
- 容器尺寸：宽 `40px`，高 `20px`，`rounded-full` 胶囊形
- 圆球：直径 `16px`，白色，`shadow-sm`
- 开启时：`translate-x-5`（从 2px 移动到 22px）
- 过渡：`transition-colors duration-300`（背景）+ `transition-transform duration-300`（圆球）
- 交互：`active:scale-90` 按压反馈
- 禁用：`opacity-30 cursor-not-allowed`

#### `SettingsDisclosure`

**源码位置**：`src/components/settings/SettingsDisclosure.vue`

**Props**：

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `title` | `string` | — | 标题（必填） |
| `description` | `string` | — | 描述 |
| `icon` | `Component` | — | 左侧图标（Lucide 组件） |
| `defaultOpen` | `boolean` | `false` | 默认展开 |
| `accentColor` | `string` | — | 图标颜色类 |

**过渡动画**：
```vue
<Transition
  enter-active-class="transition-all duration-300 ease-out"
  leave-active-class="transition-all duration-200 ease-in"
  enter-from-class="max-h-0 opacity-0"
  enter-to-class="max-h-[2000px] opacity-100"
  leave-from-class="max-h-[2000px] opacity-100"
  leave-to-class="max-h-0 opacity-0"
>
```

使用 `max-h-[2000px]` 作为展开态的最大高度上限，Vue Transition 自动处理高度过渡。箭头图标通过 `:class="{ 'rotate-180': isOpen }"` 旋转指示状态。

### 6.3 按钮与操作触发

#### `SettingsActionButton`

**源码位置**：`src/components/settings/SettingsActionButton.vue`

**变体体系**：

| variant | 背景 | 文字 | 阴影 |
|---------|------|------|------|
| `primary` | `bg-blue-600 hover:bg-blue-500` | 白色 | `shadow-lg shadow-blue-900/20` |
| `secondary` | `bg-black/5 dark:bg-white/10 hover:bg-black/10 dark:hover:bg-white/20` | `text-primary-text` | 无 |
| `danger` | `bg-red-500/10 hover:bg-red-500/20` | `text-red-500` | 无 |
| `ghost` | 透明 `hover:bg-black/5 dark:hover:bg-white/5` | `text-primary-text` | 无，hover 时 opacity 100% |

**尺寸体系**：

| size | 垂直内边距 | 字体大小 | 圆角 |
|------|:---------:|:-------:|:----:|
| `sm` | `py-2` | `11px` | `rounded-xl` |
| `md`（默认） | `py-2.5` | `12px` | `rounded-xl` |
| `lg` | `py-4` | `14px` | `rounded-2xl` |

**Loading 状态**：显示 CSS `animate-spin` 圆环（`w-3 h-3 border-2 border-current border-t-transparent`），同时按钮进入 `disabled` 态。

#### `SettingsActionWithStatus`

**源码位置**：`src/components/settings/SettingsActionWithStatus.vue`

**设计模式**：将 `SettingsRow`（标题+描述） + `SettingsActionButton`（操作按钮） + `SettingsInlineStatus`（状态回显）垂直堆叠的复合组件。

**布局策略**：
- 若传入 `title`：按钮嵌入 `SettingsRow` 的右侧 `#action` 插槽，形成左右分布
- 若未传入 `title`：按钮独立右对齐（`flex justify-end`）
- 状态区始终显示在按钮下方，`mt-2` 间距

**Props**（节选关键）：

| Prop | 类型 | 说明 |
|------|------|------|
| `title` | `string` | 左侧标题 |
| `description` | `string` | 左侧描述 |
| `buttonVariant` | `'primary' \| 'secondary' \| 'danger' \| 'ghost'` | 按钮变体 |
| `buttonSize` | `'sm' \| 'md' \| 'lg'` | 按钮尺寸 |
| `buttonLabel` | `string` | 按钮文案 |
| `buttonLoading` | `boolean` | 按钮加载态 |
| `statusType` | `'success' \| 'error' \| 'loading' \| 'info' \| null` | 状态类型 |
| `statusMessage` | `string` | 状态文案 |
| `statusMono` | `boolean` | 状态等宽字体 |
| `statusMultiline` | `boolean` | 状态多行显示 |

#### `SettingsInlineStatus`

**源码位置**：`src/components/settings/SettingsInlineStatus.vue`

**颜色映射**：

| type | 颜色类 | 附加效果 |
|------|--------|---------|
| `success` | `text-green-500 dark:text-green-400` | — |
| `error` | `text-red-500 dark:text-red-400` | — |
| `loading` | `text-blue-500 dark:text-blue-400` | `animate-pulse` + 左侧 `animate-ping` 圆点 |
| `info` | `text-primary-text opacity-40` | — |

**布局模式**：
- 默认：`flex items-center`，文案 `truncate` 单行截断
- `multiline`：`flex items-start`，文案 `whitespace-pre-wrap leading-relaxed` 多行换行

#### `SettingsRow`

**源码位置**：`src/components/settings/SettingsRow.vue`

**Props**：

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `title` | `string` | — | 标题（14px font-semibold） |
| `description` | `string` | — | 描述（10px opacity-40） |
| `icon` | `Component` | — | 左侧图标 |
| `clickable` | `boolean` | `false` | 可点击态（显示右箭头 + 按压反馈） |
| `danger` | `boolean` | `false` | 危险态（标题变红） |
| `disabled` | `boolean` | `false` | 禁用态 |

**交互反馈**：
- `clickable && !disabled`：`cursor-pointer active:scale-[0.98] active:bg-black/5 dark:active:bg-white/5 rounded-xl`
- 右箭头仅在 `clickable && !slots.action` 时显示
- 布局：`flex items-center justify-between`，左侧 `flex-1` 占据剩余空间，右侧 `shrink-0 ml-4`

#### `SettingsCard`

**源码位置**：`src/components/settings/SettingsCard.vue`

**变体体系**：

| variant | 背景 | 边框 | 用途 |
|---------|------|------|------|
| `default` | `bg-black/5 dark:bg-white/5` | `border-black/5 dark:border-white/10` | 标准设置卡片 |
| `glass` | `bg-white/5 border-white/10` | 半透明 | `UserProfileSection` 头像卡片 |
| `danger` | `bg-red-500/5` | `border-red-500/20` | 危险操作容器 |
| `warning` | `bg-yellow-500/5` | `border-yellow-500/20` | 警告信息容器 |

**通用样式**：`rounded-2xl shadow-sm overflow-hidden transition-all duration-300`
- 默认内边距：`px-3 py-4`
- `noPadding` 时去除内边距（用于嵌套其他容器）

#### `SettingsSection`

**源码位置**：`src/components/settings/SettingsSection.vue`

**用途**：区块标题组件，左侧 4px 彩色竖条 + 11px uppercase 标题 + 可选 9px 描述。
- 标题样式：`text-[11px] font-black uppercase tracking-[0.15em] opacity-50 dark:opacity-40`
- 在 `SettingsView.vue` 中实际多用内联 `<h3>` 替代，该组件作为标准化封装保留

### 6.4 与 UnoCSS 快捷类的配合

原子组件大量依赖 UnoCSS 原子类，同时也有少量自定义语义化类名（用于 `:deep()` 覆盖和 Scoped CSS）。典型配合模式：

```vue
<!-- SettingsTextField 内部 -->
<input class="w-full bg-black/5 dark:bg-white/5 p-3.5 rounded-2xl 
  outline-none border transition-all duration-200
  focus:border-blue-500/50 focus:bg-black/10 dark:focus:bg-white/10" />

<!-- SettingsCard 内部 -->
<div class="settings-card overflow-hidden transition-all duration-300
  border rounded-2xl shadow-sm px-3 py-4" />
```

组件内部不使用 UnoCSS 快捷类（如 `glass-panel`、`card`、`btn`），而是显式写出完整原子类。原因：
1. 设置组件需要精确控制每一个 Tailwind 原子类的响应式行为
2. 深色/亮色模式的 `dark:` 前缀需要显式声明，快捷类无法覆盖所有状态组合
3. `:deep()` 覆盖时需要匹配具体类名，快捷类的组合类会增加选择器复杂度

---

## 7. 数据流时序

### 7.1 缓存刷新会话时序

```
用户点击“开始刷新” (MaintenanceSection.vue)
         │
         ▼
overlayStore.openRebuildSession('preRender')
         │
         ▼
useRebuildSessionStore.open('preRender')
    ├── taskType = 'preRender'
    ├── isOpen = true
    ├── canDismiss = true
    ├── status = 'idle'
    ├── progress = { current: 0, total: 0 }
    ├── needsReload = false
    └── registerListener()
         │
         ▼
RebuildSessionView.vue 弹出 (SlidePage)
    │
    ├── Props: zIndex = overlayStore.getPageZIndex('rebuildSession')
    ├── 渲染 idle 状态 UI
    └── 等待用户点击“开始刷新”
         │
         ▼
用户点击“开始刷新”
         │
         ▼
useRebuildSessionStore.startRebuild()
    │
    ├──► status = 'running'
    ├──► canDismiss = false
    ├──► progress = { current: 0, total: 0 }
    └──► invoke('rebuild_all_pre_renders')
             │
             └──► Rust: message_repository.rs
                  1. 查询已有 render_cache 对应消息
                  2. 并行编译并按 content_hash CAS 更新缓存
                  3. emit('render_rebuild_progress')
                  4. RETURN Ok(())
             │
             ▼
        ┌────────────────┐
        │ 成功:           │
        │ needsReload=true│
        │ status='completed'
        │ canDismiss=true │
        └───────┬────────┘
                │
        │
        ▼
用户点击"完成"
        │
        ▼
confirm("缓存刷新完成...")
    │
    └──► useDataReload().performFullReload()
           1. clearHtmlCache()
           2. 刷新 Agent / Group / Avatar
           3. topicStore.invalidateAllTopicCaches()
           4. 若仍有当前 Topic，重新加载历史
```

### 7.2 更新检查时序

```
用户点击"检查更新" (UpdateSection.vue)
         │
         ▼
status = { type: 'checking' }
         │
         ▼
invoke('check_for_update')
         │
         ▼
┌──────────────────────────────┐
│ Rust: update_manager.rs      │
│                              │
│ 1. current_version =         │
│    app.package_info().version│
│                              │
│ 2. GET /releases/latest      │
│    Host: api.github.com      │
│    User-Agent: VCPMobile     │
│    timeout: 15s              │
│                              │
│ 3. IF status == 404:         │
│      GET /releases?per_page=1│
│    (prerelease fallback)     │
│                              │
│ 4. Parse GitHubRelease JSON  │
│                              │
│ 5. Find asset:               │
│    name.ends_with(           │
│      "arm64-v8a.apk")        │
│                              │
│ 6. RETURN UpdateInfo {       │
│      has_update,             │
│      current_version,        │
│      latest_version,         │
│      download_url,           │
│      release_page_url,       │
│      release_notes,          │
│      apk_size                │
│    }                         │
└────────┬─────────────────────┘
         │
    ┌────┴────┐
    ▼         ▼
 no-update  update-available
    │         │
    ▼         ▼
status =   status = {
{ type:    type: 'update-available',
  'no-update' } info }
    │         │
    ▼         ▼
4s 后自动   用户点击"下载并安装"
回到 idle        │
                 ▼
    ┌────────────────────────┐
    │ useUpdateDownloader()  │
    │ .downloadAndInstall()  │
    └───────────┬────────────┘
                │
                ├──► updateStore.startDownload()
                │      // invoke('start_update_download') 无参数
                │      // Rust 状态机驱动：Range 续传 + 停滞判死 + SHA-256 校验
                │      // 进度经 vcp-update://status 事件广播回 store
                │      // 前端 watch store 进度，300ms 节流更新系统通知栏
                │
                ├──► updateStore.install()
                │      // invoke('install_update') 无参数
                │      // 安装未知应用权限检查 → 签名连续性校验
                │      // → Android 系统安装器
                │
                └──► 若失败:
                       openUrl(releasePageUrl) 手动下载
                       或按 error.stage 提示（install 阶段引导授权）
```

---

## 8. 与 Rust 后端的 IPC 交互

### 8.1 Tauri Commands 调用表

| 前端组件 | Command | 参数 | 返回类型 | Rust 模块 | 说明 |
|---------|---------|------|---------|----------|------|
| `VcpCoreSettingsSection` | `test_vcp_connection` | `{ vcpUrl: string, vcpApiKey: string }` | `{ success, status, modelCount, models }` | `vcp_client.rs` | 验证 VCP 后端连通性并拉取模型列表 |
| `MaintenanceSection` | `clear_webview_cache` | — | `string` | `maintenance_manager.rs` | 清除 WebView 浏览数据 |
| `RebuildSessionView` | `rebuild_all_pre_renders` | — | `()` | `message_repository.rs` | 全量消息 AST 重新解析与固化 |
| `SyncSettingsSection` | `regenerate_emoticon_library` | — | `number` | `emoticon_manager.rs` | 远程同步表情包元数据 |
| `SyncLogBrowserCore` | `list_sync_log_files` | — | `LogFile[]` | `sync_service.rs` | 列出同步日志文件 |
| `SyncLogBrowserCore` | `read_sync_log_file` | `{ filename: string }` | `string` | `sync_service.rs` | 读取单日志文件内容 |
| `SyncLogBrowserCore` | `clear_old_sync_logs` | `{ keepDays: number }` | `{removed:number, failed:number}` | `sync_service.rs` | 清理指定天数前的日志，分别报告成功与失败数 |
| `updateStore.check` | `check_for_update` | — | `UpdateStatus` | `update_manager.rs` | 查询 GitHub Release 并解析 SHA-256 旁车文件 |
| `updateStore.startDownload` | `start_update_download` | — | `UpdateStatus` | `update_manager.rs` | Range 续传下载 + SHA-256 校验 |
| `updateStore.cancelDownload` | `cancel_update_download` | — | `UpdateStatus` | `update_manager.rs` | 停止下载，保留 `.part` |
| `updateStore.init` | `get_update_status` | — | `UpdateStatus` | `update_manager.rs` | 补拉当前状态快照 |
| `updateStore.install` | `install_update` | — | `UpdateStatus` | `update_manager.rs` | 权限/签名校验后唤起 Android 安装器 |
| `SettingsView` | `read_settings` | — | `AppSettings` | `settings_manager.rs` | 读取全部设置 |
| `SettingsView` | `update_settings` | `{ updates: Record }` | `AppSettings` | `settings_manager.rs` | 增量更新设置 |
| `AssistantSettingsSection` | `plugin:vcp-mobile\|toggle_floating_ball` | `{ show: boolean }` | `{ success: boolean }` | `system.rs` → `FloatingWindowManager.kt` | 显示/隐藏悬浮球 |
| `AssistantSettingsSection` | `reconcile_local_server_cmd` | `{ enable: boolean }` | `()` | `local_server.rs` | 启动/停止本地 HTTP 服务器 |
| `AssistantSettingsSection` | `plugin:vcp-mobile\|request_overlay_permission` | — | `()` | `system.rs` → `FloatingWindowManager.kt` | 打开系统覆盖层权限设置 |
| `AssistantSettingsSection` | `plugin:vcp-mobile\|check_all_permissions` | — | `{ overlay: boolean, ... }` | `system.rs` → `VcpMobilePlugin.kt` | 检查 overlay 权限状态 |

### 8.2 事件监听表

| 事件名 | 方向 | Payload 类型 | 监听方 | 触发源 | 说明 |
|--------|------|-------------|--------|--------|------|
| `render_rebuild_progress` | Rust → 前端 | `{ current: number, total: number }` | `useRebuildSessionStore` | `message_repository.rs` | 现有预渲染缓存刷新进度 |
| `vcp-lifecycle-changed` | Rust → 前端 | `{ state: 'resume' \| 'pause' \| 'stop' }` | `useAppLifecycle()` | `lifecycle_controller.rs` | 应用前后台切换；`AssistantSettingsSection` 监听 `lifecycleStore.isBackground` 恢复悬浮球 |

**插件调用表**（非标准 Tauri Command，通过 `invoke('plugin:vcp-mobile|...')`）：

| 调用方 | 插件方法 | 参数 | 说明 |
|--------|---------|------|------|
| `useRebuildSessionStore` | `start_streaming_service` | `{ agentName: string }` | 启动 Android 前台服务，显示 `IMPORTANCE_HIGH` 通知，防止 OEM 杀后台 |
| `useRebuildSessionStore` | `stop_streaming_service` | `{ agentName: string }` | 停止前台服务，释放通知栏 |
| `AssistantSettingsSection` | `toggle_floating_ball` | `{ show: boolean }` | 显示/隐藏 Android 悬浮球与助手窗口 |
| `AssistantSettingsSection` | `check_all_permissions` | — | 检查 overlay 等系统权限状态 |
| `AssistantSettingsSection` | `request_overlay_permission` | — | 跳转系统覆盖层权限设置 |

---

## 9. 设计决策与注意事项

1. **设置副本 vs 直接绑定 Store**：
   `SettingsView` 不直接让子组件 `v-model` 绑定 `settingsStore.settings`，而是维护本地编辑副本与打开基线。原因：避免输入时频繁持久化，并在点击“返回”时只提交实际 Patch。这样既保留批量编辑体验，也不会用页面打开时的旧全量对象覆盖其他模块的新值。

2. **AiLogicSettingsSection 的占位策略**：
   该组件在 `v0.9.14` 中仅渲染两个"暂未启用"行。Agent 级别的 Temperature / Max Tokens 配置目前仍由桌面端 `config.env` 控制，移动端尚未实现全局覆盖逻辑。保留该组件是为了 UI 架构的完整性，避免未来新增字段时破坏布局或需要重构路由分类。

3. **RebuildSessionView 的独占式设计**：
   重建操作使用独立的 `SlidePage` 页面栈项（而非内联在 `MaintenanceSection` 中），是因为重建过程可能持续数分钟，需要阻止用户离开（`canDismiss = false` + 全局遮罩），且完成后可能触发全量数据重载，需要占据用户全部注意力。

4. **AboutSection 的深色/亮色深度覆盖**：
   关于页面使用 `WebGLFluidBackground` 作为全屏动态背景，其颜色与主题无关。因此该组件通过 `<style scoped>` 中的 `:deep()` 选择器，对子组件（`SettingsCard`、`SettingsRow`、`SettingsInlineStatus`、`SettingsActionButton`）进行深度样式覆盖，确保在亮色模式下文字可读、卡片有半透明毛玻璃质感。这是项目中少数允许深度选择器覆盖的场景。

5. **SyncLogBrowserCore 的亮色缺失**：
   该组件当前仅针对深色场景设计（固定 `text-white/70` 等色值）。若未来需要在亮色设置页中复用，需补充 CSS 变量适配或使用 `text-primary-text` 等语义化颜色类。

6. **UpdateSection 的状态机与降级**：
   更新状态由 Rust `UpdateSession` 唯一持有并经事件广播，前端只镜像；下载失败保留 `.part` 可续传重试，安装权限/签名问题按 `error.stage` 给出引导，最终兜底是打开 Release 页面手动下载。

7. **头像的有界内存处理**：
   Android 原生层先将外部 URI 流式 staging，并通过 `inJustDecodeBounds + inSampleSize` 生成最长边 1120px 的 EXIF 归一化 WebP；WebView 只裁剪该工作副本。最终圆形 PNG 在业务层保持为 `Uint8Array`，但 Tauri 对嵌套 IPC 参数仍会转成 `number[]` JSON；由于输出被限制在最大 360×360，该段峰值有界。启动/同步只批量拉取头像 hash/颜色/时间戳，二进制在进入视口前 300px 时按需读取、全局最多 2 路并发，并受 50 项 LRU 约束；离开预加载区的组件卸载 `<img>` 以释放解码位图。

8. **MaintenanceSection 的状态隔离**：
   每个维护操作拥有独立的 `ref` 状态对象（`gcStatus`、`cacheStatus`），而非共享一个全局状态。这种设计允许用户同时触发多个操作（虽然 UI 上通常不会这么做），且错误信息不会互相覆盖。

9. **BatteryOptimizationGuide 的纯前端实现**：
   该组件完全基于 `navigator.userAgent` 字符串匹配，不调用任何 Rust Command。其数据（品牌、步骤）硬编码于组件内部，因为 OEM 电池优化路径通常随系统版本变化缓慢，无需后端动态配置。

10. **SettingsTextField 的安全遮罩策略**：
    使用 `-webkit-text-security: disc` 而非 `<input type="password">`，是因为后者在移动端会自动触发键盘的"密码建议"和"自动填充"行为，干扰 API Key 等人工输入场景。`-webkit-text-security` 仅做视觉遮罩，不影响输入法行为。

11. **AssistantSettingsSection 的历史权限自愈与生命周期恢复（DORMANT ASSET）**：
    该组件已从 `SettingsView` 生产入口移除，悬浮命令、local-server handler 与 `SYSTEM_ALERT_WINDOW` 也已注销。以下仅记录未来可能复用的历史交互设计：
    覆盖层权限（`SYSTEM_ALERT_WINDOW`）可被用户在系统设置中随时撤销。`AssistantSettingsSection` 通过双重保障维持状态一致性：(a) `checkPermission()` 在 `onMounted` 时检测权限并重置不一致的 UI 开关状态；(b) 监听 `useAppLifecycleStore().isBackground` 在应用切前台时重新验证并恢复悬浮球。这确保用户不会遇到"开关开启但悬浮球消失"的困惑状态。
    该组件同时管理 `reconcile_local_server_cmd`——本地服务器的生命周期严格跟随悬浮球开关，避免空闲时占用端口 14202。

---

## 10. 术语速查表

| 术语 | 说明 |
|------|------|
| **Section** | 设置子模块组件的统称，每个 Section 对应一个功能域 |
| **SettingsView** | 设置中心的根组件，以 SlidePage 形式从右侧滑入，z-page 层级 |
| **预渲染 (Pre-render)** | 将消息原始内容提前解析为带语法高亮的 HTML，存入数据库避免运行时重复解析 |
| **GC (Garbage Collection)** | Core Ready 前回收无有效消息引用的附件 CAS 与派生文件 |
| **Screen Keep** | 屏幕常亮锁定，通过 `tauri-plugin-vcp-mobile` 的 `setKeepScreenOn` 实现 |
| **前台服务 (Foreground Service)** | Android 机制，`StreamKeepaliveService` 以 `START_NOT_STICKY` + `IMPORTANCE_HIGH` 通知保活（v1.1.2+ 将 WakeLock/WifiLock 管理移交 `ForegroundGuardian`），`:helper` 进程 `SseProxyService` 以 `START_STICKY` 共同降低 OEM 杀后台概率 |
| **Channel** | Tauri v2 的双向通信通道，基于 Rust `tauri::ipc::Channel`，用于流式下载进度推送 |
| **OEM 电池优化** | 国内定制 Android 系统（ColorOS/MIUI/HarmonyOS/OriginOS/One UI）的后台进程冻结策略 |
| **AST** | Abstract Syntax Tree，消息内容结构化解析的中间表示，用于代码块高亮与富文本渲染 |
| **副本模式** | SettingsView 本地维护 `settings` 副本，返回时才持久化的设计模式 |
| **VCP** | Virtual Chat Platform，后端 AI 服务接入平台，提供模型列表、流式聊天、日志推送 |
| **三阶段同步** | VCP Mobile 同步协议：WebSocket 实时通道 + HTTP 差异下载 + SHA-256 Hash 校验 |
| **Discriminated Union** | TypeScript 类型模式，`UpdateStatus` 通过 `type` 字段区分不同状态 shape |
| **Avatar working copy** | Native 采样生成的最长边 1120px 临时 WebP，通过 asset URL 供头像裁剪器使用 |
| **Blob** | 浏览器二进制大对象，头像裁剪后的输出格式，最终转为 `Uint8Array` IPC 传递 |
| ** orphaned attachment** | 数据库 `attachments` 表中存在记录，但未被任何 `message_attachments` 引用的附件 |

---
*最后更新：2026-07-04 | VCP Mobile v1.1.3*
