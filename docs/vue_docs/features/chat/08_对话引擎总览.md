---
id: VUE-CHAT-008
title: 对话引擎总览
description: VCP Mobile 前端对话引擎主视图、输入增强、消息发送流程、历史加载优化与搜索栏优化的全景说明
version: "1.1.3"
date: 2026-08-10
---

# 08. 对话引擎总览

## 1. 概述

### 1.1 领域定位

`features/chat/` 是 VCP Mobile 前端**对话引擎**的核心领域目录，承载用户与 Agent/Group 进行交互的完整前端链路。该领域负责：

- 聊天主视图的布局渲染与生命周期管理
- 消息历史的分页加载、流式接收与状态同步
- 用户输入的多模态采集（文本、语音、附件）
- 附件的暂存、预览、预处理与上传
- 群聊模式下的多 Agent 并发流式控制

该领域**不**涉及：
- 模型推理与 SSE 解析本身（由 Rust 后端 `vcp_client.rs` / `agent_chat_application_service.rs` 负责）
- 消息持久化的通用逻辑（由 Rust `message_service.rs` 负责）
- Agent/Group 的配置管理（由 `features/agent/` 及 Rust `agent_service.rs` 负责）

### 1.2 模块构成表

`src/features/chat/` 目录下包含以下核心文件（按职责分类）：

| 文件 | 行数 | 职责 |
|------|------|------|
| `ChatView.vue` | 312 | 聊天主视图：Header、消息列表、输入区外壳、生命周期统筹 |
| `InputEnhancer.vue` | 608 | 输入增强栏：多模态输入、语音交互、附件菜单、发送控制 |
| `StagedAttachmentPreview.vue` | 63 | 暂存附件缩略图预览与移除触发 |
| `MessageRenderer.vue` | — | 单条消息渲染器（引用，详见 `09_消息渲染管线.md`） |
| `blocks/ToolSummaryBlock.vue` | 45 | 工具调用摘要气泡（v1.1.0 新增）：状态标签色彩映射、CSS 玻璃态容器 |
| `composables/useLongTextPaste.ts` | 175 | 长文本粘贴的三层拦截与自动附件化 |
| `components/GroupStopAllButton.vue` | 50 | 群聊全局停止按钮 |

关联 Store（位于 `src/core/stores/`）：

| Store | 行数 | 职责 |
|-------|------|------|
| `chatSessionStore.ts` | 103 | 当前选中项（Agent/Group）、当前话题 ID、话题切换逻辑 |
| `chatHistoryStore.ts` | 536 | 消息历史的分页加载、发送消息、删除/编辑/重生成、AI 总结标题 |
| `chatStreamStore.ts` | 355 | 流式消息的全局池、SSE 事件处理、流状态管理、中断控制 |
| `attachmentStore.ts` | 520 | 附件暂存列表、文件选择、Android 原生拦截、大小文件分流上传 |

关联 Composable（位于 `src/core/composables/`）：

| Composable | 行数 | 职责 |
|------------|------|------|
| `useChatScroll.ts` | 329 | 消息列表滚动场景状态机 + 锚定恢复 + 自动加载历史 |

### 1.3 在整体架构中的位置

```
┌─────────────────────────────────────────────────────────────┐
│                      Vue 3 前端层                            │
│                                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              features/chat/ 领域 (本域)                │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │  │
│  │  │ ChatView    │  │InputEnhancer│  │ StagedAtt   │   │  │
│  │  │ (主视图)     │◄─┤ (输入核心)   │◄─┤ (附件预览)   │   │  │
│  │  └──────┬──────┘  └──────┬──────┘  └─────────────┘   │  │
│  │         │                │                           │  │
│  │         └────────────────┼────────────────────┐      │  │
│  │                          │                    │      │  │
│  │              ┌───────────┴───────────┐       ▼      │  │
│  │              ▼                       ▼              │  │
│  │  ┌─────────────────────────┐  ┌──────────────────┐  │  │
│  │  │ useLongTextPaste        │  │ GroupStopAllBtn  │  │  │
│  │  │ (长文本粘贴拦截)         │  │ (群聊停止)        │  │  │
│  │  └─────────────────────────┘  └──────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│                           │                                  │
│         ┌─────────────────┼─────────────────┐               │
│         ▼                 ▼                 ▼               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐     │
│  │chatSession  │  │chatHistory  │  │ chatStream      │     │
│  │   Store     │  │   Store     │  │    Store        │     │
│  └─────────────┘  └─────────────┘  └─────────────────┘     │
│         │                 │                 │               │
│         └─────────────────┼─────────────────┘               │
│                           ▼                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐     │
│  │attachment   │  │ useChatScroll│  │ useKeyboard     │     │
│  │   Store     │  │ (滚动管理)   │  │    Insets       │     │
│  └─────────────┘  └─────────────┘  └─────────────────┘     │
└──────────────────────────┬──────────────────────────────────┘
                           │ IPC (Tauri Commands + Channel)
┌──────────────────────────▼──────────────────────────────────┐
│                   src-tauri (Rust 核心层)                    │
│                                                              │
│  ┌─────────────┐  ┌─────────────────────┐  ┌─────────────┐  │
│  │agent_chat_  │  │ message_service.rs  │  │ vcp_client  │  │
│  │application_ │  │ (消息持久化)         │  │ (SSE/HTTP)  │  │
│  │service.rs   │  │                     │  │             │  │
│  └─────────────┘  └─────────────────────┘  └─────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 1.4 核心设计原则

| 原则 | 说明 |
|------|------|
| **Backend-Driven Streaming** | Thinking 消息 ID 由后端生成并通过 SSE 事件推送，前端不再预创建占位消息。`chatStreamStore` 作为全局流消息池，保证前后台切换时流式状态不丢失。 |
| **Store 垂直拆分** | 会话状态 (`chatSessionStore`)、历史消息 (`chatHistoryStore`)、流式状态 (`chatStreamStore`)、附件暂存 (`attachmentStore`) 四者物理隔离，避免单一 God Store。 |
| **场景化滚动状态机** | `useChatScroll` 采用 `"initial" → "following" ↔ "free" → "loading-top"` 四状态机，替代 IntersectionObserver 的模糊边界判定，根治滚动回弹与误置底。 |
| **乐观更新 + 骨架接续** | 用户消息立即追加到 `currentChatHistory`，流式响应通过 `activeStreamMessages` 全局池维护；历史加载时通过 Object Hydration 将池中的"活"对象替换 DB 回流的骨架，实现无缝接续。 |
| **多模态输入统一收口** | 文本、语音(STT)、音频附件、文件附件、相机/相册全部收敛到 `InputEnhancer.vue`，最终通过 `historyStore.sendMessage()` 统一发射。 |

---

## 2. 聊天主视图（ChatView.vue）

> 文件位置：`src/features/chat/ChatView.vue`

### 2.1 组件结构

ChatView 采用 **Header + 消息区 + 输入区** 的三段式纵向 Flex 布局：

```
┌──────────────────────────────────────┐
│  Header (固定高度, 适配刘海屏)        │  <-- vcp-header-fixed
│  [☰] [Avatar] [Name+Status] [☀] [🔔] │
├──────────────────────────────────────┤
│                                      │
│  消息展示区 (flex-1, overflow-y-auto) │  <-- messageListRef
│  ┌────────────────────────────────┐  │
│  │  零数据引导状态 (绝对定位)      │  │
│  │  或 MessageRenderer 列表        │  │
│  └────────────────────────────────┘  │
│                                      │
├──────────────────────────────────────┤
│  [↓ 一键置底] (绝对定位, z-local)     │
├──────────────────────────────────────┤
│  InputEnhancer (固定底部)             │  <-- vcp-input-footer
│  [安全区留白]                         │
└──────────────────────────────────────┘
```

**关键 DOM 结构**（第 142–272 行）：

1. **容器层** (`chatViewContainerRef`)：全高 Flex 列，监听 `@touchmove` 以阻止非交互空白区域的 WebView 视口拖动（橡皮筋效应）。
2. **Header**：固定高度，使用 `calc(var(--vcp-safe-top, 24px))` 动态适配刘海屏；展示当前选中项头像、名称、`CoreStatusIndicator`、主题切换与通知按钮。
3. **消息列表** (`messageListRef`)：`flex-1 overflow-y-auto`，内部使用 `.messages-inner-container` 作为 `ResizeObserver` 的观测目标。
4. **一键置底按钮**：绝对定位，`z-local` 层级，动态计算 `bottom` 以避让输入框、键盘高度与扩展菜单。
5. **输入区** (`vcp-input-footer`)：固定底部，背景使用 `color-mix` 实现毛玻璃质感；内部渲染 `InputEnhancer`。

### 2.2 与 Store 的绑定关系

ChatView 是 4 个核心 Store 的**消费端协调器**，自身不持有业务状态，仅做组合与生命周期联动：

| Store | 绑定方式 | 用途 |
|-------|----------|------|
| `chatSessionStore` | 直接解构 | 读取 `currentSelectedItem` / `currentTopicId` 驱动 Header 展示与话题切换监听 |
| `chatHistoryStore` | 直接解构 | 读取 `currentChatHistory` 渲染消息列表；调用 `loadMoreHistory` 加载历史 |
| `chatStreamStore` | 直接解构 | 读取 `activeStreamingIds` 控制自动滚动启停 |
| `topicStore` | 直接解构 | 话题切换时 `markTopicAsRead`；零数据引导态判断 |

**话题切换监听**（第 62–81 行）：

```typescript
watch(
  [() => sessionStore.currentTopicId, () => sessionStore.currentSelectedItem],
  ([newTopicId, newSelectedItem]) => {
    showScrollToBottom.value = false;
    resetChatScroll();
    if (newTopicId && newSelectedItem) {
      topicStore.markTopicAsRead(newTopicId);
      historyStore.loadHistoryPaginated(
        newSelectedItem.id,
        newSelectedItem.type,
        newTopicId
      );
    } else {
      historyStore.currentChatHistory = [];
    }
  },
  { immediate: true }
);
```

关键行为：
- 话题切换时立即重置滚动状态机（`resetChatScroll` → `"initial"`）
- 若目标话题/选中项有效，触发分页历史加载（初始 limit = 5）
- 若无效（如未选中 Agent），清空当前历史数组

**空态处理（零数据引导）**（第 221–242 行）：

当 `historyStore.currentChatHistory.length === 0` 时，消息列表区展示绝对定位的引导层：

```
┌────────────────────────────────────────┐
│  大图标 (渐变背景 + 对话气泡 SVG)       │
│  "开启智能对话"                        │
│                                        │
│  条件文案 A:                            │
│    "当前话题暂无消息，开始发送第一条吧！" │
│  条件文案 B:                            │
│    "请从左侧列表选择一个助手..."        │
│                                        │
│  [立即开始] 按钮 (未选中 Agent 时展示)   │
└────────────────────────────────────────┘
```

- 文案 A：已选中 Agent/Group 但当前话题无消息
- 文案 B：未选中任何 Agent/Group（侧边栏未展开或首次进入）
- "立即开始"按钮调用 `layoutStore.toggleLeftDrawer()` 打开左侧抽屉
- 引导层使用 `rotate-3` 微旋转与 `shadow-2xl` 大投影，在极简布局中形成视觉焦点

**底部留白策略**：

`.messages-inner-container` 末尾固定插入 `<div class="h-20"></div>`（第 253 行），确保最后一条消息不会被输入框遮挡，同时为一键置底按钮留出呼吸空间。

### 2.3 生命周期钩子

**`onMounted`**（第 121–129 行）：
1. 注册 `window.addEventListener("vcp-button-click", ...)` —— 接收来自 VcpButton 组件的外部文本注入（如快捷指令）。
2. 在容器上监听 `focusin`，触发 `forceRecalculate` 以重新计算键盘 Insets（应对 Android 软键盘弹出时的布局变化）。
3. 调用 `checkAndLoadMore()` 触发首屏自动加载判定。

**`onUnmounted`**（第 131–139 行）：
1. 清理 `vcp-button-click` 事件监听。
2. 清理 `focusin` 监听。
3. 调用 `disposeChatScroll()` 彻底断开 `ResizeObserver` 与 `scroll` 事件。

**流式状态联动**（第 103–105 行）：
```typescript
watch(isStreamingActive, (active) => {
  active ? startAutoScroll() : stopAutoScroll();
});
```
当 `streamStore.activeStreamingIds` 从空变非空时启动自动滚动；流结束时停止。在新版 `useChatScroll` 架构中，`startAutoScroll` / `stopAutoScroll` 为空函数（保留 API 兼容），实际滚动由 `ResizeObserver` 自动驱动。

**键盘高度联动**（第 107–119 行）：
```typescript
watch(keyboardHeight, (height) => {
  chatViewContainerRef.value?.style.setProperty("--keyboard-offset", `${height}px`);
  if (height > 0 && historyStore.currentChatHistory.length > 0) {
    scrollToBottom(true); // 软键盘弹出时平滑置底
  }
});
```
将键盘高度写入 CSS 变量，供置底按钮与输入区安全区留白动态计算。

### 2.4 虚拟滚动与性能优化

ChatView **不采用传统虚拟滚动**（如 `vue-virtual-scroller`），而是基于以下策略保证长列表性能：

1. **分页加载**：初始仅加载 5 条最新消息（`loadHistoryPaginated`），用户上滑到顶时再以 10 条为增量加载旧消息（`loadMoreHistory`）。
2. **`useChatScroll` 场景状态机**：封装于 `src/core/composables/useChatScroll.ts`，详见下文。
3. **ResizeObserver + RAF 节流**：监听 `.messages-inner-container` 的物理尺寸变化，而非依赖 `nextTick` 的"猜时机"。
4. **滚动条隐藏**：通过 CSS `scrollbar-width: none` / `::-webkit-scrollbar { display: none }` 消除原生滚动条视觉干扰。

**消息列表的 DOM 结构细节**：

```vue
<div ref="messageListRef" class="flex-1 overflow-y-auto relative no-rubber-band">
  <!-- 零数据状态 (条件渲染) -->
  <div v-if="historyStore.currentChatHistory.length === 0" class="absolute inset-0 ...">
    ...
  </div>
  
  <!-- 消息列表容器 (始终渲染，ResizeObserver 的观测目标) -->
  <div class="messages-inner-container py-4 space-y-2 flex flex-col min-h-full">
    <MessageRenderer
      v-for="msg in historyStore.currentChatHistory"
      :key="msg.id"
      :message="msg"
      :agent-id="sessionStore.currentSelectedItem?.id"
      :data-message-id="msg.id"
    />
    <div class="h-20"></div>  <!-- 底部留白 -->
  </div>
</div>
```

- `:data-message-id="msg.id"` 是 `useChatScroll` 锚定机制的关键属性，用于在分页加载时识别视口顶部消息
- `.messages-inner-container` 必须始终存在于 DOM 中，因为它是 `ResizeObserver` 的观测目标；若使用 `v-if` 条件渲染，Observer 会在空态与列表态切换时断开
- `space-y-2` 提供消息间的紧凑间距，符合"高密度线性布局"规范

**`useChatScroll` 四状态机**：

| 状态 | 触发条件 | 行为 |
|------|----------|------|
| `initial` | 话题切换/消息清空后 | 首屏加载期间，滚动行为由 `ResizeObserver` 自动接管，首次内容到达后 `scrollToBottom(false)` 并迁移到 `following` |
| `following` | 用户位于底部 150px 内 | 新消息/流式内容到达时自动置底；持续评估是否需要自动续载历史 |
| `free` | 用户主动上滑离开底部 | 不再自动置底；允许用户自由浏览历史；上滑到顶部 100px 内触发 `loading-top` |
| `loading-top` | 上滑到顶触发分页加载 | 记录锚定元素（`prepareLoadAnchor`），加载完成后通过锚定偏移恢复滚动位置（`restoreScrollByAnchor`），回到 `free` |

**关键优化：加载锚定**

```typescript
const prepareLoadAnchor = () => {
  // 记录视口中最顶部可见消息的 data-message-id 及其距顶偏移
  loadAnchor = {
    messageId: el.getAttribute("data-message-id")!,
    offsetFromTop: rect.top - listRect.top,
  };
};
```

当旧消息 prepend 到列表后，通过锚定元素的 `offsetTop - offsetFromTop` 精确恢复用户视线位置，避免"加载后突然跳转"的糟糕体验。

**关键优化：物理高度守卫**

```typescript
const currentScrollHeight = list.scrollHeight;
if (currentScrollHeight === lastScrollHeight) return; // 高度无变化，直接拦截
lastScrollHeight = currentScrollHeight;
```

此守卫从物理上秒杀了用户手动上滑时的误置底无限回弹 Bug，同时极大释放 CPU 性能。

---

## 3. 输入增强栏（InputEnhancer.vue）

> 文件位置：`src/features/chat/InputEnhancer.vue`

### 3.1 输入框与工具栏

InputEnhancer 是前端对话引擎中**交互最密集**的组件，单文件 608 行，管理 4 种输入模式的切换与手势识别。

**整体布局**：

```
┌──────────────────────────────────────────────────────┐
│  GroupStopAllButton (绝对定位, 条件渲染)               │
├──────────────────────────────────────────────────────┤
│  附件选择菜单 (绝对定位浮层, fade-scale 动画)           │
│  [📷 拍摄] [🖼 相册] [📄 文件]                         │
├──────────────────────────────────────────────────────┤
│  暂存附件预览区 (横向滚动, TransitionGroup)            │
├──────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────┐  │
│  │ [🎤/⌨] │ 输入区                        │ [+] [➤]│  │
│  │ 按钮   │ (4 种状态互斥)                 │ 附件 发送│  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

**输入区 4 种互斥状态**：

| 状态 | 条件 | 展示内容 | 用户操作 |
|------|------|----------|----------|
| 键盘输入 | `!isAudioMode && !isLongPressRecording` | `<textarea>` 多行文本框 | 打字、粘贴、快捷键发送 |
| 按住说话 (待机) | `isAudioMode && !isSTTActive` | 灰色大条，文字"按住 说话" | 触摸开始 STT |
| STT 识别中 | `isSTTActive` | 蓝色脉冲指示器 + 实时转写文本 | 上滑取消，松手确认 |
| 长按录音中 | `isLongPressRecording` | 蓝色脉冲指示器 + 录音时长 | 上滑取消，松手以附件直发 |

**输入框自动增高**（第 306–318 行）：

```typescript
const autoResize = () => {
  if (textareaRef.value) {
    textareaRef.value.style.height = 'auto'; // Reset
    const scrollHeight = textareaRef.value.scrollHeight;
    textareaRef.value.style.height = `${scrollHeight}px`;
  }
};

watch(input, () => {
  nextTick(() => { autoResize(); });
});
```

- `rows="1"` 配合 `height: auto → scrollHeight` 实现从单行到多行的平滑扩展
- `max-height: 114px` 限制最大高度，超出后内部滚动
- `leading-[1.25]` 保持行高紧凑，符合项目"高密度线性布局"美学宪法

**Placeholder 逻辑**：
- `disabled` 为真时显示 `"请先选择话题以开启对话"`
- 正常状态显示 `"说点什么..."`
- `disabled` 由父组件 `ChatView` 通过 `:disabled="!sessionStore.currentTopicId"` 传入

**语音交互手势状态机**：

InputEnhancer 的语音交互涉及两组互斥的手势识别系统，共用左侧麦克风/键盘切换按钮：

**交互 A：STT（按住说话 → 实时转文字）**

```
┌─────────────────────────────────────────────────────────────┐
│ Tap 麦克风图标                                               │
│   │                                                         │
│   ▼                                                         │
│ ┌──────────────┐                                            │
│ │ isAudioMode  │──→ true                                    │
│ │ = true       │                                            │
│ └──────────────┘                                            │
│   │                                                         │
│   ▼                                                         │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ TouchStart on "按住 说话" 大条                          │  │
│ │   ├── preventDefault() 阻断默认上下文菜单               │  │
│ │   ├── isSTTActive = true                               │  │
│ │   ├── 记录 touchStartY                                  │  │
│ │   └── await startListening() 启动浏览器 SpeechRecognition│  │
│ └────────────────────────────────────────────────────────┘  │
│   │                                                         │
│   ▼                                                         │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ TouchMove                                               │  │
│ │   ├── diffY = currentY - touchStartY                   │  │
│ │   ├── diffY < -60 → isSwipeCancel = true (上滑取消)     │  │
│ │   └── 振动反馈 (navigator.vibrate)                      │  │
│ └────────────────────────────────────────────────────────┘  │
│   │                                                         │
│   ▼                                                         │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ TouchEnd                                                │  │
│ │   ├── isSwipeCancel = true → cancelListening() + Toast  │  │
│ │   └── isSwipeCancel = false → stopListening()           │  │
│ │       ├── 获取 recognizedText                           │  │
│ │       ├── input.value += recognizedText                 │  │
│ │       ├── isAudioMode = false (自动切回键盘)             │  │
│ │       └── textareaRef.focus() + autoResize()            │  │
│ └────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**交互 B：长按录音（音频附件直发）**

```
┌─────────────────────────────────────────────────────────────┐
│ TouchStart on 麦克风图标 (非语音模式时)                      │
│   ├── preventDefault()                                     │
│   ├── 记录 iconTouchStartTime                              │
│   ├── isIconLongPress = false                              │
│   └── setTimeout(350ms) → isIconLongPress = true           │
│       └── await startRecording()                           │
│           └── isLongPressRecording = true                  │
│                                                            │
│ TouchMove (仅当 isLongPressRecording)                       │
│   └── 同 STT 的上滑取消逻辑 (diffY < -60)                  │
│                                                            │
│ TouchEnd                                                   │
│   ├── duration < 350ms && !isIconLongPress                 │
│   │   └── 判定为 Tap → toggleAudioMode() (切换语音/键盘)    │
│   ├── isLongPressRecording → stopRecording()               │
│   │   ├── 获取 audio blob                                  │
│   │   ├── invoke('store_file') 保存为 .webm               │
│   │   ├── 塞入 stagedAttachments                           │
│   │   └── await handleSend() (松手直接发送)                │
│   └── isSwipeCancel → cancelRecording() + Toast            │
└─────────────────────────────────────────────────────────────┘
```

核心设计原则：**右侧的 `+` 按钮与发送/停止按钮在任何状态下都保持静止**，通过仅改变中间输入区的形态来实现模式切换，消除视觉抖动。

### 3.2 附件选择触发

附件菜单为**绝对定位浮层**（`bottom-16 right-4`），通过 `fade-scale` 动画进出，提供三个入口：

| 按钮 | 模式 | Android 行为 | 非 Android 行为 |
|------|------|-------------|----------------|
| 拍摄 | `camera` | 调用原生 `plugin:vcp-mobile|pick_file` | `<input accept="image/*" capture="environment">` |
| 相册 | `gallery` | 调用原生 File Picker | `<input accept="image/*">` |
| 文件 | `file` | 调用原生 File Picker | `<input accept="*/*">` |

点击后执行 `triggerFilePick(mode)`（第 364–369 行）：

```typescript
const triggerFilePick = async (mode: 'camera' | 'gallery' | 'file') => {
  showAttachMenu.value = false;
  emit('attach');
  await attachmentStore.handleAttachment(mode);
};
```

`emit('attach')` 通知父组件 `ChatView` 关闭菜单（若未来需要联动处理），实际附件操作完全委托 `attachmentStore.handleAttachment`。

### 3.3 发送按钮状态管理

发送/操作按钮的状态由两个计算属性联合决定：

```typescript
const isGenerating = computed(() => streamStore.activeStreamingIds.size > 0);
const hasContent = computed(() => 
  input.value.trim() !== '' || attachmentStore.stagedAttachments.length > 0
);
```

**按钮显隐与行为**：

| 场景 | `isGenerating` | `hasContent` | 按钮展示 | 点击行为 |
|------|----------------|--------------|----------|----------|
| 空闲无内容 | false | false | 隐藏 | — |
| 空闲有内容 | false | true | 蓝色纸飞机 | `handleSend()` → `emit('send', input.value)` |
| 生成中 | true | * | 红色停止方块 | `handleAction()` → `streamStore.stopMessage(id)` |

**发送快捷键**：`Ctrl/Cmd + Enter` 触发 `handleAction()`（第 357–362 行）。

**编辑消息回填**（第 327–338 行）：
```typescript
watch(() => historyStore.editMessageContent, async (newContent) => {
  if (newContent) {
    input.value = newContent;
    historyStore.editMessageContent = ''; // 消费掉
    isAudioMode.value = false;
    await nextTick();
    if (textareaRef.value) {
      textareaRef.value.focus();
      textareaRef.value.dispatchEvent(new Event('input', { bubbles: true }));
    }
  }
});
```

当用户点击"重新生成"或"编辑"某条历史消息时，`chatHistoryStore` 将目标内容写入 `editMessageContent`，InputEnhancer 消费后自动回填到输入框并聚焦。

**发送按钮的动画设计**：

发送/停止按钮使用 `pop-slide` 过渡动画（`<style scoped>` 第 569–587 行）：
- **进入**：从 `scale(0.4) translateX(20px)` 弹性放大到正常尺寸，使用 `cubic-bezier(0.34, 1.56, 0.64, 1)` 产生轻微的过冲感
- **离开**：收缩到 `width: 0` 并从右侧滑出，`margin-left: -6px` 确保相邻的 `+` 按钮平滑靠拢
- 此动画与 `+` 按钮的 `rotate-45` 旋转联动，形成"展开菜单时发送按钮出现，收起菜单时发送按钮消失"的视觉呼应

### 3.4 长文本粘贴处理（useLongTextPaste）

> 文件位置：`src/features/chat/composables/useLongTextPaste.ts`

**职责**：提供针对 Android 端长文本粘贴的三层递进拦截，将超过阈值的文本自动转存为 `.txt` 附件，避免输入框卡顿与上下文爆炸。

**函数签名**：

```ts
export function useLongTextPaste(input: Ref<string>): {
  handlePaste: (event: ClipboardEvent) => Promise<void>;
  handleBeforeInput: (event: InputEvent) => void;
  isProcessing: Ref<boolean>;
  LONG_TEXT_THRESHOLD: number;
}
```

| 参数/返回值 | 类型 | 说明 |
|-------------|------|------|
| `input` | `Ref<string>` | 输入框的响应式文本引用，用于 Layer 3 突变检测与旧值回退 |
| `handlePaste` | `(ClipboardEvent) => Promise<void>` | Layer 1 拦截器，绑定 `paste` 事件；优先级：文件 > 图片 > 长文本 |
| `handleBeforeInput` | `(InputEvent) => void` | Layer 2 拦截器，绑定 `beforeinput` 事件；覆盖 Android 键盘粘贴按钮 |
| `isProcessing` | `Ref<boolean>` | 是否正在将长文本转为附件，防止重复触发 |
| `LONG_TEXT_THRESHOLD` | `number` | 阈值常量（1000 字符），超过即触发自动附件化 |

**问题背景**：Android 端直接粘贴数千字甚至上万字的长文本到 `<textarea>` 会导致：
1. 输入框严重卡顿甚至 ANR
2. 后端上下文爆炸，Token 消耗失控
3. WebView 内存压力骤增

**三层拦截策略**：

```
Layer 1: ClipboardEvent (paste)
         │
         ├── 文件类型? ──→ 放行，由 attachmentStore 处理
         ├── 图片类型? ──→ 放行，由 attachmentStore 处理
         └── 纯文本且 > 1000 字符? ──→ event.preventDefault() → stageTextAsFile()
         
Layer 2: InputEvent (beforeinput)
         │
         └── inputType === 'insertFromPaste' 且 data.length > 1000?
             ──→ event.preventDefault() → stageTextAsFile()
             
Layer 3: watch(input) 突变检测
         │
         └── delta > 1000 字符?
             ──→ 回退旧值 (input.value = oldVal)
             ──→ stageTextAsFile(增量文本)
```

阈值常量：`LONG_TEXT_THRESHOLD = 1000`（字符数，非字节数）。

**`stageTextAsFile` 流程**：

```
用户粘贴长文本
    │
    ▼
┌─────────────────────┐
│ 校验话题已选择       │──> 无选中项则 Toast 提示并退出
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ 生成 .txt 文件名     │──> `pasted_text_YYYY-MM-DD-HH-mm-ss.txt`
│ 编码为 Uint8Array    │
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ 插入 loading 占位    │──> stagedAttachments.unshift({ status: 'loading', ... })
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ invoke('store_file') │──> Rust 后端持久化到附件沙盒
│ 传递 bytes + mime    │
└──────────┬──────────┘
           ▼
    ┌──────┴──────┐
   成功           失败
    │              │
    ▼              ▼
┌────────┐   ┌────────────┐
│更新为   │   │移除占位卡片 │
│done状态 │   │Toast 错误提示│
└────────┘   └────────────┘
```

关键设计：长文本被转换为 `.txt` 附件后，随用户下一条消息一起发送，由后端 `message_service` 在组装上下文时提取文本内容。这既保护了前端输入体验，也保留了文本的完整语义。

**为何需要三层拦截？**

不同平台与输入法的粘贴行为差异极大：
- **桌面浏览器**：标准 `paste` 事件可靠，Layer 1 即可拦截
- **Android Gboard/搜狗**：某些版本使用 `beforeinput` 而非标准 `paste`，需 Layer 2
- **第三方输入法（如微信键盘）**：可能直接突变 DOM 值而不触发上述任一事件，需 Layer 3 的 `watch(input)` 做最终兜底

三层呈**递进防御**关系，而非互斥。任何一层成功拦截都会阻止长文本直接进入输入框。

---

## 4. 附件暂存预览（StagedAttachmentPreview.vue）

> 文件位置：`src/features/chat/StagedAttachmentPreview.vue`

### 4.1 暂存附件展示

该组件负责渲染 `attachmentStore.stagedAttachments` 中每一项的**缩略图/卡片**。本身仅 63 行，是轻量级的展示壳，实际渲染委托 `AttachmentRenderer`（位于 `src/features/chat/attachment/AttachmentRenderer.vue`）。

**尺寸策略**：
- 图片类型：固定 `w-14 h-14`（56×56px 小方块）
- 非图片类型：`max-w-[180px]` 自适应宽度卡片

**Loading Overlay**：
当 `file.status === "loading"` 时，覆盖半透明黑色蒙层，展示：
- 旋转的 `animate-spin` 圆环
- 若存在 `file.progress`，显示百分比数字（`text-[9px]`，`tabular-nums` 保证等宽数字不抖动）

### 4.2 移除与重选

**移除**：
- 点击附件卡片上的移除按钮 → `emit('remove', index)` → `attachmentStore.removeStaged(index)`
- `removeStaged` 只解除草稿关联；无引用 CAS 由冷启动回收处理

**重选**：
- 用户可随时再次点击 `+` 按钮打开附件菜单添加更多附件
- `stagedAttachments` 是一个列表，支持多个附件同时暂存

### 4.3 与 attachmentStore 的联动

InputEnhancer 模板中渲染暂存区的逻辑（第 404–414 行）：

```vue
<div v-if="attachmentStore.stagedAttachments.length > 0" 
     class="flex items-center gap-2 mb-2 px-2 overflow-x-auto pb-1 pt-2">
  <TransitionGroup name="list">
    <StagedAttachmentPreview 
      v-for="(file, idx) in attachmentStore.stagedAttachments" 
      :key="file.id || idx" 
      :file="file" 
      :index="idx"
      @remove="removeStagedAttachment"
    />
  </TransitionGroup>
</div>
```

- 使用 `TransitionGroup name="list"` 实现附件卡片的进入/离开动画
- `overflow-x-auto` 支持横向滚动（多条附件时）
- `unshift` 插入策略保证**最新附件始终在最左侧**（符合视觉动线：刚添加的附件最先看到）

**附件状态生命周期**：

暂存附件在 `attachmentStore.stagedAttachments` 中经历以下状态流转：

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ 选择中   │ → │ loading │ → │  done   │ → │ 发送后   │
│(无状态)  │    │(进度圈)  │    │(可移除)  │    │(随消息)  │
└─────────┘    └─────────┘    └─────────┘    └─────────┘
                    │
                    ▼
               ┌─────────┐
               │  失败    │
               │(自动移除) │
               └─────────┘
```

- `loading`：文件正在上传或后端注册中，展示进度圈
- `done`：上传完成，可点击移除按钮取消暂存
- 发送后：附件对象被嵌入到 `ChatMessage.attachments` 数组中，随消息一起传递给 Rust
- 失败：上传异常时 Toast 提示并自动从暂存列表移除

---

## 5. 消息发送完整流程

### 5.1 用户点击发送后的时序（ASCII）

```
用户点击发送 (InputEnhancer.handleSend)
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ InputEnhancer                                               │
│   emit('send', input.value)                                 │
│   input.value = ''                                          │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ ChatView                                                    │
│   @send="historyStore.sendMessage"                          │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ ChatHistoryStore.sendMessage(content)                       │
│   1. 校验: currentSelectedItem / currentTopicId 非空         │
│   2. 检查 editingOriginalMessageId → 走编辑重发分支          │
│   3. 取出 stagedAttachments → attachmentStore.clearStaged() │
│   4. 构造 ChatMessage (role='user')                         │
│   5. 乐观追加: currentChatHistory.push(userMsg)             │
│   6. topicStore.incrementTopicMsgCount()                    │
│   7. 调用 triggerGeneration(userMsg)                        │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ ChatHistoryStore.triggerGeneration                          │
│   1. invoke('append_single_message')                        │
│      → Rust 将用户消息持久化到 SQLite                        │
│   2. 接收 compiledBlocks → 更新用户消息的 blocks            │
│   3. 创建 Channel 用于接收 SSE 事件                          │
│   4. channel.onmessage = (event) =>                         │
│      streamStore.processStreamEvent(event, callbacks)       │
│   5. 判断类型: group → invoke('handle_group_chat_message')  │
│               agent → invoke('handle_agent_chat_message')   │
└─────────────────────┬───────────────────────────────────────┘
                      │ IPC (Tauri Command + Channel)
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Rust Backend                                                │
│   agent_chat_application_service.rs                         │
│     internal_process_agent_chat_message                     │
│       1. read_agent_config → 获取 model/temperature/prompt  │
│       2. message_service::append_single_message (用户消息)  │
│       3. load_chat_history_internal → 组装上下文             │
│       4. assemble_history_for_vcp                           │
│       5. 注入 System Prompt (mobile_system_prompt 优先)      │
│       6. 生成 thinking_id                                   │
│       7. 推送 StreamEvent::thinking → Channel               │
│       8. start_stream_service (Android 前台保活)             │
│       9. perform_vcp_request → SSE 流式请求                  │
│      10. 逐 chunk 推送 StreamEvent::data / aurora            │
│      11. stop_stream_service                                │
│      12. 预渲染 blocks → patch_single_message               │
│      13. 推送 StreamEvent::end (携带 blocks + topicUpdatedAt)│
└─────────────────────┬───────────────────────────────────────┘
                      │ Channel.onmessage
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ ChatStreamStore.processStreamEvent                          │
│   type === 'thinking':                                      │
│     → 创建 reactive ChatMessage 骨架                        │
│     → activeStreamMessages.set(id, msg)                     │
│     → callbacks.onMessageCreated → historyStore.push(msg)   │
│     → invoke('append_single_message') 异步持久化骨架         │
│                                                             │
│   type === 'data':                                          │
│     → msg.content += chunk                                  │
│     → msg.tailContent = msg.content                         │
│                                                             │
│   type === 'aurora':                                        │
│     → msg.content / tailContent / blocks / tailBlock 更新   │
│                                                             │
│   type === 'end' / 'error':                                 │
│     → msg.tailContent = ''                                  │
│     → removeSessionStream()                                 │
│     → invoke('process_message_content') → 编译 blocks        │
│     → callbacks.onStreamFinished → summarizeTopic()         │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 附件预处理与上传

附件在发送前经历**两道处理关卡**：

**关卡 1：选择阶段上传**

***Android 原生选择器的双轨事件监听***（`attachmentStore.ts` 第 79–248 行）：

Android 端由于 WebView 文件选择器的限制，采用原生 Kotlin 插件实现文件选择。前端通过**双轨监听**（Invoke 返回值 + CustomEvent 事件流）确保可靠性：

```
invoke('plugin:vcp-mobile|pick_file')
    │
    ├── 轨道 A: Invoke 返回值 (Promise  resolve)
    │       └── 正常流程：返回 { path, name, mime, size, hash, thumbnailPath }
    │
    └── 轨道 B: CustomEvent 事件流 (Kotlin → WebView evaluateJavascript)
            ├── vcp-mobile-file-start  → 插入 loading 占位
            ├── vcp-mobile-file-progress → 更新进度条
            └── vcp-mobile-file-picked  → 更新为完成状态
```

双轨设计的必要性：
1. **大文件场景**：Kotlin 侧的文件拷贝与哈希计算可能耗时数秒，若仅依赖 Invoke 返回值，前端在这段时间内没有任何反馈，用户会以为点击未生效
2. **事件丢失容错**：若 WebView 错过了 `file-start` 事件，`file-progress` 回调中包含文件名与 MIME 类型，可"自我修复"补建卡片（第 114–125 行）
3. **5 分钟熔断**：`setTimeout(300000)` 防止 Kotlin 侧异常崩溃后前端永远挂起

***非 Android 端的标准流程***（`attachmentStore.ts` 第 251–452 行）：

```
<input type="file"> 选择文件
    │
    ▼
┌─────────────────────────────────────────┐
│ 1. 大小拦截 (非 GIF 图片 > 10MB)        │──> Toast 拒绝
│ 2. 分辨率拦截 (> 8K×8K)                 │──> Toast 拒绝
└────────────────────┬────────────────────┘
                     ▼
         ┌───────────┴───────────┐
         ▼                       ▼
    小文件 (< 2MB)           大文件 (≥ 2MB)
         │                       │
         ▼                       ▼
┌─────────────────┐     ┌─────────────────────┐
│ store_file IPC  │     │ prepare_vcp_upload  │
│ Uint8Array 直传 │     │ 获取临时 TCP 端点    │
│ 零拷贝序列化    │     │ XHR POST 流式上传    │
└─────────────────┘     │ upload.onprogress   │
                        │ (33ms 节流刷新)     │
                        └─────────────────────┘
```

附件派生文本在 Rust 注册 CAS 时提取并持久化。发送链只传递已完成注册的附件 DTO；暂存区清空不会在线删除物理文件。

### 5.3 调用 Tauri Command 发送消息

**单聊入口**：`handle_agent_chat_message`

```typescript
await invoke("handle_agent_chat_message", {
  payload: {
    agentId,
    topicId,
    userMessage: userMsg,
    vcpUrl: settings.vcpServerUrl || "",
    vcpApiKey: settings.vcpApiKey || "",
  },
  streamChannel
});
```

**群聊入口**：`handle_group_chat_message`

```typescript
await invoke("handle_group_chat_message", {
  payload: {
    groupId: sessionStore.currentSelectedItem.id,
    topicId,
    userMessage: userMsg,
    vcpUrl: settings.vcpServerUrl || "",
    vcpApiKey: settings.vcpApiKey || "",
  },
  streamChannel
});
```

两者结构对称，区别仅在于 `agentId` vs `groupId`，后端分别路由到 `agent_chat_application_service.rs` 与 `group_chat_application_service.rs`。

**Payload 的字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `agentId` / `groupId` | `string` | 是 | 消息归属的 Agent 或 Group 唯一标识 |
| `topicId` | `string` | 是 | 话题唯一标识，决定消息在数据库中的分区 |
| `userMessage` | `ChatMessage` | 是 | 前端构造的完整用户消息对象（含 content、attachments、blocks、shell 等）|
| `vcpUrl` | `string` | 是 | VCP 服务器基础 URL，来自 `settingsStore.settings.vcpServerUrl` |
| `vcpApiKey` | `string` | 是 | VCP API 鉴权密钥，来自 `settingsStore.settings.vcpApiKey` |

`vcpUrl` 与 `vcpApiKey` 由前端传入而非后端直接读取配置，是为了支持多服务器切换场景（未来可能扩展为按 Agent 配置不同服务器）。

**`userMessage` 的构造细节**（`chatHistoryStore.ts` 第 316–327 行）：

```typescript
const userMsg: ChatMessage = {
  id: `msg_${now}_user_${Math.random().toString(36).substring(2, 9)}`,
  role: "user",
  name: userName,
  content,
  timestamp: now,
  attachments: currentStaged.length > 0 ? currentStaged : undefined,
  shell: streamStore.computeShell({ role: "user", name: userName }),
  blocks: [{ type: "markdown" as const, content }],
};
```

- `id` 格式：`msg_{timestamp}_user_{随机后缀}`，确保前端生成的 ID 在单次会话内唯一
- `shell` 由前端本地计算，避免后端重复查询 avatar 颜色；包含 `avatarColor`、`displayName`、`isUser`
- `blocks` 初始为单元素 markdown 数组，后端 `append_single_message` 会重新编译为结构化 blocks 并回传更新

### 5.4 乐观更新与错误回滚

**乐观更新**：用户消息在调用任何 IPC 之前就已经 `currentChatHistory.push(userMsg)`（第 329 行）。这保证了无论网络延迟多大，用户都能立即在列表底部看到自己的消息。

**流式骨架所有权**：Rust application service 先调用 `begin_stream_message`，以单个事务 insert-only 创建 pending message、render cache、FTS 与 `active_generations`，提交成功后才发送 `thinking`。`processStreamEvent` 只创建/更新前端响应式对象，绝不再发异步 skeleton 写入；因此迟到的前端 IPC 不可能覆盖 terminal 正文。

**`chatHistoryStore` 的分页加载策略**：

```typescript
// 初始加载（切换话题时）
const loadHistoryPaginated = async (ownerId, ownerType, topicId) => {
  hasMoreHistory.value = true;
  await loadHistory(ownerId, ownerType, topicId, 5);
};

// 加载更多（用户上滑到顶时）
const loadMoreHistory = async () => {
  if (!hasMoreHistory.value || isLoadingHistory.value) return;
  await loadHistory(ownerId, ownerType, topicId, 10, true);
};
```

- 初始 limit = 5：仅加载最新消息，确保首屏快速渲染
- 增量 limit = 10：平衡加载速度与请求频率
- 加载旧页时以后端 `(timestamp, msg_id)` 游标定位当前最老消息
- `hasMoreHistory` 在返回消息数 < limit 时置为 false，阻止后续空请求

**会话一致性校验**：

`loadHistory` 在请求入口冻结完整 `ConversationKey` 与 loadId：
```typescript
const key = { ownerId, ownerType, topicId, epoch };
const loadId = ++currentLoadId;
const messages = await invoke("load_chat_history", { ownerId, ownerType, topicId, limit, offset });
if (loadId !== currentLoadId || !sessionStore.isConversationCurrent(key)) return;
```

这不仅防止快速切换话题污染，也能识别 A→B→A 的旧 A 回调。旧 finally 同样无权释放新请求的 loading/controller。

**错误处理**：
- 若 `processStreamEvent` 收到 `type === "error"`，将错误信息追加到消息内容末尾（`msg.content += errorText`），并标记 `finishReason = "error"`。
- 若 `process_message_content` 调用失败，消息仍保留原始文本内容，仅 blocks 回退为空数组。
- 用户可通过点击"重新渲染"按钮触发 `reRenderMessage` 重试 Block 解析。

**编辑重发分支**：

当 `editingMessage` 非空时，`sendMessage` 进入编辑重发模式：

```
用户点击"编辑重发"
    │
    ▼
┌────────────────────────────────────────┐
│ 1. 读取并清空 editingMessage           │
│ 2. 找到原消息在 currentChatHistory 中的索引│
│ 3. 更新原消息的 content 与 blocks       │
│ 4. invoke('truncate_history_after_message')│
│    → Rust 按稳定消息锚点删除后续记录      │
│ 5. currentChatHistory.splice(targetIndex+1)│
│    → 前端同步截断                       │
│ 6. 使用后端返回值设置权威 msgCount     │
│ 7. triggerGeneration(targetMsg)        │
│    → 以修改后的消息作为最后一条用户消息   │
│      重新触发 AI 生成                   │
└────────────────────────────────────────┘
```

此机制实现了"修改历史消息并重新生成后续回复"的能力，是 VCP Mobile 对话体验的核心交互之一。

---

## 6. 群聊模式

### 6.1 GroupStopAllButton

> 文件位置：`src/features/chat/components/GroupStopAllButton.vue`

**职责**：在群聊模式下提供一个全局中断入口，一键停止所有正在生成的 Agent 回复。

**关键内部状态**：

| 状态/计算属性 | 类型 | 说明 |
|--------------|------|------|
| `isVisible` | `ComputedRef<boolean>` | 显隐条件：`streamStore.isGroupGenerating && streamStore.activeStreamingIds.size > 0` |
| `activeCount` | `ComputedRef<number>` | 当前活跃流数量，显示在按钮右侧计数徽章中 |
| `handleStopAll` | `() => void` | 点击回调：调用 `streamStore.stopGroupTurn(sessionStore.currentTopicId)` |

在群聊模式下，多个 Agent 可能同时生成回复（接力赛或并发模式）。`GroupStopAllButton` 提供一个**全局中断入口**。

**显隐条件**（第 10–12 行）：
```typescript
const isVisible = computed(() => {
  return streamStore.isGroupGenerating && streamStore.activeStreamingIds.size > 0;
});
```

- 仅在 `isGroupGenerating`（当前选中项为 group 且存在活跃流）时展示
- 按钮位于 InputEnhancer 上方 (`absolute -top-12`)，居中悬浮

**点击行为**：
```typescript
const handleStopAll = () => {
  if (sessionStore.currentTopicId) {
    streamStore.stopGroupTurn(sessionStore.currentTopicId);
  }
};
```

**视觉设计**：
- 按钮使用 `bg-black/75 backdrop-blur-md` 实现暗色毛玻璃背景
- 左侧 `Octagon` 图标（来自 `lucide-vue-next`）带有 `animate-pulse` 红色脉冲动画
- 右侧计数徽章使用 `font-mono` 等宽字体与 `bg-red-950/60` 暗红背景，形成技术精确感
- 整体位置在 InputEnhancer 上方 `-top-12`，`left-1/2 -translate-x-1/2` 水平居中，层级为 `z-local`
- 进入/离开动画为 `slide-fade`，从下方 15px 处滑入并伴随 `scale(0.95)` 到 `scale(1)` 的弹性恢复

**`stopGroupTurn` 实现**（`chatStreamStore.ts` 第 321–333 行）：
```typescript
const stopGroupTurn = async (topicId: string) => {
  await invoke("interruptGroupTurn", { topicId });
  const activeIds = Array.from(activeStreamingIds.value);
  if (activeIds.length > 0) {
    await Promise.all(activeIds.map(id => stopMessage(id)));
  }
};
```

- 先调用 Rust `interruptGroupTurn` 向后端发送全局中断信号
- 再对前端本地所有活跃流 ID 逐个调用 `stopMessage`，确保 UI 状态同步清理

### 6.2 群聊与单聊的状态差异

| 维度 | 单聊 (Agent) | 群聊 (Group) |
|------|-------------|-------------|
| Tauri Command | `handle_agent_chat_message` | `handle_group_chat_message` |
| 流消息标识 | `agentId` 作为 ownerId | `groupId` 作为 ownerId，`isGroupMessage = true` |
| 停止按钮 | 单条消息旁的独立停止 | `GroupStopAllButton` 全局停止所有 |
| 头像展示 | 固定为该 Agent 头像 | 每条消息可能来自不同 Agent，通过 `msg.agentId` 动态计算 `shell` |
| 消息归属 | 仅 `role = user/assistant` | 增加 `agentId` / `groupId` 字段区分发言者 |
| 流式并发 | 单一流 | 多并发流（`activeStreamingIds` 为 Set，可含多个 ID） |
| AI 总结标题 | 单 Agent 名称 | 使用群组上下文或默认名称 |

**群聊头像动态计算**：`chatStreamStore.computeShell`（第 31–53 行）在生成流消息骨架时，通过 `ctx.agentId` 从 `assistantStore.agents` 中查找对应 Agent，提取其 `avatarCalculatedColor` 与 `name`，保证每条群聊消息的气泡颜色与头像正确归属。

**`computeShell` 的着色逻辑**：

```typescript
function computeShell(msg: { role: string; agentId?: string; name?: string }): MessageShell {
  if (msg.role === "user") {
    const userColor = avatarStore.getDominantColor("user", "user_avatar") || "rgb(226,54,56)";
    return { avatarColor: userColor, displayName: msg.name || "User", isUser: true, ... };
  }
  const agent = msg.agentId ? assistantStore.agents.find(a => a.id === msg.agentId) : undefined;
  return {
    avatarColor: agent?.avatarCalculatedColor || "",
    displayName: msg.name || agent?.name || "AI",
    isUser: false,
    ...
  };
}
```

- 用户消息固定使用红色系 (`rgb(226,54,56)`) 作为头像背景
- Assistant 消息使用 Agent 配置中的 `avatarCalculatedColor`，未配置时为空（由 UI 层回退到默认灰色）
- `bubbleBorderColor` 与 `bubbleBoxShadow` 当前留空，为未来的消息高亮/选中样式预留扩展点

---

## 7. 与 Rust 后端的 IPC 交互

### 7.1 Tauri Commands 调用表

以下命令由对话引擎领域直接或间接调用：

| Command | 调用方 | 输入 | 输出 | 说明 |
|---------|--------|------|------|------|
| `handle_agent_chat_message` | `chatHistoryStore.triggerGeneration` | `{ payload, streamChannel }` | `{ status, messageId }` | 单聊流式请求入口，Backend-Driven Streaming |
| `handle_group_chat_message` | `chatHistoryStore.triggerGeneration` | `{ payload, streamChannel }` | `{ status, messageId }` | 群聊流式请求入口 |
| `append_single_message` | `triggerGeneration` | `{ ownerId, ownerType, topicId, message }` | `{ blocks, topicUpdatedAt }` | 持久化单条消息，返回预编译 blocks 与消息冒泡后的 Topic 时间 |
| `patch_single_message` | `updateMessageContent` | `{ ownerId, ownerType, topicId, message }` | `{ blocks, topicUpdatedAt }` | 增量更新消息并返回权威 Topic 时间 |
| `truncate_history_after_message` | `sendMessage` (编辑重发) | `{ ownerId, ownerType, topicId, anchorMessageId }` | `{ msgCount, updatedAt }` | 截断后返回剩余未删除消息的聚合状态 |
| `delete_messages` | `chatHistoryStore.deleteMessage` | `{ ownerId, ownerType, topicId, msgIds }` | `{ msgCount, updatedAt }` | 删除指定消息，并按剩余未删除消息返回可回落的 Topic 活动时间 |
| `regenerate_topic_response` | `chatHistoryStore.regenerateResponse` | `{ ownerId, ownerType, topicId, targetResponseMsgId, streamChannel }` | `{ msgCount }` | 后端定位用户锚点并重新生成 |
| `re_render_message` | `chatHistoryStore.reRenderMessage` | `{ messageId, topicId }` | `ContentBlock[]` | 重新解析消息内容为结构化 blocks |
| `fetch_raw_message_content` | `chatHistoryStore.fetchRawContent` | `{ messageId }` | `string` | 获取消息原始文本（用于编辑） |
| `summarize_topic` | `chatHistoryStore.summarizeTopic` | `{ ownerId, ownerType, topicId, agentName }` | `string` | AI 生成话题标题 |
| `process_message_content` | `chatStreamStore.processStreamEvent` | `{ content }` | `ContentBlock[]` | 将纯文本解析为结构化 blocks |
| `interruptRequest` | `chatStreamStore.stopMessage` | `{ messageId }` | `void` | 中止单条消息的生成请求 |
| `interruptGroupTurn` | `chatStreamStore.stopGroupTurn` | `{ topicId }` | `void` | 中止整个群聊回合 |
| `store_file` | `attachmentStore` / `useLongTextPaste` / `InputEnhancer` | `{ originalName, fileBytes, mimeType }` | `{ type, name, size, hash, internalPath }` | 小文件 IPC 直存 |
| `prepare_vcp_upload` | `attachmentStore` (大文件) | `{ metadata }` | `{ url, token }` | 开启高速 TCP 上传端点 |
| `register_local_file` | `attachmentStore` (Android) | `{ localPath, originalName, mimeType, thumbnailPath, stableId, expectedHash }` | 同 `store_file` | Android 原生选择文件的后端注册 |

### 7.2 事件/Channel 监听表

| 来源 | 名称 | 类型 | 接收方 | 说明 |
|------|------|------|--------|------|
| Rust → 前端 | `Channel<StreamEvent>` | Channel | `chatHistoryStore.triggerGeneration` / `regenerateResponse` | SSE 流式事件（thinking/data/aurora/end/error） |
| Rust → 前端 | `vcp-file-register-progress` | Tauri Event | `attachmentStore` | 大文件注册进度通知（稳定 ID + 百分比） |
| Kotlin → 前端 | `vcp-mobile-file-start` | CustomEvent | `attachmentStore.handleAttachment` | Android 原生选择器开始 |
| Kotlin → 前端 | `vcp-mobile-file-progress` | CustomEvent | `attachmentStore.handleAttachment` | Android 原生选择器进度 |
| Kotlin → 前端 | `vcp-mobile-file-picked` | CustomEvent | `attachmentStore.handleAttachment` | Android 原生选择器完成 |
| 前端内部 | `vcp-button-click` | CustomEvent | `ChatView` | VcpButton 外部文本注入 |

**Channel 与历史分页的边界**：

Tauri v2 `Channel<StreamEvent>` 只用于 thinking/data/aurora/end/error 这类真正持续到达的流式响应，由 `chatStreamStore.processStreamEvent` 统一调度。历史分页每批仅 10 条，通过 `load_chat_history` 一次返回 `ChatMessage[]`；请求 Promise 的 resolve/reject 就是唯一完成信号，避免 Channel 尾帧丢失后无限等待。

`Channel<StreamEvent>` 为 Rust → 前端的单向推送；前端显式创建 `new Channel<T>()` 并将其传入单次 `invoke`。

---

## 8. 设计决策与注意事项

### 8.1 为何放弃 IntersectionObserver 改用 scroll 事件状态机？

早期版本使用 `IntersectionObserver` 配合哨兵元素实现无限滚动与自动置底，但存在三个致命问题：
1. `rootMargin` 在移动 WebView 中行为不一致，导致"即将到底"的判定飘忽
2. `nextTick` 后操作滚动位置时，异步图片加载会导致布局持续变化，时机难以预测
3. 用户手动上滑浏览历史时，新消息到达后的自动置底会造成"回弹"，严重干扰阅读

`useChatScroll` 采用 **scroll 事件精确几何检测 + ResizeObserver 物理高度守卫 + 四状态机** 的组合，从根本上解决了上述问题。

### 8.2 为何 `sendMessage` 中先做乐观更新再调用 IPC？

用户消息的乐观追加（第 329 行）发生在 `triggerGeneration` 之前。这带来了两个好处：
1. **零感知延迟**：用户点击发送后立刻在列表底部看到消息，无需等待 Rust 持久化完成
2. **流式连贯性**：用户消息与后续的 thinking 骨架在列表中相邻，视觉上是连续的

代价：若 Rust `append_single_message` 失败，前端存在"已展示但未持久化"的消息。当前策略是依赖 Rust 侧的错误日志与后续加载时的状态校准，暂不实现前端主动回滚。

### 8.3 语音交互的双轨设计

InputEnhancer 实现了**两种语音输入模式**，彼此独立又共享同一物理按钮：

| 模式 | 触发方式 | 产物 | 适用场景 |
|------|----------|------|----------|
| **STT 语音转文字** | Tap 切换为语音模式 → 按住说话大条 | 文本填入输入框 | 用户想说一段话，但想编辑后再发 |
| **音频附件直发** | 长按左侧麦克风图标 (350ms+) | `.webm` 附件 + 立即发送 | 用户想发语音消息，不转文字 |

核心修复（第 178–184 行）：当处于语音模式时，点击麦克风图标判定为"切换回键盘"而非"开始长按录音"，彻底解决了 touchstart.prevent 导致 click 无法触发的"点不回去"手势缺陷。

### 8.4 全局流消息池的 OOM 防御

`chatStreamStore.activeStreamMessages` 是一个全局 `Map<string, ChatMessage>`，理论上无界增长会导致内存泄漏。防御策略：

1. **上限保护**：`MAX_STREAM_MESSAGES = 100`（第 76 行）
2. **按插入顺序清理最旧非活跃消息**（`enforceStreamPoolLimit`，第 78–89 行）
3. **延迟清理**：流结束后 1 秒才从池中删除（第 130–135 行），确保 `finalizeStream` 期间仍能通过 `messageId` 获取对象

### 8.5 附件大小分流的阈值选择

- **2MB**：`store_file` IPC 与 `prepare_vcp_upload` 高速链路的分界线
  - <2MB：IPC 直接传递 `Uint8Array`，Tauri v2 支持二进制跨层零拷贝
  - ≥2MB：XHR 流式上传，避免 IPC payload 过大导致序列化阻塞与内存峰值
- **10MB**：非 GIF 图片的拒绝阈值，防止 WebView 解码超限图片导致 OOM
- **8K×8K**：非 GIF 图片的分辨率拒绝阈值，超出此范围直接 Toast 提示压缩后重试

### 8.6 为何 `chatStreamStore` 需要全局流消息池？

在 Backend-Driven Streaming 架构之前，流式消息直接插入 `chatHistoryStore.currentChatHistory`，存在两个致命问题：

1. **前后台切换断流**：用户切到别的 Agent 再切回来，历史重新加载时会得到 DB 中的旧版本消息对象，失去响应式链接，流式动画停止
2. **群聊并发混乱**：多个 Agent 同时生成时，各自的增量更新相互覆盖

`activeStreamMessages: Map<string, ChatMessage>` 作为全局池解决了上述问题：
- 任何组件、任何会话都可以通过 `messageId` 获取到同一个响应式对象
- 历史加载时的 Object Hydration（第 113–114 行）用池中活对象替换骨架，保证流式状态不丢失
- 群聊模式下每个 Agent 的回复拥有独立的 `messageId`，天然隔离

### 8.7 `useChatScroll` 的 RAF 双重节流机制

`useChatScroll` 在两个层面使用 `requestAnimationFrame` 进行性能保护：

1. **ResizeObserver 回调内**：`scrollRafId = requestAnimationFrame(() => handleContentChange())`
   - 合并单帧内的高频尺寸变动（如批量图片加载完成时的连续重排）
2. **scroll 事件内**：`scrollThrottleId = requestAnimationFrame(() => { ... })`
   - 将 scroll 事件从"每像素触发"降频到"每帧触发"，在 60fps 下相当于 16ms 节流

此外，`triggerAutoLoadMoreWithDebounce` 使用 200ms 的 `setTimeout` 防抖，等待图片、头像与异步排版彻底稳定后再执行高度判定，避免"加载过早"导致的连续请求。

---

## 9. 术语速查表

| 术语 | 英文/缩写 | 定义 | 相关文件 |
|------|----------|------|----------|
| 对话引擎 | Chat Engine | `features/chat/` 领域的总称，负责用户与 AI 交互的完整前端链路 | 本域全部 |
| Backend-Driven Streaming | — | Thinking 消息 ID 由后端生成并通过 SSE 事件推送，前端不再预创建占位消息的架构模式 | `chatStreamStore.ts`, `agent_chat_application_service.rs` |
| Object Hydration | — | 历史加载时，若消息 ID 存在于 `activeStreamMessages` 全局池，则用池中"活"的响应式对象替换 DB 回流的骨架，实现流式接续 | `chatHistoryStore.ts` |
| 场景状态机 | Scroll Scene FSM | `useChatScroll` 中的 `"initial" → "following" ↔ "free" → "loading-top"` 四状态机，管理滚动行为 | `useChatScroll.ts` |
| 加载锚定 | Load Anchor | 分页加载前记录视口顶部可见消息的 ID 与偏移，加载后通过该锚点精确恢复滚动位置 | `useChatScroll.ts` |
| 物理高度守卫 | Height Guard | `handleContentChange` 中通过 `scrollHeight === lastScrollHeight` 拦截无实质变化的调用，防止误置底 | `useChatScroll.ts` |
| 乐观更新 | Optimistic Update | 用户消息在 IPC 调用前即追加到 UI 列表，保证零感知延迟 | `chatHistoryStore.ts` |
| 骨架消息 | Skeleton Message | 流式开始时后端持久化的空内容 Assistant 消息，用于前后台切换后的状态接续 | `chatStreamStore.ts` |
| JIT 文档处理 | JIT Document Processing | 发送前即时提取 txt/md/pdf/docx 等附件的文本内容，供后端组装上下文 | `attachmentStore.ts` |
| 高速链路 | High-Speed Channel | 针对大文件（≥2MB）的临时 TCP 上传端点，绕过 IPC 序列化瓶颈 | `attachmentStore.ts` |
| STT | Speech-to-Text | 语音转文字，通过 `useSpeechRecognition` 实现实时流式识别 | `InputEnhancer.vue` |
| 音频附件直发 | Voice Attachment Direct Send | 长按麦克风图标录制音频，松手后直接以 `.webm` 附件形式发送 | `InputEnhancer.vue` |
| 三层粘贴拦截 | Triple-Layer Paste Interception | `paste` → `beforeinput` → `watch(input)` 的三层防御，覆盖不同输入法与平台的粘贴行为 | `useLongTextPaste.ts` |
| 流消息池 | Stream Message Pool | `activeStreamMessages: Map<string, ChatMessage>`，全局维护所有正在生成的响应对象 | `chatStreamStore.ts` |
| 会话活跃流 | Session Active Streams | `sessionActiveStreams: Record<"itemId:topicId", string[]>`，按会话隔离活跃流 ID 列表 | `chatStreamStore.ts` |
| 前台服务保活 | Foreground Service Keepalive | Android 端通过 `tauri-plugin-vcp-mobile` 启动 `FOREGROUND_SERVICE_TYPE_REMOTE_MESSAGING`，降低 OEM 杀后台概率 | `agent_chat_application_service.rs` |
| 按 ID 隔离锁 | Per-ID Mutex | Agent 领域在 Rust 侧为每个 Agent ID 分配独立互斥锁，避免全局写锁瓶颈 | `agent_service.rs` |
| 聚合哈希冒泡 | Hash Bubble | 将子级变更的 SHA-256 哈希向上汇总到父级，供同步协议快速判断变更范围 | `agent_service.rs` |
| 消息骨架 | Message Skeleton | 流式响应开始时创建的仅含 id/role/timestamp 的空内容消息对象，用于占位与状态接续 | `chatStreamStore.ts` |
| 消息壳 | Message Shell | 包含头像色、气泡边框、显示名称、isUser 标志的纯展示属性对象，由前端本地计算 | `chatStreamStore.ts` |
| 尾部内容 | Tail Content | 流式输出过程中尚未被解析为结构化 Block 的原始文本尾部，用于实时打字机效果 | `chatStreamStore.ts` |
| Aurora 模式 | Aurora Mode | 后端预渲染流水线，在流式过程中即推送已稳定的结构化 Block 与尾部文本 | `chatStreamStore.ts` |
| 重新渲染 | Re-Render | 对已持久化的消息调用 `re_render_message`，重新执行 Block 解析与内容编译 | `chatHistoryStore.ts` |
| 截断历史 | Truncate History | 按时间戳删除其后的所有消息，用于编辑重发与批量删除 | `chatHistoryStore.ts` |
| 孤儿附件 | Orphaned Attachment | 被用户选择后又取消、未被任何消息引用的物理文件 | `attachmentStore.ts` |
| 高速上传端点 | Upload Endpoint | `prepare_vcp_upload` 返回的临时本地 TCP 接收器 URL 与 Token | `attachmentStore.ts` |
| 原生选择器 | Native File Picker | Android 端通过 `tauri-plugin-vcp-mobile` 调用的 Kotlin 文件选择器 | `attachmentStore.ts` |
| 双轨监听 | Dual-Track Listening | 同时监听 Invoke Promise 返回值与 CustomEvent 事件流的容错策略 | `attachmentStore.ts` |
| 编辑重发 | Edit & Regenerate | 修改历史用户消息后截断后续历史并重新触发 AI 生成的交互模式 | `chatHistoryStore.ts` |

---

## 10. 历史加载优化（v1.1.0）

本节覆盖 `chatHistoryStore.ts` 和 `useChatScroll.ts` 在 v1.1.0 中引入的历史消息加载优化机制。

### 10.1 分页加载策略

历史消息采用**两阶段分页**加载：

| 阶段 | 调用方 | limit | 游标 | 说明 |
|------|--------|:-----:|:----:|------|
| 首次加载 | `loadHistoryPaginated()` | 5 | 无 | 话题切换时触发，快速展示最近消息 |
| 加载更多 | `loadMoreHistory()` | 10 | 当前最老消息 | 用户上滑或自动触发，追加旧消息 |

Rust 后端使用 `(timestamp, msg_id)` 游标，前端只保留是否仍有旧消息和是否正在加载：

```typescript
const hasMoreHistory = ref(true);   // 是否还有更多旧消息
const isLoadingHistory = ref(false); // 防并发重复触发
```

当 Rust 返回的批次消息数 < limit 时，`hasMoreHistory` 置为 `false`，前端停止触发加载。

### 10.2 小页批量历史加载（load_chat_history）

首屏 5 条和分页 10 条都通过 `load_chat_history` 一次返回数组。分页上限很小，而旧 `load_chat_history_streamed` 后端本身也是先 `fetch_all` 再逐条 send；保留 Channel 只会增加一个可能永不 settle 的 completion Promise，没有真实流式收益。

```typescript
const messages = await invoke<ChatMessage[]>("load_chat_history", {
  ownerId,
  ownerType,
  topicId,
  limit,
  offset,
});
```

结果提交要求完整 `ConversationKey + epoch + loadId` 仍为当前 owner。offset=0 替换列表；offset>0 先按 message ID 去重再 prepend。加载函数显式返回 `HistoryPageResult { addedCount, error?, aborted? }`，供滚动状态机收敛。

### 10.3 显式分页完成协议

历史加载不再依赖 Channel `is_last` 或 DOM 高度变化判断完成。`useChatScroll.requestLoadMore` 在四个入口之间共享同一个 in-flight gate：

1. 分页前记录锚点并进入 `loading-top`；
2. await `loadMoreHistory()` 返回的 Promise；
3. finally + nextTick 后恢复锚点，并无条件回到 `free` / `following`；
4. reset/dispose 推进本地 generation，取消旧 throttle/rAF/debounce，旧 finally 无权污染新话题。

因此零条结果、请求异常、会话切换都不会把滚动永久留在 `loading-top`。

### 10.4 滚动锚点恢复

`useChatScroll.ts` 实现了**四场景滚动状态机**，在加载更多历史消息时保持用户的阅读位置不变：

```typescript
type ScrollScene = "initial" | "following" | "free" | "loading-top";
```

**锚定机制**：

```
1. 加载触发前: prepareLoadAnchor()
   ├── 查找视口中最顶部可见的消息 DOM 元素（data-message-id）
   └── 记录该元素的 messageId + offsetFromTop（距列表容器顶部的距离）

2. 加载完成后: restoreScrollByAnchor()
   ├── 通过 messageId 查找该元素的新 DOM 位置
   └── scrollTop = newOffsetTop - offsetFromTop（精确恢复）
```

**四个场景的状态转换**：

```
initial ──(首屏加载完成)──→ following ←──(用户滚到底部)── free
                                │                            │
                                │    (用户上滑到顶部 +        │
                                │     hasMoreHistory)         │
                                │                            ▼
                                └────────────── loading-top ──┘
                                              (加载完成后恢复锚点)
```

- **`initial`**：话题刚切换，等待首屏加载完成
- **`following`**：用户在底部，新消息自动置底
- **`free`**：用户手动上滑查看历史，新消息不打扰
- **`loading-top`**：正在加载更多历史，阻止重复触发

**物理高度守卫**：`handleContentChange` 中通过 `scrollHeight === lastScrollHeight` 拦截无实质高度变化的调用，彻底消除"误置底无限回弹"Bug。

### 10.5 AbortController 并发安全

每次调用 `loadHistory` 时，先中止前一次未完成的请求：

```typescript
let currentLoadAbortController: AbortController | null = null;

if (currentLoadAbortController) {
  currentLoadAbortController.abort();
}
const controller = new AbortController();
currentLoadAbortController = controller;
const { signal } = controller;
```

在 `load_chat_history` 返回后同时检查 `signal.aborted`、`loadId` 和不可变 `ConversationKey`（含 epoch），任一不匹配就丢弃整批过时数据。

### 10.6 活跃流消息水合（Object Hydration）

```typescript
const hydrated = messages.map((message) =>
  streamStore.activeStreamMessages.get(message.id) || message
);
```

当历史加载返回的消息 ID 存在于 `activeStreamMessages` 全局流池中时，优先使用池中的响应式对象。这确保了：
- 用户切到后台再切回来时，正在生成的流消息恢复流式动画
- DB 中的静态骨架被响应式"活"对象无缝替换

### 10.7 自动加载判定

`evaluateAutoLoadMore()` 利用 `ResizeObserver` 回调触发，在以下条件同时满足时自动加载更多：

1. `scrollScene !== "initial"`（首屏已过）
2. `list.scrollHeight <= list.clientHeight + 10`（内容不足以填满视口）
3. `hasMoreHistory && !isLoadingHistory`（有更多数据且未在加载中）

判定带 200ms 防抖，等待图片、头像与异步排版彻底稳定。

---

## 11. 搜索栏优化（v1.1.0）

本节覆盖 `AgentSidebar.vue`、`SidebarSearch.vue`、`AgentList.vue` 和 `TopicList.vue` 中的搜索与过滤机制。

### 11.1 架构总览

```
AgentSidebar.vue
├── SidebarTabs (agents / topics 切换)
├── SidebarSearch (通用搜索输入框)
│   ├── placeholder 随 activeTab 动态切换
│   └── 清除按钮 (v-if="modelValue")
├── AgentList (activeTab === 'agents')
│   ├── filteredCombinedItems (combined 层过滤 → 空结果提示)
│   ├── orderedGroups.filter (per-section 二次过滤)
│   └── orderedAgents.filter (per-section 二次过滤)
└── TopicList (activeTab === 'topics')
    ├── useVirtualList (虚拟滚动)
    └── filteredTopics (标题 + 日期双重匹配)
```

### 11.2 SidebarSearch.vue：通用搜索输入组件

> 文件位置：`src/features/agent/SidebarSearch.vue`

**Props**：
- `modelValue: string` — 搜索词（双向绑定）
- `activeTab: 'agents' | 'topics'` — 当前激活的标签页

**动态 placeholder**：
```typescript
const placeholderText = computed(() => {
  return props.activeTab === 'agents' ? '搜索助手...' : '搜索话题...';
});
```

**清除按钮**：当 `modelValue` 非空时，搜索框右侧显示 X 按钮，点击清除搜索词。使用 `@mousedown.prevent` 防止清除按钮触发输入框失焦。

### 11.3 标签切换自动清空搜索词

```typescript
const activeTab = ref<'agents' | 'topics'>('agents');
const searchQuery = ref('');

watch(activeTab, () => {
  searchQuery.value = ''; // 标签切换时自动清空
});
```

这避免了用户从 Agent 搜索切换到 Topic 搜索时，残留的搜索词干扰 Topic 列表的过滤结果。

### 11.4 AgentList.vue：双层过滤

**第一层：combined 全量过滤**（`filteredCombinedItems`）

```typescript
const filteredCombinedItems = computed(() => {
  const query = props.searchQuery.toLowerCase().trim();
  if (!query) return assistantStore.combinedItems;
  return assistantStore.combinedItems.filter((item) =>
    item.name.toLowerCase().includes(query),
  );
});
```

`combinedItems` 是 Agent 和 Group 的合并列表。此层过滤用于判断"是否有任何匹配结果"——当结果为空时显示"未找到助手或群组"的空状态。

**第二层：per-section 独立过滤**

```html
<!-- Group 分组 -->
<div v-for="group in orderedGroups.filter(
  (group) => !searchQuery.trim() ||
    group.name.toLowerCase().includes(searchQuery.toLowerCase().trim())
)">

<!-- Agent 分组 -->
<div v-for="agent in orderedAgents.filter(
  (agent) => !searchQuery.trim() ||
    agent.name.toLowerCase().includes(searchQuery.toLowerCase().trim())
)">
```

每个分组（"Agent Groups" 和 "Individual Agents"）独立过滤，搜索词为空时不过滤（展示全部）。双层设计的好处：
- `combined` 层判定整体空结果（展示空状态提示）
- `per-section` 层允许在"Groups 有匹配但 Agents 无匹配"的情况下仍然显示 Groups 分组

### 11.5 TopicList.vue：虚拟滚动 + 日期搜索

**虚拟滚动实现**：

```typescript
import { useVirtualList } from '@vueuse/core';

const { list, containerProps, wrapperProps } = useVirtualList(currentTopics, {
  itemHeight: 74,
  overscan: 10,
});
```

- **`itemHeight: 74`**：单条话题卡片的高度估算值
- **`overscan: 10`**：在可见区域外额外渲染 10 项，减少滚动时的空白闪烁
- **自动清理**：`onUnmounted` 时 `useVirtualList` 自动解绑滚动监听

**双字段搜索**（`topicListManager.ts:54-77`）：

```typescript
const filteredTopics = computed(() => {
  const term = searchTerm.value.toLowerCase().trim();
  if (!term) return topics.value;

  return topics.value.filter((topic) => {
    // 1. 标题匹配
    const nameMatch = topic.name.toLowerCase().includes(term);

    // 2. 日期匹配（支持两种格式）
    let dateMatch = false;
    const createdAt = (topic as any).createdAt || (topic as any).created_at;
    if (createdAt) {
      const date = new Date(createdAt > 1e11 ? createdAt : createdAt * 1000);
      const fullDateStr = `${YYYY}-${MM}-${DD} ${HH}:${mm}`;   // "2026-06-15 14:30"
      const shortDateStr = `${YYYY}-${MM}-${DD}`;                // "2026-06-15"
      dateMatch = fullDateStr.includes(term) || shortDateStr.includes(term);
    }

    return nameMatch || dateMatch;
  });
});
```

日期搜索支持两种格式：
- 完整日期时间：`"2026-06-15 14:30"` — 精确到分钟
- 短日期：`"2026-06-15"` — 按天搜索

搜索词在 `TopicList.vue` 组件挂载时通过 `watch` 同步到 Store：

```typescript
watch(
  () => props.searchQuery,
  (newVal) => { topicListStore.searchTerm = newVal || ""; },
  { immediate: true }
);
onUnmounted(() => { topicListStore.searchTerm = ""; });
```

组件卸载时自动清空 `searchTerm`，防止切回 Agent 标签后残留搜索状态。

### 11.6 性能特征

| 维度 | 策略 | 说明 |
|------|------|------|
| **Agent/Group 过滤** | 计算属性缓存 | `computed` 在依赖不变时复用上次结果 |
| **Topic 过滤** | Store 级 `computed` | 过滤结果在 Store 中计算，组件只读 |
| **Topic 列表渲染** | `useVirtualList` 虚拟滚动 | 仅渲染可见区域 ± overscan 的 DOM 节点 |
| **搜索清空** | 标签切换自动清空 | 防止跨标签残留搜索词导致的无关过滤 |
| **清除按钮** | `@mousedown.prevent` | 防止点击清除时触发输入框 blur + 重聚焦闪烁 |

---

## 12. 术语速查表（补充）

以下术语为 v1.1.0 新增或修订，补充至 §9 术语速查表：

| 术语 | 英文/缩写 | 定义 | 相关文件 |
|------|----------|------|----------|
| 分页加载 | Paginated Loading | 首次 5 条 + 加载更多 10 条的两阶段历史消息加载策略 | `chatHistoryStore.ts` |
| 小批量历史 | Batched History | 通过 `load_chat_history` 一次返回每页数组，Promise 结算即完成信号 | `chatHistoryStore.ts` |
| 会话代纪 | Conversation Epoch | 每次会话身份转换递增；异步结果只能提交到原 `ConversationKey` | `chatSessionStore.ts` |
| 加载锚定 | Load Anchor | 分页加载前记录视口顶部可见消息 ID 与偏移，加载后精确恢复滚动位置 | `useChatScroll.ts` |
| 四场景状态机 | 4-Scene State Machine | `initial → following ↔ free → loading-top` 滚动场景管理 | `useChatScroll.ts` |
| Object Hydration | 对象水合 | 历史加载时用 `activeStreamMessages` 池中活跃对象替换 DB 静态骨架 | `chatHistoryStore.ts` |
| 双层过滤 | Dual-Layer Filter | AgentList 中 `combined` 全量过滤 + `per-section` 独立过滤的组合策略 | `AgentList.vue` |
| 虚拟滚动 | Virtual Scroll | `@vueuse/core` 的 `useVirtualList`，仅渲染可见区域 DOM 节点 | `TopicList.vue` |
| 日期搜索 | Date Search | 话题搜索支持 `YYYY-MM-DD` 和 `YYYY-MM-DD HH:mm` 两种日期格式匹配 | `topicListManager.ts` |

---

*最后更新：2026-06-15 | VCP Mobile v1.1.0*
