---
id: VUE-TOPIC-014
title: 话题管理与虚拟导航
description: VCP Mobile 前端话题 CRUD、SlidePage 虚拟页面栈与 Hash 路由的导航架构
version: 1.1.5
date: 2026-08-29
---

# 14. 话题管理与虚拟导航

## 1. 概述

### 1.1 领域定位

`features/topic/` 是 VCP Mobile 前端**话题生命周期管理**与**虚拟导航**的集中承载域。该领域负责：

- **话题 CRUD**：创建、读取、更新标题、切换锁定/未读状态、删除话题
- **话题列表渲染**：基于虚拟列表的高性能长列表展示，支持搜索、创建/更新排序、本机置顶分区与状态角标
- **话题切换编排**：协调 `chatSessionStore`、`chatHistoryStore`、`chatStreamStore` 完成无闪烁的话题上下文迁移
- **虚拟页面栈（SlidePage）**：在单页 Hash 路由架构下，通过覆盖层堆叠模拟多页导航体验，并集成物理返回键拦截

该领域**不**涉及：
- 消息流式传输的协议解析（由 `chatStreamStore` 负责）
- 消息内容的结构化渲染（由 `MessageRenderer` 负责）
- 消息持久化的数据库操作（由 Rust `message_service.rs` 负责）

### 1.2 模块构成表

| 文件 | 行数 | 职责 |
|------|------|------|
| `TopicList.vue` | 328 | 话题列表渲染、虚拟列表、长按上下文菜单、话题选中 |
| `TopicCreator.vue` | 138 | 新建话题按钮、创建流程、创建后自动切换 |
| `topicListManager.ts` | 720 | 话题状态管理（Pinia）：加载、创建、删除、排序、置顶与乐观更新 |
| `chatSessionStore.ts` | 330 | 会话状态：当前选中项、当前话题 ID、最后活跃话题映射、分享意图会话 |
| `chatHistoryStore.ts` | 1035 | 聊天历史管理：流式加载、分页、发送消息、AI 总结标题 |
| `overlay.ts` | 456 | 全局覆盖层状态：SlidePage 栈、Prompt、ContextMenu、Editor |
| `useModalHistory.ts` | 179 | Operation Aegis：模态历史栈与物理返回键拦截 |

### 1.3 在整体架构中的位置

```
┌─────────────────────────────────────────────────────────────┐
│                      Vue 3 前端层                            │
│                                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              features/topic/ (本域)                    │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │  │
│  │  │ TopicList   │  │TopicCreator │  │topicListMgr │   │  │
│  │  │ (列表渲染)   │  │ (新建流程)   │  │ (状态管理)   │   │  │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘   │  │
│  │         │                │                │          │  │
│  │         └────────────────┼────────────────┘          │  │
│  │                          ▼                           │  │
│  │              ┌─────────────────────┐                 │  │
│  │              │  chatSessionStore   │                 │  │
│  │              │  (当前话题/选中项)   │                 │  │
│  │              └──────────┬──────────┘                 │  │
│  │                         ▼                            │  │
│  │              ┌─────────────────────┐                 │  │
│  │              │  chatHistoryStore   │                 │  │
│  │              │  (历史加载/发送)     │                 │  │
│  │              └─────────────────────┘                 │  │
│  └───────────────────────────────────────────────────────┘  │
│                           │                                  │
│         ┌─────────────────┼─────────────────┐               │
│         ▼                 ▼                 ▼               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐     │
│  │   overlay   │  │useModalHist │  │  router/index   │     │
│  │ (SlidePage) │  │(返回键拦截)  │  │ (Hash 路由)      │     │
│  └─────────────┘  └─────────────┘  └─────────────────┘     │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼ IPC (Tauri Commands)
┌─────────────────────────────────────────────────────────────┐
│                   src-tauri (Rust 核心层)                    │
│         message_service.rs / topic_service.rs                │
└─────────────────────────────────────────────────────────────┘
```

### 1.4 核心设计原则

| 原则 | 说明 |
|------|------|
| **流式加载优先** | 话题列表与历史记录均通过 `Channel` 流式获取，避免大列表阻塞主线程 |
| **乐观更新** | 创建/删除/锁定/未读状态在前端立即生效，后台命令异步同步 |
| **会话隔离** | `chatStreamStore` 按 `"itemId:topicId"` 隔离活跃流，切换话题天然不污染 |
| **虚拟导航** | 单路由 `/chat` 不变，所有"页面切换"通过 `SlidePage` 覆盖层堆叠实现 |
| **返回键消费** | Operation Aegis 保证返回手势先关闭 Modal → 再关闭 Page → 再退出应用 |

---

## 2. 话题列表（TopicList.vue）

`TopicList.vue` 是话题域的**核心展示组件**，负责话题列表的渲染、交互与上下文菜单。

> 文件位置：`src/features/topic/TopicList.vue`

### 2.1 组件结构

```
TopicList.vue
├── 加载态（loading 且尚无首批话题）
├── 空态（加载完成且无话题）
│   └── 图标 + "暂无话题，请先选择助手"
└── 虚拟列表容器（v-else）
    ├── 滚动容器（useVirtualList containerProps）
    │   └── 包装层（wrapperProps）
    │       ├── 分区标题（置顶 / 其他话题）
    │       └── 话题项（v-for item in list）
    │           ├── 未读角标（绝对定位红点/数字）
    │           ├── 话题图标（固定色块 + SVG）
    │           ├── 标题 + 消息计数
    │           ├── 话题 ID（monospace 截断）
    │           └── 解锁标签（Unlock 徽标）
    └── 新建话题按钮（TopicCreator，外部注入）
```

### 2.2 数据源与过滤

话题列表的基础顺序由 SQLite 按当前模式确定；`topicListManager.filteredTopics` 保持该顺序完成搜索过滤，`topicSections` 只拆分置顶/普通分区。

```ts
const { pinned, regular } = topicListStore.topicSections;
// 有可见置顶项时才插入“置顶/其他话题”分区行。
```

底层 `filteredTopics` 位于 `topicListManager.ts`：

```ts
const filteredTopics = computed(() => {
  const term = searchTerm.value.toLowerCase().trim();
  if (!term) return topics.value;
  return topics.value.filter((topic) => {
    const nameMatch = topic.name.toLowerCase().includes(term);
    // ... 日期匹配逻辑
    return nameMatch || dateMatch;
  });
});
```

**过滤与排序规则**：
1. **标题匹配**：不区分大小写的子串搜索
2. **日期匹配**：支持 `YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM` 格式搜索话题创建日期
3. **创建排序**：后端按 `createdAt DESC → id DESC` 返回
4. **更新排序**：后端按 `updatedAt DESC → createdAt DESC → id DESC` 返回
5. **本地变化**：排序模式切换或单条活动时间变化时，Store 只重排一次状态数组
6. **置顶**：先过滤，再将可见 Topic 按现有基础顺序分为置顶/普通两区

Rust 初次加载列表时直接 `LEFT JOIN messages`，只聚合 `deleted_at IS NULL` 的消息：`updatedAt = max(topic.updated_at, max(live_message.updated_at))`；没有未删除消息时使用 `topic.updated_at`，该值为 0 时再回退 `topic.created_at`。这使旧数据或尚未回填物化列的话题也能得到正确默认值。

列表加载后不再用前端 `Date.now()` 或单向 `Math.max()` 猜测活动时间。Topic 元数据命令、消息写入/编辑/删除以及 durable `StreamEvent::end` 都返回事务提交后的权威时间，`setTopicUpdatedAt()` 直接覆盖本地字段；因此删除最新消息后允许回落到上一条未删除消息的更新时间。

**虚拟列表参数**：

| 参数 | 值 | 说明 |
|------|-----|------|
| `itemHeight` | 32 / 74 px | 分区标题行 / 话题卡片的动态高度 |
| `overscan` | 10 | 视口外多渲染 10 项，减少滚动白屏 |

> 源码位置：`src/features/topic/TopicList.vue` 第 30–33 行

### 2.3 排序选择与本机置顶

- Topic 搜索框右侧的按钮打开锚定小菜单，提供“创建时间”和“更新时间”两个互斥选项。
- 排序模式是全局 Mobile 展示偏好；置顶键使用 `ownerType + ownerId + topicId` 复合身份。两者由 Pinia persisted state 保存在本机，不进入 Topic 同步 DTO 或 `.vcpcfg` 备份。
- 长按 Topic 后，`Topic Options` 顶栏右侧显示“置顶/取消置顶”；最后一个可见置顶项消失后，列表自动回到无分区布局。

### 2.4 当前话题高亮

当前选中话题通过 `sessionStore.currentTopicId` 与话题项 ID 比对实现高亮：

> 源码位置：`src/features/topic/TopicList.vue` 第 191–195 行

```vue
:class="[
  sessionStore.currentTopicId === item.data.id
    ? 'glass-panel-active'
    : 'border-transparent hover:bg-black/5 dark:hover:bg-white/5'
]"
```

- **选中态**：`glass-panel-active`（主题色高亮边框 + 背景）
- **非选中态**：透明边框 + hover 微亮反馈

### 2.5 话题状态（锁定、未读）

#### 2.5.1 未读角标

```
unreadCount > 0
    → 数字角标（min-w-[18px]，99+ 封顶）

unreadCount === -1 || unread === true
    → 小红点（w-3 h-3，绝对定位右上角）
```

> 源码位置：`src/features/topic/TopicList.vue` 第 197–204 行

#### 2.5.2 锁定状态

```
locked === false
    → 右下角显示 "Unlock" 徽标（LockOpen 图标 + 文字）
locked === true
    → 无额外标识（默认锁定态为静默）
```

> 源码位置：`src/features/topic/TopicList.vue` 第 233–237 行

**注意**：锁定/未读切换仅在 **Agent 模式**下通过长按菜单暴露。Group 模式的话题固定为锁定态，不提供解锁选项（见 `showTopicContextMenu` 第 93–115 行）。

---

## 3. 话题创建（TopicCreator.vue）

`TopicCreator.vue` 是话题创建的**触发入口**，封装了创建流程与防抖保护。

> 文件位置：`src/features/topic/TopicCreator.vue`

### 3.1 触发方式

| 触发方式 | 位置 | 说明 |
|----------|------|------|
| **+ 按钮** | 话题列表底部 | 常驻绿色按钮，disabled 时透明度 40% |
| **长按菜单** | 话题列表项 | `TopicList.vue` 的 `v-longpress` 可触发相关操作，但新建话题仅通过 + 按钮 |

### 3.2 创建流程

```
用户点击 "新建话题"
    │
    ▼
┌─────────────────────┐
│ 检查 currentItemId   │──> 无选中项 → alert("请先选择一个助手或群组")
│ 与 isCreating 状态   │──> 正在创建中 → 直接返回（防抖）
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ 生成默认名称          │──> `新话题 HH:MM:SS`（本地时间）
│ newTopicName         │    示例："新话题 14:32:07"
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ 判断 ownerType       │──> agent / group（通过 assistantStore.agents 比对）
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ topicStore.createTopic│──> 异步调用，显示 loading 态
│ (ownerId, type, name) │
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ 创建成功？           │──> 是 → selectTopic() 自动切换
│                      │──> 否 → 错误通知（store 层已处理）
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ 1 秒锁定解除         │──> setTimeout(1000) 后 isCreating = false
│ （防止快速连击）      │
└─────────────────────┘
```

> 源码位置：`src/features/topic/TopicCreator.vue` 第 45–88 行

### 3.3 与 create_topic Command 的集成

`topicListManager.ts` 中的 `createTopic` 方法封装了与 Rust 的 IPC：

> 源码位置：`src/core/stores/topicListManager.ts` 第 139–189 行

```ts
const newTopic = await invoke<Topic>("create_topic", {
  ownerId,
  ownerType,
  name,
});
```

创建成功后，前端执行**乐观更新**：
1. 将新话题 `unshift` 到 `topics.value` 数组头部
2. 强制触发虚拟列表重绘：`topics.value = [...topics.value]`
3. 显示 Toast 通知：`"话题创建成功"`
4. 新话题默认状态：`locked: true`, `unread: false`, `msgCount: 0`

### 3.4 创建后自动切换

`TopicCreator.vue` 在 `createTopic` resolve 后，立即调用 `selectTopic`：

> 源码位置：`src/features/topic/TopicCreator.vue` 第 76–77 行

```ts
if (newTopic?.id) {
  await selectTopic(currentItemId.value, newTopic.id, newTopic.name);
}
```

`selectTopic` 内部逻辑：
1. 若当前不在 `/chat` 路由，先 `router.push("/chat")`
2. 调用 `sessionStore.selectTopicById(itemId, topicId)`
3. 更新顶部栏显示名为话题标题
4. 移动端自动关闭左侧抽屉：`layoutStore.setLeftDrawer(false)`

---

## 4. 虚拟页面栈（SlidePage）

VCP Mobile 采用 **Hash 路由 + SlidePage 覆盖层** 的混合导航架构。绝大多数用户感知的"页面切换"并非真实路由跳转，而是固定 DOM 的覆盖层堆叠。

### 4.1 与 Vue Router 的关系

```
真实路由（Vue Router Hash 模式）
    │
    ├── /         → redirect → /chat
    └── /chat     → ChatView.vue（唯一路由页）
    
虚拟导航（SlidePage 覆盖层）
    │
    ├── SettingsView        → pushPage('settings')
    ├── AgentSettingsView   → pushPage('agentSettings', id)
    ├── GroupSettingsView   → pushPage('groupSettings', id)
    ├── SyncSessionView     → openSyncSession()
    └── RebuildSessionView  → openRebuildSession()
```

**本质区别**：
- Vue Router 负责应用级别的**根上下文切换**（目前仅 `/chat`）
- SlidePage 负责**功能视图的层叠展示**，URL 始终为 `/#/chat`，不产生路由历史

### 4.2 页面打开/关闭机制

`overlayStore` 维护一个 `pageStack` 数组，记录当前打开的虚拟页面：

> 源码位置：`src/core/stores/overlay.ts` 第 22–66 行

```ts
interface PageStackItem {
  type: string;
  id?: string;
  modalId: string;
}

const pageStack = ref<PageStackItem[]>([]);
```

**打开页面**（`pushPage`）：

```ts
const pushPage = (type: string, id?: string) => {
  const modalId = `Page:${type}:${id || ''}`;
  const top = pageStack.value[pageStack.value.length - 1];
  if (top && top.type === type && top.id === id) return; // 去重

  pageStack.value.push({ type, id, modalId });
  registerModal(modalId, () => { popPageInternal(); });
};
```

**关闭页面**：
- **UI 关闭按钮** → `popPage()` → 同步移除栈顶 + `unregisterModal`
- **返回键关闭** → `popPageInternal()` → 由 `handlePopState` 触发回调

> 源码位置：`src/core/stores/overlay.ts` 第 49–82 行

**常驻 DOM 策略**：`SettingsView`、`AgentSettingsView`、`GroupSettingsView` 等使用 `v-show`（或 `:is-open` 控制内部 `SlidePage` 的 `v-show`），保持常驻 DOM 以保留表单草稿和确保 leave 动画完整。`SyncSessionView` 和 `RebuildSessionView` 使用 `v-if` 按需挂载（状态完全由 Store 托管）。

> 参考：`src/components/FeatureOverlays.vue` 第 36–69 行

### 4.3 动态 Z-Index 计算

SlidePage 的层级通过 `getPageZIndex` 动态计算，保证后打开的页面始终压在前一页之上：

> 源码位置：`src/core/stores/overlay.ts` 第 43–47 行

```ts
const getPageZIndex = (type: string) => {
  const index = pageStack.value.findIndex(p => p.type === type);
  if (index === -1) return LAYER_PAGE_BASE;
  return LAYER_PAGE_BASE + Math.min(index, LAYER_PAGE_MAX_OFFSET);
};
```

层级常量定义：

| 语义名 | 数值 | 用途 |
|--------|------|------|
| `LAYER_PAGE_BASE` | 40 | 页面栈基准层 |
| `LAYER_PAGE_MAX_OFFSET` | 9 | 最多支持 10 层堆叠（40–49） |

实际计算示例：

```
打开 SettingsView        → z-index: 40  (base + 0)
再打开 AgentSettingsView  → z-index: 41  (base + 1)
再打开 SyncSessionView    → z-index: 42  (base + 2)
```

> 源码位置：`src/core/constants/layers.ts` 第 25–35 行

### 4.4 模态历史栈（Operation Aegis）集成

Operation Aegis 是 VCP Mobile 的**全局模态历史栈**，解决移动端物理返回键的逐层关闭问题。

**核心机制**：

```
用户按物理返回键 / 手势返回
    │
    ▼
┌─────────────────────┐
│ popstate 事件触发   │
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ 当前路径非 /chat ?   │──> 是 → 交给 Vue Router 处理（真实路由页）
│                     │──> 否 → 进入模态拦截逻辑
└──────────┬──────────┘
           ▼
┌─────────────────────┐
│ modalStack 有内容？  │──> 是 → 关闭栈顶 Modal（LIFO）
│                     │──> 否 → 触发 vcp-exit-requested（应用退出）
└─────────────────────┘
```

**`registerModal` 与 `pushState` 绑定**：

当 `overlayStore.pushPage()` 调用 `registerModal(modalId, closeCallback)` 时，Aegis 会执行：

```ts
window.history.pushState({ vcpRoot: true, vcpModalId: id }, '');
modalStack.value.push({ id, close: closeHandler });
```

这确保每一次打开虚拟页面都会在浏览器历史中压入一个 dummy state。用户按返回键时，浏览器先 pop 这个 dummy state，Aegis 拦截到 `popstate` 事件后调用对应的 `close` 回调，实现"按返回关闭页面"的原生体验。

> 源码位置：`src/core/composables/useModalHistory.ts` 第 110–119 行

### 4.5 物理返回键处理

App.vue 中注册了多级返回消费策略：

> 源码位置：`src/App.vue` 第 91–148 行

```
返回键触发
    │
    ▼
1. closeTopModal() ──> 关闭 SlidePage / Dialog / ContextMenu / Prompt
    │                    （Operation Aegis 的 LIFO 消费）
    ▼
2. 若当前在聊天中（READY 且已选中项）
   ──> 清空 currentSelectedItem + currentTopicId，退回零数据引导页
    │
    ▼
3. 已在引导页 → 双击退出到后台（move_task_to_back）
```

**路由后置守护**：每次路由切换后自动校准 Aegis 的 dummy state：

```ts
router.afterEach(() => {
  initRootHistory();
});
```

> 源码位置：`src/App.vue` 第 212–214 行

这保证了从其他真实路由页（如未来可能扩展的 `/settings`）返回 `/chat` 时，dummy state 始终处于正确位置。

---

## 5. 话题切换数据流

### 5.1 切换话题的完整时序

```
用户点击话题列表中的某一项
    │
    ▼
TopicList.vue: selectTopic(itemId, topicId, topicName)
    │
    ├── 1. 路由兜底
    │      if (router.currentRoute.value.path !== "/chat")
    │          await router.push("/chat");
    │
    ├── 2. 更新会话状态
    │      await sessionStore.selectTopicById(itemId, topicId);
    │      ├── currentTopicId = topicId        (实时响应，列表高亮)
    │      ├── lastActiveTopicMap[itemId] = topicId  (Pinia 持久化)
    │      ├── 设置 currentSelectedItem（头像/色调同步）
    │      └── 若有 loadHistoryCallback，则异步调用
    │
    ├── 3. 顶部栏显示话题标题
    │      sessionStore.currentSelectedItem.name = topicName;
    │
    ├── 4. 关闭侧边栏
    │      layoutStore.setLeftDrawer(false);
    │
    └── 5. emit("select-topic") 通知父组件
    │
    ▼
ChatView.vue: watcher 响应 [currentTopicId, currentSelectedItem]
    │
    ├── showScrollToBottom = false
    ├── resetChatScroll()          (重置滚动状态)
    ├── topicStore.markTopicAsRead(newTopicId)  (清空未读标记)
    └── historyStore.loadHistoryPaginated(itemId, type, topicId)
         │
         ├── hasMoreHistory = true
         └── loadHistory(..., limit=5)  // Fast Path
              │
              ├── currentChatHistory = []   (清空旧历史)
              ├── 命中 preloadedHistory?    (PRELOADING 阶段缓存)
              │      └── 直接使用缓存
              ├── 未命中? ──► invoke("load_chat_history") 批量拉取
              │
              ├── Object Hydration（从 streamStore 取活跃消息对象）
              ├── attachmentStore.resolveMessageAssets(msg)  (附件解析)
              └── 串行触发 streamStore.checkAndRecoverInterruptedStreams()
    │
    ▼
chatStreamStore: activeStreamingIds 计算属性自动更新
    └── 新话题的 session key 无活跃流 → 返回空 Set
        （流式状态自然隔离，无需显式 reset）
```

> 相关源码：
> - `src/features/topic/TopicList.vue` 第 149–169 行
> - `src/core/stores/chatSessionStore.ts` 第 96–123 行
> - `src/features/chat/ChatView.vue` 第 62–81 行
> - `src/core/stores/chatHistoryStore.ts` 第 112–243 行

### 5.2 状态重置

话题切换时，以下状态被**显式重置**：

| 状态 | 重置点 | 说明 |
|------|--------|------|
| `currentChatHistory` | `loadHistory` (offset=0, index=0) | 清空旧消息数组 |
| `hasMoreHistory` | `loadHistory` (offset=0, index=0) | 重置为 true |
| `showScrollToBottom` | `ChatView.vue` watcher | 隐藏置底按钮 |
| 滚动状态 | `resetChatScroll()` | 重置 IO 哨兵与 RAF 轮询 |
| 未读标记 | `markTopicAsRead(newTopicId)` | 清空当前话题未读数 |

**不**需要显式重置的状态：
- `chatStreamStore.activeStreamMessages`：按 `"itemId:topicId"` 键隔离，切换后 `activeStreamingIds` 自动为空
- `chatStreamStore.sessionActiveStreams`：同理，旧话题的流记录保留在 Map 中，不影响新会话

### 5.3 历史加载与缓存策略

#### 流式 Channel 加载（offset > 0）

当用户上拉加载更多历史（`offset > 0`）时，`loadHistory` 使用 Tauri v2 的 `Channel` 接收流式历史数据；话题切换的首屏加载（`offset === 0`）走 Fast Path，直接调用 `load_chat_history` 批量拉取：

> 源码位置：`src/core/stores/chatHistoryStore.ts` 第 172–215 行

```ts
const channel = new Channel<HistoryChunk>();
channel.onmessage = (chunk) => {
  // 会话一致性校验
  if (sessionStore.currentTopicId !== requestedTopicId && requestedTopicId !== null) return;
  
  const activeMsg = streamStore.activeStreamMessages.get(chunk.message.id);
  const msgToUse = activeMsg || chunk.message; // Object Hydration
  
  buffer.push(msgToUse);
  // ...
};

await invoke('load_chat_history_streamed', {
  ownerId, ownerType, topicId, limit, offset, onMessage: channel,
});
```

#### Object Hydration（对象劫持）

这是话题切换时的**关键修复**：如果某条消息正在活跃生成中（存在于 `activeStreamMessages` 全局池），则从池中取出**活的响应式对象**替换 DB 拉回的骨架。这确保了用户切换话题后再切回，流式动画能无缝接续。

> 源码位置：`src/core/stores/chatHistoryStore.ts` 第 157–159 行

#### 分页策略

| 场景 | limit | 游标 | 说明 |
|------|-------|------|------|
| 初始加载 | 15 | 无 | 首次进入话题，加载最近 15 条 |
| 话题切换 | 5 | 无 | `loadHistoryPaginated` 调用，轻量快速 |
| 加载更多 | 10 | 当前最老消息 | 用户上拉加载旧消息 |

---

## 6. Hash 路由设计

### 6.1 路由表（仅 /chat）

> 文件位置：`src/core/router/index.ts`

```ts
const routes = [
  { path: '/', redirect: '/chat' },
  { path: '/chat', name: 'chat', component: ChatView },
];

export const router = createRouter({
  history: createWebHashHistory(),
  routes,
});
```

**路由表极简设计的原因**：

VCP Mobile 是**单页覆盖型应用**，绝大多数"导航"通过抽屉、BottomSheet、SlidePage 和 Teleport 实现。真实路由仅负责：
1. 启动时重定向到 `/chat`
2. 作为 ChatView 的挂载点
3. 与其他覆盖层协同时的路径校验（如 `useModalHistory` 中判断 `currentPath !== '/chat'`）

### 6.2 路由与覆盖层的分工

| 职责 | 路由（Vue Router） | 覆盖层（SlidePage / Drawer） |
|------|-------------------|------------------------------|
| **URL 变化** | 无（始终 `/#/chat`） | 无 |
| **历史记录** | 仅初始 `/chat` dummy state | Operation Aegis 管理 dummy state 栈 |
| **动画** | 无 | SlidePage 右入右出（cubic-bezier 缓动） |
| **层级** | 无 | 动态 z-index（40 + stackIndex） |
| **典型场景** | 应用启动、分享链接恢复 | 设置页、Agent 设置、同步会话 |

### 6.3 路由守卫与后置钩子

当前路由配置**无前置守卫**（`beforeEach`）。唯一的路由级干预是后置钩子：

```ts
router.afterEach(() => {
  initRootHistory();
});
```

**作用**：任何路由切换（包括重定向、参数变化、手动 `router.push`）完成后，自动检查并重新注入 Aegis 的 dummy state。这防止了以下边界情况：

- 用户通过分享链接进入 `#/chat` 时，dummy state 未初始化
- 路由跳转后浏览器历史栈状态不一致，导致返回键行为异常
- 真机调试时热重载导致 history state 丢失

> 源码位置：`src/App.vue` 第 207–214 行

---

## 7. 与 Rust 后端的 IPC 交互

### 7.1 Tauri Commands 调用表

| Command | 调用方 | 输入 | 输出 | 触发场景 |
|---------|--------|------|------|----------|
| `get_topics_streamed` | `topicListManager.loadTopicList` | `{ ownerId, ownerType, sortMode, onChunk: Channel<Topic[]> }` | `void`（流式） | 按当前模式加载后端有序的话题列表 |
| `create_topic` | `topicListManager.createTopic` | `{ ownerId, ownerType, name }` | `Topic` | 点击新建话题按钮 |
| `delete_topic` | `topicListManager.deleteTopic` | `{ ownerId, ownerType, topicId }` | `void` | 长按菜单删除（双确认） |
| `update_topic_title` | `topicListManager.updateTopicTitle` | `{ ownerId, ownerType, topicId, title }` | `number \| null` | 修改成功返回 Topic `updated_at`，CAS 未命中返回 null |
| `toggle_topic_lock` | `topicListManager.toggleTopicLock` | `{ ownerId, ownerType, topicId, locked }` | `number \| null` | 状态变化时返回 Topic `updated_at` |
| `set_topic_unread` | `topicListManager.setTopicUnread` | `{ ownerId, ownerType, topicId, unread }` | `number \| null` | 状态变化时返回 Topic `updated_at`；无变化不制造排序时间 |
| `load_chat_history_streamed` | `chatHistoryStore.loadHistory` | `{ ownerId, ownerType, topicId, limit, offset, onMessage: Channel<HistoryChunk> }` | `number`（总数） | 话题切换、上拉加载更多 |
| `summarize_topic` | `chatHistoryStore.summarizeTopic` | `{ ownerId, ownerType, topicId, agentName }` | `string`（新标题） | 消息数 ≥4 且标题为默认值时自动触发 |
| `get_topics` | `chatSessionStore.selectItem` | `{ ownerId, ownerType }` | `Topic[]` | 选择 Agent/Group 时的 fallback 话题获取 |

### 7.2 事件监听表

前端通过 `window.addEventListener` 和 Tauri Event API 监听以下与话题/导航相关的事件：

| 事件名 | 来源 | 触发条件 | 处理逻辑 |
|--------|------|----------|----------|
| `vcp-exit-requested` | `useModalHistory`（popstate 到达根） | 物理返回键无 Modal 可关 | App.vue 三级退出：关 Modal → 回引导页 → 双击退出后台 |
| `vcp-hardware-back` | Android 原生插件（LifecycleBridge） | 物理返回键按下 | 同 `vcp-exit-requested`，由 App.vue 统一消费 |
| `vcp-lifecycle-changed` | Rust `lifecycle_controller.rs`（通过标准 Tauri Event 通道广播） | App 前后台切换 | 更新 `isBackground`、恢复中断流、校准系统快照 |
| `vcp-button-click` | 前端自定义（VcpButton） | 快捷按钮点击 | ChatView 捕获后发送消息 |

**注意**：话题领域本身**不直接监听** Rust 后端 push 的事件。话题列表更新由前端主动调用 Command 获取，或在前端操作后通过乐观更新即时反映。同步完成后如需刷新话题列表，由 `syncSessionStore` 回调触发 `topicListManager.invalidateAllTopicCaches()`。

---

## 8. 设计决策与注意事项

### 8.1 为何话题列表使用流式加载而非一次性返回？

`get_topics_streamed` 通过 `Channel` 分块推送话题数据，而非一次性返回完整数组。原因：

1. **首屏响应**：用户切换 Agent 后可立即看到前几条话题，无需等待全部加载
2. **大列表友好**：某些 Agent 可能拥有数百个话题，流式加载避免 JSON 序列化阻塞
3. **顺序稳定**：SQLite 在发送首批数据前按 `sortMode` 完成全局排序，后续 Chunk 只追加到末尾
4. **竞态防护**：`owner + sortMode + loadGeneration` 确保旧 owner 或旧排序请求不能覆盖当前列表

> 源码位置：`src/core/stores/topicListManager.ts` 第 86–134 行

### 8.2 为何话题变化后要替换数组引用？

`useVirtualList` 来自 `@vueuse/core`，其窗口计算依赖源数组引用。流式 Chunk、创建、删除及活动时间更新都会提交一个新数组引用；其中会影响顺序的本地变化先调用统一比较器重排一次，再整体替换，确保虚拟列表的高度和窗口位置及时校准。

### 8.3 话题切换时为何不显式调用 `chatStreamStore.reset()`？

`chatStreamStore` 采用**会话键隔离**设计：`sessionActiveStreams` 的 key 为 `"${ownerId}:${topicId}"`。这意味着：

- 切换话题后，`activeStreamingIds` 计算属性自动返回新会话键对应的流集合（通常为空）
- 旧话题的流记录保留在 Map 中，用户切回时仍能恢复流式状态
- 全局池 `activeStreamMessages` 通过 `MAX_STREAM_MESSAGES` 和延迟清理机制控制内存上限

这种设计避免了"切换话题必须中断所有流"的粗暴策略，支持后台流式继续完成。

### 8.4 `Object Hydration` 的必要性

`load_chat_history_streamed` 从 DB 拉回的骨架消息对象**不是响应式的**。如果该消息正在流式生成中（存在于 `activeStreamMessages` Map），直接 push 骨架对象会导致：

1. 消息内容不随流式增量更新
2. `isThinking` 动画状态丢失
3. 同一条消息在列表中出现两个实例

通过 `activeMsg || chunk.message` 的劫持策略，DB 骨架被替换为全局池中**活的响应式对象**，实现无缝接续。

> 源码位置：`src/core/stores/chatHistoryStore.ts` 第 112–115 行

### 8.5 删除话题的双确认设计

话题删除采用**双确认对话框**：

```ts
if (window.confirm(`确定要删除话题 "${topic.name}" 吗？此操作不可逆转。`)) {
  if (window.confirm(`【最终确认】真的要永久删除 "${topic.name}" 吗？`)) {
    topicListStore.deleteTopic(...);
  }
}
```

这是**血训产物**：移动端误触率远高于桌面端，单次确认不足以阻止情绪化操作。双确认将误删概率降至可接受水平。

> 源码位置：`src/features/topic/TopicList.vue` 第 121–128 行

---

## 9. 术语速查表

| 术语 | 英文/缩写 | 定义 | 相关模块 |
|------|----------|------|----------|
| 话题 | Topic | Agent/Group 下的独立对话上下文，含标题、锁定态、未读态 | `topicListManager`, `TopicList` |
| 话题列表管理器 | TopicListManager | 前端 Pinia Store，管理话题的 CRUD 与过滤搜索 | `topicListManager.ts` |
| 虚拟列表 | Virtual List | 仅渲染视口内 + overscan 项的列表技术，避免 DOM 膨胀 | `TopicList.vue` |
| 乐观更新 | Optimistic Update | UI 先变更、后同步后端，失败时回滚的更新策略 | `topicListManager.ts` |
| SlidePage | — | 从右侧滑入的固定全屏覆盖层，模拟页面切换 | `SlidePage.vue`, `overlay.ts` |
| 虚拟页面栈 | Page Stack | `overlayStore` 维护的已打开 SlidePage 数组，支持 LIFO 关闭 | `overlay.ts` |
| Operation Aegis | — | 全局模态历史栈系统，管理 dummy state 与物理返回键消费 | `useModalHistory.ts` |
| dummy state | — | `history.pushState({ vcpRoot: true, vcpMain: true }, '')` 注入的占位状态 | `useModalHistory.ts` |
| Hash 路由 | Hash Routing | `createWebHashHistory()`，URL 为 `/#/chat`，无需服务端配置 | `router/index.ts` |
| Object Hydration | — | 从全局流池取出活的响应式对象替换 DB 骨架的技术 | `chatHistoryStore.ts`（第 157–159 行） |
| 会话键隔离 | Session Key Isolation | `chatStreamStore` 按 `"itemId:topicId"` 隔离流状态的机制 | `chatStreamStore.ts` |
| 流式 Channel | Channel<T> | Tauri v2 的异步双工通信通道，用于 SSE 式数据推送 | `topicListManager.ts`, `chatHistoryStore.ts` |
| 消息计数 | msgCount | 话题下的消息总数，UI 乐观更新维护 | `TopicList.vue` |
| 未读计数 | unreadCount | 话题下用户未读的消息数，-1 表示仅标记未读（无具体计数） | `TopicList.vue` |
| 锁定态 | locked | `true` 时话题不可被删除（Agent 默认锁定，Group 固定锁定） | `TopicList.vue` |
| AI 总结标题 | Topic Summary | 消息数 ≥4 且标题为默认格式时，自动调用 `summarize_topic` 生成标题 | `chatHistoryStore.ts` |
| 路由后置钩子 | afterEach | `router.afterEach(() => initRootHistory())`，校准 Aegis 防护盾 | `App.vue` |

---
*最后更新：2026-07-04 | VCP Mobile v1.1.3*
