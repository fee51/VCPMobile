---
id: VUE-COMP-020
title: 布局外壳与UI原语
description: VCP Mobile 前端布局层（layout/）与 UI 原语层（ui/）的组件体系与交互规范
version: 1.1.4
date: 2026-08-13
---

# 20. 布局外壳与UI原语

## 1. 概述

### 1.1 领域定位

`src/components/layout/` 与 `src/components/ui/` 构成 VCP Mobile 前端**空间结构层**与**交互原语层**。`layout/` 负责应用级的空间划分（左右抽屉、启动屏、权限门禁），决定用户在宏观尺度上的导航与视野边界；`ui/` 提供可复用的原子级交互容器（弹层、页面、通知、编辑器等），是各 Feature 模块构建具体业务界面的基础材料。

产品运行范围仅为 Android `arm64-v8a` 触控手机、平板和按窗口宽度响应的折叠屏。host 浏览器只用于开发测试；下文所有宽度模式均指 Android WebView viewport，不表示 desktop 支持。完整规范见 [`docs/ANDROID_UI_COMPATIBILITY.md`](../../ANDROID_UI_COMPATIBILITY.md)。

该层级的边界严格限定为：
- **空间结构**：侧边栏抽屉、启动加载层、权限引导页
- **交互原语**：BottomSheet、SlidePage、Toast、Prompt、全屏编辑器等通用容器
- **全局调度**：覆盖层的集中挂载与层级管理

该层级**不**涉及：
- 具体业务表单（由 `features/settings/` 等承担）
- 消息渲染逻辑（由 `features/chat/` 承担）
- 数据持久化（由 Rust 后端承担）

### 1.2 模块构成表

| 目录 | 文件数 | 总代码量 | 职责 |
|------|--------|---------|------|
| `src/components/layout/` | 4 | ~684 行 | 应用级空间结构：左侧抽屉、右侧抽屉、启动屏、权限门禁 |
| `src/components/ui/` | 13 | ~1,475 行 | 交互原语：BottomSheet、SlidePage、Toast、Prompt、编辑器等 |
| `src/components/` 顶层 | 3 | ~529 行 | 全局覆盖层调度器、业务挂载点、模型选择器 |

### 1.3 在整体架构中的位置

```
┌─────────────────────────────────────────────────────────────┐
│                      Vue 3 前端层                            │
│                                                              │
│  ┌─────────────────────┐  ┌─────────────────────────────┐   │
│  │    layout/          │  │  ui/ + GlobalOverlayManager │   │
│  │  (空间结构骨架)      │  │  (交互原语 + 覆盖层调度)     │   │
│  │                     │  │                             │   │
│  │ AgentSidebar        │  │  BottomSheet / SlidePage    │   │
│  │ RightSidebar        │  │  ToastManager / VcpPrompt   │   │
│  │ BootScreen          │  │  FullScreenEditor ...       │   │
│  │ PermissionGate      │  │                             │   │
│  └──────────┬──────────┘  └──────────────┬──────────────┘   │
│             │                            │                  │
│             └────────────────────────────┼──────────────────┘
│                                          ▼                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  FeatureOverlays (SlidePage 等持久化 DOM 容器)        │   │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐    │   │
│  │  │SettingsView │ │AgentSettings│ │ SyncSession │    │   │
│  │  └─────────────┘ └─────────────┘ └─────────────┘    │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │ IPC (Tauri Commands / Events)
┌─────────────────────────────▼──────────────────────────────┐
│              src-tauri (Rust 核心层 + Android 插件)         │
│         (vcp_modules/infra, tauri-plugin-vcp-mobile)       │
└────────────────────────────────────────────────────────────┘
```

### 1.4 核心设计原则

| 原则 | 说明 |
|------|------|
| **语义化层级** | 所有覆盖层组件通过 `src/core/constants/layers.ts` 获取 z-index，禁止在任何组件中直接使用 `z-50`、`z-[999]` 等魔法数字 |
| **物理返回键拦截 (Operation Aegis)** | 所有模态组件通过 `useModalHistory` 注册到浏览器 history stack，确保 Android 物理返回键/侧滑手势优先关闭弹窗，而非直接退出应用 |
| **手势优先** | 侧边栏滑动关闭、BottomSheet 下滑关闭、Toast 左右滑动关闭，均基于 `@vueuse/core` 的 `useSwipe` 实现 |
| **常驻 DOM 策略** | SettingsView、AgentSettingsView 等通过 `FeatureOverlays` 保持常驻 DOM（`v-show` 控制），确保 leave 动画完整、表单草稿不丢失 |
| **安全区适配** | 原生四边 Insets 在唯一桥接点转为 `--vcp-safe-*`，IME 使用净增量 `--vcp-ime-offset`；组件消费语义变量，Web 环境才由 `env()` fallback |
| **Teleport 脱离文档流** | 模态组件统一使用 `<Teleport to="body">`（或 `#vcp-feature-overlays`），避免受父级 `overflow: hidden` 或 `transform` 裁剪影响 |

---

## 2. 布局外壳（layout/）

### 2.1 AgentSidebar — 左侧抽屉

> 文件位置：`src/components/layout/AgentSidebar.vue`（157 行）

AgentSidebar 是应用的主导航抽屉，承载智能体与话题的浏览、搜索、新建入口。

#### 三栏布局

```
┌─────────────────────────────┐
│  VCP MOBILE                 │  ← 头部：品牌标题 + SidebarTabs + SidebarSearch
│  [Agents] [Topics]          │
│  ┌─────────────────────┐    │
│  │ Search...           │    │
│  └─────────────────────┘    │
├─────────────────────────────┤
│                             │
│  ● Nova                    │  ← 列表区：AgentList / TopicList
│  ● 艾米莉亚                 │
│  ● 测试助手                 │
│                             │
├─────────────────────────────┤
│  [+ 新建助手]               │  ← 底部：动作区 + 全局设置按钮
│  [ 全局设置  > ]            │
└─────────────────────────────┘
```

- **头部**：`SidebarTabs`（`agents`/`topics` 切换）+ `SidebarSearch`（实时过滤）
- **列表区**：`activeTab === 'agents'` 时渲染 `AgentList`；`activeTab === 'topics'` 时渲染 `TopicList`
- **底部**：根据当前 Tab 显示 `AgentsCreator` 或 `TopicCreator`，外加全局设置快捷入口

#### 状态与手势

与 `layoutStore.leftDrawerOpen` 单向绑定：

```vue
<aside :class="{ 'is-open': layoutStore.leftDrawerOpen }">
```

内部通过 `useSidebarSwipe` 监听左滑关闭、右滑切回 agents Tab：

```ts
useSidebarSwipe(sidebarRef, {
  type: 'left',
  onTabSwitch: () => { activeTab.value = 'agents'; }
});
```

#### CSS 硬件加速动画

```css
.vcp-drawer {
  transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  transform: translateX(-100%);        /* 关闭态 */
  z-index: var(--layer-drawer);        /* 20 */
}
.vcp-drawer-left.is-open {
  transform: translateX(0);            /* 打开态 */
}
```

抽屉外层只负责原始 `±100%` 位移，内容由内层 `vcp-drawer-surface` 裁剪；外沿常驻一条静态渐变阴影，仅通过 `opacity` 在 `0.28s` 内淡入淡出，早于 `visibility` 的 `0.4s` 隐藏时点。使用 `transform` 而非持续修改 `left`，避免每帧改变文档流几何；具体帧率仍需具名设备证据，文档不承诺固定 60fps。

#### 大宽度 Android 窗口常驻适配

```css
@media (min-width: 1024px) {
  .vcp-drawer {
    position: relative;
    transform: translateX(0) !important;
    width: 280px;
    z-index: var(--layer-local);        /* 降级为 10，不参与覆盖层竞争 */
  }
}
```

在 CSS viewport `>=1024px` 时，左侧边栏从 overlay 降级为 280px 常驻 pane；窗口因分屏或折叠变窄后自动恢复抽屉。该阈值是 Android 工作区契约，不是“桌面断点”。

### 2.2 RightSidebar — 右侧抽屉

> 文件位置：`src/components/layout/RightSidebar.vue`

RightSidebar 是**通知中心**（Notification Center）的物理容器，由父组件通过 `isOpen` prop 控制显隐，通常绑定 `layoutStore.rightDrawerOpen`。

#### 内部结构

- **顶部操作栏**：`Notifications` 标题 + 未读角标 + Debug 推送按钮（调试用）/ 清空历史 / 关闭按钮
- **状态栏**：`NotificationStatusBar`（连接状态摘要）
- **列表**：`NotificationList`（通知历史列表）
- **工具托盘**：插件中心、灵视中心、日记中心三个胶囊入口，以及锚定在“更多”按钮上方的可扩展工具浮窗

#### 打开时的副作用

```ts
watch(() => props.isOpen, (isOpen) => {
  store.isDrawerOpen = isOpen;
  if (isOpen) store.markAllRead();    // 打开时自动标为已读
});
```

#### 手势与适配

内部通过 `useSidebarSwipe` 监听右滑关闭（`type: 'right'`）。右栏只在 Android CSS viewport `>=1280px` 时降级为 `position: relative` 的 300px 常驻 pane；1024–1279px 仍是可开闭的右抽屉。

### 2.3 BootScreen — 启动屏

> 文件位置：`src/components/layout/BootScreen.vue`（67 行）

BootScreen 是应用冷启动时的全屏加载层，与 `appLifecycleStore.state` 深度绑定。

#### 两态设计

| 状态条件 | 视觉表现 |
|----------|----------|
| `state !== 'READY' && state !== 'ERROR'` | 蓝色旋转 Spinner + `statusText` + `currentPhaseLabel` + 当前 state 标签 |
| `state === 'ERROR'` | 红色错误卡片 + 错误详情 + "重试启动"按钮 |

```vue
<!-- 加载态 -->
<div v-if="lifecycleStore.state !== 'READY' && lifecycleStore.state !== 'ERROR'"
     class="fixed inset-0 z-boot ...">
  <div class="animate-spin">...</div>
  <h2>{{ lifecycleStore.statusText }}</h2>
  <p>{{ lifecycleStore.currentPhaseLabel }}</p>
</div>

<!-- 错误态 -->
<div v-if="lifecycleStore.state === 'ERROR'"
     class="fixed inset-0 z-boot ...">
  <div class="border-red-500/20 ...">
    <button @click="reloadApp()">重试启动</button>
  </div>
</div>
```

#### 状态流转关联

BootScreen 本身不驱动状态机，仅被动响应 `appLifecycleStore`：

```
PERMISSIONS → BOOTING → CONNECTING → PRELOADING → READY
                ↓
              ERROR
```

当状态到达 `READY` 时，加载层通过 Vue `<Transition name="fade">` 淡出（0.3s）；若进入 `ERROR`，则切换为错误看板。

### 2.4 PermissionGate — 权限门禁

> 文件位置：`src/components/layout/PermissionGate.vue`（255 行）

PermissionGate 是 Android 启动时的**全屏权限引导门禁**，只收齐保活核心能力：`notification`（系统通知）+ `battery`（电池优化豁免/后台运行）+ 通知栏监听守护三者全部就绪后，用户才能进入应用主界面。存储（全媒体读取）等非核心权限不在门禁内，由对应功能按需申请。

#### 三步骤向导

```
┌─────────────────────────────────────────────────────────────┐
│  Step 1          Step 2          Step 3                     │
│   ●──────────────●──────────────○                           │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  授予权限                                            │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐                  │   │
│  │  │通知 去授权│ 存储 去授权│ 电池 去授权│                  │   │
│  │  └────────┘ └────────┘ └────────┘                  │   │
│  │            [ 下一步 ]                               │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

- **Step 1（权限授予）**：通知、存储空间、后台运行三项权限卡片，每项显示状态（OK / 去授权按钮）
- **Step 2（电池白名单）**：解释品牌厂商电池管理策略，引导用户进入设置页添加白名单
- **Step 3（占位）**："更多功能即将上线"

#### 与 Rust 后端的 IPC

```ts
// 批量检查权限
const res = await invoke<PermissionStatus>('plugin:vcp-mobile|check_all_permissions');

// 请求单项权限
await invoke('plugin:vcp-mobile|request_android_permission', { pType: type });

// 退出到后台（用户拒绝权限时）
await invoke('plugin:vcp-mobile|move_task_to_back');
```

#### 三重检测机制

1. **主动检查**：`onMounted` 立即调用 `check()`
2. **事件驱动**：监听 Kotlin 侧通过 `evaluateJavascript` 注入的 `vcp-permission-change` 自定义事件
3. **轮询兜底**：10 秒间隔 `setInterval(check, 10000)`，防止极端情况下事件丢失

```ts
onMounted(() => {
  check();
  window.addEventListener('visibilitychange', onVisibilityChange);
  window.addEventListener('vcp-permission-change', onPermissionChange);
  checkTimer = setInterval(check, 10000);
});
```

层级：`z-gate`（110），全局最高，确保权限未授予前用户无法通过任何手段绕过。

---

## 3. UI 原语（ui/）

### 3.1 BottomSheet — 底部弹层

> 文件位置：`src/components/ui/BottomSheet.vue`（115 行）

BottomSheet 是从底部滑出的模态操作面板，用于呈现一组相关的操作选项。

#### 结构与层级

```vue
<Teleport to="body">
  <!-- 遮罩层 -->
  <div v-if="modelValue" class="fixed inset-0 bg-black/50 z-sheet" @click="close">
  </div>
  <!-- 内容区 -->
  <div v-if="modelValue" class="fixed bottom-0 left-0 right-0 z-sheet ...">
    <div class="w-12 h-1.5 bg-black/10 rounded-full mx-auto mb-4"></div>  <!-- 拉手条 -->
    <button v-for="action in actions">...</button>
    <button @click="close">取消</button>
  </div>
</Teleport>
```

- **层级**：`z-sheet`（50）
- **遮罩**：`bg-black/50`，点击关闭
- **安全区**：`padding-bottom: calc(env(safe-area-inset-bottom, 20px) + 16px)` 适配 iPhone 刘海/灵动岛

#### 操作项接口

```ts
export interface ActionItem {
  label: string;
  icon?: any;            // lucide-vue-next component
  danger?: boolean;      // 红色警示样式
  disabled?: boolean;
  handler: () => void;
}
```

#### 模态历史集成

通过 `useModalHistory` 注册，支持物理返回键关闭：

```ts
watch(() => props.modelValue, (newVal) => {
  if (newVal) {
    registerModal('BottomSheet', () => emit('update:modelValue', false));
  } else {
    unregisterModal('BottomSheet');
  }
});
```

### 3.2 SlidePage — 虚拟页面

> 文件位置：`src/components/ui/SlidePage.vue`（39 行）

SlidePage 是**虚拟页面栈**的原子组件。VCP Mobile 本质上是单页应用（仅 `/chat` 一条路由），所有"页面跳转"（如设置页、Agent 设置页）均通过 SlidePage 从右侧滑入模拟。

#### 核心实现

```ts
const props = withDefaults(defineProps<Props>(), {
  zIndex: LAYER_PAGE_BASE,    // 40
});
```

```vue
<Transition name="slide-page">
  <div v-show="props.isOpen" class="fixed inset-0 pointer-events-auto" :style="{ zIndex: props.zIndex }">
    <slot />
  </div>
</Transition>
```

#### 动态层级

z-index 由 `overlayStore.getPageZIndex(type)` 动态计算：

```ts
const getPageZIndex = (type: string) => {
  const index = pageStack.value.findIndex(p => p.type === type);
  if (index === -1) return LAYER_PAGE_BASE;
  return LAYER_PAGE_BASE + Math.min(index, LAYER_PAGE_MAX_OFFSET);  // 封顶 49
};
```

这确保后打开的页面（如从设置页进入 Agent 设置）具有更高的 z-index，视觉上覆盖前者。

#### 动画曲线

```css
.slide-page-enter-active {
  transition: transform 0.35s cubic-bezier(0.32, 0.72, 0, 1);
}
.slide-page-leave-active {
  transition: transform 0.3s cubic-bezier(0.32, 0.72, 0, 1);
}
```

采用 iOS 风格的 `cubic-bezier(0.32, 0.72, 0, 1)`（俗称"流畅曲线"），起始段快速响应，末尾段柔和减速，在 120Hz 屏幕上呈现原生级的滑动手感。

#### 物理返回键集成（Operation Aegis）

`FeatureOverlays` 中每个 SlidePage 的关闭回调都会调用 `overlayStore.popPage()`，后者通过 `unregisterModal` 与浏览器 history stack 同步，实现物理返回键逐层关闭页面。

### 3.3 ToastManager / ToastItem — 通知气泡

ToastManager 文件：`src/components/ui/ToastManager.vue`（38 行）  
ToastItem 文件：`src/components/ui/ToastItem.vue`（90 行）

#### ToastManager

```vue
<div class="fixed top-safe left-0 right-0 z-toast pointer-events-none px-4 pt-4 flex flex-col items-center gap-2.5">
  <TransitionGroup name="toast">
    <ToastItem v-for="toast in store.activeToasts" :key="toast.id" :toast="toast" />
  </TransitionGroup>
</div>
```

- **层级**：`z-toast`（90）
- **容器**：`pointer-events-none`，仅子项恢复 `pointer-events-auto`
- **列表动画**：`TransitionGroup`，enter 时从上方弹入带弹性（cubic-bezier(0.18, 0.89, 0.32, 1.28)），leave 时向上淡出

#### ToastItem

**类型与视觉映射**：

| 类型 | 图标 | 颜色 |
|------|------|------|
| `success` | `CheckCircle` | `text-green-500` |
| `warning` | `AlertTriangle` | `text-amber-500` |
| `error` | `X` | `text-red-500` |
| `tool` | `Cpu` | `text-purple-500` |
| `agent` | `User` | `text-blue-500` |
| 默认 | `Info` | `text-blue-400` |

**交互**：
- 点击整项关闭
- 左右滑动关闭（`useSwipe`，`Math.abs(lengthX) > 60`）
- 手动关闭按钮（右上角 X）
- 滑动时实时计算 opacity：`opacity = Math.max(0, 1 - Math.abs(lengthX) / 200)`

**自动消失**：由 `notificationStore.addNotification` 管理，默认 3000ms，`duration: 0` 表示永不消失（用于审批类通知）。

#### 单例抑制与冷却

`notificationStore` 实现了 P0 级优化：
1. 同 ID 的 Toast 已在展示中 → 原地更新，不新建
2. 同 ID 在 30 秒内已在历史中出现 → 抑制新 Toast，仅更新时间戳

### 3.4 VcpPrompt / ContextMenuSheet — 对话框与上下文菜单

VcpPrompt 文件：`src/components/ui/VcpPrompt.vue`（118 行）  
ContextMenuSheet 文件：`src/components/ui/ContextMenuSheet.vue`

#### VcpPrompt

单文本输入确认对话框，用于重命名、快速输入等场景。

```vue
<Teleport to="body">
  <div class="fixed inset-0 z-dialog flex items-start justify-center pt-[15vh] bg-black/40">
    <div class="vcp-prompt-modal w-11/12 max-w-sm rounded-2xl ...">
      <h3>{{ title }}</h3>
      <input ref="inputRef" v-model="inputValue" @keydown.enter="handleConfirm" @keydown.esc="handleCancel" />
      <button @click="handleCancel">取消</button>
      <button @click="handleConfirm">确认</button>
    </div>
  </div>
</Teleport>
```

- **层级**：`z-dialog`（60）
- **自动聚焦**：`onMounted` + `nextTick` 实现打开即聚焦并全选文本
- **键盘支持**：Enter 确认、Esc 取消
- **模态动画**：外层 fade + 内层 scale + translateY 弹性效果

#### ContextMenuSheet

底部弹出的上下文操作菜单，通常由长按触发。普通 action 保持按钮语义；只要任一 `OverlayActionItem` 提供可选 `selected` 字段，菜单就切换为单选语义。

```vue
<Teleport to="body">
  <div v-if="isOpen" class="fixed inset-0 bg-black/20 pointer-events-auto z-dialog">
    <div class="absolute left-1/2 bottom-6 -translate-x-1/2 ...">
      <div :role="isSelectionMenu ? 'radiogroup' : undefined">
      <button v-for="action in actions"
        :role="isSelectionMenu ? 'radio' : undefined"
        :aria-checked="isSelectionMenu ? action.selected === true : undefined"
        :data-selected="isSelectionMenu ? String(action.selected === true) : undefined"
        @click="handleAction(action)">
        <component :is="action.icon" />
        <span>{{ action.label }}</span>
        <span v-if="action.selected">✓</span>
      </button>
      </div>
    </div>
  </div>
</Teleport>
```

与 BottomSheet 的区别：
- 视觉更紧凑，圆角更小（`rounded-3xl` vs `rounded-t-[2rem]`）
- 操作项为横向图标 + 文本，非大按钮
- 无独立"取消"按钮，点击遮罩关闭
- selection menu 使用 `radiogroup` / `radio` / `aria-checked`，当前项同时显示 2px Accent Bar 与 check，不只依赖颜色
- `selected` 是窄扩展，不改变复制、删除等既有普通 action 调用方

### 3.5 UpdatePrompt — 更新提示

> 文件位置：`src/components/ui/UpdatePrompt.vue`（119 行）

OTA 更新提示弹窗，由 `update_manager.rs` 检测到新版本后触发。

```vue
<div class="fixed inset-0 z-dialog flex items-start justify-center pt-[15vh] bg-black/40">
  <div class="w-11/12 max-w-sm rounded-2xl ...">
    <h3>发现新版本</h3>
    <div class="font-mono text-blue-500">{{ version }}</div>
    <div class="max-h-40 overflow-y-auto whitespace-pre-wrap">{{ releaseNotes }}</div>
    <div class="font-mono">APK 大小: {{ formatBytes(apkSize) }}</div>
    <button @click="handleDismiss">取消</button>
    <button @click="handleConfirm">立即更新</button>
  </div>
</div>
```

- **版本号**：等宽字体展示，强调技术精确感
- **发布说明**：`whitespace-pre-wrap` 保留后端传来的换行格式，最大高度 40 行可滚动
- **APK 大小**：`formatBytes` 自动转换（B/KB/MB/GB，保留 2 位小数）

### 3.6 FullScreenEditor — 全屏编辑器

> 文件位置：`src/components/ui/FullScreenEditor.vue`（147 行）

用于编辑长文本（如 Agent 系统提示词）的全屏文本编辑器。

#### 关键特性

**1. 软键盘适配**

```ts
const { keyboardHeight } = useKeyboardInsets();
// 动态 paddingBottom
:style="{ paddingBottom: `calc(env(safe-area-inset-bottom) + ${keyboardHeight}px)` }"
```

软键盘弹出时，通过 `watch(keyboardHeight)` 重新定位光标，确保输入区域始终可见。

**2. Rubber-band 防护**

Android/iOS WebView 中，textarea 滚动到边界后继续滑动会导致整个页面被拖动（rubber-band 效应）。`FullScreenEditor` 通过 `touchmove` 拦截实现三态防护：

```ts
// 内容未溢出 → 直接阻止
if (scrollHeight <= clientHeight) { e.preventDefault(); return; }

// 在顶部且继续下滑 → 阻止
if (scrollTop <= 0 && deltaY < 0) { e.preventDefault(); return; }

// 在底部且继续上滑 → 阻止
if (scrollTop + clientHeight >= scrollHeight - 1 && deltaY > 0) { e.preventDefault(); return; }
```

**3. Android WebView 优化**

```css
.vcp-fullscreen-textarea {
  touch-action: manipulation;
  -webkit-tap-highlight-color: transparent;
  cursor: text;
  overscroll-behavior: contain;
}
```

层级：`z-viewer`（80），高于 `z-editor`（70）与 dialog 层，确保全屏文本编辑器覆盖业务弹窗。

### 3.7 AvatarCropper — 头像裁剪

> 文件位置：`src/components/ui/AvatarCropper.vue`

基于 `vue-cropper` 的头像裁剪组件，用于 Agent、Group 与用户头像上传前的裁剪确认。

#### 核心配置

```ts
const options = reactive({
  img: props.img,
  fixed: true,
  fixedNumber: [1, 1],          // 锁定裁剪比例 1:1
  fixedBox: true,
  autoCropWidth: 360,
  autoCropHeight: 360,
  high: false,                  // 禁止 DPR 放大
  maxImgSize: 1120,
  enlarge: 1,                   // 禁止按 DPR 放大，确保输出 360px
  outputType: 'png',
  canMoveBox: false,            // 只移动图片，不移动裁剪框
  centerBox: true,
  mode: 'contain'
});
```

#### 视觉与交互

- **圆形预览**：通过 `:deep(.cropper-view-box) { border-radius: 50% }` 强制圆形裁剪预览
- **圆形文件**：确认后绘制到最大 360×360 的透明 PNG，圆外像素保持透明
- **操作按钮**：放大 (+)、缩小 (-)、左旋
- **Teleport 目标**：`#vcp-feature-overlays`（即 `FeatureOverlays` 提供的锚点）

#### Ref 结构兼容

由于 `vue-cropper` 在不同版本下的 Ref 结构不同，组件采用运行时探测：

```ts
const realCropper = cropper.value.getCropBlob
  ? cropper.value
  : cropper.value.value;
```

这避免了因依赖版本升级导致 `getCropBlob` 未定义而使裁剪功能完全失效。

层级：`z-viewer`（80）。

### 3.8 VcpAvatar — 头像组件

> 文件位置：`src/components/ui/VcpAvatar.vue`

头像展示组件，支持图片、首字母占位、Dominant Color 背景三种渲染策略。

#### 三级渲染策略

```
┌─────────────────────────────┐
│  ┌───────────────────────┐  │
│  │  Fallback 层 (底层)    │  │  ← 首字母 + 确定性背景色
│  │        "N"             │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │  图片层 (顶层，条件渲染)│  │  ← avatarStore.getAvatarUrl()
│  │   [实际头像图片]        │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

**1. 图片层**：
- 进入视口前后 300px 时才调用 `avatarStore.getAvatarUrl(ownerType, ownerId, version)`
- **缓存优先**：通过 `getCachedAvatar` 同步命中 LRU Blob URL
- 离开预加载区后卸载图片元素，释放 WebView 解码位图
- `imgExists` 控制渲染；`@error="handleImgError"` 触发降级

**2. Fallback 层**：
- `initial`：取 `fallbackName` 或 `ownerId` 的首字母大写
- `fallbackBg`：基于 `ownerId` 字符串 hash 从 5 色轮盘中选取确定性颜色（红/蓝/绿/琥珀/紫）
- 若传入 `dominantColor`，则直接使用主色调作为背景

**3. 主色调边框**：

```ts
const borderStyle = computed(() => {
  if (!color) return {};
  return {
    borderColor: color,
    boxShadow: `0 0 8px ${color}33`
  };
});
```

动态 inline style 直接使用已计算颜色，并配合低透明度阴影；这样旧版 Android WebView 不依赖 `color-mix()` 也能保留头像边界。

### 3.9 VcpScrollArea — 滚动区域封装

> 文件位置：`src/components/ui/VcpScrollArea.vue`（26 行）

极简的滚动区域封装，主要解决全局侧滑手势与内部水平滚动的冲突。

```ts
defineProps<{ disableSwipe?: boolean }>();
```

- 默认添加 `vcp-scrollable` 类
- `disableSwipe=true` 时添加 `no-swipe` 类，`useSidebarSwipe` 会跳过这些区域，防止横向滚动时误触发侧滑关闭

### 3.10 CoreStatusIndicator — 核心状态指示器

> 文件位置：`src/components/ui/CoreStatusIndicator.vue`（76 行）

VCP 核心引擎连接状态的微观指示器，通常嵌入设置页或状态栏角落。

#### 状态映射

| 状态 | 圆点颜色 | 阴影 | 动画 | 文字 |
|------|----------|------|------|------|
| `ready` | `bg-green-500` | `shadow-green-500/50` | `vcp-core-pulse` (自定义呼吸) | Core Active |
| `initializing` / `connecting` | `bg-yellow-500` | `shadow-yellow-500/50` | `animate-pulse` | Booting... |
| `error` | `bg-red-500` | `shadow-red-500/50` | `animate-bounce` | Core Error |
| 默认 | `bg-gray-400` | `shadow-gray-400/20` | — | Unknown |

视觉规格：1.5px 圆点 + 9px 等宽字体标签，符合项目"技术精确感"美学宪法。

### 3.11 WebGLFluidBackground — WebGL 流体背景

> 文件位置：`src/components/ui/WebGLFluidBackground.vue`（351 行）

WebGL 实时渲染的流体极光背景，用于启动屏、空态页或特定主题页面。

#### 渲染管线

```
Vertex Shader (全屏 Quad)
         │
         ▼
Fragment Shader
  ├─ Domain Warping (UV 正弦扰动，产生液态凝胶边界)
  ├─ 4 条异向高斯极光带 (Cyan/Magenta/Violet/Blue)
  ├─ Isotropic Repulsion (鼠标/触摸排斥力场)
  ├─ 气态光学逐层叠染 (正向 mixing，消除互补色脏污)
  └─ 暗色模式自适应 (u_is_dark uniform)
```

#### 关键参数

| 参数 | 数值 | 说明 |
|------|------|------|
| 排斥力半径 | 25% 屏幕宽度 | 触摸点周围产生流体扰动的范围 |
| 最大位移 | 15% 屏幕宽度 | 极光带被排斥的最大距离 |
| DPR 限制 | 2 | 避免高 DPR 设备 GPU 过载 |
| 颜色强度（亮色模式） | 0.35–0.40 | 确保亮色背景下不刺眼 |
| 颜色强度（暗色模式） | 0.45–0.52 | 暗色背景下增强表现力 |

#### 性能与生命周期

- **启动延迟**：`setTimeout(initWebGL, 100)`，让浏览器先完成布局与样式计算
- **尺寸同步**：`ResizeObserver` 监听父元素，完美解决 SlidePage 缓动滑入期间宽度动态变更导致的 Canvas 尺寸锁定问题
- **卸载清理**：`onUnmounted` 中调用 `WEBGL_lose_context` 立即释放 GPU 内存，取消 `requestAnimationFrame`
- **主题切换**：`watch(themeStore.isDarkResolved)` 直接更新 uniform，无需重建 shader

#### 低电量模式降级建议

WebGLFluidBackground 持续占用 GPU。在电池电量低于 20% 或系统开启省电模式时，建议父组件通过条件渲染将其替换为静态 CSS 渐变背景。

---

## 4. 全局覆盖层管理

### 4.1 GlobalOverlayManager — 覆盖层调度器

> 文件位置：`src/components/GlobalOverlayManager.vue`（52 行）

GlobalOverlayManager 是全局覆盖层的集中调度与挂载点，职责是协调 Prompt、ContextMenu、Editor、Toast 等基础 UI 的显隐状态。

```vue
<div class="fixed inset-0 pointer-events-none z-toast">
  <!-- 基础 UI -->
  <VcpPrompt v-if="overlayStore.promptConfig" class="pointer-events-auto" ... />
  <ContextMenuSheet v-if="overlayStore.contextMenuConfig" ... />
  <FullScreenEditor v-if="overlayStore.editorConfig" class="pointer-events-auto" ... />
  <ToastManager class="pointer-events-auto" />

  <!-- Feature 投射目标 -->
  <div id="vcp-feature-overlays" class="absolute inset-0 pointer-events-none"></div>
</div>
```

#### 设计要点

1. **容器 `pointer-events-none`**：自身不拦截任何交互，由子组件通过 `pointer-events-auto` 恢复
2. **ToastManager 不离散 Teleport**：直接渲染在容器内，确保始终位于 `z-toast` 层级
3. **其他组件内部 Teleport**：VcpPrompt、ContextMenuSheet、FullScreenEditor 内部均使用 `<Teleport to="body">`，实际渲染位置脱离 GlobalOverlayManager，不受其 z-index 约束
4. **`#vcp-feature-overlays`**：供 `AvatarCropper` 等业务组件通过 Teleport 投射，避免与 body 内其他元素产生层级竞争

### 4.2 FeatureOverlays — 业务覆盖层挂载点

> 文件位置：`src/components/FeatureOverlays.vue`（77 行）

FeatureOverlays 是所有全局业务视图的统一挂载点，核心设计目标是**平衡 DOM 常驻与按需渲染**。

#### 常驻 DOM 策略

以下组件保持常驻（`v-show` 由 `is-open` 控制），以保留组件本地状态：

| 组件 | 状态控制 | 保留的状态 |
|------|----------|-----------|
| `SettingsView` | `overlayStore.isSettingsOpen` | 表单草稿、滚动位置 |
| `AgentSettingsView` | `overlayStore.isAgentSettingsOpen` | Agent 配置表单 |
| `GroupSettingsView` | `overlayStore.isGroupSettingsOpen` | Group 配置表单 |
| `TarvenSettingsView` | `overlayStore.isTarvenSettingsOpen` | 酒馆设置 |

#### 按需渲染策略

以下组件使用 `v-if`，因其状态完全由独立 Store 托管：

| 组件 | 状态控制 | 原因 |
|------|----------|------|
| `SyncSessionView` | `overlayStore.isSyncSessionOpen` | 同步会话结束后应完全释放资源 |
| `RebuildSessionView` | `overlayStore.isRebuildSessionOpen` | 重建任务结束后释放资源 |

#### 动态 z-index

所有页面均通过 `SlidePage` 包装：

```vue
<SettingsView
  :is-open="overlayStore.isSettingsOpen"
  :z-index="overlayStore.getPageZIndex('settings')"
  @close="overlayStore.closeSettings()"
/>
```

`getPageZIndex` 根据页面在 `pageStack` 中的索引动态分配，确保页面打开顺序与层级正相关。

---

## 5. 组件层级与 Z-Index 映射

### 5.1 各组件对应的语义化层级

| 组件 | 语义层级 | 数值 | CSS/UnoCSS 类 | 源码位置 |
|------|---------|------|---------------|----------|
| AgentSidebar / RightSidebar | drawer/local | 20/10 | overlay 使用 `z-drawer`；Android 常驻 pane 使用 `z-local` | `layers.ts` |
| BottomSheet | sheet | 50 | `z-sheet` | `BottomSheet.vue` |
| ModelSelector | sheet | 50 | `z-sheet` | `ModelSelector.vue` |
| SlidePage | page | 40–49 | 动态 `:style="{ zIndex }"` | `SlidePage.vue` |
| ToastManager | toast | 90 | `z-toast` | `ToastManager.vue` |
| VcpPrompt | dialog | 60 | `z-dialog` | `VcpPrompt.vue` |
| ContextMenuSheet | dialog | 60 | `z-dialog` | `ContextMenuSheet.vue` |
| UpdatePrompt | dialog | 60 | `z-dialog` | `UpdatePrompt.vue` |
| HtmlPreviewBlock（全屏） | editor | 70 | `z-editor` | `HtmlPreviewBlock.vue` |
| FullScreenEditor | viewer | 80 | `z-viewer` | `FullScreenEditor.vue` |
| AvatarCropper | viewer | 80 | `z-viewer` | `AvatarCropper.vue` |
| CoreStatusIndicator | content | 0 | 无（继承父级上下文） | `CoreStatusIndicator.vue` |
| WebGLFluidBackground | content | 0 | 无（Canvas 全屏填充） | `WebGLFluidBackground.vue` |
| BootScreen | boot | 100 | `z-boot` | `BootScreen.vue` |
| PermissionGate | gate | 110 | `z-gate` | `PermissionGate.vue` |
| GlobalOverlayManager | toast | 90 | `z-toast` | `GlobalOverlayManager.vue` |

> **注意**：权威顺序是 `editor=70`、`viewer=80`。`HtmlPreviewBlock` 使用 `z-editor`；`FullScreenEditor`、附件查看与头像裁剪使用更高的 `z-viewer`。

### 5.2 层级冲突规避策略

1. **SlidePage 动态递增**  
   `LAYER_PAGE_BASE + stackIndex`，最多叠加 9 层（封顶 49）。后打开的页面（如从设置页进入 Agent 设置）始终覆盖前者，避免页面栈中的层级倒置。

2. **抽屉互斥**  
   `layoutStore.setLeftDrawer(true)` 会自动调用 `setRightDrawer(false)`，反之亦然。两侧抽屉不会同时打开，避免 82vw 宽度在窄屏上产生视觉挤压和交互冲突。

3. **Toast 独立于模态历史栈**  
   Toast 不注册到 `modalHistory`，不拦截物理返回键。这确保用户快速关闭弹窗（如 BottomSheet）时，不会被 Toast 阻碍；同时 Toast 的滑动关闭属于独立交互通道。

4. **Teleport 脱离文档流**  
   BottomSheet、VcpPrompt、ContextMenuSheet、UpdatePrompt 均 `Teleport to="body"`，其层级不受父级 `overflow: hidden`、`transform` 或 `opacity` 的影响，从根本上消除裁剪问题。

5. **FeatureOverlays 内部无 DOM 顺序竞争**  
   所有 SlidePage 共享同一个 `fixed inset-0` 容器，靠动态 `zIndex` 区分前后关系，不依赖 Vue 渲染顺序或 DOM 插入顺序。

---

## 6. 与 Rust 后端的 IPC 交互

### 6.1 Tauri Commands 调用表

| 调用方 | Command | 参数 | 返回值 | 用途 | 来源 |
|--------|---------|------|--------|------|------|
| `PermissionGate` | `plugin:vcp-mobile\|check_all_permissions` | — | `{ notification, storage, battery }` | 批量检查 Android 权限 | `tauri-plugin-vcp-mobile` |
| `PermissionGate` | `plugin:vcp-mobile\|request_android_permission` | `{ pType }` | `void` | 请求单项权限 | `tauri-plugin-vcp-mobile` |
| `PermissionGate` | `plugin:vcp-mobile\|move_task_to_back` | — | `void` | 应用退到后台 | `tauri-plugin-vcp-mobile` |
| `BootScreen` (via `appLifecycleStore`) | `get_core_status` | — | `string` | 获取核心引擎状态 | `lib.rs` |
| `BootScreen` (via `appLifecycleStore`) | `get_last_error` | — | `string \| null` | 获取最后错误信息 | `lib.rs` |
| `BootScreen` (via `appLifecycleStore`) | `get_system_snapshot` | — | `{ core, log, sync }` | 获取系统状态快照 | `lib.rs` |
| `ToastItem` (审批动作) | `send_vcp_log_message` | `{ payload }` | `void` | 发送工具审批响应 | `vcp_log_service` |

### 6.2 事件监听表

| 监听方 | 事件名 | 来源 | 触发时机 | 用途 |
|--------|--------|------|----------|------|
| `PermissionGate` | `visibilitychange` | 浏览器原生 | 应用从后台切回前台 | 重检权限状态 |
| `PermissionGate` | `vcp-permission-change` | Kotlin 插件 (`evaluateJavascript`) | 用户在系统设置中变更权限 | 实时同步权限状态 |
| `useModalHistory` | `popstate` | 浏览器原生 | 用户点击物理返回键/侧滑返回 | Operation Aegis 弹窗关闭 |
| `App.vue` | `vcp-exit-requested` | `useModalHistory` | 模态栈为空时再次返回 | 触发双击退出提示 |
| `WebGLFluidBackground` | `mousemove` / `touchmove` | 父元素 | 用户触摸/鼠标移动 | 驱动流体排斥力场 |
| `ToastManager` | Store 响应式 | `notificationStore.activeToasts` | 通知添加/移除 | Toast 列表动态渲染 |

---

## 7. 设计决策与注意事项

### 7.1 为何使用 `useModalHistory` 而非 Android 原生返回拦截？

Operation Aegis 选择基于浏览器 `history.pushState` 的栈管理，而非 Android `onBackPressed`：
1. **单路径可调试性**：host 浏览器可快速调试同一套 history 逻辑，但不能据此声明与 Android 返回手势完全一致
2. **与 Vue Router 共存**：仅在主界面（`/` 或 `/chat`）拦截，真实路由页返回交给 vue-router
3. **状态机防重入**：`IDLE` / `POPSTATE_HANDLING` / `INTERNAL_BACK` 三态机避免 `history.back()` 递归调用

### 7.2 常驻 DOM vs 按需渲染的权衡

`FeatureOverlays` 中采用混合策略：
- **SettingsView 等常驻**：保留表单草稿、滚动位置、组件内部缓存；确保 `SlidePage` 的 leave 动画（0.3s 滑出）有完整的 DOM 生命周期
- **SyncSessionView `v-if`**：同步会话状态完全由 `syncSessionStore` 托管，会话结束后组件实例应完全销毁以释放 WebSocket 连接和内存；且同步界面不含需要保留的表单草稿

### 7.3 BottomSheet 与 ContextMenuSheet 的区分

| 维度 | BottomSheet | ContextMenuSheet |
|------|-------------|------------------|
| 视觉 | 大圆角（`rounded-t-[2rem]`）+ 拉手条 | 小圆角（`rounded-3xl`）+ 更紧凑 |
| 操作项 | 带图标的大按钮，占据整行，支持 `danger` | 图标 + 文本的横向列表 |
| 取消按钮 | 内置独立"取消"大按钮 | 无，点击遮罩关闭 |
| 典型场景 | 模型选择、主要操作分支 | 长按菜单、次要操作 |

### 7.4 AvatarCropper 的 Ref 探测防御

`vue-cropper` 在不同版本下 Ref 结构不同（直接 ref vs `.value` 嵌套）。`AvatarCropper` 采用运行时探测而非编译时假设：

```ts
const realCropper = cropper.value.getCropBlob
  ? cropper.value
  : cropper.value.value;
```

这种防御性编程避免了依赖升级导致的裁剪功能完全失效，是移动端 WebView 环境中"不信任依赖 ABI"原则的体现。

### 7.5 WebGLFluidBackground 的触摸监听策略

事件监听器绑定在 **Canvas 父元素**而非 Canvas 本身：

```ts
boundParent.addEventListener('touchmove', trackInteraction, { passive: false });
```

原因：Canvas 自身设置 `pointer-events-none`，且父元素通常是被覆盖的完整视口容器。通过父元素监听实现了"零侵入"——组件不抢占交互，只读取坐标驱动视觉效果。

### 7.6 AgentSidebar 大宽度 Android 层级降级

Android CSS viewport `>=1024px` 时，AgentSidebar 从 `z-drawer`（20）降级为 `z-local`（10），且 `position: relative`。RightSidebar 只在 `>=1280px` 进行同样转换。该设计确保：
1. 侧边栏不再产生"覆盖"右侧内容的行为，而是与内容区并列布局
2. 常驻 pane 禁用抽屉过渡；窗口变窄恢复 overlay 后继续使用触控关闭逻辑
3. 降低层级避免与可能出现的 dialog/sheet 产生不必要的竞争

### 7.7 ModelSelector 的定位说明

`ModelSelector.vue`（位于 `src/components/ModelSelector.vue`）在视觉与交互上属于 BottomSheet 的特化实例，已在文档 `23_ModelSelector与模型管理.md` 中详细覆盖。其核心技术特征包括：VueUse `useVirtualList` 虚拟滚动（固定 62px itemHeight）、厂商 Tag 动态聚合、120px 动力学下滑关闭阈值、以及骨架屏 Shimmer 动画。本节仅将其纳入全局层级表（`z-sheet`，50），不再展开细节。

---

## 8. 术语速查表

| 术语 | 英文/缩写 | 定义 | 相关组件/文件 |
|------|----------|------|--------------|
| Operation Aegis | — | 统一模态历史栈系统，确保物理返回键优先关闭弹窗而非退出应用 | `useModalHistory.ts` |
| 语义化层级 | Semantic Layer | 禁止魔法数字 z-index，使用 `LAYER_*` 常量与 `--layer-*` CSS 变量 | `layers.ts` |
| SlidePage | — | 从右侧滑入的虚拟页面原子组件，构成无路由的页面栈 | `SlidePage.vue` |
| BottomSheet | — | 从底部滑出的模态操作面板 | `BottomSheet.vue` |
| Toast | — | 顶部自动消失的通知气泡，支持滑动关闭 | `ToastManager.vue` / `ToastItem.vue` |
| Prompt | — | 单文本输入确认对话框 | `VcpPrompt.vue` |
| ContextMenu | — | 底部弹出的上下文操作菜单，通常由长按触发 | `ContextMenuSheet.vue` |
| AvatarCropper | — | 基于 `vue-cropper` 的头像裁剪组件，固定 1:1 比例 | `AvatarCropper.vue` |
| Dominant Color | — | 从头像提取的主色调，用于 UI 主题色与 Glassmorphism 背景 | `VcpAvatar.vue` |
| Rubber-band | — | iOS/Android 滚动到边界时的弹性回弹效果 | `FullScreenEditor.vue` |
| Foreground Service | — | Android 前台服务，通过通知保持进程存活 | `PermissionGate.vue` |
| Teleport | — | Vue 3 内置组件，将 DOM 投射到指定目标，脱离父级布局约束 | 多个模态组件 |
| Modal History Stack | — | 模态历史栈，管理弹窗的打开/关闭与浏览器 history 同步 | `useModalHistory.ts` |
| Feature Overlays | — | 业务级覆盖层挂载点，供 SlidePage 等持久化 DOM 投射 | `FeatureOverlays.vue` |
| WebGL Fluid | — | WebGL 实时渲染的流体极光背景，含物理排斥力场 | `WebGLFluidBackground.vue` |
| CoreStatusIndicator | — | VCP 核心引擎状态的微观呼吸灯指示器 | `CoreStatusIndicator.vue` |
| Safe Area | — | 移动设备屏幕边缘的安全显示区域（刘海、圆角、手势条） | 多个组件的 `env(safe-area-inset-*)` |
| 虚拟滚动 | Virtual Scroll | 仅渲染可视区域内列表项的技术 | `ModelSelector.vue` |

---

*最后更新：2026-08-13 | VCP Mobile v1.1.4*
