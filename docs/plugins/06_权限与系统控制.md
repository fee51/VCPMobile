---
id: PLUGIN-PERMISSION-006
title: 权限与系统控制
description: 运行时权限检查、OEM 自启动/电源管理、通知栏监听权限、存储空间检测、系统级控制命令。v1.1.2 新增 OEM/存储命令，v1.1.3 新增通知栏监听与 helper 服务命令。
version: 1.1.3
date: 2026-07-04
related_files:
  - src-tauri/plugins/vcp-mobile/src/system.rs
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/VcpMobilePlugin.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/service/VcpNotificationListenerService.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/service/ForegroundGuardian.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/BatteryStatusManager.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/SensorStatusManager.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/CpuStatusManager.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/GpuStatusManager.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/NetworkStatusManager.kt
  - src-tauri/plugins/vcp-mobile/permissions/default.toml
  - src-tauri/plugins/vcp-mobile/android/src/main/AndroidManifest.xml
---

# 权限与系统控制

## 1. 功能概述

提供 Android 运行时权限的查询与请求能力、OEM 厂商自启动/电源管理权限引导、通知栏监听权限、存储空间检测，以及系统级控制命令。涵盖通知、存储、麦克风、相机、定位、电池优化、悬浮窗、通知栏监听八项权限/能力，并处理 Android 版本差异。

> **v1.1.2 新增**：4 条 OEM/存储命令（`request_auto_start_permission`、`request_power_management_permission`、`check_auto_start_permission`、`get_free_disk_space`）+ OomScoreGuard 进程保护。WakeLock/WifiLock 保活已整合至 [ForegroundGuardian](11_ForegroundGuardian_前台守护者.md)。
> **v1.1.3 新增**：通知栏监听权限命令（`check_notification_listener_permission`、`request_notification_listener_permission`）、`start_helper_service`、`get_pending_notification`，并在 `AndroidManifest.xml` 中声明 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 与 `BIND_NOTIFICATION_LISTENER_SERVICE`。

---

## 2. 代码结构

### Rust 侧

```
src-tauri/plugins/vcp-mobile/src/system.rs (~1096 lines)
├── PermissionStatus { notification, storage, battery, microphone, camera, overlay, location }
├── check_all_permissions<R>(app) -> Result<PermissionStatus, String>
├── request_android_permission<R>(app, p_type: String) -> Result<(), String>
├── check_notification_listener_permission<R>(app) -> Result<ListenerPermissionResponse, String>   ← v1.1.3 新增
├── request_notification_listener_permission<R>(app) -> Result<(), String>                         ← v1.1.3 新增
├── request_auto_start_permission<R>(app) -> Result<bool, String>            ← v1.1.2 新增，v1.1.3 返回 bool
├── request_power_management_permission<R>(app) -> Result<bool, String>      ← v1.1.2 新增，v1.1.3 返回 bool
├── check_auto_start_permission<R>(app) -> Result<String, String>          ← v1.1.2 新增
├── get_free_disk_space<R>(app) -> Result<DiskSpaceInfo, String>           ← v1.1.2 新增
├── move_task_to_back<R>(app) -> Result<(), String>
├── ...（其余命令保持不变）
```

### Kotlin 侧

```
VcpMobilePlugin.kt（权限与系统控制相关部分，约 614 行）
├── 管理器持有
│   ├── batteryStatusManager: BatteryStatusManager       ← 电量检测
│   ├── networkStatusManager: NetworkStatusManager       ← 网络状态
│   ├── cpuStatusManager: CpuStatusManager               ← CPU 热状态
│   ├── gpuStatusManager: GpuStatusManager               ← GPU 型号
│   ├── sensorStatusManager: SensorStatusManager         ← 传感器采集
│   ├── floatingWindowManager: FloatingWindowManager     ← 悬浮窗
│   └── shareIntentHandler: ShareIntentHandler           ← 分享文件
│
├── 权限检查与请求
│   ├── checkAllPermissions(invoke: Invoke)
│   ├── requestAndroidPermission(invoke: Invoke)
│   ├── requestOverlayPermission(invoke: Invoke)
│   ├── check_notification_listener_permission(invoke: Invoke)   ← v1.1.3
│   ├── request_notification_listener_permission(invoke: Invoke) ← v1.1.3
│   ├── checkAutoStartPermission(invoke: Invoke)
│   ├── requestAutoStartPermission(invoke: Invoke)
│   ├── requestPowerManagementPermission(invoke: Invoke)
│   ├── onPermissionResult(invoke: Invoke)          @PermissionCallback
│   ├── onBatteryOptimizationResult(invoke, result)  @ActivityCallback
│   └── emitPermissionsToWebView()
│
├── 系统控制
│   ├── moveTaskToBack(invoke: Invoke)
│   ├── getFreeDiskSpace(invoke: Invoke)
│   ├── startHelperService(invoke: Invoke)          ← v1.1.3
│   └── getPendingNotification(invoke: Invoke)      ← v1.1.3
│
├── 硬件状态
│   ├── getBatteryStatus(invoke: Invoke)            → BatteryStatusManager
│   ├── getCpuThermalStatus(invoke: Invoke)         → CpuStatusManager
│   ├── getGpuStatus(invoke: Invoke)                → GpuStatusManager
│   └── getNetworkStatus(invoke: Invoke)            → NetworkStatusManager
│
├── 传感器 (SensorStatusManager)
│   ├── startSensorCollection(invoke: Invoke)
│   ├── stopSensorCollection(invoke: Invoke)
│   └── getSensorData(invoke: Invoke)
│
├── Root 访问
│   ├── checkRootAccess(invoke: Invoke)
│   ├── runRootCommand(invoke: Invoke)
│   └── launchRootManager(invoke: Invoke)
│
├── 保活控制
│   ├── acquireWakeLock(invoke: Invoke)
│   └── releaseWakeLock(invoke: Invoke)
│
├── 网络监控
│   ├── startNetworkMonitoring(invoke: Invoke)
│   └── emitNetworkStatusToWebView()                ← 内部
│
├── 生命周期
│   ├── load(webView)                               ← 保存 WebView 引用
│   ├── onDestroy(activity)                         ← 清理 WakeLock/WifiLock/NetworkCallback
│   ├── onConfigurationChanged(newConfig)
│   └── onNewIntent(intent)
│
├── @InvokeArg class RequestPermissionArgs { lateinit var type: String }
├── @InvokeArg class RunRootCommandArgs { lateinit var command: String }
└── @InvokeArg class OpenFileArgs { lateinit var path: String }
```

### microphone（requestPermissionForAlias）

v1.0.3 将麦克风权限请求从原生 `ActivityCompat.requestPermissions` 迁移回 Tauri 的 `requestPermissionForAlias("microphone")` 机制，与 `notification`、`storage`、`camera`、`location` 共享统一的 `@PermissionCallback` 回调通道：

```
前端: invoke('request_android_permission', { type: 'microphone' })
    │
    ▼
Kotlin: requestAndroidPermission → requestPermissionForAlias("microphone", invoke, "onPermissionResult")
    │
    ▼
系统弹出麦克风权限请求对话框
    │
    ▼
用户选择 允许/拒绝
    │
    ▼
@PermissionCallback onPermissionResult(invoke)
    │
    ├── emitPermissionsToWebView()
    │   ├── 查询全部 7 项权限最新状态
    │   └── evaluateJavascript("window.dispatchEvent(new CustomEvent('vcp-permission-change', { detail: {...} }))")
    │
    ▼
前端状态实时刷新
```

**Manifest 声明**：
```xml
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
```

`MODIFY_AUDIO_SETTINGS` 用于语音交互模式中的音频路由控制（如扬声器/听筒切换）。

---

## 3. 权限检查

### 3.1 接口

```rust
#[derive(Serialize, Deserialize)]
pub struct PermissionStatus {
    pub notification: bool,
    pub storage: bool,
    pub battery: bool,
    pub microphone: bool,
    pub camera: bool,
    pub overlay: bool,
    pub location: bool,
}

#[tauri::command]
pub fn check_all_permissions<R: Runtime>(app: AppHandle<R>) -> Result<PermissionStatus, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|check_all_permissions` |
| 参数 | 无 |
| 返回值 | `PermissionStatus` JSON 对象（含 7 个字段） |
| 桌面端行为 | 始终返回所有字段为 `true` |

**新增字段说明（v1.0.3）**：

| 字段 | 含义 | 检查方式 |
|------|------|----------|
| `camera` | 相机权限 | `ContextCompat.checkSelfPermission(CAMERA)` |
| `overlay` | 悬浮窗权限 | `Settings.canDrawOverlays()` 系统设置查询 |
| `location` | 定位权限 | `ACCESS_FINE_LOCATION` 或 `ACCESS_COARSE_LOCATION` 任一授予即视为已授权 |

### 3.2 Kotlin 实现

```kotlin
@Command
fun checkAllPermissions(invoke: Invoke) {
    val pm = activity.getSystemService(Context.POWER_SERVICE) as PowerManager

    val notificationGranted = if (Build.VERSION.SDK_INT >= 33) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.POST_NOTIFICATIONS) == PackageManager.PERMISSION_GRANTED
    } else {
        true
    }

    val storageGranted = if (Build.VERSION.SDK_INT >= 34) {
        val hasAll = ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_MEDIA_IMAGES) == PackageManager.PERMISSION_GRANTED
        val hasVisualSelected = ContextCompat.checkSelfPermission(activity, "android.permission.READ_MEDIA_VISUAL_USER_SELECTED") == PackageManager.PERMISSION_GRANTED
        hasAll || hasVisualSelected
    } else if (Build.VERSION.SDK_INT >= 33) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_MEDIA_IMAGES) == PackageManager.PERMISSION_GRANTED
    } else if (Build.VERSION.SDK_INT >= 29) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_EXTERNAL_STORAGE) == PackageManager.PERMISSION_GRANTED
    } else {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_EXTERNAL_STORAGE) == PackageManager.PERMISSION_GRANTED &&
            ContextCompat.checkSelfPermission(activity, android.Manifest.permission.WRITE_EXTERNAL_STORAGE) == PackageManager.PERMISSION_GRANTED
    }

    val microphoneGranted = ContextCompat.checkSelfPermission(activity, android.Manifest.permission.RECORD_AUDIO) == PackageManager.PERMISSION_GRANTED
    val cameraGranted = ContextCompat.checkSelfPermission(activity, android.Manifest.permission.CAMERA) == PackageManager.PERMISSION_GRANTED
    val locationGranted = ContextCompat.checkSelfPermission(activity, android.Manifest.permission.ACCESS_FINE_LOCATION) == PackageManager.PERMISSION_GRANTED ||
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.ACCESS_COARSE_LOCATION) == PackageManager.PERMISSION_GRANTED

    val batteryOptimizationIgnored = pm.isIgnoringBatteryOptimizations(activity.packageName)
    val overlayGranted = floatingWindowManager.hasOverlayPermission()

    val result = JSObject()
    result.put("notification", notificationGranted)
    result.put("storage", storageGranted)
    result.put("microphone", microphoneGranted)
    result.put("camera", cameraGranted)
    result.put("location", locationGranted)
    result.put("battery", batteryOptimizationIgnored)
    result.put("overlay", overlayGranted)
    invoke.resolve(result)
}
```

#### 版本分支详解

| 权限 | Android 14+ (API 34+) | Android 13 (API 33) | Android 10-12 (API 29-32) | Android 9 及以下 (API ≤28) |
|------|----------------------|---------------------|---------------------------|---------------------------|
| **通知** | `POST_NOTIFICATIONS` 运行时检查 | `POST_NOTIFICATIONS` 运行时检查 | 无需运行时权限，始终 `true` | 无需运行时权限，始终 `true` |
| **存储** | `READ_MEDIA_IMAGES` **或** `READ_MEDIA_VISUAL_USER_SELECTED` | `READ_MEDIA_IMAGES` 细分权限 | `READ_EXTERNAL_STORAGE` | `READ_EXTERNAL_STORAGE` + `WRITE_EXTERNAL_STORAGE` |
| **电池优化** | `PowerManager.isIgnoringBatteryOptimizations()` | 同上 | 同上 | 同上 |

> **Android 14 存储权限变化**：API 34 引入 `READ_MEDIA_VISUAL_USER_SELECTED`，允许用户仅授权部分图片/视频。`checkAllPermissions` 中同时检查 `READ_MEDIA_IMAGES` 和 `READ_MEDIA_VISUAL_USER_SELECTED`，任一满足即视为已授权。

> **注意**：电池优化检查**不是**标准的 Android 运行时权限模型，而是通过 `PowerManager` API 查询应用是否被列入系统电池优化白名单。

---

## 4. 权限请求

### 4.1 接口

```rust
#[tauri::command]
pub fn request_android_permission<R: Runtime>(app: AppHandle<R>, p_type: String) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|request_android_permission` |
| 参数 | `p_type: String` —— 权限类型，支持 `"notification"` / `"storage"` / `"microphone"` / `"camera"` / `"location"` / `"battery"` |
| 返回值 | `Result<(), String>` —— 成功返回 `Ok(())`，请求发起即算成功 |
| 桌面端行为 | 空操作 |

### 4.2 Kotlin 实现

```kotlin
@TauriPlugin(permissions = [
    Permission(strings = ["android.permission.POST_NOTIFICATIONS"], alias = "notification"),
    Permission(strings = ["android.permission.READ_MEDIA_IMAGES"], alias = "storage"),
    Permission(strings = ["android.permission.READ_EXTERNAL_STORAGE", "android.permission.WRITE_EXTERNAL_STORAGE"], alias = "storageLegacy"),
    Permission(strings = ["android.permission.RECORD_AUDIO"], alias = "microphone"),
    Permission(strings = ["android.permission.CAMERA"], alias = "camera"),
    Permission(strings = ["android.permission.ACCESS_FINE_LOCATION", "android.permission.ACCESS_COARSE_LOCATION"], alias = "location")
])
class VcpMobilePlugin(private val activity: Activity) : Plugin(activity) {
    // ...
}
```

`@TauriPlugin` 注解中通过 `@Permission` 定义了**五个权限别名**（v1.0.3 新增 `camera` 和 `location`），将 Android 权限字符串映射为易用的别名：

| 别名 | Android 权限字符串 | 适用版本 |
|------|-------------------|----------|
| `notification` | `POST_NOTIFICATIONS` | Android 13+ |
| `storage` | `READ_MEDIA_IMAGES` | Android 13+ |
| `storageLegacy` | `READ_EXTERNAL_STORAGE` + `WRITE_EXTERNAL_STORAGE` | Android 12 及以下 |
| `camera` | `CAMERA` | 全部版本 |
| `location` | `ACCESS_FINE_LOCATION` + `ACCESS_COARSE_LOCATION` | 全部版本 |

> **注意**：`READ_MEDIA_VISUAL_USER_SELECTED`（Android 14+）不在 `@TauriPlugin` 注解中声明，因为 `requestPermissionForAlias` 机制无法处理「部分授权」语义。`checkAllPermissions` 中通过 `ContextCompat.checkSelfPermission` 手动检测该权限。

#### Tauri 权限命令矩阵（`permissions/default.toml`）

`default.toml` 定义前端可调用命令的默认白名单（v1.1.3 共 43 条命令）：

| 命令 | 说明 | 分类 |
|------|------|------|
| `set_keep_screen_on` | 屏幕常亮 | 屏幕控制 |
| `clear_keep_screen_on` | 清除屏幕常亮 | 屏幕控制 |
| `start_streaming_service` | 启动流式前台保活服务 | 流式服务 |
| `stop_streaming_service` | 停止流式前台保活服务 | 流式服务 |
| `start_helper_service` | 启动 `:helper` 进程 `SseProxyService` | 流式服务 |
| `check_all_permissions` | 查询全部 7 项运行时权限状态 | 权限 |
| `request_android_permission` | 请求指定运行时权限 | 权限 |
| `check_notification_listener_permission` | 检查通知栏监听服务是否启用 | 权限 |
| `request_notification_listener_permission` | 跳转通知栏监听设置页 | 权限 |
| `request_auto_start_permission` | 打开 OEM 自启动设置页 | 权限 |
| `request_power_management_permission` | 打开 OEM 电源管理设置页 | 权限 |
| `check_auto_start_permission` | 检查自启动状态（仅小米/HyperOS 有 AppOps 反射） | 权限 |
| `request_overlay_permission` | 请求悬浮窗（SYSTEM_ALERT_WINDOW）权限 | 权限 |
| `move_task_to_back` | 返回桌面 | 系统控制 |
| `get_free_disk_space` | 获取 `/data` 分区剩余空间 | 系统控制 |
| `get_pending_notification` | 读取点击通知带来的 pending topic/owner/requestId | 系统控制 |
| `pick_file` | 原生文件选择器（见 `08_文件选择与缩略图生成.md`） | 文件 |
| `get_battery_status` | 获取电量与省电模式状态 | 硬件状态 |
| `get_network_status` | 获取网络连接类型、带宽与 IP | 硬件状态 |
| `get_cpu_thermal_status` | 获取 CPU 热状态分级 | 硬件状态 |
| `get_gpu_status` | 获取 GPU 渲染器型号 | 硬件状态 |
| `capture_window_snapshot` | 截取 WebView 根视图为 WebP data URL | 媒体 |
| `save_image_to_gallery` | 从 URL 下载图片并写入 MediaStore | 媒体 |
| `save_image_from_path` | 从本地路径保存图片到相册 | 媒体 |
| `write_temp_file` | 将字节写入缓存目录 | 文件 |
| `delete_temp_file` | 删除缓存目录下指定临时文件 | 文件 |
| `start_download_notification` | 启动前台下载通知 | 通知 |
| `update_download_notification` | 更新下载通知进度 | 通知 |
| `cancel_download_notification` | 取消下载通知 | 通知 |
| `toggle_floating_ball` | 显示/隐藏悬浮助手浮球 | 悬浮窗 |
| `register_shared_files` | 处理来自分享 Intent 的文件 | 文件 |
| `start_sensor_collection` | 启动传感器数据采集（定位 + 运动 + 环境） | 传感器 |
| `stop_sensor_collection` | 停止传感器数据采集，释放所有监听器 | 传感器 |
| `get_sensor_data` | 读取指定类型的缓存传感器数据 | 传感器 |
| `check_root_access` | 检测设备是否已获取 Root 权限 | Root |
| `run_root_command` | 以 Root 身份执行 Shell 命令 | Root |
| `launch_root_manager` | 启动已安装的 Root 管理器应用 | Root |
| `acquire_wake_lock` | 获取 WakeLock + WifiLock 双锁（委托 ForegroundGuardian） | 保活 |
| `release_wake_lock` | 释放 WakeLock 与 WifiLock（委托 ForegroundGuardian） | 保活 |
| `start_network_monitoring` | 启动原生网络状态变化监听 | 网络监控 |
| `acquire_foreground` | 语义化申请前台锁（ForegroundGuardian） | 保活 |
| `release_foreground` | 语义化释放前台锁（ForegroundGuardian） | 保活 |

> **注意**：
> - `open_file_native` 已加入 `default.toml` 白名单，前端可直接调用；Kotlin 侧对应方法为 `openFile()`，会先做沙箱路径校验再通过 `FileProvider` 调起系统查看器。
> - `start_helper_service` 由 `VcpMobilePlugin.init` 自动调用一次，前端亦可显式调用以兜底启动 `:helper` 进程。
> - `get_pending_notification` 用于在应用冷启动后读取用户点击通知栏提醒时携带的 `topicId`/`ownerId`/`requestId`。

#### 权限请求命令

```kotlin
@Command
fun requestAndroidPermission(invoke: Invoke) {
    val args = invoke.parseArgs(RequestPermissionArgs::class.java)
    when (args.type) {
        "notification" -> {
            if (Build.VERSION.SDK_INT >= 33) {
                requestPermissionForAlias("notification", invoke, "onPermissionResult")
            } else {
                emitPermissionsToWebView()
                invoke.resolve()
            }
        }
        "storage" -> {
            if (Build.VERSION.SDK_INT >= 33) {
                requestPermissionForAlias("storage", invoke, "onPermissionResult")
            } else {
                requestPermissionForAlias("storageLegacy", invoke, "onPermissionResult")
            }
        }
        "microphone" -> {
            requestPermissionForAlias("microphone", invoke, "onPermissionResult")
        }
        "camera" -> {
            requestPermissionForAlias("camera", invoke, "onPermissionResult")
        }
        "location" -> {
            requestPermissionForAlias("location", invoke, "onPermissionResult")
        }
        "battery" -> {
            try {
                val intent = Intent(Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS).apply {
                    data = Uri.parse("package:${activity.packageName}")
                }
                startActivityForResult(invoke, intent, "onBatteryOptimizationResult")
            } catch (e: Exception) {
                val intent = Intent(Settings.ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS)
                startActivityForResult(invoke, intent, "onBatteryOptimizationResult")
            }
        }
    }
}
```

| 权限类型 | 请求机制 | 回调 |
|----------|----------|------|
| `notification` | `requestPermissionForAlias("notification", invoke, "onPermissionResult")` | `@PermissionCallback` |
| `storage` | `requestPermissionForAlias("storage"/"storageLegacy", invoke, "onPermissionResult")` | `@PermissionCallback` |
| `microphone` | `requestPermissionForAlias("microphone", invoke, "onPermissionResult")` | `@PermissionCallback` |
| `camera` | `requestPermissionForAlias("camera", invoke, "onPermissionResult")` | `@PermissionCallback` |
| `location` | `requestPermissionForAlias("location", invoke, "onPermissionResult")` | `@PermissionCallback` |
| `battery` | `startActivityForResult(invoke, intent, "onBatteryOptimizationResult")` | `@ActivityCallback` |

#### 回调与事件推送

```kotlin
@PermissionCallback
fun onPermissionResult(invoke: Invoke) {
    emitPermissionsToWebView()
    invoke.resolve()
}

@ActivityCallback
fun onBatteryOptimizationResult(invoke: Invoke, result: ActivityResult) {
    emitPermissionsToWebView()
    invoke.resolve()
}

private fun emitPermissionsToWebView() {
    val pm = activity.getSystemService(Context.POWER_SERVICE) as PowerManager

    val notificationGranted = if (Build.VERSION.SDK_INT >= 33) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.POST_NOTIFICATIONS) == PackageManager.PERMISSION_GRANTED
    } else {
        true
    }

    val storageGranted = if (Build.VERSION.SDK_INT >= 34) {
        val hasAll = ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_MEDIA_IMAGES) == PackageManager.PERMISSION_GRANTED
        val hasVisualSelected = ContextCompat.checkSelfPermission(activity, "android.permission.READ_MEDIA_VISUAL_USER_SELECTED") == PackageManager.PERMISSION_GRANTED
        hasAll || hasVisualSelected
    } else if (Build.VERSION.SDK_INT >= 33) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_MEDIA_IMAGES) == PackageManager.PERMISSION_GRANTED
    } else if (Build.VERSION.SDK_INT >= 29) {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_EXTERNAL_STORAGE) == PackageManager.PERMISSION_GRANTED
    } else {
        ContextCompat.checkSelfPermission(activity, android.Manifest.permission.READ_EXTERNAL_STORAGE) == PackageManager.PERMISSION_GRANTED &&
            ContextCompat.checkSelfPermission(activity, android.Manifest.permission.WRITE_EXTERNAL_STORAGE) == PackageManager.PERMISSION_GRANTED
    }

    val microphoneGranted = ContextCompat.checkSelfPermission(activity, android.Manifest.permission.RECORD_AUDIO) == PackageManager.PERMISSION_GRANTED

    val batteryOptimizationIgnored = pm.isIgnoringBatteryOptimizations(activity.packageName)

    val json = """{"notification":$notificationGranted,"storage":$storageGranted,"microphone":$microphoneGranted,"battery":$batteryOptimizationIgnored}"""
    val script = "window.dispatchEvent(new CustomEvent('vcp-permission-change', { detail: $json }))"
    webViewRef?.evaluateJavascript(script, null)
}
```

- **`requestPermissionForAlias`**：调用 Tauri 框架封装的 `registerForActivityResult(ActivityResultContracts.RequestMultiplePermissions())`，生命周期安全，无需手动处理 `onRequestPermissionsResult`。
- **`startActivityForResult`**：同理使用 Tauri 框架的 `ActivityResultLauncher`，用户从电池优化设置页返回后触发 `@ActivityCallback`。
- **`emitPermissionsToWebView`**：回调内重新查询 notification / storage / microphone / battery 四项权限状态，通过 `evaluateJavascript` 注入 `vcp-permission-change` CustomEvent，前端 `window.addEventListener` 即可实时接收。

> **注意**：电池优化请求返回的 `ActivityResult.resultCode` 因 ROM 而异（部分系统对话框不返回标准 `RESULT_OK`），因此回调内**不依赖 resultCode**，始终执行 `checkAllPermissions` 逻辑并以实际查询结果为准。

---

### 4.3 BatteryStatusManager

`BatteryStatusManager.kt` 是独立的设备电量与系统省电状态检测模块，与权限系统共用 `PowerManager` 但职责分离：前者负责**检测**，后者负责**白名单状态查询与请求**。

#### 接口

```rust
#[derive(Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct BatteryStatus {
    pub level: i32,
    pub is_power_save_mode: bool,
    pub status: Option<String>,
    pub temperature: Option<f64>,
}

#[tauri::command]
pub fn get_battery_status<R: Runtime>(app: AppHandle<R>) -> Result<BatteryStatus, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|get_battery_status` |
| 参数 | 无 |
| 返回值 | `BatteryStatus` JSON 对象（`level` 为 0-100，`status` 为充电状态字符串，`temperature` 为摄氏度，`isPowerSaveMode` 为布尔值） |
| 桌面端行为 | 始终返回 `{ level: 100, status: "未充电", temperature: 25.0, isPowerSaveMode: false }` |

#### 6 层 OEM 省电模式检测逻辑

```kotlin
class BatteryStatusManager(private val context: Context) {
    fun isPowerSaveMode(): Boolean {
        // 1. 原生 PowerManager 状态检测
        val powerManager = context.getSystemService(Context.POWER_SERVICE) as PowerManager
        if (powerManager.isPowerSaveMode) return true

        val resolver = context.contentResolver

        // 2. 原生 Android 广播全局低电量标记
        if (android.provider.Settings.Global.getInt(resolver, "low_power", 0) == 1) return true

        // 3. 通用系统省电标记
        if (android.provider.Settings.System.getInt(resolver, "power_save_mode", 0) == 1) return true

        // 4. 小米 (MIUI / HyperOS) 专用标记
        if (android.provider.Settings.System.getInt(resolver, "POWER_SAVE_MODE_OPEN", 0) == 1) return true

        // 5. 华为 (EMUI / HarmonyOS) 专用标记
        // SmartModeStatus: 1=正常, 2=省电, 3=超级省电, 4=性能
        val hwPowerSave = android.provider.Settings.System.getInt(resolver, "SmartModeStatus", 1)
        if (hwPowerSave == 2 || hwPowerSave == 3) return true

        // 6. Vivo / Oppo 等其他定制 ROM Global 标记
        if (android.provider.Settings.Global.getInt(resolver, "power_save_mode", 0) == 1) return true

        return false
    }
}
```

| 层级 | 检测目标 | 数据来源 | 说明 |
|------|---------|----------|------|
| 1 | 原生 Android | `PowerManager.isPowerSaveMode()` | AOSP 标准 API，Google / 三星 / 索尼等原生或类原生系统 |
| 2 | 全局低电量广播 | `Settings.Global.low_power` | Android 全局低电量模式标记 |
| 3 | 通用系统省电 | `Settings.System.power_save_mode` | 部分国产 ROM 复用的通用键 |
| 4 | 小米 MIUI/HyperOS | `Settings.System.POWER_SAVE_MODE_OPEN` | MIUI 私有 Settings 键，HyperOS 兼容 |
| 5 | 华为 EMUI/HarmonyOS | `Settings.System.SmartModeStatus` | 华为四态电源管理：1=正常, 2=省电, 3=超级省电, 4=性能 |
| 6 | Vivo/OPPO 等 | `Settings.Global.power_save_mode` | 部分 ROM 将省电标记放在 Global 命名空间 |

#### 与前端 `BatteryOptimizationGuide.vue` 的联动

前端通过 `get_battery_status` 获取 `isPowerSaveMode`，但当前 UI 层面并不直接阻断用户操作，而是作为**引导信息**展示：

- `PermissionGate.vue`（引导页 Step 2）提示用户「品牌电池白名单设置」，告知国产 ROM 有独立的电池管理策略。
- `BatteryOptimizationGuide.vue`（全局设置 → 电池优化）提供 5 大品牌的详细白名单配置步骤：OPPO/一加/realme（ColorOS）、小米/Redmi（HyperOS）、华为/荣耀（HarmonyOS）、vivo/iQOO（OriginOS）、三星（One UI）。
- 前端通过 `navigator.userAgent` 自动检测设备品牌并高亮对应步骤，同时提供「物理锁定多任务卡片」的终极防御建议。

**关系澄清**：`BatteryStatusManager` 检测的是「系统是否处于省电模式」，而 `request_android_permission({ type: 'battery' })` 请求的是「应用是否被加入电池优化白名单」。两者互补：前者用于 UI 预警（省电模式可能限制后台网络），后者用于系统级保活（白名单决定 Doze 模式行为）。

---

### 4.4 `open_file_native` — Android 原生文件打开器

`open_file_native` 是 Rust→Kotlin 桥接命令，**已加入 `default.toml` 白名单**，前端可直接调用 `invoke("plugin:vcp-mobile|open_file_native", { path })`；`file_manager.rs` 的 `open_file` 内部也复用该命令。

#### 接口

```rust
#[tauri::command]
pub fn open_file_native<R: Runtime>(app: AppHandle<R>, path: String) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|open_file_native`（也可通过 `file_manager.rs` 的 `open_file` 间接调用） |
| 参数 | `path: String` —— 本地文件的绝对路径 |
| 返回值 | `Result<(), String>` |

#### Kotlin 实现

```kotlin
@Command
fun openFile(invoke: Invoke) {
    val args = invoke.parseArgs(OpenFileArgs::class.java)
    val path = args.path
    if (path.isEmpty()) {
        invoke.reject("Path is empty")
        return
    }

    fileIoExecutor.execute {
        try {
            val context = activity

            // 安全边界拦截：禁止通过 openFile 访问沙箱外部物理文件
            if (!isSafeLocalPath(context, path)) {
                invoke.reject("安全拒绝：禁止打开沙箱外部的敏感文件")
                return@execute
            }

            val file = java.io.File(path)
            if (!file.exists()) {
                invoke.reject("文件不存在: $path")
                return@execute
            }

            val ext = file.extension.lowercase()
            val mimeType = MimeTypeMap.getSingleton().getMimeTypeFromExtension(ext) ?: "*/*"

            // 借助 FileProvider 生成临时读取授权的 content:// URI
            val uri = try {
                FileProvider.getUriForFile(
                    context,
                    "${context.packageName}.fileprovider",
                    file
                )
            } catch (e: Exception) {
                FileProvider.getUriForFile(
                    context,
                    "${context.packageName}.opener.fileprovider",
                    file
                )
            }

            val intent = Intent(Intent.ACTION_VIEW).apply {
                setDataAndType(uri, mimeType)
                addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION)
                addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
            }

            context.startActivity(intent)
            invoke.resolve()
        } catch (e: android.content.ActivityNotFoundException) {
            val ext = java.io.File(path).extension.lowercase()
            invoke.reject("您的手机上未安装能打开此类文件 (.$ext) 的应用，请先安装相关阅读器 (如 WPS Office)。")
        } catch (e: Throwable) {
            invoke.reject("打开文件失败: ${e.message}")
        }
    }
}
```

- **FileProvider**：生成 `content://` 临时授权 URI，避免直接暴露 `file://` URI（Android 7.0+ 禁止）。
- **双 authority 回退**：优先使用 `.fileprovider`，失败时回退到 `.opener.fileprovider`。
- **沙箱边界校验**：`isSafeLocalPath()` 仅允许打开 `cacheDir`、`filesDir`、`externalFilesDir`、`externalCacheDir` 内的文件，防止路径遍历。
- **权限要求**：`openFile` 本身不需要运行时权限，因为 FileProvider 分享的是应用私有目录下的文件（如 `cacheDir`）。但应用 Manifest 中必须声明 `FileProvider`：
  ```xml
  <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
  </provider>
  ```

#### 与 `08_文件选择与缩略图生成.md` 的关系

| 功能 | 对应命令 | 职责 |
|------|---------|------|
| **选择文件** | `pick_file` → `pickFile` | 唤起 `Intent.ACTION_GET_CONTENT`，用户从系统文件管理器中选择文件，返回后拷贝至 `cacheDir` 并生成缩略图 |
| **打开文件** | `open_file` → `openFile`（本小节） | 对**已保存到本地的文件**（如聊天记录中的附件），用系统默认应用（如 WPS、图片浏览器）打开 |

两者使用相同的 `fileIoExecutor` 单线程池避免并发冲突，但生命周期完全不同：`pickFile` 是「摄入」，`openFile` 是「输出」。

---

### 4.5 通知栏监听权限（v1.1.3 新增）

#### 接口

```rust
#[derive(Serialize, Deserialize)]
pub struct ListenerPermissionResponse {
    pub enabled: bool,
}

#[tauri::command]
pub fn check_notification_listener_permission<R: Runtime>(
    app: AppHandle<R>,
) -> Result<ListenerPermissionResponse, String>;

#[tauri::command]
pub fn request_notification_listener_permission<R: Runtime>(
    app: AppHandle<R>,
) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|check_notification_listener_permission` / `plugin:vcp-mobile\|request_notification_listener_permission` |
| 参数 | 无 |
| 返回值 | `{ enabled: bool }` / `()` |
| 桌面端行为 | check 始终返回 `{ enabled: true }`，request 空操作 |

#### Kotlin 实现

`check_notification_listener_permission` 读取 `Settings.Secure.ENABLED_NOTIFICATION_LISTENERS`，按 `:` 分隔并匹配本包名：

```kotlin
val flat = Settings.Secure.getString(context.contentResolver, "enabled_notification_listeners")
var isEnabled = false
if (!flat.isNullOrEmpty()) {
    val names = flat.split(":")
    for (name in names) {
        val cn = ComponentName.unflattenFromString(name)
        if (cn != null && cn.packageName == pkgName) {
            isEnabled = true
            break
        }
    }
}
```

`request_notification_listener_permission` 跳转系统通知栏监听设置页：

```kotlin
val intent = Intent(Settings.ACTION_NOTIFICATION_LISTENER_SETTINGS).apply {
    addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
}
activity.startActivity(intent)
```

#### Manifest 声明

```xml
<uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

<service
    android:name=".service.VcpNotificationListenerService"
    android:label="VCP 连接守护助手"
    android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
    </intent-filter>
</service>
```

`VcpNotificationListenerService.kt` 当前为占位实现：`onNotificationPosted` / `onNotificationRemoved` 不做任何处理以保护用户隐私。

---

## 5. OEM 自启动权限引导（v1.1.2 新增）

### 5.1 概述

国内主流 OEM（小米/华为/OPPO/VIVO/魅族）在系统设置中提供独立的"自启动管理"入口，控制应用是否能在后台自动启动。对于依赖前台服务保活的 VCP Mobile，自启动权限是 ForegroundGuardian 生效的前置条件。

### 5.2 接口

```rust
#[tauri::command]
pub fn request_auto_start_permission<R: Runtime>(app: AppHandle<R>) -> Result<bool, String>;

#[tauri::command]
pub fn check_auto_start_permission<R: Runtime>(app: AppHandle<R>) -> Result<String, String>;
```

| 命令 | 前端调用 | 参数 | 说明 |
|------|---------|------|------|
| `request_auto_start_permission` | `plugin:vcp-mobile\|request_auto_start_permission` | 无 | 打开 OEM 自启动设置页；返回是否成功打开 |
| `check_auto_start_permission` | `plugin:vcp-mobile\|check_auto_start_permission` | 无 | 检查自启动状态（仅 Xiaomi/HyperOS 支持 AppOps 反射） |

### 5.3 多厂商 Intent 链

```kotlin
@Command
fun requestAutoStartPermission(invoke: Invoke) {
    val manufacturer = Build.MANUFACTURER.lowercase(Locale.ROOT)
    var success = false
    val intents = mutableListOf<Intent>()

    if (manufacturer.contains("xiaomi") || manufacturer.contains("redmi")) {
        intents.add(Intent().setComponent(ComponentName("com.miui.securitycenter", "com.miui.permcenter.autostart.AutoStartManagementActivity")))
    } else if (manufacturer.contains("huawei") || manufacturer.contains("honor")) {
        intents.add(Intent().setComponent(ComponentName("com.huawei.systemmanager", "com.huawei.systemmanager.startupmgr.ui.StartupNormalAppListActivity")))
        intents.add(Intent().setComponent(ComponentName("com.huawei.systemmanager", "com.huawei.systemmanager.optimize.bootstart.BootStartActivity")))
    } else if (manufacturer.contains("oppo") || manufacturer.contains("oneplus") || manufacturer.contains("realme")) {
        intents.add(Intent(Settings.ACTION_MANAGE_APPLICATIONS_SETTINGS))
    } else if (manufacturer.contains("vivo")) {
        intents.add(Intent().setComponent(ComponentName("com.iqoo.secure", "com.iqoo.secure.ui.phoneoptimize.BgStartUpManager")))
        intents.add(Intent().setComponent(ComponentName("com.vivo.permissionmanager", "com.vivo.permissionmanager.activity.BgStartUpManagerActivity")))
        intents.add(Intent().setComponent(ComponentName("com.iqoo.secure", "com.iqoo.secure.MainActivity")))
    } else if (manufacturer.contains("meizu")) {
        intents.add(Intent().setComponent(ComponentName("com.meizu.safe", "com.meizu.safe.permission.SmartBGActivity")))
        intents.add(Intent().setComponent(ComponentName("com.meizu.safe", "com.meizu.safe.MainActivity")))
    }

    for (intent in intents) {
        try {
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
            activity.startActivity(intent)
            success = true
            break
        } catch (e: Exception) { /* Try next */ }
    }

    if (!success) {
        try {
            val intent = Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS).apply {
                data = Uri.parse("package:${activity.packageName}")
                addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
            }
            activity.startActivity(intent)
            success = true
        } catch (e: Exception) {
            try {
                val intent = Intent(Settings.ACTION_SETTINGS).apply {
                    addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
                }
                activity.startActivity(intent)
                success = true
            } catch (e2: Exception) {}
        }
    }

    val result = JSObject()
    result.put("success", success)
    invoke.resolve(result)
}
```

| 厂商 | 目标 Activity | 包名 |
|------|-------------|------|
| **小米/Redmi (MIUI/HyperOS)** | `AutoStartManagementActivity` | `com.miui.securitycenter` |
| **华为/荣耀 (EMUI/HarmonyOS)** | `StartupNormalAppListActivity` → `BootStartActivity` | `com.huawei.systemmanager` |
| **OPPO/一加/Realme (ColorOS)** | `ACTION_MANAGE_APPLICATIONS_SETTINGS` | — |
| **VIVO/iQOO** | `BgStartUpManager` / `BgStartUpManagerActivity` / `MainActivity` | `com.iqoo.secure` / `com.vivo.permissionmanager` |
| **魅族** | `SmartBGActivity` / `MainActivity` | `com.meizu.safe` |
| **兜底** | `ACTION_APPLICATION_DETAILS_SETTINGS` → `ACTION_SETTINGS` | — |

### 5.4 Xiaomi AppOps 反射检测

```kotlin
private fun checkAutoStartStatus(): String {
    val manufacturer = Build.MANUFACTURER.lowercase(Locale.ROOT)
    if (manufacturer.contains("xiaomi") || manufacturer.contains("redmi")) {
        val ops = activity.getSystemService(Context.APP_OPS_SERVICE) as? android.app.AppOpsManager
        if (ops != null) {
            try {
                val method = ops.javaClass.getMethod(
                    "checkOpNoThrow",
                    Int::class.javaPrimitiveType,
                    Int::class.javaPrimitiveType,
                    String::class.java
                )
                val mode = method.invoke(
                    ops,
                    10008,
                    activity.applicationInfo.uid,
                    activity.packageName
                ) as Int
                return if (mode == android.app.AppOpsManager.MODE_ALLOWED) "true" else "false"
            } catch (e: Exception) {
                Log.e(TAG, "checkAutoStartStatus: reflection failed", e)
            }
        }
    }
    return "unsupported"
}

@Command
fun checkAutoStartPermission(invoke: Invoke) {
    val status = checkAutoStartStatus()
    val result = JSObject()
    result.put("status", status)
    invoke.resolve(result)
}
```

- **OP_AUTO_START = 10008**：MIUI/HyperOS 私有 AppOps 操作码，非 AOSP 标准。
- **反射调用**：使用 `checkOpNoThrow` 绕过 `@hide` 注解限制。在非 MIUI 设备上返回 `"unsupported"`。
- **返回值**：`"true"`（已允许）、`"false"`（被拒绝/未设置）、`"unsupported"`（非小米设备或反射失败）。

---

## 6. OEM 电源管理权限引导（v1.1.2 新增）

### 6.1 概述

各厂商在电池优化之外设有独立的"电源管理"或"省电策略"入口，控制应用后台行为。此命令链打开对应设置页，引导用户将 VCP Mobile 加入白名单。

### 6.2 接口

```rust
#[tauri::command]
pub fn request_power_management_permission<R: Runtime>(app: AppHandle<R>) -> Result<bool, String>;
```

| 命令 | 前端调用 | 说明 |
|------|---------|------|
| `request_power_management_permission` | `plugin:vcp-mobile\|request_power_management_permission` | 打开 OEM 电源管理设置页；返回是否成功打开 |

### 6.3 多厂商 Intent 链

| 厂商 | Intent Action / 目标 | 说明 |
|------|---------------------|------|
| **小米** | `miui.intent.action.OP_POWER_PRIORITY_SETTINGS` / `com.miui.powerkeeper` 相关 Activity | PowerKeeper 白名单 |
| **OPPO** | `com.coloros.oppoguardelf` → `PowerUsageModelActivity` / `PowerSavedModeActivity` | PowerGuard 电源守护 |
| **华为** | `com.huawei.systemmanager` → `PowerConsumptionActivity` / `ProtectActivity` | 电源消耗管理 |
| **VIVO** | `com.iqoo.secure` → `PowerOptimizeActivity` | 电源优化 |
| **兜底** | `ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS` → `ACTION_APPLICATION_DETAILS_SETTINGS` | 通用电池优化设置 |

### 6.4 Kotlin 实现

```kotlin
@Command
fun requestPowerManagementPermission(invoke: Invoke) {
    val manufacturer = Build.MANUFACTURER.lowercase(Locale.ROOT)
    var success = false
    val intents = mutableListOf<Intent>()

    if (manufacturer.contains("xiaomi") || manufacturer.contains("redmi")) {
        try {
            intents.add(Intent("miui.intent.action.OP_POWER_PRIORITY_SETTINGS").apply {
                putExtra("package_name", activity.packageName)
                putExtra("package_label", activity.applicationInfo.loadLabel(activity.packageManager).toString())
            })
        } catch (e: Exception) {}
        intents.add(Intent().setComponent(ComponentName("com.miui.powerkeeper", "com.miui.powerkeeper.ui.HiddenAppsConfigActivity")).apply {
            putExtra("package_name", activity.packageName)
            putExtra("package_label", activity.applicationInfo.loadLabel(activity.packageManager).toString())
        })
        intents.add(Intent().setComponent(ComponentName("com.miui.securitycenter", "com.miui.powercenter.PowerSettings")))
    } else if (manufacturer.contains("oppo") || manufacturer.contains("oneplus") || manufacturer.contains("realme")) {
        intents.add(Intent().setComponent(ComponentName("com.coloros.oppoguardelf", "com.coloros.powermanager.fuelgaurd.PowerUsageModelActivity")))
        intents.add(Intent().setComponent(ComponentName("com.coloros.oppoguardelf", "com.coloros.powermanager.fuelgaurd.PowerSavedModeActivity")))
        try { intents.add(Intent(Intent.ACTION_POWER_USAGE_SUMMARY)) } catch (e: Exception) {}
    } else if (manufacturer.contains("huawei") || manufacturer.contains("honor")) {
        intents.add(Intent().setComponent(ComponentName("com.huawei.systemmanager", "com.huawei.systemmanager.power.ui.PowerConsumptionActivity")))
        intents.add(Intent().setComponent(ComponentName("com.huawei.systemmanager", "com.huawei.systemmanager.optimize.process.ProtectActivity")))
    } else if (manufacturer.contains("vivo")) {
        intents.add(Intent().setComponent(ComponentName("com.iqoo.secure", "com.iqoo.secure.ui.poweroptimize.PowerOptimizeActivity")))
    }

    for (intent in intents) {
        try {
            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
            activity.startActivity(intent)
            success = true
            break
        } catch (e: Exception) { /* Try next */ }
    }

    if (!success) {
        try {
            val intent = Intent(Settings.ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS).apply {
                addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
            }
            activity.startActivity(intent)
            success = true
        } catch (e: Exception) {
            try {
                val intent = Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS).apply {
                    data = Uri.parse("package:${activity.packageName}")
                    addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
                }
                activity.startActivity(intent)
                success = true
            } catch (e2: Exception) {}
        }
    }

    val result = JSObject()
    result.put("success", success)
    invoke.resolve(result)
}
```

- 与 `requestAutoStartPermission` 类似，按厂商分组尝试，失败后回退到通用电池优化设置页。
- 返回 `{"success": true/false}`，供前端判断是否需要进一步引导。

---

## 7. 存储空间检测（v1.1.2 新增）

### 7.1 接口

```rust
#[derive(Serialize, Deserialize)]
pub struct DiskSpaceInfo {
    pub free_bytes: u64,
    pub free_gb: f64,
    pub total_bytes: u64,
    pub total_gb: f64,
}

#[tauri::command]
pub fn get_free_disk_space<R: Runtime>(app: AppHandle<R>) -> Result<DiskSpaceInfo, String>;
```

| 前端命令 | `plugin:vcp-mobile\|get_free_disk_space` |
|----------|-------------------------------------------|
| 参数 | 无 |
| 返回值 | `DiskSpaceInfo { freeBytes, freeGb, totalBytes, totalGb }` |

### 7.2 Kotlin 实现

```kotlin
@Command
fun getFreeDiskSpace(invoke: Invoke) {
    try {
        val path = Environment.getDataDirectory()
        val stat = android.os.StatFs(path.path)
        val blockSize = stat.blockSizeLong
        val availableBlocks = stat.availableBlocksLong
        val totalBlocks = stat.blockCountLong

        val freeBytes = availableBlocks * blockSize
        val totalBytes = totalBlocks * blockSize

        val freeGB = freeBytes.toDouble() / (1024.0 * 1024.0 * 1024.0)
        val totalGB = totalBytes.toDouble() / (1024.0 * 1024.0 * 1024.0)

        val result = JSObject()
        result.put("freeBytes", freeBytes.toDouble())
        result.put("freeGb", freeGB)
        result.put("totalBytes", totalBytes.toDouble())
        result.put("totalGb", totalGB)
        invoke.resolve(result)
    } catch (e: Exception) {
        Log.e(TAG, "getFreeDiskSpace failed", e)
        invoke.reject(e.message ?: "Failed to get free disk space")
    }
}
```

- **目标分区**：`/data`（用户数据分区）——模型下载、附件存储、数据库均在此。
- **前端使用**：`PermissionGate.vue` Step 3 存储空间验证条。要求 ≥ 5.0 GB 空闲方可进入应用，不足时显示红色警告并阻止进入。

---

## 8. OomScoreGuard 进程保护（v1.1.2 新增）

### 8.1 概述

在已 Root 设备上，`OomScoreGuard` 通过后台线程每 20 秒向 `/proc/$pid/oom_score_adj` 写入 `-900`，主动对抗 Android LMK（Low Memory Killer）的进程查杀，将应用进程伪装为"系统级不可杀"。

```kotlin
fun startOomScoreGuard() {
    fileIoExecutor.execute {
        while (Shell.getShell().isRoot) {
            try {
                Shell.cmd("echo -900 > /proc/${android.os.Process.myPid()}/oom_score_adj").exec()
                Thread.sleep(20_000)
            } catch (e: Exception) {
                Log.w(TAG, "OomScoreGuard error", e)
            }
        }
    }
}
```

| 属性 | 值 |
|------|-----|
| 执行线程 | `fileIoExecutor` 单例线程池 |
| 写入间隔 | 20 秒 |
| 写入值 | `-900`（低于 `ProcessList.NATIVE_ADJ = -1000`，高于 `ProcessList.SYSTEM_ADJ = -900`） |
| 依赖 | `libsu`（topjohnwu）提供 Root Shell |
| 退出条件 | `Shell.getShell().isRoot == false`（Root 会话断开） |

> **注意**：OomScoreGuard 仅在已 Root 设备上生效。无 Root 设备上 `isRoot` 为 `false`，循环永不进入。

---

## 9. 返回桌面

### 9.1 接口

```rust
#[tauri::command]
pub fn move_task_to_back<R: Runtime>(app: AppHandle<R>) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|move_task_to_back` |
| 参数 | 无 |
| 桌面端行为 | 空操作 |

### 9.2 Kotlin 实现

```kotlin
@Command
fun moveTaskToBack(invoke: Invoke) {
    activity.moveTaskToBack(true)
    invoke.resolve()
}
```

- **`moveTaskToBack(true)`**：将当前任务移到后台，参数 `true` 表示如果当前任务是根 Activity，则整个任务进入后台；`false` 则仅当前 Activity。
- **效果**：等同于用户点击 Home 键，应用仍在运行，不触发 `onDestroy()`。

---

## 10. 数据流完整链路

### notification / storage（标准运行时权限）

```
前端: invoke('request_android_permission', { type: 'notification' })
    │
    ▼
system.rs :: request_android_permission("notification")
    │
    ▼
run_mobile_plugin("requestAndroidPermission", { "type": "notification" })
    │
    ▼
Kotlin: VcpMobilePlugin.requestAndroidPermission(invoke)
    │
    ├── parseArgs(RequestPermissionArgs) → { type: "notification" }
    ├── Build.VERSION.SDK_INT >= 33 ?
    │   ├── YES: requestPermissionForAlias("notification", invoke, "onPermissionResult")
    │   └── NO: emitPermissionsToWebView() + invoke.resolve()
    │
    ▼
Tauri PluginManager :: requestPermissionsLauncher.launch([POST_NOTIFICATIONS])
    │
    ▼
系统弹出权限请求对话框（Android 13+）
    │
    ▼
用户选择 允许/拒绝
    │
    ▼
Tauri 框架回调 → @PermissionCallback onPermissionResult(invoke)
    │
    ├── emitPermissionsToWebView()
    │   ├── 查询 notification/storage/microphone/battery 最新状态
    │   └── evaluateJavascript("window.dispatchEvent(new CustomEvent('vcp-permission-change', { detail: {...} }))")
    │
    ▼
前端 window.addEventListener('vcp-permission-change') 收到事件
    │
    ▼
PermissionGate.vue 状态实时刷新为 "OK"
```

### battery（设置页跳转）

```
前端: invoke('request_android_permission', { type: 'battery' })
    │
    ▼
Kotlin: requestAndroidPermission → startActivityForResult(intent, "onBatteryOptimizationResult")
    │
    ▼
系统弹出电池优化对话框 / 或跳转设置页
    │
    ▼
用户操作后返回应用
    │
    ▼
Tauri 框架回调 → @ActivityCallback onBatteryOptimizationResult(invoke, result)
    │
    ├── emitPermissionsToWebView() → 推送 vcp-permission-change 事件
    │
    ▼
前端状态实时刷新
```

### microphone / camera / location（Tauri PermissionAlias 请求）

```
前端: invoke('request_android_permission', { type: 'microphone' | 'camera' | 'location' })
    │
    ▼
Kotlin: requestAndroidPermission → requestPermissionForAlias("microphone"/"camera"/"location", invoke, "onPermissionResult")
    │
    ▼
系统弹出权限请求对话框
    │
    ▼
用户选择 允许/拒绝
    │
    ▼
@PermissionCallback onPermissionResult(invoke)
    │
    ├── emitPermissionsToWebView() → 推送 vcp-permission-change 事件（含全部 7 项权限）
    │
    ▼
前端 PermissionGate.vue / 设置页状态实时刷新
```

> **注意**：v1.0.3 已将 `microphone`、`camera`、`location` 统一纳入 Tauri 的 `requestPermissionForAlias` 机制，回调链路统一收敛到 `@PermissionCallback onPermissionResult` → `emitPermissionsToWebView()`。不再有原生 `ActivityCompat.requestPermissions` 的特殊路径。

---

## 11. 关键约束

1. **`p_type` 字符串硬编码**：前端传入 `"notification"` / `"storage"` / `"microphone"` / `"battery"`，拼写错误会导致 Kotlin `when` 分支未命中，静默无操作。
   - **防范**：前端封装为枚举或常量，如 `PermissionType.NOTIFICATION`。

2. **事件通道依赖 `webViewRef`**：`emitPermissionsToWebView()` 需要在 `load(webView)` 时保存 WebView 引用。若插件加载失败或 WebView 未初始化，事件将静默丢失（前端仍可通过 `visibilitychange` 和兜底轮询恢复）。

3. **权限请求的回调通道分为两类**：
   - `notification` / `storage` / `microphone` / `camera` / `location` 使用 `@PermissionCallback`（Tauri 框架的 `ActivityResultLauncher`），共享 `onPermissionResult` 回调。
   - `battery` 使用 `@ActivityCallback`（Tauri 框架的 `startActivityForResult`），触发 `onBatteryOptimizationResult` 回调。
   - 两种回调触发路径不同，但统一收敛到 `emitPermissionsToWebView()` 推送 `vcp-permission-change` 事件（含全部 7 项权限最新状态）。

4. **`open_file_native` 的权限边界**：
   - 该函数通过 `FileProvider` 分享应用私有文件，**不需要** `READ_EXTERNAL_STORAGE` 或 `MANAGE_EXTERNAL_STORAGE` 运行时权限。
   - 但如果试图打开的文件位于应用私有目录之外（如用户通过 `pick_file` 选择后未拷贝到 `cacheDir` 的文件），`FileProvider` 会抛出 `IllegalArgumentException`，此时会回退到第二个 authority 尝试。
   - 参见 `docs/ANDROID_PLUGIN_MANAGEMENT.md` §3.1 关于 `FileProvider` 的路径配置规范。

5. **与规范文档的交叉引用**：
   - `POST_NOTIFICATIONS` 血训：参见 `docs/ANDROID_PLUGIN_MANAGEMENT.md` §3.1 / §7.2。
   - Tauri 权限回调机制官方文档：参见 `docs/ANDROID_PLUGIN_MANAGEMENT.md` §5.2。

6. **传感器采集生命周期**：
   - `start_sensor_collection` 在 `DistributedNode::start()` 成功后由 Rust 端自动调用，**不由前端直接触发**。
   - `stop_sensor_collection` 在 `DistributedNode::stop()` 时由 Rust 端自动调用，确保传感器随节点生命周期同步启停。
   - 前端仅通过 `get_sensor_data` 被动读取缓存值，不对传感器进行启停控制。

7. **定位权限检查边界**：
   - `SensorStatusManager.startLocationListening()` 在启动前检查 `ACCESS_FINE_LOCATION` 和 `ACCESS_COARSE_LOCATION`。**两项均未授予时，定位采集静默降级**（缓存值设为"未获得定位权限"），不阻塞运动和环境的采集。
   - 定位权限请求**不在** `SensorStatusManager` 内部触发，而是由上层调用方（如 `DistributedSettingsSection`）在连接前通过 `request_android_permission({ type: 'notification' })` 等现有权限通道统一处理。

---

## 12. SensorStatusManager —— Android 原生传感器采集

> 源码：`src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/SensorStatusManager.kt`（319 行）

### 12.1 架构概览

`SensorStatusManager` 是 v1.0.3 新增的 Android 原生传感器采集管理器，替代了此前基于前端 Web API（`navigator.geolocation`、`devicemotion`、`AmbientLightSensor`）的传感器方案。它是一个自包含的单例式类，由 `VcpMobilePlugin.kt` 持有引用，通过 Plugin IPC 通道向 Rust 工具层和前端提供传感器遥测数据。

```
VcpMobilePlugin.kt
├── sensorStatusManager: SensorStatusManager     ← 唯一持有者
│
SensorStatusManager.kt
├── sensorManager: SensorManager                 ← Android 系统传感器服务
├── locationManager: LocationManager             ← Android 系统定位服务
├── scheduler: ScheduledExecutorService          ← 单线程调度器（预留）
├── mainHandler: Handler(Looper.getMainLooper()) ← 主线程定时器
│
├── @Volatile latestLocationStr                  ← 缓存的定位信息字符串
├── @Volatile latestMotionStr                    ← 缓存的运动状态字符串
├── @Volatile latestAmbientStr                   ← 缓存的环境传感器字符串
│
├── start()                                      ← 启动全部传感器
├── stop()                                       ← 停止全部传感器，清理监听器
├── getSensorData(type: String): JSObject        ← 读取缓存数据（按类型）
│
├── 定位监听器 (locationListener)
├── 运动突发监听器 (motionListener)
└── 环境持续监听器 (ambientListener)
```

### 12.2 三级传感器采集体系

`SensorStatusManager` 管理三种物理传感器/定位源，各自采用不同的采集策略以适应功耗、时效性和准确性的需求：

```
┌─────────────────────────────────────────────────────────────┐
│              SensorStatusManager 采集体系                    │
├───────────────┬─────────────────┬───────────────────────────┤
│  采集层        │ 数据源           │ 策略与参数                 │
├───────────────┼─────────────────┼───────────────────────────┤
│ 定位 (Location)│ GPS Provider    │ 首 30s Fast-Fix: 1s / 0m  │
│               │ Network Provider│ 稳态: 30s / 5m             │
│               │                 │ 启动时读取 lastKnown + 单次更新 │
│               │                 │ 权限感知：无权限则降级      │
├───────────────┼─────────────────┼───────────────────────────┤
│ 运动 (Motion) │ TYPE_ACCELEROMETER│ 突发采样: 2s 活跃 / 28s 休眠 │
│               │ TYPE_GYROSCOPE  │ 采样率: 10Hz (100ms/次)    │
│               │ TYPE_MAGNETIC_FIELD│ 后处理: 加速度/陀螺仪/磁场 │
│               │                 │ 状态分类: 静止/轻微移动/步行中/运动中 │
├───────────────┼─────────────────┼───────────────────────────┤
│ 环境 (Ambient) │ TYPE_LIGHT       │ 持续监听 @ SENSOR_DELAY_NORMAL │
│               │ TYPE_PRESSURE    │ 推送模式: 传感器数据变化即回调 │
│               │                 │ 输出: lux + hPa + 场景描述  │
└───────────────┴─────────────────┴───────────────────────────┘
```

#### 12.2.1 定位采集（Location）

```kotlin
private fun startLocationListening() {
    val hasFine = ContextCompat.checkSelfPermission(
        context, android.Manifest.permission.ACCESS_FINE_LOCATION
    ) == PackageManager.PERMISSION_GRANTED
    val hasCoarse = ContextCompat.checkSelfPermission(
        context, android.Manifest.permission.ACCESS_COARSE_LOCATION
    ) == PackageManager.PERMISSION_GRANTED

    if (!hasFine && !hasCoarse) {
        latestLocationStr = "位置信息: 未获得定位权限"
        return  // 静默降级，不阻塞运动与环境采集
    }

    try {
        // 1. 立即用 lastKnownLocation 种子化缓存
        seedLastKnownLocation(LocationManager.NETWORK_PROVIDER)
        seedLastKnownLocation(LocationManager.GPS_PROVIDER)

        // 2. 若仍无有效位置，请求单次更新
        if (!hasValidLocation()) {
            requestSingleUpdate(LocationManager.NETWORK_PROVIDER)
            requestSingleUpdate(LocationManager.GPS_PROVIDER)
        }

        // 3. 前 30 秒使用激进 fast-fix：1s / 0m，收到首点后再切到稳态 30s / 5m
        registerFastFixUpdates()
        scheduleFastFixTimeout()
    } catch (e: SecurityException) {
        latestLocationStr = "位置信息: 获取异常 (${e.message})"
    } catch (e: Exception) {
        latestLocationStr = "位置信息: 未开启定位服务"
    }
}
```

**双 Provider 策略**：

| Provider | 定位源 | 精度 | 功耗 | 适用场景 |
|----------|--------|------|------|----------|
| `NETWORK_PROVIDER` | 基站 + WiFi 指纹 | 50m--500m | 低 | 室内、快速首次定位 |
| `GPS_PROVIDER` | GPS 卫星 | 3m--30m | 高 | 室外精确定位 |

- 两个 Provider **并行注册**，任一有新数据即更新缓存。
- 启动流程：`seedLastKnownLocation()` → `requestSingleUpdate()` → `registerFastFixUpdates()`（1s/0m，持续 30s）。收到首个定位后切换为稳态 `registerSteadyLocationUpdates()`（30s/5m）。
- 若 30s 内仍未收到定位，自动降级为稳态监听，并将缓存置为 `"位置信息: 定位超时，请检查定位开关或移动到开阔地带"`。
- 定位结果格式化：`坐标: xx.xxxx°N/S, xx.xxxx°E/W | 精度: 15m | 海拔: 50m`

#### 12.2.2 运动突发采样（Motion Burst Sampling）

运动传感器（加速度计）采用**突发采样（Burst Sampling）**策略以最小化功耗：

```
┌────────────────────────────────────────────────────────────┐
│  一周期 = 30 秒                                             │
│                                                            │
│  ┌──────────┐                                              │
│  │ 活跃 2s  │───────────────────────────────────────        │
│  │ 10Hz 采 │                  休眠 28s                      │
│  │ 样(~20  │                (传感器关闭)                     │
│  │ 个样本) │                                              │
│  └──────────┘                                              │
│  t=0       t=2s                                t=30s       │
│                                                            │
│  活跃期: SensorManager.registerListener(accel, 100ms)       │
│  休眠期: SensorManager.unregisterListener(accel)            │
│  过渡:   mainHandler.postDelayed(nextBurst, 28000)          │
└────────────────────────────────────────────────────────────┘
```

**关键参数**：

| 参数 | 值 | 说明 |
|------|-----|------|
| `BURST_ACTIVE_DURATION` | 2000ms | 每周期采样窗口长度 |
| `BURST_SLEEP_DURATION` | 28000ms | 每周期间隔休眠长度 |
| `SAMPLING_PERIOD_US` | 100000us (100ms) | 活跃期内采样间隔，等价 10Hz |
| 内联限流 | `now - lastSampleTime < 100ms --> return` | 双重保障，防止回调过于频繁 |

**运动状态分类**（基于加速度 + 陀螺仪综合判定）：

| 平均加速度 `avg` | 陀螺仪平均 `gyroAvg` | 状态 | 阈值设计依据 |
|-----------------|---------------------|------|-------------|
| `avg <= 9.5` 且 `gyroAvg <= 0.1` | 静止 | 静止设备仅受重力（~9.8 m/s²）影响 |
| `avg <= 10.5` 或 `gyroAvg <= 0.5` | 轻微移动 | 设备轻微晃动 |
| `avg <= 12.0` 或 `gyroAvg <= 1.5` | 步行中 | 典型步行步态加速度 |
| 否则 | 运动中 | 跑步或交通工具震动 |

**采样数据源**：突发窗口内同时注册 `TYPE_ACCELEROMETER`、`TYPE_GYROSCOPE`（如有）和 `TYPE_MAGNETIC_FIELD`（如有），各自计算幅度后合成最终字符串。

**后处理输出格式**（含可折叠语义标记）：

```text
[===vcp_fold: 0.0 ::desc: 物理运动姿态粗略状态(... )===]
状态: <分类>

[===vcp_fold: 0.50 ::desc: 九轴高频遥测指标、旋转角速度、加速度峰值、三轴磁敏度物理强度===]
状态: <分类> | 平均加速度: xx.xx m/s² (峰值: xx.xx m/s²) | 旋转角速度: xx.xx rad/s (峰值: xx.xx) | 磁场强度: xx.x μT
```

**线程安全**：`burstAccelSamples`、`burstGyroSamples`、`burstMagSamples` 各自使用 `synchronized(...)` 保护；`start()` / `stop()` 通过 `@Synchronized` + `isRunning` 标志位保证幂等。

#### 12.2.3 环境持续监听（Ambient Continuous Listening）

环境光传感器（`TYPE_LIGHT`）和气压传感器（`TYPE_PRESSURE`）以**持续低频率模式**监听，功耗极低：

```kotlin
// 持续监听，不启用突发/休眠模式
if (lightSensor != null) {
    sensorManager.registerListener(ambientListener, lightSensor, SensorManager.SENSOR_DELAY_NORMAL)
}
if (pressureSensor != null) {
    sensorManager.registerListener(ambientListener, pressureSensor, SensorManager.SENSOR_DELAY_NORMAL)
}
```

- **`SENSOR_DELAY_NORMAL`**：~200ms 采样间隔（约 5Hz），Android 默认速率。环境传感器（光/气压）变化缓慢，此速率完全满足需求。
- 两个传感器共享同一个 `ambientListener` 实例，在 `onSensorChanged` 中通过 `event.sensor.type` 区分数据来源。
- 传感器数据变化时自动回调 `updateAmbientString()`，拼接当前的光照和气压描述。

**环境光描述语义**：

| 光照强度范围 (lux) | 描述 | 典型场景 |
|-------------------|------|----------|
| 0--49 | 暗 | 夜间、关闭灯光 |
| 50--199 | 室内 | 办公室、客厅照明 |
| 200--999 | 明亮 | 靠近窗户、商场 |
| >= 1000 | 户外 | 日光直射或强光环境 |

**输出格式**（含可折叠语义标记）：

```text
[===vcp_fold: 0.0 ::desc: 当前所处的物理环境光照度大体描述(... )===]
环境光: 150 lux (室内)

[===vcp_fold: 0.45 ::desc: 物理环境大气压强、精确光照度数值与场景气压监测===]
环境光: 150 lux (室内) | 气压: 1013 hPa
```

### 12.3 线程安全与缓存机制

#### @Volatile 缓存字段

三个输出字符串使用 `@Volatile` 注解：

```kotlin
@Volatile private var latestLocationStr = "位置信息: 等待数据采集..."
@Volatile private var latestMotionStr = "运动状态: 静止"
@Volatile private var latestAmbientStr = "环境传感器: 设备不支持或权限未授予"
```

- **`@Volatile`** 确保一个线程写入后，其他线程立即看到最新值（JVM happens-before 语义）。
- 写入仅发生在各自的监听器回调线程（Android Sensor 框架的传感器事件线程或主线程）。
- 读取仅发生在 `getSensorData()` 方法中，该方法由 Plugin IPC 在工作线程调用。
- 这种单写-单读模式配合 `@Volatile` 保证了无需加锁的线程安全。

#### getSensorData() 方法

```kotlin
fun getSensorData(type: String): JSObject {
    val obj = JSObject()
    when (type) {
        "location" -> obj.put("value", latestLocationStr)
        "motion"   -> obj.put("value", latestMotionStr)
        "ambient"  -> obj.put("value", latestAmbientStr)
        "all"      -> {
            obj.put("location", latestLocationStr)
            obj.put("motion", latestMotionStr)
            obj.put("ambient", latestAmbientStr)
        }
    }
    return obj
}
```

| type 参数 | 返回值结构 | 使用方 |
|-----------|-----------|--------|
| `"location"` | `{ value: "坐标: ..." }` | `location.rs::read_current()` |
| `"motion"` | `{ value: "状态: ..." }` | `motion_sensor.rs::read_current()` |
| `"ambient"` | `{ value: "环境光: ..." }` | `ambient_sensor.rs::read_current()` |
| `"all"` | `{ location, motion, ambient }` | `DistributedView.vue`（前端遥测面板） |

- **零开销读取**：直接返回 `@Volatile` 字段的当前值，无 I/O、无阻塞、无额外系统调用。
- **即时返回**：Rust 工具层的 `read_current()` 以 30 秒周期调用此方法，每次调用的响应时间 < 1ms。

### 12.4 生命周期管理

```
┌─────────────────────────────────────────────────────────────┐
│  DistributedNode::start()  (Rust)                           │
│      │                                                      │
│      ▼                                                      │
│  system.rs::start_sensor_collection()                       │
│      │                                                      │
│      ▼  run_mobile_plugin("startSensorCollection")          │
│      │                                                      │
│      ▼                                                      │
│  VcpMobilePlugin.kt::startSensorCollection(invoke)          │
│      │                                                      │
│      ▼                                                      │
│  SensorStatusManager.start()                                │
│      │                                                      │
│      ├── startLocationListening()   ← 权限检查 + 双Provider │
│      ├── registerListener(light)    ← 环境光持续监听       │
│      ├── registerListener(pressure) ← 气压持续监听         │
│      └── scheduleNextMotionBurst()  ← 运动突发采样启动     │
│                                                              │
│  ... (传感器持续运行) ...                                    │
│                                                              │
│  DistributedNode::stop()  (Rust)                            │
│      │                                                      │
│      ▼                                                      │
│  SensorStatusManager.stop()                                 │
│      │                                                      │
│      ├── locationManager.removeUpdates()                    │
│      ├── sensorManager.unregisterListener(ambientListener)  │
│      ├── sensorManager.unregisterListener(motionListener)   │
│      └── mainHandler.removeCallbacksAndMessages(null)       │
└─────────────────────────────────────────────────────────────┘
```

- **`stop()` 的幂等性**：`@Synchronized` 保护 + `isRunning` 标志位，重复调用不会重复清理。
- **运动突发的中断**：`mainHandler.removeCallbacksAndMessages(null)` 清除主线程待处理的所有延时任务，包括活跃期结束时排期的休眠等待和下一个突发启动回调。
- **摇动传感器监听器**：即使运动传感器在休眠期（已取消注册），`stop()` 仍尝试取消注册，确保极端时序下的资源释放。

---

## 13. 传感器 IPC 命令

v1.0.3 在 `system.rs` 中新增了 3 个 `#[tauri::command]` 处理器，在 Kotlin 插件中新增了对应的 3 个 `@Command` 方法，并在 `permissions/default.toml` 和 `permissions/all.toml` 中将它们加入了命令白名单。

### 13.1 Rust 侧命令签名

```rust
// system.rs 新增的三个传感器命令

#[tauri::command]
pub fn start_sensor_collection<R: Runtime>(app: AppHandle<R>) -> Result<(), String>;

#[tauri::command]
pub fn stop_sensor_collection<R: Runtime>(app: AppHandle<R>) -> Result<(), String>;

#[tauri::command]
pub fn get_sensor_data<R: Runtime>(
    app: AppHandle<R>,
    sensor_type: String,    // "location" | "motion" | "ambient" | "all"
) -> Result<serde_json::Value, String>;
```

### 13.2 Kotlin 侧命令处理器

| Rust 命令 | Kotlin 方法名 | 参数 | 行为 |
|-----------|--------------|------|------|
| `start_sensor_collection` | `startSensorCollection(invoke)` | 无 | 调用 `SensorStatusManager.start()`，启动全部传感器 |
| `stop_sensor_collection` | `stopSensorCollection(invoke)` | 无 | 调用 `SensorStatusManager.stop()`，释放所有监听器 |
| `get_sensor_data` | `getSensorData(invoke)` | `{ type: "location"/"motion"/"ambient"/"all" }` | 调用 `SensorStatusManager.getSensorData(type)`，即时返回缓存值 |

### 13.3 前端调用示例

```typescript
// 读取运动传感器数据（由 Rust distributed/motion_sensor.rs 内部调用）
const motion = await invoke('plugin:vcp-mobile|get_sensor_data', {
    sensorType: 'motion'
});
// 返回: { value: "状态: 静止 | 平均加速度: 9.82m/s² | 峰值: 10.15m/s²" }

// 一次性读取全部传感器数据（由 DistributedView.vue 遥测面板调用）
const all = await invoke('plugin:vcp-mobile|get_sensor_data', {
    sensorType: 'all'
});
// 返回: { location: "...", motion: "...", ambient: "..." }
```

> **注意**：`start_sensor_collection` 和 `stop_sensor_collection` 由 Rust `distributed/client.rs` 在 `DistributedNode::start()` / `stop()` 生命周期方法中自动调用，前端不应直接调用这两个命令。

### 13.4 桌面端 fallback 行为

三个传感器命令在非 Android 平台上均返回模拟/占位数据：

```rust
#[cfg(not(target_os = "android"))]
pub fn get_sensor_data<R: Runtime>(app: AppHandle<R>, sensor_type: String) -> Result<serde_json::Value, String> {
    match sensor_type.as_str() {
        "location" => Ok(json!({"value": "坐标: 39.9000°N, 116.4000°E | 精度: 15m | 海拔: 50m"})),
        "motion"   => Ok(json!({"value": "状态: 静止 | 平均加速度: 9.80m/s² | 峰值: 9.80m/s²"})),
        "ambient"  => Ok(json!({"value": "环境光: 150 lux (室内) | 气压: 1013 hPa"})),
        "all"      => Ok(json!({"location": "...", "motion": "...", "ambient": "..."})),
        _          => Ok(json!({"value": ""})),
    }
}
```

| 命令 | 桌面端行为 |
|------|-----------|
| `start_sensor_collection` | 空操作（`Ok(())`） |
| `stop_sensor_collection` | 空操作（`Ok(())`） |
| `get_sensor_data` | 返回硬编码的演示数据（北京天安门坐标、安静状态、室内光照） |

---

## 14. 定位权限（ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION）

### 14.1 Manifest 声明

v1.0.3 在 `AndroidManifest.xml` 中新增了两项定位权限声明：

```xml
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
```

| 权限 | 定位精度 | 数据来源 | 调用 API |
|------|---------|----------|----------|
| `ACCESS_FINE_LOCATION` | ~3m--30m | GPS 卫星 + 精确 WiFi | `LocationManager.requestLocationUpdates(GPS_PROVIDER)` |
| `ACCESS_COARSE_LOCATION` | ~50m--500m | 基站 + WiFi 指纹 | `LocationManager.requestLocationUpdates(NETWORK_PROVIDER)` |

**权限关系**：`ACCESS_FINE_LOCATION` **包含** `ACCESS_COARSE_LOCATION` 的全部能力。若用户仅授予 `ACCESS_COARSE_LOCATION`，GPS Provider 仍然无法使用，仅 Network Provider 可用。

### 14.2 权限检查流程

```
SensorStatusManager.start()
    │
    ▼
startLocationListening()
    │
    ├── checkSelfPermission(ACCESS_FINE_LOCATION) → hasFine: Boolean
    ├── checkSelfPermission(ACCESS_COARSE_LOCATION) → hasCoarse: Boolean
    │
    ├── !hasFine && !hasCoarse ?
    │   ├── YES → latestLocationStr = "位置信息: 未获得定位权限"
    │   │         Log.w(TAG, "Location permissions not granted.")
    │   │         return (静默降级，继续启动运动和环境的采集)
    │   │
    │   └── NO → 继续注册 Provider
    │            ├── Network Provider: requestLocationUpdates(120s, 10m)
    │            └── GPS Provider: requestLocationUpdates(120s, 10m)
    │
    ▼
运动和环境的采集正常启动（不依赖定位权限结果）
```

### 14.3 降级语义对照表

| 权限状态 | hasFine | hasCoarse | 定位行为 | latestLocationStr 内容 |
|----------|---------|-----------|----------|----------------------|
| 两项均未授予 | false | false | 不注册任何 Provider | `"位置信息: 未获得定位权限"` |
| 仅粗略位置 | false | true | Network Provider 工作，GPS 跳过 | 格式化坐标（精度较低） |
| 精确位置已授予 | true | true (隐式) | 双 Provider 均工作 | 格式化坐标（GPS 精度优先） |
| 任一 Provider 异常 | -- | -- | `catch SecurityException` | `"位置信息: 获取异常 (msg)"` |
| 定位服务未开启 | -- | -- | `isProviderEnabled() == false` | `"位置信息: 未开启定位服务"` |

> **与现有权限系统的关系**：`@TauriPlugin` 注解中已为定位声明别名 `location`，前端也可通过 `request_android_permission({ type: 'location' })` 主动请求。`SensorStatusManager` 本身遵循"被动检查 + 静默降级"模式，不在内部触发权限弹窗，只做状态检查。

### 14.4 与分布式工具层的映射

| 权限 | 涉及的 Rust 工具 | 数据流 |
|------|-----------------|--------|
| `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION` | `MobileLocation` (`location.rs`) | Android `LocationManager` → `SensorStatusManager` 缓存 → Plugin IPC `getSensorData("location")` → Rust `read_current()` → 主服务器 `update_static_placeholders` |
| 无（硬件访问无需运行时权限） | `MobileMotion` (`motion_sensor.rs`) | Android `SensorManager(TYPE_ACCELEROMETER)` → Burst 后处理 → Plugin IPC `getSensorData("motion")` → Rust |
| 无（硬件访问无需运行时权限） | `MobileAmbient` (`ambient_sensor.rs`) | Android `SensorManager(TYPE_LIGHT, TYPE_PRESSURE)` → Plugin IPC `getSensorData("ambient")` → Rust |

> 运动传感器（加速度计）和环境传感器（光/气压）在 Android 上**不需要运行时权限**，仅需 Manifest 声明或默认可用。因此 `SensorStatusManager` 的运动和环境采集不受权限检查影响，总是能正常启动。

### 14.5 定位权限数据流

```
用户授予定位权限（系统设置或首次弹窗）
    │
    ▼
Android 系统记录权限状态
    │
    ▼
SensorStatusManager.startLocationListening()
    ├── ContextCompat.checkSelfPermission(ACCESS_FINE_LOCATION) → GRANTED
    ├── ContextCompat.checkSelfPermission(ACCESS_COARSE_LOCATION) → GRANTED
    │
    ▼
LocationManager.requestLocationUpdates() 注册双 Provider
    │
    ▼
onLocationChanged(location) 回调
    ├── updateLocationString(location) → @Volatile latestLocationStr 更新
    │
    │  (30 秒周期)
    ▼
Rust location.rs::read_current()
    │
    ▼
IPC: plugin:vcp-mobile|get_sensor_data({ type: "location" })
    │
    ▼
@Volatile latestLocationStr → JSObject { value: "坐标: ..." }
    │
    ▼
Rust client.rs → 主服务器 update_static_placeholders → LLM Context
```

---

## 15. CpuStatusManager —— CPU 热状态分级

> 源码：`src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/CpuStatusManager.kt`（38 行）

### 15.1 概述

`CpuStatusManager` 是 v1.0.3 新增的独立模块，通过 Android `PowerManager.currentThermalStatus` API 获取设备当前 CPU 热状态，并以中文描述返回 7 级热分级。该模块零依赖、无状态，每次调用直接查询系统 API。

### 15.2 Rust 接口

```rust
#[tauri::command]
pub fn get_cpu_thermal_status<R: Runtime>(app: AppHandle<R>) -> Result<String, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|get_cpu_thermal_status` |
| 参数 | 无 |
| 返回值 | `String` —— 中文热状态描述 |
| 桌面端行为 | 始终返回 `"正常"` |

### 15.3 7 级热分级映射

```kotlin
class CpuStatusManager(private val context: Context) {
    fun getThermalStatus(): JSObject {
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
            val powerManager = context.getSystemService(Context.POWER_SERVICE) as PowerManager
            val status = powerManager.currentThermalStatus
            val statusStr = when (status) {
                PowerManager.THERMAL_STATUS_NONE      -> "正常"
                PowerManager.THERMAL_STATUS_LIGHT     -> "轻微发热"
                PowerManager.THERMAL_STATUS_MODERATE  -> "中等发热"
                PowerManager.THERMAL_STATUS_SEVERE    -> "严重发热"
                PowerManager.THERMAL_STATUS_CRITICAL  -> "极热(限频)"
                PowerManager.THERMAL_STATUS_EMERGENCY -> "紧急(限流)"
                PowerManager.THERMAL_STATUS_SHUTDOWN  -> "即将关机"
                else -> "未知"
            }
            result.put("status", statusStr)
        } else {
            result.put("status", "不支持(API<29)")
        }
    }
}
```

| 热状态常量 | 中文描述 | 系统行为 |
|-----------|----------|----------|
| `THERMAL_STATUS_NONE` | 正常 | 无限制 |
| `THERMAL_STATUS_LIGHT` | 轻微发热 | 轻度限制，用户不可感知 |
| `THERMAL_STATUS_MODERATE` | 中等发热 | 开始降低 CPU/GPU 频率 |
| `THERMAL_STATUS_SEVERE` | 严重发热 | 大幅降频，屏幕亮度可能降低 |
| `THERMAL_STATUS_CRITICAL` | 极热(限频) | CPU 被强制限频，UI 卡顿 |
| `THERMAL_STATUS_EMERGENCY` | 紧急(限流) | 严重限制，后台任务被暂停 |
| `THERMAL_STATUS_SHUTDOWN` | 即将关机 | 系统即将因过热关机 |

> **API 版本要求**：`currentThermalStatus` 需要 API 29 (Android 10 Q) 及以上。低于此版本返回 `"不支持(API<29)"`。

### 15.4 数据流

```
前端: invoke('plugin:vcp-mobile|get_cpu_thermal_status')
    │
    ▼
system.rs :: get_cpu_thermal_status(app)
    │
    ▼
run_mobile_plugin("getCpuThermalStatus", {})
    │
    ▼
Kotlin: VcpMobilePlugin.getCpuThermalStatus(invoke)
    │
    ▼
CpuStatusManager.getThermalStatus() → JSObject { status: "正常" }
    │
    ▼
Rust 解包 ThermalResponse { status } → Result<String, String>
```

---

## 16. GpuStatusManager —— GPU 硬件信息拉取

> 源码：`src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/GpuStatusManager.kt`（84 行）

### 16.1 概述

`GpuStatusManager` 是 v1.0.3 新增的独立模块，通过**动态创建临时 EGL 上下文**读取 GPU 渲染器型号（例如 "Adreno (TM) 740"）。GPU 型号信息仅需查询一次，使用双重检查锁定（Double-Checked Locking）+ `@Volatile` 缓存避免重复的 EGL 初始化开销。

### 16.2 Rust 接口

```rust
#[derive(Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct GpuStatus {
    pub renderer: String,
    pub restricted: bool,
}

#[tauri::command]
pub fn get_gpu_status<R: Runtime>(app: AppHandle<R>) -> Result<GpuStatus, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|get_gpu_status` |
| 参数 | 无 |
| 返回值 | `GpuStatus { renderer, restricted }` —— `restricted` 固定为 `true`，表示 GPU 信息受限（无 sysfs 功耗/温度数据） |
| 桌面端行为 | 返回 `{ renderer: "PC Mock GPU", restricted: true }` |

### 16.3 EGL 临时上下文技术

GpuStatusManager 采用轻量级 EGL Pbuffer Surface 方案读取 GPU 型号，避免需要 GLSurfaceView 或 Activity：

```
fetchGpuRendererFromEgl()
    │
    ├── EGLContext.getEGL() as EGL10
    ├── eglGetDisplay(EGL_DEFAULT_DISPLAY)
    ├── eglInitialize(dpy, vers)
    │
    ├── eglChooseConfig(dpy, configAttr, configs, 1, numConfig)
    │   └── configAttr: [RED_SIZE=8, GREEN_SIZE=8, BLUE_SIZE=8]
    │
    ├── eglCreatePbufferSurface(dpy, config, [WIDTH=1, HEIGHT=1])
    │   └── 1x1 像素 Pbuffer Surface，最小化 GPU 资源占用
    │
    ├── eglCreateContext(dpy, config, EGL_NO_CONTEXT, [EGL_CONTEXT_CLIENT_VERSION=2])
    │   └── GLES 2.0 上下文
    │
    ├── eglMakeCurrent(dpy, surf, surf, ctx)
    │
    ├── GLES20.glGetString(GLES20.GL_RENDERER) → "Adreno (TM) 740"
    │
    └── 清理（严格按顺序）
        ├── eglMakeCurrent(dpy, EGL_NO_SURFACE, EGL_NO_SURFACE, EGL_NO_CONTEXT)
        ├── eglDestroyContext(dpy, ctx)
        ├── eglDestroySurface(dpy, surf)
        └── eglTerminate(dpy)
```

**为什么 `restricted` 固定为 `true`？**
在大多数 Android 设备上，`/sys/class/kgsl/` 或 `/sys/class/devfreq/` 等 GPU 性能计数器路径需要 Root 权限读取。`GpuStatusManager` 仅通过 EGL 读取渲染器名称字符串，无法获取 GPU 使用率、频率、温度等运行时指标。`restricted: true` 向前端明确告知此限制。

### 16.4 双重检查锁定缓存

```kotlin
@Volatile
private var cachedGpuRenderer: String? = null

fun getGpuRenderer(): String {
    cachedGpuRenderer?.let { return it }     // 第一次检查（无锁，快速路径）
    synchronized(this) {
        cachedGpuRenderer?.let { return it } // 第二次检查（持锁，防止竞争）
        val renderer = fetchGpuRendererFromEgl()
        cachedGpuRenderer = renderer
        return renderer
    }
}
```

| 场景 | 行为 |
|------|------|
| 首次调用 | `cachedGpuRenderer == null` → 进入 `synchronized` → 创建 EGL → 读取 GL_RENDERER → 写入缓存 |
| 并发调用（首次） | 第二个线程在 `synchronized` 外的 `?.let` 返回 `null` → 等待锁 → 持锁后第二次检查 → 发现已缓存 → 直接返回 |
| 后续调用 | `@Volatile` 保证可见性 → 无锁快速路径返回缓存值 → 零 EGL 开销 |

---

## 17. NetworkStatusManager —— 网络连接与带宽状态

> 源码：`src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/NetworkStatusManager.kt`（86 行）

### 17.1 概述

`NetworkStatusManager` 是 v1.0.3 新增的独立模块，通过 Android `ConnectivityManager` API 获取当前活跃网络连接的类型、链路带宽估计值和本地 IPv4 地址。该模块零状态，每次调用即时查询系统 API。

### 17.2 Rust 接口

```rust
#[derive(Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct NetworkStatus {
    pub connected: bool,
    pub r#type: String,
    pub down_speed_kbps: i32,
    pub up_speed_kbps: i32,
    pub ip: String,
}

#[tauri::command]
pub fn get_network_status<R: Runtime>(app: AppHandle<R>) -> Result<NetworkStatus, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|get_network_status` |
| 参数 | 无 |
| 返回值 | `NetworkStatus` JSON 对象（camelCase 序列化） |
| 桌面端行为 | 返回 `{ connected: true, type: "以太网", downSpeedKbps: 100000, upSpeedKbps: 100000, ip: "127.0.0.1" }` |

### 17.3 网络类型与带宽检测

```kotlin
fun getNetworkStatus(): JSObject {
    val cm = context.getSystemService(Context.CONNECTIVITY_SERVICE) as ConnectivityManager
    val activeNetwork = cm.activeNetwork
    if (activeNetwork == null) {
        // 无活跃网络
        return { connected: false, type: "未连接", downSpeedKbps: 0, upSpeedKbps: 0, ip: "未分配" }
    }

    val capabilities = cm.getNetworkCapabilities(activeNetwork) ?: return /* 同上 */

    val type = when {
        capabilities.hasTransport(NetworkCapabilities.TRANSPORT_WIFI)     -> "WiFi"
        capabilities.hasTransport(NetworkCapabilities.TRANSPORT_CELLULAR) -> "移动数据"
        capabilities.hasTransport(NetworkCapabilities.TRANSPORT_ETHERNET) -> "以太网"
        else -> "未知"
    }

    val downSpeed = capabilities.linkDownstreamBandwidthKbps  // 下行估计带宽 (Kbps)
    val upSpeed   = capabilities.linkUpstreamBandwidthKbps    // 上行估计带宽 (Kbps)
    val ip        = getLocalIpAddress()                       // 本地 IPv4 地址
}
```

| 字段 | 来源 | 说明 |
|------|------|------|
| `connected` | `ConnectivityManager.activeNetwork` | `null` 表示无网络连接 |
| `type` | `NetworkCapabilities.hasTransport()` | 优先级：WiFi > 移动数据 > 以太网 |
| `downSpeedKbps` | `linkDownstreamBandwidthKbps` | 链路层估计值，非实时测速 |
| `upSpeedKbps` | `linkUpstreamBandwidthKbps` | 链路层估计值，非实时测速 |
| `ip` | `NetworkInterface.getNetworkInterfaces()` | 非回环 `Inet4Address` 的第一个地址 |

### 17.4 本地 IP 获取

```kotlin
private fun getLocalIpAddress(): String? {
    val en = NetworkInterface.getNetworkInterfaces()
    while (en.hasMoreElements()) {
        val intf = en.nextElement()
        val enumIpAddr = intf.inetAddresses
        while (enumIpAddr.hasMoreElements()) {
            val inetAddress = enumIpAddr.nextElement()
            if (!inetAddress.isLoopbackAddress && inetAddress is Inet4Address) {
                return inetAddress.hostAddress
            }
        }
    }
    return null
}
```

- 遍历所有网络接口，返回**第一个非回环 IPv4 地址**。
- 如无可用 IPv4 地址，`ip` 字段设为 `"未分配"`。

### 17.5 桌面端与异常 fallback

| 场景 | connected | type | downSpeedKbps | upSpeedKbps | ip |
|------|-----------|------|---------------|-------------|-----|
| 正常连接（WiFi） | true | "WiFi" | 链路估计值 | 链路估计值 | 实际 IPv4 |
| 正常连接（移动数据） | true | "移动数据" | 链路估计值 | 链路估计值 | 实际 IPv4 |
| 无活跃网络 | false | "未连接" | 0 | 0 | "未分配" |
| capabilities 为 null | false | "未连接" | 0 | 0 | "未分配" |
| 异常 | false | "未连接" | 0 | 0 | "未分配" |
| 桌面端 | true | "以太网" | 100000 | 100000 | "127.0.0.1" |

---

## 18. Root 访问体系

> 源码：`VcpMobilePlugin.kt`（`checkRootAccess` / `runRootCommand` / `launchRootManager`）、`system.rs`

### 18.1 概述

v1.0.3 新增 Root 访问体系，基于 [libsu](https://github.com/topjohnwu/libsu)（`com.topjohnwu.superuser`）库提供三项能力：Root 状态检测、Root 命令执行、Root 管理器应用启动。所有操作在 `fileIoExecutor` 单线程池上执行，避免阻塞主线程。

### 18.2 check_root_access —— Root 状态检测

```rust
#[derive(Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct RootAccessStatus {
    pub is_root: bool,
}

#[tauri::command]
pub fn check_root_access<R: Runtime>(app: AppHandle<R>) -> Result<RootAccessStatus, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|check_root_access` |
| 参数 | 无 |
| 返回值 | `RootAccessStatus { is_root: bool }` |
| 桌面端行为 | 返回 `{ isRoot: false }` |

**Kotlin 实现**：

```kotlin
@Command
fun checkRootAccess(invoke: Invoke) {
    fileIoExecutor.execute {
        try {
            val isRoot = Shell.getShell().isRoot
            val result = JSObject().apply { put("isRoot", isRoot) }
            invoke.resolve(result)
        } catch (e: Exception) {
            invoke.resolve(JSObject().apply { put("isRoot", false) })
        }
    }
}
```

- 使用 `Shell.getShell().isRoot`（libsu API）检测设备是否已获取 Root 权限。
- 异常时安全降级为 `{ isRoot: false }`。

### 18.3 run_root_command —— 以 Root 身份执行 Shell 命令

```rust
#[derive(Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct RootCommandResult {
    pub success: bool,
    pub output: String,
}

#[tauri::command]
pub fn run_root_command<R: Runtime>(
    app: AppHandle<R>,
    command: String,
) -> Result<RootCommandResult, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|run_root_command` |
| 参数 | `command: String` —— 要执行的 Shell 命令 |
| 返回值 | `RootCommandResult { success, output }` —— `output` 为命令的标准输出（多行用 `\n` 拼接） |
| 桌面端行为 | 返回 `{ success: false, output: "非Android物理端无法运行Root指令" }` |

**Kotlin 实现**：

```kotlin
@Command
fun runRootCommand(invoke: Invoke) {
    val args = invoke.parseArgs(RunRootCommandArgs::class.java)
    fileIoExecutor.execute {
        try {
            val output = Shell.cmd(args.command).exec().out
            val result = JSObject().apply {
                put("success", true)
                put("output", output.joinToString("\n"))
            }
            invoke.resolve(result)
        } catch (e: Exception) {
            val result = JSObject().apply {
                put("success", false)
                put("output", e.message ?: "Unknown Shell execution error")
            }
            invoke.resolve(result)
        }
    }
}
```

- `Shell.cmd(command).exec().out`：通过 libsu 以 Root 身份执行命令，返回标准输出行列表。
- 在 `fileIoExecutor` 上运行，避免阻塞 UI 线程。
- 参数解析失败直接 `invoke.reject()`，不上报执行级错误。

### 18.4 launch_root_manager —— 启动 Root 管理器应用

```rust
#[derive(Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct LaunchRootManagerResult {
    pub success: bool,
    pub manager: Option<String>,
    pub message: Option<String>,
}

#[tauri::command]
pub fn launch_root_manager<R: Runtime>(
    app: AppHandle<R>,
) -> Result<LaunchRootManagerResult, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|launch_root_manager` |
| 参数 | 无 |
| 返回值 | `LaunchRootManagerResult { success, manager, message }` |
| 桌面端行为 | 返回 `{ success: false, manager: null, message: "该接口仅在 Android 物理端可用" }` |

**Kotlin 实现**：

```kotlin
@Command
fun launchRootManager(invoke: Invoke) {
    val managers = listOf(
        "com.topjohnwu.magisk" to "Magisk",
        "me.weishu.kernelsu" to "KernelSU",
        "me.tool.apatch" to "APatch"
    )
    for ((pkg, name) in managers) {
        try {
            val intent = activity.packageManager.getLaunchIntentForPackage(pkg)
            if (intent != null) {
                intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
                activity.startActivity(intent)
                invoke.resolve(JSObject().apply {
                    put("success", true)
                    put("manager", name)
                })
                return
            }
        } catch (e: Exception) { /* 尝试下一个 */ }
    }
    // 未找到任何已安装的 Root 管理器
    invoke.resolve(JSObject().apply {
        put("success", false)
        put("message", "未找到支持的 Root 管理器 (Magisk, KernelSU, APatch)。")
    })
}
```

**支持的 Root 管理器**：

| 包名 | 显示名称 | 说明 |
|------|---------|------|
| `com.topjohnwu.magisk` | Magisk | 最主流的 Android Root 方案 |
| `me.weishu.kernelsu` | KernelSU | 基于内核的 Root 方案 |
| `me.tool.apatch` | APatch | 新一代内核级 Root 方案 |

- 按顺序遍历三个包名，使用 `packageManager.getLaunchIntentForPackage()` 查找启动 Intent。
- 找到第一个已安装的管理器即启动并立即返回。
- 全部未找到返回 `success: false` + 错误消息。

### 18.5 AndroidManifest.xml 包可见性声明

Android 11 (API 30) 引入了包可见性限制，默认情况下应用无法查询其他已安装应用。为支持 `launch_root_manager` 中通过 `getLaunchIntentForPackage` 查询 Root 管理器，需在 `AndroidManifest.xml` 中声明 `<queries>`：

```xml
<queries>
    <package android:name="com.topjohnwu.magisk" />
    <package android:name="me.weishu.kernelsu" />
    <package android:name="me.tool.apatch" />
</queries>
```

> **不声明 `<queries>` 的后果**：`getLaunchIntentForPackage()` 始终返回 `null`，`launch_root_manager` 会错误报告"未找到支持的 Root 管理器"，即使设备上已安装了 Magisk/KernelSU/APatch。

### 18.6 Root 访问数据流总览

```
前端调用
    │
    ├── invoke('plugin:vcp-mobile|check_root_access')
    │   → system.rs :: check_root_access(app)
    │   → run_mobile_plugin("checkRootAccess", {})
    │   → Kotlin: checkRootAccess(invoke) on fileIoExecutor
    │   → Shell.getShell().isRoot
    │   → JSObject { isRoot: bool }
    │
    ├── invoke('plugin:vcp-mobile|run_root_command', { command: "..." })
    │   → system.rs :: run_root_command(app, command)
    │   → run_mobile_plugin("runRootCommand", { "command": "..." })
    │   → Kotlin: runRootCommand(invoke) on fileIoExecutor
    │   → Shell.cmd(command).exec().out
    │   → JSObject { success, output }
    │
    └── invoke('plugin:vcp-mobile|launch_root_manager')
        → system.rs :: launch_root_manager(app)
        → run_mobile_plugin("launchRootManager", {})
        → Kotlin: launchRootManager(invoke)
        → packageManager.getLaunchIntentForPackage(pkg)
        → JSObject { success, manager, message }
```

---

## 19. Helper 进程服务与通知点击兜底（v1.1.3 新增）

> 源码：`src-tauri/plugins/vcp-mobile/src/stream.rs`、`VcpMobilePlugin.kt`、`service/SseProxyService.kt`

### 19.1 `start_helper_service` —— 启动 `:helper` 进程 SSE 代理

```rust
#[tauri::command]
pub fn start_helper_service<R: Runtime>(app: AppHandle<R>) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|start_helper_service` |
| 参数 | 无 |
| 桌面端行为 | 空操作 |

`VcpMobilePlugin.init` 中会自动调用 `startHelperServiceInternal()` 启动一次；前端亦可在冷启动后显式调用以兜底。该命令通过 `startForegroundService` 拉起 `SseProxyService`，服务运行在 `:helper` 进程，负责 SSE 代理、断线缓存与后台通知。

### 19.2 `get_pending_notification` —— 读取通知点击带来的会话信息

```rust
#[tauri::command]
pub fn get_pending_notification<R: Runtime>(app: AppHandle<R>) -> Result<serde_json::Value, String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|get_pending_notification` |
| 参数 | 无 |
| 返回值 | `{ topicId, ownerId, requestId }` 或空对象 `{}` |
| 桌面端行为 | 始终返回 `{}` |

当应用在后台时，`SseProxyService.showStreamNotification` 会发送完成/中断提醒；用户点击通知后，`VcpMobilePlugin.handleNotificationIntent` 提取 `topicId`/`ownerId`/`requestId`，并通过 `window.dispatchEvent(new CustomEvent('vcp-notification-click', ...))` 立即注入当前 WebView，同时缓存到 `pendingNotificationData`。若应用已被划掉或 WebView 尚未就绪，前端在冷启动后调用 `get_pending_notification` 可读取缓存值。

---

## 20. WakeLock/WifiLock 保活机制（v1.1.2: 委托 ForegroundGuardian）

> 源码：`VcpMobilePlugin.kt`, `system.rs`, `ForegroundGuardian.kt`

### 20.1 概述

v1.0.3 引入的 WakeLock + WifiLock 双锁保活机制，在 v1.1.2 中已**委托给 `ForegroundGuardian`** 统一管理。`acquire_wake_lock` / `release_wake_lock` 命令现在通过 `ForegroundGuardian.acquire(tag="manual_keepalive", priority=10)` / `release(tag="manual_keepalive")` 实现，不再直接操作 `PowerManager` / `WifiManager` API。完整的锁管理机制请参见 [11_ForegroundGuardian 前台守护者](11_ForegroundGuardian_前台守护者.md)。

#### 分布式节点的三阶段调用

在 `distributed/client.rs` 中，WakeLock/WifiLock 由 helper 函数成对自动调用，无需前端介入：

| 阶段 | 位置 (client.rs) | 锁持有时长 | 目的 |
|------|------------------|-----------|------|
| WebSocket 连接 | `connection_loop` connect 前后 | 毫秒级 | 防止 TCP 握手期间 CPU 休眠 |
| 工具执行 | `tokio::select!` 内 `execute_tool` 前后 | 取决于工具（硬上限 5 分钟） | 保证工具执行期间 CPU+WiFi 不中断 |
| 占位推送 | `tokio::select!` 内 `push_static_placeholders` 前后 | 毫秒级 | 30 秒定时推送不被 Doze 模式阻断 |

#### 与前台保活服务的协同

`StreamKeepaliveService` 与 WakeLock/WifiLock 构成**双重保活**体系：

| 层级 | 触发 | 停止 |
|------|------|------|
| Service 层 | `DistributedClient::start()` → `set_keepalive_mode(true)` | `DistributedClient::stop()` → `set_keepalive_mode(false)` |
| 锁层 | 上述三阶段操作开始前 | 对应操作完成后立即释放 |

锁仅在关键操作期间**短暂持有**，操作完成后立即释放。Service 提供通知栏可见性与进程优先级。两者协同确保分布式长连接在 Android Doze/App Standby 模式下可靠运行。

### 20.2 acquire_wake_lock —— 获取双锁

```rust
#[tauri::command]
pub fn acquire_wake_lock<R: Runtime>(app: AppHandle<R>) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|acquire_wake_lock` |
| 参数 | 无 |
| 桌面端行为 | 空操作 |
| 安全性 | 由 `ForegroundGuardian` 统一超时兜底（`manual_keepalive` 默认 2 小时） |

**Kotlin 实现**：

```kotlin
@Command
fun acquireWakeLock(invoke: Invoke) {
    com.vcp.mobile.service.ForegroundGuardian.acquire(
        activity, "manual_keepalive",
        com.vcp.mobile.service.ForegroundGuardian.PRIORITY_DISTRIBUTED,
        "[后台保活]"
    )
    invoke.resolve()
}
```

- `acquire_wake_lock` 不再直接创建 `PowerManager.WakeLock` / `WifiManager.WifiLock`，而是作为 `ForegroundGuardian` 的一个消费者（`tag="manual_keepalive"`，`priority=10`）。
- 物理锁的获取、通知栏文案、超时自动释放全部由 `ForegroundGuardian` 处理。

### 20.3 release_wake_lock —— 释放双锁

```rust
#[tauri::command]
pub fn release_wake_lock<R: Runtime>(app: AppHandle<R>) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|release_wake_lock` |
| 参数 | 无 |
| 桌面端行为 | 空操作 |

```kotlin
@Command
fun releaseWakeLock(invoke: Invoke) {
    com.vcp.mobile.service.ForegroundGuardian.release(activity, "manual_keepalive")
    invoke.resolve()
}
```

### 20.4 生命周期清理

`VcpMobilePlugin` 不再持有独立的 `wakeLock` / `wifiLock` 字段。物理锁由 `ForegroundGuardian` 管理，并在 `StreamKeepaliveService.onDestroy()` 中通过 `ForegroundGuardian.releaseAllLocks()` 兜底释放。`VcpMobilePlugin.onDestroy()` 仅负责清理网络回调、生命周期观察者和线程池。

---

## 21. 网络监控与自动重连

> 源码：`VcpMobilePlugin.kt` (lines 524--563), `system.rs` (lines 787--804)

### 21.1 概述

v1.0.3 新增基于 Android `ConnectivityManager.NetworkCallback` 的原生网络状态监控，在网络可用/丢失/能力变化时自动触发事件推送到 WebView。相比前端 `navigator.onLine` + `online`/`offline` 事件，原生回调更可靠且能感知网络能力变化（如从 WiFi 切换到移动数据）。

**自动激活时机**：网络监控由 `lifecycle_manager.rs` 的 `bootstrap()` 在**步骤 10** 自动启动——在 `CoreStatus::Ready` 发射之后，通过 `tauri::async_runtime::spawn` 异步调用。延迟激活是为了规避冷启动时 JNI WebView 尚未完全就绪导致的死锁——此时所有 Rust 业务服务已就绪，Native 回调可安全触发分布式节点自动重连。前端无需手动调用 `start_network_monitoring`。

### 21.2 start_network_monitoring —— 启动网络监控

```rust
#[tauri::command]
pub fn start_network_monitoring<R: Runtime>(app: AppHandle<R>) -> Result<(), String>;
```

| 属性 | 说明 |
|------|------|
| 前端命令 | `plugin:vcp-mobile\|start_network_monitoring` |
| 参数 | 无 |
| 桌面端行为 | 空操作 |
| 幂等性 | `isNetworkMonitoringStarted` 标志位防止重复注册 |

**Kotlin 实现**：

```kotlin
private var networkCallback: ConnectivityManager.NetworkCallback? = null
private var lastConnected: Boolean? = null
private var isNetworkMonitoringStarted = false

@Command
fun startNetworkMonitoring(invoke: Invoke) {
    if (isNetworkMonitoringStarted) {
        invoke.resolve()
        return
    }
    val cm = activity.getSystemService(Context.CONNECTIVITY_SERVICE) as ConnectivityManager
    val request = NetworkRequest.Builder()
        .addCapability(NetworkCapabilities.NET_CAPABILITY_INTERNET)
        .build()
    networkCallback = object : ConnectivityManager.NetworkCallback() {
        override fun onAvailable(network: Network) {
            emitNetworkStatusToWebView()
        }
        override fun onLost(network: Network) {
            emitNetworkStatusToWebView()
        }
        override fun onCapabilitiesChanged(network: Network, caps: NetworkCapabilities) {
            emitNetworkStatusToWebView()
        }
    }
    cm.registerNetworkCallback(request, networkCallback!!)
    isNetworkMonitoringStarted = true
    invoke.resolve()
}
```

### 21.3 回调触发条件

| 回调方法 | 触发时机 | 典型场景 |
|----------|----------|----------|
| `onAvailable` | 满足 `NET_CAPABILITY_INTERNET` 的网络变为可用 | WiFi 连接成功、移动数据开启 |
| `onLost` | 之前可用的网络丢失 | WiFi 断开、飞行模式 |
| `onCapabilitiesChanged` | 网络能力发生变化 | WiFi → 移动数据切换、带宽限制变化 |

### 21.4 变化检测与事件推送

`emitNetworkStatusToWebView()` 在推送事件前进行连接状态变化检测，避免重复通知：

```kotlin
private fun emitNetworkStatusToWebView() {
    val status = networkStatusManager.getNetworkStatus()
    val connected = status.optBoolean("connected", false)
    if (connected != lastConnected) {
        lastConnected = connected
        trigger("vcp-network-status-changed", status)
    }
}
```

| 变量 | 类型 | 说明 |
|------|------|------|
| `lastConnected` | `Boolean?` | 上次已知的连接状态。初始 `null`，首次回调必定触发事件 |
| `vcp-network-status-changed` | Tauri Plugin `trigger` 事件 | 携带完整 `NetworkStatus` JSON 对象 |

### 21.5 事件监听（前端）

```typescript
// 前端通过 Tauri event system 监听网络状态变化
import { listen } from '@tauri-apps/api/event';

listen('vcp-network-status-changed', (event) => {
    const status = event.payload as NetworkStatus;
    if (status.connected) {
        console.log(`网络已连接: ${status.type}, IP: ${status.ip}`);
        // 触发 WebSocket 重连 / SSE 重连逻辑
    } else {
        console.log('网络已断开');
        // 暂停连接尝试，UI 显示离线状态
    }
});
```

### 21.6 生命周期清理

```kotlin
override fun onDestroy(activity: AppCompatActivity) {
    try {
        if (networkCallback != null) {
            val cm = activity.getSystemService(Context.CONNECTIVITY_SERVICE) as ConnectivityManager
            cm.unregisterNetworkCallback(networkCallback!!)
            networkCallback = null
            isNetworkMonitoringStarted = false
        }
    } catch (_: Exception) {}
    // ...
}
```

- `unregisterNetworkCallback` 必须在 Activity 销毁前调用，否则会导致内存泄漏。
- `networkCallback` 和 `isNetworkMonitoringStarted` 重置为初始状态，确保 Activity 重建后可以重新注册。

---

*最后更新：2026-07-04 | VCP Mobile v1.1.3*
