---
id: PLUGIN-STREAM-003
title: 流式前台保活服务
description: 通过 ForegroundGuardian 引用计数 + 优先级调度实现 Agent 流式输出期间的进程保活；v1.1.3 新增 :helper 进程 SSE 代理。
version: 1.1.3
date: 2026-07-03
related_files:
  - src-tauri/plugins/vcp-mobile/src/stream.rs
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/service/StreamKeepaliveService.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/service/ForegroundGuardian.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/service/SseProxyService.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/VcpMobilePlugin.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/AndroidManifest.xml
---

# 流式前台保活服务

## 1. 功能概述

当 Agent 正在流式生成回复时，通过 **ForegroundGuardian**（前台守护者单例）启动 Android **前台服务**（Foreground Service）并显示常驻通知，向系统声明"用户感知的重要任务"，显著降低进程被 OEM（ColorOS/EMUI/HarmonyOS/MIUI）杀后台的概率。

> **v1.1.2 架构重构**：引用计数已从 Rust 侧 `VcpMobileState.active_streams` 迁移至 Kotlin 侧 `ForegroundGuardian.consumers`。`StreamKeepaliveService` 不再管理 WakeLock/WifiLock，降级为纯粹的"通知壳"，仅负责 `startForeground()` 调用。锁管理与调度逻辑统一由 `ForegroundGuardian`（参见 [11_ForegroundGuardian 前台守护者](11_ForegroundGuardian_前台守护者.md)）负责。

---

## 2. 代码结构

### Rust 侧

```
src-tauri/plugins/vcp-mobile/src/stream.rs (149 lines)
├── acquire_foreground_inner(app, tag, priority, label, screen_keep_on)   // 语义化申请（核心）
├── release_foreground_inner(app, tag)                                     // 语义化释放（核心）
├── start_stream_service_inner(app, agent_name)  // 兼容旧 API → 委托 acquire
├── stop_stream_service_inner(app, agent_name)   // 兼容旧 API → 委托 release
├── set_keepalive_mode_inner(app, is_keepalive)  // 分布式保活
├── acquire_foreground(app, ...)                  #[tauri::command]
├── release_foreground(app, ...)                  #[tauri::command]
├── start_streaming_service(app, agent_name)      #[tauri::command]
├── stop_streaming_service(app, agent_name)       #[tauri::command]
└── set_keepalive_mode(app, is_keepalive)         #[tauri::command]
```

### Kotlin 侧

```
ForegroundGuardian.kt (257 lines)                              ← v1.1.2 新增
├── consumers: ConcurrentHashMap<String, ConsumerEntry>       ← 引用计数注册表
├── acquire(context, tag, priority, label, screenKeepOn, timeoutMs)
├── release(context, tag)
└── releaseAllLocks()

StreamKeepaliveService.kt (~143 lines)                        ← v1.1.2 简化
├── onCreate()
├── onStartCommand(intent, flags, startId) -> Int             ← 返回 START_NOT_STICKY
├── createNotificationChannel()
├── buildNotification(label: String) -> Notification          ← 现在接收 label 而非 agentName
└── onDestroy() → ForegroundGuardian.releaseAllLocks()

SseProxyService.kt (~934 lines)                               ← v1.1.3 新增
├── onCreate() → startForeground() + startTcpServer()
├── onStartCommand() -> START_STICKY
├── handleClientSocket(socket) / readLengthPrefixed()
├── handleStartStream / handleResumeStream / handleQueryStream
├── updateLocks() → 独立的 WakeLock/WifiLock（:helper 进程内）
└── dumpSessionToFile() / showStreamNotification()
```

### 架构对比

```
v1.1.1 (旧):
  Rust active_streams (引用计数) → run_mobile_plugin → Kotlin 自行管理锁 + FGS

v1.1.2 (新):
  Rust acquire/release_foreground_inner → run_mobile_plugin → ForegroundGuardian (引用计数 + 双锁)
                                                                    │
                                                          StreamKeepaliveService (仅 FGS 通知壳)

v1.1.3 (新增):
  Rust start_helper_service → VcpMobilePlugin.startHelperService → SseProxyService (:helper 进程)
                                                                    │
                                                          本地 TCP 长度前缀帧协议 + SSE 代理 + 断线内存缓存
```

---

## 3. 引用计数机制（v1.1.2: ForegroundGuardian 管理）

### 3.1 数据结构

引用计数已从 Rust 侧迁移至 Kotlin 侧 `ForegroundGuardian`：

```kotlin
// ForegroundGuardian.consumers: ConcurrentHashMap<String, ConsumerEntry>
data class ConsumerEntry(
    val priority: Int,        // 优先级（数值越大越高）
    val displayLabel: String, // 通知栏中文案
    val screenKeepOn: Boolean // 是否需要屏幕常亮
)
```

### 3.2 优先级驱动的通知文案

旧版通过 Agent 名字列表格式化（"A、B、C..."）决定通知内容。新版通过 `ForegroundGuardian.getNotificationLabel()` 返回**优先级最高者**的 `displayLabel`：

| 消费者 Tag | 优先级 | displayLabel | 触发条件 |
|-----------|:------:|-------------|----------|
| `"sync"` | 40 | `[数据同步]` | Agent 名含 `[数据同步]` |
| `"prerender"` | 30 | `[预渲染重建]` | Agent 名含 `[预渲染重建]` |
| `"stream:{name}"` | 20 | 用户可见 Agent 名 | 普通 Agent 对话 |
| `"distributed"` | 10 | `distributed`（`StreamKeepaliveService.buildNotification` 将其映射为"分布式后台连接维系中..."） | 分布式节点保活 |

> **关键行为变化**：多 Agent 同时流式输出时，旧版显示"Agent-A、Agent-B、Agent-C..."；新版显示优先级最高者的 displayLabel（若有同步在跑，则始终显示同步状态文案）。

### 3.3 计数规则（语义化幂等）

```rust
// stream.rs: start_stream_service_inner()
let tag = format!("stream:{}", agent_name);
let priority = if agent_name.contains("[数据同步]") {
    40  // PRIORITY_SYNC
} else if agent_name.contains("[预渲染重建]") {
    30  // PRIORITY_PRERENDER
} else {
    20  // PRIORITY_STREAM
};
let screen_keep_on = priority >= 30;
acquire_foreground_inner(app, &tag, priority, agent_name, screen_keep_on)
```

- **同一 Agent 重复调用**：`ForegroundGuardian.acquire()` 对同一 tag 是幂等的（覆盖更新 ConsumerEntry），不累积计数——这与旧版 `<String, u32>` 递增模型不同。
- **不同 Agent**：各自持有独立的 tag，互不干扰。
- **全部停止**：当 `stop_streaming_service` 释放最后一个 tag，`ForegroundGuardian` 检测 `consumers.isEmpty()` → 释放物理锁 + 停止 FGS。

---

## 4. Rust → Kotlin 调用（v1.1.2: 语义化接口）

### 4.1 acquire_foreground_inner —— 申请前台锁

```rust
pub fn acquire_foreground_inner<R: Runtime>(
    _app: &AppHandle<R>,
    tag: &str,           // 唯一业务标识（如 "stream:Agent-A"）
    priority: i32,       // 优先级（40/30/20/10）
    label: &str,         // 通知栏文案
    screen_keep_on: bool,// 是否保持屏幕常亮
) -> Result<(), String> {
    #[cfg(target_os = "android")]
    {
        let state = _app.state::<VcpMobileState<R>>();
        let handle = state.plugin_handle.lock().map_err(|e| e.to_string())?;
        let plugin_handle = handle.as_ref().ok_or("Plugin handle not initialized")?;
        plugin_handle
            .run_mobile_plugin::<serde_json::Value>(
                "acquireForeground",
                serde_json::json!({
                    "tag": tag,
                    "priority": priority,
                    "label": label,
                    "screenKeepOn": screen_keep_on
                }),
            )
            .map_err(|e| format!("run_mobile_plugin failed: {}", e))?;
    }
    Ok(())
}
```

### 4.2 release_foreground_inner —— 释放前台锁

```rust
pub fn release_foreground_inner<R: Runtime>(
    _app: &AppHandle<R>,
    tag: &str,
) -> Result<(), String> {
    #[cfg(target_os = "android")]
    {
        // ... 通过 run_mobile_plugin("releaseForeground", { "tag": tag }) 调用 Kotlin
    }
    Ok(())
}
```

### 4.3 兼容旧 API 的包装函数

```rust
// start_stream_service_inner → 委托 acquire_foreground_inner
pub fn start_stream_service_inner<R: Runtime>(
    app: &AppHandle<R>,
    agent_name: &str,
) -> Result<(), String> {
    let tag = format!("stream:{}", agent_name);
    let priority = if agent_name.contains("[数据同步]") {
        40
    } else if agent_name.contains("[预渲染重建]") {
        30
    } else {
        20
    };
    let screen_keep_on = agent_name.contains("[数据同步]")
        || agent_name.contains("[预渲染重建]");
    acquire_foreground_inner(app, &tag, priority, agent_name, screen_keep_on)
}

// stop_stream_service_inner → 委托 release_foreground_inner
pub fn stop_stream_service_inner<R: Runtime>(
    app: &AppHandle<R>,
    agent_name: &str,
) -> Result<(), String> {
    let tag = format!("stream:{}", agent_name);
    release_foreground_inner(app, &tag)
}
```

| 命令 | Kotlin 侧 | Payload |
|------|-----------|---------|
| `"acquireForeground"` | `VcpMobilePlugin.acquireForeground(invoke)` | `{"tag", "priority", "label", "screenKeepOn"}` |
| `"releaseForeground"` | `VcpMobilePlugin.releaseForeground(invoke)` | `{"tag"}` |

---

## 5. Kotlin 侧实现

### 5.1 `VcpMobilePlugin.kt` 中的命令路由

**acquireForeground（新命令 — v1.1.2）**：

```kotlin
@Command
fun acquireForeground(invoke: Invoke) {
    val args = invoke.parseArgs(AcquireForegroundArgs::class.java)
    ForegroundGuardian.acquire(
        activity,
        args.tag,
        args.priority,
        args.label,
        args.screenKeepOn
    )
    invoke.resolve()
}

data class AcquireForegroundArgs(
    val tag: String,
    val priority: Int,
    val label: String,
    val screenKeepOn: Boolean = false
)
```

**startStreamingService（重构 — v1.1.2 / v1.1.3 参数修正）**：

```kotlin
@Command
fun startStreamingService(invoke: Invoke) {
    val args = invoke.parseArgs(StartStreamArgs::class.java)
    val hasKeepaliveParam = args.isKeepaliveMode != null
    val isKeepalive = args.isKeepaliveMode ?: false

    if (args.agentName.isEmpty()) {
        if (hasKeepaliveParam) {
            if (!isKeepalive) {
                ForegroundGuardian.release(activity, "distributed")
            } else {
                ForegroundGuardian.acquire(
                    activity, "distributed",
                    ForegroundGuardian.PRIORITY_DISTRIBUTED, "distributed"
                )
            }
        } else {
            // 老版 Rust 停止信号：释放所有流式相关的默认锁
            ForegroundGuardian.release(activity, "sync")
            ForegroundGuardian.release(activity, "prerender")
            ForegroundGuardian.release(activity, "stream_default")
        }
        invoke.resolve()
        return
    }

    if (args.agentName.contains("[数据同步]")) {
        ForegroundGuardian.acquire(
            activity, "sync",
            ForegroundGuardian.PRIORITY_SYNC,
            args.agentName, true
        )
        activity.runOnUiThread {
            activity.window.addFlags(android.view.WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)
        }
    } else if (args.agentName.contains("[预渲染重建]")) {
        ForegroundGuardian.acquire(
            activity, "prerender",
            ForegroundGuardian.PRIORITY_PRERENDER,
            args.agentName, true
        )
        activity.runOnUiThread {
            activity.window.addFlags(android.view.WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)
        }
    } else {
        ForegroundGuardian.acquire(
            activity, "stream:${args.agentName}",
            ForegroundGuardian.PRIORITY_STREAM,
            args.agentName, false
        )
    }

    invoke.resolve()
}
```

> **对比旧版**：旧版直接构建 Intent → `startForegroundService()`。新版全部经由 `ForegroundGuardian` 统一调度，不再直接操作 Service Intent。
> **v1.1.3 修正**：`StartStreamArgs` 的保活标志字段为 `isKeepaliveMode`（可空 `Boolean?`），不再是旧文档中的 `isKeepalive`。

**stopStreamingService（v1.1.3 变为全局清理）**：

```kotlin
@Command
fun stopStreamingService(invoke: Invoke) {
    ForegroundGuardian.releaseAllLocks()
    activity.runOnUiThread {
        activity.window.clearFlags(android.view.WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)
    }
    invoke.resolve()
}
```

### 5.2 `StreamKeepaliveService`（v1.1.2 简化）

#### 通知构建

```kotlin
private fun buildNotification(label: String): Notification {
    val contentText = when {
        label.contains("[数据同步]") -> "正在与云端服务器进行高精度同步..."
        label.contains("[预渲染重建]") -> "正在优化与加速本地响应缓存..."
        label.contains("分布式") -> "分布式后台连接维系中..."
        label.isNotEmpty() -> "思考中……"
        else -> "已连接"
    }
    val cleanTitle = label.replace("[数据同步]", "").replace("[预渲染重建]", "").trim()
    val title = if (cleanTitle.isEmpty() || cleanTitle == "distributed") "VCP Mobile" else cleanTitle

    return NotificationCompat.Builder(this, CHANNEL_ID)
        .setContentTitle(title)
        .setContentText(contentText)
        .setSmallIcon(applicationInfo.icon)
        .setOngoing(true)
        .setOnlyAlertOnce(true)
        .setContentIntent(openPendingIntent)
        .build()
}
```

> **关键变化**：`buildNotification` 现在接收 `label` 参数（来自 `ForegroundGuardian.getNotificationLabel()`），而非旧版的 `agentName`。文案主要覆盖同步、预渲染、分布式与普通 Agent 四种场景。

#### onStartCommand

```kotlin
override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
    val label = ForegroundGuardian.getNotificationLabel()
    val notification = buildNotification(label)
    // ... startForeground(NOTIFICATION_ID, notification)
    isServiceRunning = true
    return START_NOT_STICKY
}
```

> **v1.1.3 调整**：`StreamKeepaliveService` 从 `START_STICKY` 改为 `START_NOT_STICKY`。`ForegroundGuardian` 已在 `onDestroy` 中做锁清理，且用户主动划掉 Task 后无需自动重启该通知壳。

#### onDestroy 兜底

```kotlin
override fun onDestroy() {
    ForegroundGuardian.releaseAllLocks()  // v1.1.2 新增：紧急自毁防泄漏
    isServiceRunning = false
    super.onDestroy()
}
```

### 5.3 `stopWithTask` 行为变更

`AndroidManifest.xml` 中 `StreamKeepaliveService` 的 `android:stopWithTask` 从 **`false` → `true`**：

| 版本 | 值 | 行为 |
|------|-----|------|
| v1.1.1 | `false` | 用户划掉应用后服务继续存活 |
| v1.1.2 | `true` | 用户划掉应用后服务随 Task 停止 |

**变更理由**：ForegroundGuardian 已接管全部锁管理。进程被划掉后锁即消亡，残留的前台服务无实际保活作用，反而显示僵尸通知。

---

### 5.4 Android 14+ 前台服务类型与 `startForeground` 兜底

（无变化 — 仍使用 `FOREGROUND_SERVICE_TYPE_REMOTE_MESSAGING` + `try-catch` 兜底）

### 5.5 `RECEIVE_BOOT_COMPLETED` 是否需要

**不需要。** 理由未变：服务生命周期严格绑定于流式输出过程。设备重启后不存在"未完成的流式任务"，无需注册 Boot Receiver。

### 5.6 特殊厂商电池优化白名单

（参见 [06_权限与系统控制](06_权限与系统控制.md) 新增的 OEM 自启动/电源管理权限链文档，以及 [11_ForegroundGuardian 前台守护者](11_ForegroundGuardian_前台守护者.md) §1）

---

## 6. `SseProxyService` 隔离进程 SSE 代理（v1.1.3）

`SseProxyService` 是 v1.1.3 引入的**独立 `:helper` 进程**后台服务，用于替代旧版 push 相关组件，解决主进程与后台 SSE 长连接之间的 Binder/生命周期耦合问题。

### 6.1 职责与定位

| 能力 | 说明 |
|------|------|
| 进程隔离 | 运行在 `:helper` 进程，避免主进程被回收时 SSE 连接同步中断 |
| 本地 TCP 通信 | 在 `127.0.0.1` 上监听随机端口，通过 `cacheDir/sse_helper.port` 暴露端口 |
| 长度前缀帧协议 | `readLengthPrefixed` / `writeLengthPrefixed`：4 字节长度 + JSON payload，单帧上限 10MB |
| 动作支持 | `start`、`resume`、`query`、`stop` |
| 断线缓存 | 主进程断开时，已完成会话 Dump 到 `cacheDir/sse_cache/sse_recovered_<sha256>.json`；内存保留 5 分钟后清理 |
| 后台通知 | 应用处于后台且 SSE 完成/失败时，通过 `vcp_agent_alerts` 渠道发送提醒，点击可带 `topicId`/`ownerId`/`requestId` 回到应用 |

### 6.2 与 `ForegroundGuardian` 的关系

`SseProxyService` **不经过** `ForegroundGuardian`：它在自己的 `:helper` 进程中独立管理 `WakeLock`/`WifiLock`（`updateLocks()`）和**静默音频播放**（`startSilentPlayback()`），作为额外的后台存活手段。这意味着：

- 主进程的 `ForegroundGuardian` 锁与服务通知壳只覆盖 Agent 流式对话的“主进程侧”保活；
- `:helper` 进程内的 SSE 连接保活由 `SseProxyService` 自行负责，并通过 `stopWithTask="false"` 在用户划掉 Task 后继续运行。

### 6.3 关键源码位置

```kotlin
// SseProxyService.kt
private fun startTcpServer()                                    // line ~165
private fun handleClientSocket(socket: Socket)                  // line ~189
private fun readLengthPrefixed(inputStream): String?            // line ~778
private fun writeLengthPrefixed(out, json)                      // line ~769
private fun handleStartStream(...)                              // line ~258
private fun handleResumeStream(...)                             // line ~332
private fun handleQueryStream(...)                              // line ~366
private fun updateLocks()                                       // line ~548
private fun acquireLocks() / releaseLocks()                     // line ~560 / ~594
private fun dumpSessionToFile(session)                          // line ~479
private fun showStreamNotification(...)                         // line ~827
private fun sha256(input: String): String                       // line ~759
```

### 6.4 `start_helper_service` 命令

Rust 侧通过 `stream.rs::start_helper_service` 调用 Kotlin `startHelperService`，在 `VcpMobilePlugin.init` 中会自动启动一次；前端亦可在冷启动后显式调用以兜底。

```rust
#[tauri::command]
pub fn start_helper_service<R: Runtime>(app: AppHandle<R>) -> Result<(), String> {
    // Android: run_mobile_plugin("startHelperService", {})
}
```

---

## 7. 数据流完整链路（v1.1.2 / v1.1.3）

```
前端: startStreamService("Agent-A")
    │
    ▼
Tauri IPC: plugin:vcp-mobile|start_streaming_service
    │
    ▼
stream.rs :: start_streaming_service()
    │
    ├── start_stream_service_inner("Agent-A")
    │     ├── tag = "stream:Agent-A"
    │     ├── priority = 20 (PRIORITY_STREAM)
    │     └── acquire_foreground_inner(tag, 20, "Agent-A", false)
    │
    ▼
run_mobile_plugin("acquireForeground", { tag, priority, label, screenKeepOn })
    │
    ▼
Kotlin: VcpMobilePlugin.acquireForeground(invoke)
    │
    ├── parseArgs(AcquireForegroundArgs)
    ├── ForegroundGuardian.acquire(context, tag, priority, label, screenKeepOn)
    │     │
    │     ├── consumers["stream:Agent-A"] = ConsumerEntry(20, "Agent-A", false)
    │     ├── wasEmpty? → YES (首个)
    │     │   ├── acquireLocks()  → WakeLock + WifiLock 物理获取
    │     │   └── startFgs()      → startForegroundService(intent)
    │     │                           │
    │     │                           ▼
    │     │              StreamKeepaliveService.onStartCommand()
    │     │                ├── label = ForegroundGuardian.getNotificationLabel()
    │     │                ├── buildNotification("Agent-A") → "思考中……"
    │     │                └── startForeground(NOTIFICATION_ID, notification)
    │     │
    │     └── (后续 Agent-B 进入时: wasEmpty? → NO → updateFgs()
    │           → onStartCommand → 通知更新为最高优先级者的文案)
    │
    ▼
通知栏显示: [Agent-A] 思考中……

--- Agent 流式结束 ---

前端: stopStreamService("Agent-A")
    │
    ▼
stream.rs :: stop_streaming_service()
    │
    ├── stop_stream_service_inner("Agent-A")
    │     └── release_foreground_inner("stream:Agent-A")
    │   （注：v1.1.3 `stopStreamingService` Kotlin 命令改为直接调用 `releaseAllLocks()` 做全局清理）
    ▼
run_mobile_plugin("releaseForeground", { tag: "stream:Agent-A" })
    │
    ▼
Kotlin: VcpMobilePlugin.releaseForeground(invoke)
    │
    └── ForegroundGuardian.release(activity, "stream:Agent-A")
          │
          ├── consumers.remove("stream:Agent-A")
          ├── consumers.isEmpty()? → YES (最后一个)
          │   ├── releaseLocks()  → WakeLock + WifiLock 物理释放
          │   └── stopFgs()       → stopService(intent)
          │
          └── (若仍有消费者: updateFgs() 更新通知)
```

---

## 8. 关键约束与血训

1. **`StreamKeepaliveService` 范围内 `ForegroundGuardian` 是唯一的锁持有者**：`StreamKeepaliveService` 不再自行管理 WakeLock/WifiLock，所有主进程侧锁操作必须经由 `ForegroundGuardian.acquire/release`。**但 `SseProxyService` 在 `:helper` 进程内独立持有自己的双锁**，两者互不感知，文档与排查时不要混淆。

2. **`releaseAllLocks()` 不能省略**：`StreamKeepaliveService.onDestroy()` 必须调用 `ForegroundGuardian.releaseAllLocks()` 作为兜底清扫。若 Service 被系统强制杀死而未清理 consumers 注册表，下次 acquire 将检测到 `consumers.isNotEmpty()` 而跳过物理锁获取。

3. **通知不可设为 `setSilent(true)`**：部分 ROM 会将静默通知归类为不显示。当前实现使用 `IMPORTANCE_HIGH` + `setOngoing(true)`。

4. **`POST_NOTIFICATIONS` 是前提**：Android 13+ 上，缺少此权限声明会导致通知渠道被系统禁用。参见 `docs/ANDROID_PLUGIN_MANAGEMENT.md` §3.1 / §7.2。

5. **引用计数泄漏风险**：如果 Rust 侧调用 `acquire_foreground_inner` 后未在异常路径中调用 `release_foreground_inner`，对应 tag 将永远留在 ForegroundGuardian 中。前端代码应确保在 `try/finally` 或组件卸载时释放。旧版"累积计数递增"易泄漏（重复调用同一 Agent 而忘记配对停止），新版幂等覆盖降低了此风险。

6. **`StreamKeepaliveService.stopWithTask=true` 是语义正确的**：v1.1.2 前 `stopWithTask=false` 旨在防止进程被杀后服务消亡。现在 `ForegroundGuardian` 在进程内管理锁，进程死则锁亦死，不停止服务只会留下僵尸通知。**`SseProxyService` 因运行在 `:helper` 进程且自行管理锁，保留 `stopWithTask=false`**。

7. **跨文档参考**：详细锁管理机制见 [11_ForegroundGuardian 前台守护者](11_ForegroundGuardian_前台守护者.md) §4–6；OEM 权限链路见 [06_权限与系统控制](06_权限与系统控制.md)；生命周期协同见 [05_生命周期桥接](05_生命周期桥接.md)；`:helper` 进程 SSE 代理见本文 §6。

---

*最后更新：2026-07-04 | VCP Mobile v1.1.3*
