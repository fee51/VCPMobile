---
id: PLUG-FLOAT-010
title: 浮动窗口与会话共享
description: Android 原生浮动窗口管理器与会话共享意图处理
version: 1.1.3
date: 2026-07-04
related_files:
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/FloatingWindowManager.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/ShareIntentHandler.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/VcpMobilePlugin.kt
  - src-tauri/plugins/vcp-mobile/src/system.rs
  - src-tauri/plugins/vcp-mobile/src/lib.rs
---

# 浮动窗口与会话共享

> **⚠️ 已暂停的 Beta 功能（SUSPENDED BETA）**：浮动助手（划词悬浮球）功能在 v1.1.3 中已暂停使用。
> > `SettingsView.vue` 中的"划词悬浮助手"入口已注释关闭，`AssistantSettingsSection` 组件与 `AssistantView.vue` 保留代码但不再渲染。`FloatingWindowManager` / `ShareIntentHandler` 的 Kotlin 实现仍保留在仓库中，供后续重启该功能时复用。

## 1. 功能概述

VCP Mobile 的浮动窗口与会话共享系统由两个核心 Kotlin 类组成，共同提供 Android 平台的"划词助手"能力：

| 类 | 文件 | 行数 | 职责 |
|---|------|:---:|------|
| `FloatingWindowManager` | `FloatingWindowManager.kt` | 272 | WindowManager 悬浮球与助手窗口的创建、显示、隐藏、拖拽、动画 |
| `ShareIntentHandler` | `ShareIntentHandler.kt` | 199 | 处理 Android 系统分享意图（ACTION_SEND / PROCESS_TEXT），提取内容并注入 WebView |

这两个类与 Rust 侧的新增命令形成完整的浮动助手链路：

```
用户在其他应用选中文本 → ShareIntentHandler 提取内容
    │
    ▼
注入 JS 事件到 AssistView WebView → floatingAssistant Store 发起对话
    │
    ▼
WebSocket → local_server.rs → agent_chat_application_service → VCP SSE 流
```

---

## 2. FloatingWindowManager 详解

### 2.1 架构

```
FloatingWindowManager
    ├── floatingBallView (ImageView)
    │     └── 浮动球：48dp 圆形、App 图标、半透明、可拖拽、边缘吸附动画
    │
    └── assistantContainer (LinearLayout)
          ├── handleBar (FrameLayout)
          │     └── 原生关闭把手：32dp 高、圆角顶部、半透明黑底、点击关闭
          │
          └── assistantWebView (WebView)
                ├── javascriptEnabled = true
                ├── domStorageEnabled = true
                ├── AndroidBridge JS 接口
                │     ├── closeWindow(): void
                │     └── getClipboard(): String
                └── loadUrl("http://127.0.0.1:14202/floating")
```

### 2.2 浮动球 (Floating Ball)

#### 外观参数

| 属性 | 值 | 说明 |
|------|-----|------|
| 尺寸 | 48dp × 48dp | 适配不同 DPI 屏幕 |
| 初始位置 | 屏幕右侧 x = width - 48dp - 16dp, y = height × 0.7 | 右下区域，不挡主要内容 |
| 背景 | 白色圆形 (`GradientDrawable OVAL`) | 应用图标 + 1dp 灰色描边 |
| 不透明度 | 0.85 (默认) / 1.0 (触摸中) / 0.5 (闲置) | 按触摸阶段变化 |
| 窗口类型 | `TYPE_APPLICATION_OVERLAY` (API 26+) / `TYPE_PHONE` (旧版) | WindowManager overlay |
| 窗口标志 | `FLAG_NOT_FOCUSABLE \| FLAG_LAYOUT_NO_LIMITS \| FLAG_HARDWARE_ACCELERATED` | 不抢占焦点、允许全屏、硬件加速 |

#### 触摸与拖拽

```kotlin
view.setOnTouchListener { v, event ->
    when (event.action) {
        ACTION_DOWN ->
            lastX/Y = rawX/Y; startX/Y = rawX/Y
            startTime = now(); alpha = 1.0f

        ACTION_MOVE ->
            params.x += (rawX - lastX).toInt()
            params.y += (rawY - lastY).toInt()
            边界约束: x ∈ [0, screenWidth - viewWidth], y ∈ [0, screenHeight - viewHeight]
            wm.updateViewLayout(v, params)

        ACTION_UP ->
            if (位移 < 6dp && 时间 < 300ms) → onClick()
            animateBallToEdge(v, params)
    }
    true
}
```

**拖拽设计要点**：
- `ACTION_MOVE` 中每次计算增量位移，确保跟手
- 边界约束使用 `coerceIn` 防止球体被拖出屏幕
- `ACTION_UP` 后判断是否点击（小位移 + 短时间），点击则 toggle 助手窗口，否则执行边缘吸附动画

#### 边缘吸附动画

```kotlin
private fun animateBallToEdge(view: View, params: WindowManager.LayoutParams) {
    val targetX = if (params.x + view.width / 2 < screenWidth / 2)
        0  // 左侧
    else
        screenWidth - view.width  // 右侧

    ValueAnimator.ofInt(params.x, targetX)
        .duration(300)           // 300ms 减速吸附
        .interpolator(DecelerateInterpolator)
        .addUpdateListener { ... wm.updateViewLayout(view, params) }
        .addListener { onAnimationEnd → alpha = 0.5f }
        .start()
}
```

球体总是吸附到距离最近的屏幕边缘，吸附后不透明度降至 0.5，避免干扰当前应用内容。

#### 球体可见性联动

- 助手窗口打开时：`floatingBallView.visibility = View.GONE`（球体隐藏）
- 助手窗口关闭时：`floatingBallView.visibility = View.VISIBLE`（球体重新显示）

### 2.3 助手窗口 (Assistant Window)

#### 窗口参数

| 属性 | 值 | 说明 |
|------|-----|------|
| 宽度 | `MATCH_PARENT` | 全宽 |
| 高度 | `screenHeight × 0.75` | 屏幕 75% 高度 |
| 重力 | `Gravity.BOTTOM` | 底部对齐，类似底部弹出面板 |
| 窗口类型 | `TYPE_APPLICATION_OVERLAY` (API 26+) / `TYPE_PHONE` (旧版) | overlay 权限 |
| 窗口标志 | `FLAG_HARDWARE_ACCELERATED` | WebView 渲染加速 |
| 动画 | `android.R.style.Animation_InputMethod` | 类似输入法弹出动画 |

#### 原生关闭把手

```
┌─────────────────────────────────────────┐
│  ▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔   │  ← 32dp 高
│  点击此条或外部收起助手  (12px 圆角顶部)  │  ← 半透明黑色 (0xAA000000)
├─────────────────────────────────────────┤
│                                         │
│         assistantWebView               │
│         (flex=1, fills remaining)      │
│         loadUrl: http://127.0.0.1:14202/floating │
│                                         │
└─────────────────────────────────────────┘
```

把手使用 `GradientDrawable` 绘制，顶部 12dp 圆角（`floatArrayOf(radius, radius, 0f, 0f)`），内部有一个居中的 `TextView` 提示"点击此条或外部收起助手"。

#### AndroidBridge JS 接口

WebView 加载 `http://127.0.0.1:14202/floating` 前，通过 `addJavascriptInterface` 注入原生桥接对象：

```kotlin
webView.addJavascriptInterface(object {
    @JavascriptInterface
    fun closeWindow() {
        activity.runOnUiThread { hideAssistantWindow() }
    }

    @JavascriptInterface
    fun getClipboard(): String {
        val cb = context.getSystemService(CLIPBOARD_SERVICE) as ClipboardManager
        return cb.primaryClip?.getItemAt(0)?.text?.toString() ?: ""
    }
}, "AndroidBridge")
```

| 方法 | JS 调用方式 | 线程 | 用途 |
|------|-----------|------|------|
| `closeWindow()` | `AndroidBridge.closeWindow()` | 主线程 (`runOnUiThread`) | 关闭悬浮助手窗口 |
| `getClipboard()` | `AndroidBridge.getClipboard()` | WebView 线程（同步方法） | 读取系统剪贴板文本 |

#### WebView 销毁顺序

```kotlin
private fun hideAssistantWindow() {
    assistantContainer?.let { view ->
        // 1. 先从父容器移除 WebView
        assistantWebView?.let { wv ->
            (wv.parent as? ViewGroup)?.removeView(wv)
            wv.destroy()  // 2. 销毁 WebView 资源
        }
        // 3. 从 WindowManager 移除容器
        windowManager?.removeView(view)
        assistantWebView = null
        assistantContainer = null
        floatingBallView?.visibility = View.VISIBLE  // 4. 恢复悬浮球
    }
}
```

**关键设计**：先 `removeView` 再 `destroy()`，避免 Android WebView 抛出的 "destroy() called while still attached" 警告。

### 2.4 覆盖层权限管理

#### 权限检查

```kotlin
fun hasOverlayPermission(): Boolean {
    return if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
        Settings.canDrawOverlays(context)
    } else {
        true  // API < 23 无需权限
    }
}
```

Android 6.0 (API 23) 以上需要在系统设置中手动授权 `ACTION_MANAGE_OVERLAY_PERMISSION`。

#### 权限请求

```kotlin
fun requestOverlayPermission() {
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
        val intent = Intent(
            Settings.ACTION_MANAGE_OVERLAY_PERMISSION,
            Uri.parse("package:${activity.packageName}")
        ).apply { addFlags(Intent.FLAG_ACTIVITY_NEW_TASK) }
        activity.startActivity(intent)
    }
}
```

该 Intent 将用户带到系统设置页面，列出可以显示在其他应用上层的应用。应用包名通过 `package:` URI 精确定位到 VCP Mobile。

#### toggleFloatingBall 的权限守卫

```kotlin
fun toggleFloatingBall(show: Boolean): Boolean {
    if (show) {
        if (!hasOverlayPermission()) return false  // 无权限时拒绝显示
        activity.runOnUiThread { showFloatingBall() }
    } else {
        activity.runOnUiThread {
            hideAssistantWindow()
            hideFloatingBall()
        }
    }
    return true
}
```

`show == true` 时先检查权限，无权限返回 `false`（前端 `AssistantSettingsSection` 会检测此返回并重置 UI 开关状态）。

---

## 3. ShareIntentHandler 详解

### 3.1 支持的系统 Intent

| Intent Action | 来源场景 | 提取的内容 |
|---------------|---------|-----------|
| `ACTION_PROCESS_TEXT` | 浏览器/阅读器选中文字菜单（划词） | `intent.getCharSequenceExtra(Intent.EXTRA_PROCESS_TEXT)` |
| `ACTION_SEND` | 单文件/文本分享 | `EXTRA_TEXT`, `EXTRA_SUBJECT`, `EXTRA_STREAM` (单个) |
| `ACTION_SEND_MULTIPLE` | 多文件分享 | `EXTRA_TEXT`, `EXTRA_SUBJECT`, `EXTRA_STREAM` (多个) |

### 3.2 内容提取逻辑

```kotlin
private fun extractSharedContent(intent: Intent, context: Context): JSObject {
    val root = JSObject()

    // 1. 文本提取（优先级：PROCESS_TEXT > EXTRA_SUBJECT + EXTRA_TEXT）
    val processText = if (intent.action == Intent.ACTION_PROCESS_TEXT) {
        intent.getCharSequenceExtra(Intent.EXTRA_PROCESS_TEXT)?.toString()
    } else null

    val text = intent.getStringExtra(Intent.EXTRA_TEXT)
    val subject = intent.getStringExtra(Intent.EXTRA_SUBJECT)

    val combinedText = buildString {
        if (!processText.isNullOrBlank()) {
            append(processText)
        } else {
            if (!subject.isNullOrBlank()) { append(subject) }
            if (!text.isNullOrBlank()) {
                if (isNotEmpty() && !text.startsWith(subject ?: "")) {
                    append("\n")  // 避免 SUBJECT 与 TEXT 重复拼接
                }
                append(text)
            }
        }
    }
    root.put("text", combinedText.ifBlank { "" })

    // 2. 文件 URIs 提取
    // ACTION_SEND: intent.getParcelableExtra<Uri>(EXTRA_STREAM)
    // ACTION_SEND_MULTIPLE: intent.getParcelableArrayListExtra<Uri>(EXTRA_STREAM)
    // 每个 URI → copyStreamToCache() → { cachePath, mimeType, fileName, size }

    // 3. 文件存入缓存
    // targetPath: cacheDir/shared/shared_{timestamp}_{originalFileName}
    // 使用 contentResolver.openInputStream(uri) 读取并复制
    // bufferSize = 65536 (64KB)

    return root
}
```

### 3.3 文件缓存策略

分享的文件通过 `ContentResolver` 从 `content://` URI 读出，复制到应用内部缓存目录：

```
cacheDir/shared/
    ├── shared_1717500000_document.pdf
    ├── shared_1717500001_image.jpg
    └── ...
```

| 字段 | 来源 | 说明 |
|------|------|------|
| `cachePath` | `targetFile.absolutePath` | 缓存文件的绝对路径 |
| `mimeType` | `contentResolver.getType(uri)` | 文件的 MIME 类型 |
| `fileName` | `OpenableColumns.DISPLAY_NAME` | 原始文件名 |
| `size` | `targetFile.length()` | 文件字节数 |

### 3.4 JS 注入机制

```kotlin
fun injectShareData(webView: WebView?) {
    val data = pendingShareData ?: return
    val dataJson = data.toString()
    val safeJson = escapeJsonForJsString(dataJson)
    val script = "window.dispatchEvent(" +
        "new CustomEvent('vcp-share-intent', " +
        "{ detail: JSON.parse(\"$safeJson\") }))"
    webView.evaluateJavascript(script, null)
    pendingShareData = null  // 注入后清空缓存
}
```

| 步骤 | 说明 |
|------|------|
| `escapeJsonForJsString()` | 将 JSON 字符串中的 `\`、`"`、`'`、`\n`、`\r` 进行 JS 字符串转义 |
| `CustomEvent('vcp-share-intent')` | 标准化的事件名称，前端可通过 `addEventListener` 监听 |
| `evaluateJavascript` | WebView 方法，在当前 WebView JS 上下文中执行代码 |
| `pendingShareData` 缓存 | 若 WebView 尚未就绪，缓存数据等待后续 `onPageFinished` 或手动调用注入 |

### 3.5 延迟注入机制

```kotlin
// WebView 未就绪时缓存待注入数据
private var pendingShareData: JSObject? = null

fun handleShareIntent(intent: Intent) {
    val shareData = extractSharedContent(intent, context)
    pendingShareData = shareData
    if (webView != null) {
        injectShareData(webView)  // 立即注入
    } else {
        Log.w(TAG, "WebView not ready, caching share data")  // 缓存等待
    }
}
```

VcpMobilePlugin 在 `onPageFinished` 等 WebView 生命周期回调中调用 `shareIntentHandler.injectShareData(webView)`，确保缓存数据不会丢失。

---

## 4. 新增 Tauri 命令

### 4.1 命令注册（Rust 侧）

`lib.rs` 的 `generate_handler!` 宏中新增 3 条浮动窗口相关命令：

```rust
system::request_overlay_permission,
system::register_shared_files,
system::toggle_floating_ball,
```

### 4.2 命令详解

#### `request_overlay_permission`

| 属性 | 值 |
|------|-----|
| 前端调用 | `invoke("plugin:vcp-mobile\|request_overlay_permission")` |
| Rust 实现 | `system.rs:382`，通过 `run_mobile_plugin("requestOverlayPermission", {})` 调用 Kotlin |
| Kotlin 实现 | `VcpMobilePlugin.requestOverlayPermission(invoke)` → `floatingWindowManager.requestOverlayPermission()` |
| 效果 | 打开系统设置 → `ACTION_MANAGE_OVERLAY_PERMISSION` 页面 |
| 适用平台 | Android only (其他平台返回空 Ok) |

#### `register_shared_files`

| 属性 | 值 |
|------|-----|
| 前端调用 | `invoke("plugin:vcp-mobile\|register_shared_files", { files: SharedFileItem[] })` |
| Rust 实现 | `system.rs:412`，遍历文件列表，逐个调用 Kotlin `processSharedFile` |
| Kotlin 实现 | `VcpMobilePlugin.processSharedFile(invoke)` → 对缓存文件进行哈希计算、MIME 类型精修 |
| 返回 | `Vec<PickedFileInfo>` — 注册完成的文件元数据列表 |

#### `toggle_floating_ball`

| 属性 | 值 |
|------|-----|
| 前端调用 | `invoke("plugin:vcp-mobile\|toggle_floating_ball", { show: boolean })` |
| Rust 实现 | `system.rs:447`，通过 `run_mobile_plugin("toggleFloatingBall", { show })` 调用 Kotlin |
| Kotlin 实现 | `VcpMobilePlugin.toggleFloatingBall(invoke)` → `floatingWindowManager.toggleFloatingBall(args.show)` |
| 返回 | `{ success: boolean }` |
| 效果 | 显示/隐藏悬浮球；隐藏时同时关闭助手窗口 |

### 4.3 Kotlin 侧命令路由

在 `VcpMobilePlugin.kt` 中对应的 3 个 `@Command` 方法：

| Kotlin Command | 位置 | 调用链 |
|---------------|------|--------|
| `requestOverlayPermission` | `VcpMobilePlugin.kt` ~600 | → `floatingWindowManager.requestOverlayPermission()` |
| `toggleFloatingBall` | `VcpMobilePlugin.kt` ~607 | → `parseArgs(ToggleFloatingBallArgs)` → `floatingWindowManager.toggleFloatingBall(show)` |
| `processSharedFile` | `VcpMobilePlugin.kt` ~1592 | → `parseArgs(ProcessSharedFileArgs)` → 读取缓存文件 `cachePath`、计算哈希、缩略图生成 |

---

## 5. 权限与系统要求

### 5.1 覆盖层权限（`SYSTEM_ALERT_WINDOW`）

| 项目 | 说明 |
|------|------|
| 权限常量 | `Settings.ACTION_MANAGE_OVERLAY_PERMISSION` |
| 需要 API | Android 6.0 (API 23) 及以上 |
| 声明方式 | `AndroidManifest.xml` 中声明 `<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>` |
| 用户授予方式 | 系统设置页面手动授权（无法通过 `requestPermissions` 运行时弹窗） |
| 检测方式 | `Settings.canDrawOverlays(context)` |

### 5.2 检测与交互流程

```
AssistantSettingsSection.vue
    │
    ├── onMounted:
    │     invoke("plugin:vcp-mobile|check_all_permissions")
    │     → 获取 { overlay: boolean, notification: boolean, ... }
    │
    │       ├── overlay == false && enableAssistant == true:
    │       │     重置 enableAssistant = false
    │       │     toggle_floating_ball(false)
    │       │     ↑ 权限被撤销后的自愈逻辑
    │       │
    │       └── overlay == true && enableAssistant == true:
    │              toggle_floating_ball(true)
    │              reconcile_local_server_cmd(true)
    │
    ├── 用户开启悬浮球:
    │     checkPermission()
    │       ├── 无权限:
    │       │     invoke("request_overlay_permission")  → 跳转系统设置
    │       │     enableAssistant = false  → 重置开关
    │       │
    │       └── 有权限:
    │             toggle_floating_ball(true)
    │             reconcile_local_server_cmd(true)
    │
    └── 应用 resume (`vcp-lifecycle-changed` / `lifecycleStore.isBackground` 变为 false):
          重新检查权限，若权限正常且开关开启则恢复悬浮球
```

---

## 6. 前台保活与 Sensor 管理

### 6.1 SensorStatusManager

`VcpMobilePlugin.kt` 新增对 `SensorStatusManager` 的集成：

| Kotlin Command | 位置 | 调用 |
|---------------|------|------|
| `startSensorCollection` | `VcpMobilePlugin.kt` ~969 | → `sensorStatusManager.start()` |
| `stopSensorCollection` | `VcpMobilePlugin.kt` ~982 | → `sensorStatusManager.stop()` |
| `getSensorData` | `VcpMobilePlugin.kt` ~995 | → `sensorStatusManager.getSensorData(type)` |

对应的 Rust 命令：
- `start_sensor_collection`：启动传感器数据采集
- `stop_sensor_collection`：停止传感器数据采集
- `get_sensor_data`：获取当前传感器快照数据

这些命令为分布式节点环境感知提供底层支持，不仅限于浮动助手场景。

### 6.2 流式保活服务（StreamKeepaliveService）

`StreamKeepaliveService`（`service/StreamKeepaliveService.kt`）是一个 Android 前台服务，在 Agent 流式输出期间保持进程存活。详情参见 [03_流式前台保活服务](03_流式前台保活服务.md)。

与浮动助手的关系：当浮动窗口内的 Agent 正在进行流式生成时，同样触发 `start_streaming_service` / `stop_streaming_service` 命令，确保 OEM 电池优化不会在用户等待回复时杀后台。

---

## 7. 数据流全景

```
用户在其他应用分享文本/文件
    │
    ▼
Android Intent System
    │ ACTION_SEND / ACTION_SEND_MULTIPLE / ACTION_PROCESS_TEXT
    ▼
VcpMobilePlugin.onNewIntent(intent)
    │
    ▼
ShareIntentHandler.handleShareIntent(intent)
    ├── extractSharedContent(intent, context) → JSObject { text, files }
    ├── copyStreamToCache(uri, context) → JSObject { cachePath, mimeType, fileName, size }
    └── pendingShareData = shareData
         │
         ├── WebView 已就绪:
         │     injectShareData(webView)
         │       → evaluateJavascript("window.dispatchEvent(new CustomEvent('vcp-share-intent', ...))")
         │         → AssistantView.vue 监听事件
         │           → inputText.value = event.detail.text
         │             → 用户手动发送
         │
         └── WebView 未就绪:
               等待 onPageFinished → injectShareData(webView)


用户点击浮动球
    │
    ▼
FloatingWindowManager.onFloatingBallClick()
    ├── assistantContainer == null:
    │     showAssistantWindow()
    │       ├── 创建 LinearLayout root
    │       ├── 添加 handleBar (32dp 关闭把手)
    │       ├── 创建 WebView, loadUrl("http://127.0.0.1:14202/floating")
    │       ├── addJavascriptInterface(AndroidBridge)
    │       ├── wm.addView(root, params)
    │       └── floatingBallView.visibility = GONE
    │
    └── assistantContainer != null:
          hideAssistantWindow()
            ├── removeView + destroy WebView
            ├── wm.removeView(container)
            └── floatingBallView.visibility = VISIBLE
```

---

## 8. 关键约束与血训

1. **overlay 权限不可运行时弹窗**：`SYSTEM_ALERT_WINDOW` 无法通过 `ActivityCompat.requestPermissions` 获取，必须由用户手动在系统设置中授权。前端 `AssistantSettingsSection` 在检测到无权限时自动跳转系统设置页面并重置 UI 开关。

2. **WebView 销毁必须先移除再 destroy**：Android WebView 的 `destroy()` 方法要求在 `ViewGroup` 之外调用，否则抛出"called while still attached"警告。`hideAssistantWindow()` 中严格遵循 `removeView → destroy → removeView` 顺序。

3. **浮动窗口不能使用 Tauri invoke**：WindowManager overlay 创建的 WebView 不在主 Activity 的 WebView 实例中，Tauri 的 `invoke()` JS bridge 不可用。这就是为什么浮动助手走 WebSocket + 本地服务器方案。

4. **shareIntentHandler 的延迟注入**：分享数据到达时，WebView 可能尚未创建（用户可能在主应用界面而非浮动窗口中）。`pendingShareData` 缓存确保数据不丢失，等待 WebView 就绪后注入。

5. **FileProvider 的必要性**：分享的文件通过 `ContentResolver` 复制到应用缓存目录后，若需被其他应用访问或作为附件发送，需要通过 `FileProvider` 将 file:// URI 转换为 content:// URI，否则会触发 FileUriExposedException。

6. **JSON 注入安全性**：`escapeJsonForJsString()` 对反斜杠、双引号、单引号、换行符进行转义后再拼接 JS 字符串，防止恶意构造的分享文本破坏 JS 语法或执行 XSS 攻击。

---

## 9. 交叉引用

| 关联模块 | 文档 | 关系 |
|----------|------|------|
| `local_server.rs` | MOD-INFRA-022 本地服务器与浮动助手 | WebView 加载的 HTTP 服务器 |
| `floatingAssistant.ts` | VUE-ASST-024 浮动助手系统 | AndroidBridge 调用的 JS 消费端 |
| `AssistantView.vue` | VUE-ASST-024 浮动助手系统 | WebView 中渲染的前端组件 |
| `AssistantSettingsSection.vue` | VUE-SETT-016 设置子模块详解 | 浮动球开关 + 权限管理 UI |
| `StreamKeepaliveService.kt` | PLUGIN-STREAM-003 流式前台保活服务 | 流式生成期间的进程保活 |
| `VcpMobilePlugin.kt` | PLUGIN-INIT-001 插件初始化与命令路由 | 命令路由与 `onNewIntent` |

---

*最后更新：2026-07-04 | VCP Mobile v1.1.3*
