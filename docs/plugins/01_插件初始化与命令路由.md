---
id: PLUGIN-INIT-001
title: 插件初始化与命令路由
description: tauri-plugin-vcp-mobile 的入口、共享状态与 38 条命令注册表。v1.1.2 更新
version: 1.1.2
date: 2026-06-24
related_files:
  - src-tauri/plugins/vcp-mobile/src/lib.rs
  - src-tauri/plugins/vcp-mobile/src/stream.rs
  - src-tauri/plugins/vcp-mobile/src/system.rs
---

# 插件初始化与命令路由

## 1. 功能概述

`lib.rs` 是 `tauri-plugin-vcp-mobile` 的**唯一入口**。职责：

1. 定义插件共享状态 `VcpMobileState<R>`
2. 注册全部 **38 条** Tauri 命令（v1.1.2 新增 6 条：`acquire_foreground`, `release_foreground`, `request_auto_start_permission`, `request_power_management_permission`, `check_auto_start_permission`, `get_free_disk_space`）
3. 在 `setup` 钩子中完成 Android 原生插件注册，并将 `PluginHandle` 注入状态

> **设计原则**：`lib.rs` 只负责"路由与状态"，所有业务逻辑下沉到 `screen`、`stream`、`system` 三个子模块。

---

## 2. 代码结构

```
src-tauri/plugins/vcp-mobile/src/lib.rs (~50 lines)
├── VcpMobileState<R>
│   └── plugin_handle: Mutex<Option<PluginHandle<R>>>  (Android only)
├── init<R>() -> TauriPlugin<R>
│   ├── invoke_handler: 注册 38 条命令（含 v1.1.2 新增 6 条）
│   └── setup: 注册 Android 插件 + manage 状态
```

---

## 3. 共享状态详解

### 3.1 `VcpMobileState<R>`

```rust
pub struct VcpMobileState<R: Runtime> {
    #[cfg(target_os = "android")]
    pub plugin_handle: std::sync::Mutex<Option<tauri::plugin::PluginHandle<R>>>,
    #[cfg(not(target_os = "android"))]
    _marker: std::marker::PhantomData<fn() -> R>,
}
```

| 字段 | 类型 | 用途 | 访问方式 |
|------|------|------|----------|
| `plugin_handle` | `Mutex<Option<PluginHandle<R>>>` | Android 原生插件句柄，用于 `run_mobile_plugin` | `app.state::<VcpMobileState<R>>().plugin_handle.lock()` |

#### v1.1.2 变更：`active_streams` 字段已移除

旧版 `VcpMobileState` 包含 `active_streams: Mutex<Vec<(String, u32)>>` 字段，用于 Rust 侧流式服务引用计数。v1.1.2 将此逻辑迁移至 Kotlin 侧 `ForegroundGuardian.consumers`（`ConcurrentHashMap<String, ConsumerEntry>`），以优先级系统取代计数系统。详见 [11_ForegroundGuardian 前台守护者](11_ForegroundGuardian_前台守护者.md)。

### 3.2 为什么用 `std::sync::Mutex` 而非 `tokio::sync::Mutex`？

- 锁内操作极短（仅为 `Option` 的 `as_ref` 或 `PluginHandle` 访问），不存在异步 IO。
- `std::sync::Mutex` 在同步上下文中性能更优，且 Tauri command handler 本身是同步调用。

### 3.3 桌面端兼容

非 Android 编译目标下，`plugin_handle` 字段被替换为 `_marker: PhantomData`，确保泛型参数 `R` 被使用，避免编译错误。

---

## 4. 命令注册表（38 条命令）

### 4.1 screen 模块（2 条）

| Rust 函数 | 前端命令 | 说明 |
|-----------|---------|------|
| `set_keep_screen_on` | `plugin:vcp-mobile\|set_keep_screen_on` | JNI 添加 FLAG_KEEP_SCREEN_ON |
| `clear_keep_screen_on` | `plugin:vcp-mobile\|clear_keep_screen_on` | JNI 清除 FLAG_KEEP_SCREEN_ON |

### 4.2 stream 模块（5 条 — v1.1.2 新增 3 条）

| Rust 函数 | 前端命令 | 说明 | 版本 |
|-----------|---------|------|:----:|
| `start_streaming_service` | `plugin:vcp-mobile\|start_streaming_service` | 启动流式保活（兼容旧 API → 委托 ForegroundGuardian） | — |
| `stop_streaming_service` | `plugin:vcp-mobile\|stop_streaming_service` | 停止流式保活 | — |
| `acquire_foreground` | `plugin:vcp-mobile\|acquire_foreground` | 语义化申请前台锁 | v1.1.2 |
| `release_foreground` | `plugin:vcp-mobile\|release_foreground` | 语义化释放前台锁 | v1.1.2 |
| `set_keepalive_mode` | `plugin:vcp-mobile\|set_keepalive_mode` | 分布式保活模式开关 | v1.1.2 |

### 4.3 system 模块（31 条 — v1.1.2 新增 4 条）

| Rust 函数 | 前端命令 | 说明 | 版本 |
|-----------|---------|------|:----:|
| `check_all_permissions` | `plugin:vcp-mobile\|check_all_permissions` | 检查全部权限状态 | — |
| `request_android_permission` | `plugin:vcp-mobile\|request_android_permission` | 请求指定权限 | — |
| `move_task_to_back` | `plugin:vcp-mobile\|move_task_to_back` | 将应用移至后台 | — |
| `request_auto_start_permission` | `plugin:vcp-mobile\|request_auto_start_permission` | 打开 OEM 自启动设置页 | v1.1.2 |
| `request_power_management_permission` | `plugin:vcp-mobile\|request_power_management_permission` | 打开 OEM 电源管理设置页 | v1.1.2 |
| `check_auto_start_permission` | `plugin:vcp-mobile\|check_auto_start_permission` | 检查自启动权限（AppOps 反射） | v1.1.2 |
| `get_free_disk_space` | `plugin:vcp-mobile\|get_free_disk_space` | 获取 /data 分区剩余空间 | v1.1.2 |
| `pick_file` | `plugin:vcp-mobile\|pick_file` | 原生文件选择器 | — |
| `get_battery_status` | `plugin:vcp-mobile\|get_battery_status` | 电池电量与省电模式 | — |
| `get_network_status` | `plugin:vcp-mobile\|get_network_status` | 网络连接状态 | — |
| `get_cpu_thermal_status` | `plugin:vcp-mobile\|get_cpu_thermal_status` | CPU 热分级 | — |
| `get_gpu_status` | `plugin:vcp-mobile\|get_gpu_status` | GPU 型号 | — |
| `open_file_native` | `plugin:vcp-mobile\|open_file_native` | 系统默认应用打开文件 | — |
| `capture_window_snapshot` | `plugin:vcp-mobile\|capture_window_snapshot` | WebView 截图 | — |
| `save_image_to_gallery` | `plugin:vcp-mobile\|save_image_to_gallery` | 保存图片到相册 | — |
| `save_image_from_path` | `plugin:vcp-mobile\|save_image_from_path` | 从路径保存图片 | — |
| `write_temp_file` | `plugin:vcp-mobile\|write_temp_file` | 写入临时文件 | — |
| `delete_temp_file` | `plugin:vcp-mobile\|delete_temp_file` | 删除临时文件 | — |
| `start_download_notification` | `plugin:vcp-mobile\|start_download_notification` | 开始下载通知 | — |
| `update_download_notification` | `plugin:vcp-mobile\|update_download_notification` | 更新下载通知进度 | — |
| `cancel_download_notification` | `plugin:vcp-mobile\|cancel_download_notification` | 取消下载通知 | — |
| `request_overlay_permission` | `plugin:vcp-mobile\|request_overlay_permission` | 请求悬浮窗权限 | — |
| `register_shared_files` | `plugin:vcp-mobile\|register_shared_files` | 注册外部分享文件 | — |
| `toggle_floating_ball` | `plugin:vcp-mobile\|toggle_floating_ball` | 切换悬浮球（v1.1.3 浮动助手功能已暂停使用） | — |
| `start_sensor_collection` | `plugin:vcp-mobile\|start_sensor_collection` | 开始传感器采集 | — |
| `stop_sensor_collection` | `plugin:vcp-mobile\|stop_sensor_collection` | 停止传感器采集 | — |
| `get_sensor_data` | `plugin:vcp-mobile\|get_sensor_data` | 获取传感器数据 | — |
| `check_root_access` | `plugin:vcp-mobile\|check_root_access` | 检测 Root 权限 | — |
| `run_root_command` | `plugin:vcp-mobile\|run_root_command` | 执行 Root 命令 | — |
| `launch_root_manager` | `plugin:vcp-mobile\|launch_root_manager` | 启动 Root 管理器 | — |
| `acquire_wake_lock` | `plugin:vcp-mobile\|acquire_wake_lock` | 手动保活锁（→ ForegroundGuardian） | — |
| `release_wake_lock` | `plugin:vcp-mobile\|release_wake_lock` | 释放手动保活锁 | — |
| `start_network_monitoring` | `plugin:vcp-mobile\|start_network_monitoring` | 开始网络监控 | — |

> **注意**：Tauri v2 中前端通过 `invoke('plugin:vcp-mobile|command_name')` 调用，命令名使用**下划线**（Rust 风格）。详见 [07_Guest JS API](07_Guest JS API.md)。

---

## 4.1 build.rs 命令列表同步

`build.rs` 中的 `COMMANDS` 数组用于 Tauri 插件构建阶段生成权限清单与前端类型定义，必须与 `lib.rs` 中 `generate_handler!` 注册的命令名保持一致：

```rust
const COMMANDS: &[&str] = &[
    "set_keep_screen_on",
    "clear_keep_screen_on",
    "start_stream_service",      // ⚠️ 命名不一致：lib.rs 中实际注册为 start_streaming_service
    "stop_stream_service",       // ⚠️ 命名不一致：lib.rs 中实际注册为 stop_streaming_service
    "check_all_permissions",
    "request_android_permission",
    "move_task_to_back",
    "pick_file",
];
```

**当前差异**：
- `start_stream_service` / `stop_stream_service` 与 `lib.rs` 中的 `start_streaming_service` / `stop_streaming_service` 命名不同步（缺少 `ing`），可能导致权限生成不匹配。
- 缺少 `get_battery_status` 与 `open_file_native`，需补充以保持与 `generate_handler!` 完全一致。

---

## 5. Setup 流程

```rust
.setup(|app, _api| {
    #[cfg(target_os = "android")]
    let plugin_handle = _api.register_android_plugin("com.vcp.mobile", "VcpMobilePlugin")?;

    app.manage(VcpMobileState::<R> {
        #[cfg(target_os = "android")]
        plugin_handle: std::sync::Mutex::new(Some(plugin_handle)),
        #[cfg(not(target_os = "android"))]
        _marker: std::marker::PhantomData,
    });

    Ok(())
})
```

> **v1.1.2 变更**：`active_streams` 字段已移除 — 引用计数迁移至 Kotlin 侧 `ForegroundGuardian`。`app.manage()` 现在仅注入 `plugin_handle`。

### 5.1 `register_android_plugin`

- **包名**：`"com.vcp.mobile"` —— 对应 Kotlin 侧的 package 声明。
- **类名**：`"VcpMobilePlugin"` —— 对应 Kotlin 侧的 `@TauriPlugin class VcpMobilePlugin`。
- **返回**：`PluginHandle<R>`，后续通过 `run_mobile_plugin(command, payload)` 调用 Kotlin 方法。

### 5.2 `app.manage()`

将 `VcpMobileState` 注入 Tauri 应用状态容器。任何 command handler 都可以通过 `app.state::<VcpMobileState<R>>()` 获取共享状态。

---

## 6. 数据流全景（v1.1.2）

```
前端 Vue/TS
    │ invoke('plugin:vcp-mobile|xxx')
    ▼
Tauri Runtime (IPC)
    │
    ▼
lib.rs :: invoke_handler
    │ 路由到 screen / stream / system
    ▼
具体 command handler
    │
    ├── screen: 直接操作 Window flags (Raw JNI)
    │
    ├── stream: acquire/release_foreground_inner
    │            │
    │            │ run_mobile_plugin("acquireForeground" / "releaseForeground")
    │            ▼
    │       Kotlin VcpMobilePlugin
    │            │
    │            ▼
    │       ForegroundGuardian (引用计数 + 双锁)
    │            │
    │            │ startFgs / updateFgs / stopFgs
    │            ▼
    │       StreamKeepaliveService (仅 FGS 通知壳)
    │
    └── system: run_mobile_plugin
                 │
                 ▼
            Kotlin VcpMobilePlugin
                 │
                 ├── checkAllPermissions → PowerManager + ContextCompat
                 ├── requestAndroidPermission → ActivityCompat
                 ├── requestAutoStartPermission → 多 OEM Intent 链
                 ├── requestPowerManagementPermission → 多 OEM Intent 链
                 ├── checkAutoStartPermission → AppOpsManager 反射
                 ├── getFreeDiskSpace → StatFs on /data
                 ├── moveTaskToBack → activity.moveTaskToBack
                 ├── pickFile → SAF (Intent.ACTION_OPEN_DOCUMENT)
                 ├── getBatteryStatus → BatteryManager
                 ├── getCpuThermalStatus → CpuStatusManager
                 ├── getGpuStatus → GpuStatusManager
                 ├── getNetworkStatus → NetworkStatusManager
                 └── openFile → Intent.ACTION_VIEW
```

---

## 7. 关键约束

1. **`plugin_handle` 为 `Option`**：在 `setup` 之前或桌面端运行时可能为 `None`。业务代码中必须 `.ok_or("Plugin handle not initialized")?` 处理。

2. **命令扩展方式**：新增命令需要在 `invoke_handler` 数组、`build.rs` 的 `COMMANDS` 数组、`permissions/*.toml` 权限声明和 `guest-js/index.ts` 中同步添加。v1.1.2 新增的 6 条命令均已完成四重注册。

3. **引用计数已不在 Rust 侧**：不再通过 `active_streams` 管理引用计数。所有流式锁/保活操作必须经由 `acquire_foreground_inner` / `release_foreground_inner`（Rust 侧）→ `ForegroundGuardian.acquire` / `release`（Kotlin 侧）。

4. **与规范文档的交叉引用**：关于插件命令被静默拒绝的情况，参见 `docs/ANDROID_PLUGIN_MANAGEMENT.md` §7.1。

## 8. 交叉参考

| 文档 | 关系 |
|------|------|
| [11_ForegroundGuardian 前台守护者](11_ForegroundGuardian_前台守护者.md) | 锁管理核心单例，替代旧 `active_streams` |
| [03_流式前台保活服务](03_流式前台保活服务.md) | stream 模块的消费者视角 |
| [06_权限与系统控制](06_权限与系统控制.md) | system 模块的 31 条命令详解 |
| [07_Guest JS API](07_Guest JS API.md) | 前端调用封装 |
| `docs/ANDROID_PLUGIN_MANAGEMENT.md` | 插件规范与权限准则 |
