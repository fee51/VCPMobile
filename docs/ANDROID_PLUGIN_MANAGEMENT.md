# Android 插件管理体系规范

> **文档编号**: ARCH-ANDROID-001  
> **版本**: 1.1.3
> **日期**: 2026-07-04
> **附录新增**: 2026-05-19 | 附录 A（插件生态速查）+ 附录 B（权限实战手册）  
> **状态**: 已迁移至 `tauri-plugin-vcp-mobile`  
> **适用范围**: `src-tauri/plugins/vcp-mobile` 及 Android 原生层全部自定义代码

---

## 1. 插件总览

VCP Mobile 采用**单一自定义插件** `tauri-plugin-vcp-mobile` 统一管理全部 Android 原生能力。

| 功能 | Rust 模块 | Kotlin 模块 | 通信方式 |
|------|-----------|-------------|----------|
| 屏幕常亮 | `src/screen.rs` | — | Rust 侧 Raw JNI |
| 前台锁协同 | `src/stream.rs` | `service/ForegroundGuardian.kt` | `PluginHandle.run_mobile_plugin`（v1.1.2 新增） |
| 流式前台保活 | `src/stream.rs` | `service/StreamKeepaliveService.kt` | `PluginHandle.run_mobile_plugin` |
| SSE 代理（`:helper` 进程） | `src/stream.rs` | `service/SseProxyService.kt` | `startForegroundService` + 本地 TCP |
| 键盘 Insets | — | `KeyboardInsetsManager.kt` | `evaluateJavascript` 注入 CustomEvent |
| 返回键拦截 | — | `MainActivity.onWebViewCreate()` | `OnBackPressedDispatcher` |
| 生命周期事件 | — | `LifecycleBridge.kt` | `plugin.trigger("lifecycle")` → Rust `listen_any` → Tauri Event `vcp-lifecycle-changed`（v1.1.2/v1.1.3 升级） |
| 麦克风权限 | `src/system.rs` | `VcpMobilePlugin.kt` | `requestPermissionForAlias("microphone")`（Tauri 别名机制） |
| 通知栏监听 | `src/system.rs` | `service/VcpNotificationListenerService.kt` | 跳转 `ACTION_NOTIFICATION_LISTENER_SETTINGS` |
| 自启动/电源管理 | `src/system.rs` | `VcpMobilePlugin.kt` | 厂商 Intent 链 + 设置页兜底 |

---

## 2. 目录结构

```
src-tauri/plugins/vcp-mobile/
├── Cargo.toml
├── build.rs
├── android/
│   ├── build.gradle.kts
│   ├── src/main/AndroidManifest.xml
│   └── src/main/java/com/vcp/mobile/
│       ├── VcpMobilePlugin.kt
│       ├── KeyboardInsetsManager.kt
│       ├── LifecycleBridge.kt
│       ├── FloatingWindowManager.kt
│       ├── ShareIntentHandler.kt
│       ├── BatteryStatusManager.kt
│       ├── SensorStatusManager.kt
│       ├── CpuStatusManager.kt
│       ├── GpuStatusManager.kt
│       ├── NetworkStatusManager.kt
│       └── service/
│           ├── StreamKeepaliveService.kt
│           ├── ForegroundGuardian.kt
│           ├── SseProxyService.kt
│           └── VcpNotificationListenerService.kt
├── permissions/
│   ├── default.toml
│   └── autogenerated/commands/
├── src/
│   ├── lib.rs
│   ├── screen.rs
│   ├── stream.rs
│   └── system.rs
└── guest-js/
    └── index.ts
```

---

## 3. 权限准则（血训）

### 3.1 Android 13+ 通知权限（POST_NOTIFICATIONS）

**从 Android 13（API 33）开始，所有通知（包括前台服务通知）必须同时满足以下两个条件：**

1. **静态声明**：插件的 `AndroidManifest.xml` 必须显式声明：
   ```xml
   <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
   ```
   缺少此声明时，系统会在设置中将应用的通知开关置为**灰色不可交互状态**，彻底剥夺通知权利。

2. **动态请求**：实际代码通过 Tauri 的 `requestPermissionForAlias` 发起（内部使用 Activity Result API），不再直接调用 `ActivityCompat.requestPermissions`：
   ```kotlin
   if (Build.VERSION.SDK_INT >= 33) {
       requestPermissionForAlias("notification", invoke, "onPermissionResult")
   }
   ```

> **血训**：即使前台服务通知理论上不受通知权限影响，但在 ColorOS/EMUI/HarmonyOS 等国产 ROM 上，如果缺少 `POST_NOTIFICATIONS` 声明，通知渠道会被系统直接禁用，导致 `startForeground()` 调用成功但通知完全不显示。

### 3.2 前台服务权限

```xml
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
```

### 3.3 服务声明

```xml
<!-- 主进程前台服务通知壳（由 ForegroundGuardian 调度） -->
<service
    android:name=".service.StreamKeepaliveService"
    android:enabled="true"
    android:exported="false"
    android:foregroundServiceType="remoteMessaging"
    android:stopWithTask="true" />

<!-- :helper 进程 SSE 代理，独立管理锁与断线缓存 -->
<service
    android:name=".service.SseProxyService"
    android:process=":helper"
    android:enabled="true"
    android:exported="false"
    android:foregroundServiceType="remoteMessaging"
    android:stopWithTask="false" />

<!-- 系统通知栏监听占位服务，配合权限引导 -->
<service
    android:name=".service.VcpNotificationListenerService"
    android:label="VCP 连接守护助手"
    android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
    </intent-filter>
</service>
```

> **v1.1.2 变更**：`StreamKeepaliveService` 的 `stopWithTask` 从 `false` 改为 `true`。ForegroundGuardian 已接管全部锁管理，进程被划掉后锁即消亡，残留前台服务无实际保活作用。
> **v1.1.3 新增**：`SseProxyService` 运行在独立的 `:helper` 进程，`stopWithTask="false"`，与主进程生命周期解耦。`VcpNotificationListenerService` 为系统通知栏监听占位服务，需在系统设置中手动授权。

---

## 4. 事件通道规范

### 4.1 三种事件通道对比

| 通道 | 前端监听方式 | 适用场景 | 当前使用 |
|------|-------------|----------|---------|
| `Plugin.trigger()` | Rust `listen_any("vcp-mobile://<channel>")` → `app.emit("<event>")` → 前端 `listen()` | Tauri 官方插件事件通道 | `lifecycle` → `vcp-lifecycle-changed`、`vcp-network-status-changed` |
| `evaluateJavascript` + `CustomEvent` | `window.addEventListener()` | 需要前端 window 级别事件兼容性 | `vcp-keyboard-inset`、`vcp-hardware-back`、`vcp-exit-requested`、`vcp-share-intent`、`vcp-permission-change` 等（生命周期事件 v1.1.3 起已迁移至 Tauri Event `vcp-lifecycle-changed`） |
| `PluginHandle.run_mobile_plugin` | 无需监听，直接 invoke | Rust 到 Kotlin 单向命令调用 | `startStreamingService`、`acquireForeground`、`releaseForeground`、`startHelperService`、`checkAllPermissions` 等 |

### 4.2 关键约束：trigger() 与前端 listen() 不直接互通

`Plugin.trigger()` 发射到 Tauri 插件内部频道，Rust 侧通过 `listen_any("vcp-mobile://<channel>")` 接收并处理后，再通过 `app.emit("<event>")` 转发到前端全局事件总线。前端 `@tauri-apps/api/event` 的 `listen()` 监听的是 Rust 转发后的全局事件，而非直接监听 `Plugin.trigger()`。

因此：
- 若事件需要 Rust 侧参与处理（如生命周期状态、网络状态变化），使用 `Plugin.trigger()` + Rust 转发。
- 若前端使用 `window.addEventListener()` 监听，Kotlin 侧**必须**使用 `evaluateJavascript` 注入 `CustomEvent`。

### 4.3 evaluateJavascript 事件格式标准

```kotlin
val json = "{\"height\":$keyboardHeight,\"visible\":$isKeyboardVisible,\"safeAreaBottom\":$safeAreaBottom}"
val script = "window.dispatchEvent(new CustomEvent('vcp-keyboard-inset', { detail: $json }))"
webView.evaluateJavascript(script, null)
```

---

## 5. 插件开发规范

### 5.1 Rust 侧

- **状态管理**：使用 `app.manage()` 注入 `VcpMobileState<R>`，通过 `app.state::<VcpMobileState<R>>()` 获取
- **Android 插件注册**：在 `setup` 中调用 `register_android_plugin`，保存返回的 `PluginHandle` 供 `run_mobile_plugin` 调用
- **错误处理**：Tauri command handler 中严禁使用 `unwrap()` 或 `expect()`，必须转换为 `Result<T, String>`
- **条件编译**：Android 专用代码用 `#[cfg(target_os = "android")]` 包围

### 5.2 Kotlin 侧

- **类注解**：`@TauriPlugin` 必须标注在 `Plugin(activity)` 子类上
- **命令注解**：`@Command` 标注公开方法，方法参数固定为 `Invoke`
- **参数解析**：`invoke.parseArgs(ArgsClass::class.java)`，参数类必须标注 `@InvokeArg`
- **返回值**：成功调用 `invoke.resolve()`；失败调用 `invoke.reject(message)`

### 5.3 通知规范（前台服务）

| 属性 | 要求 | 说明 |
|------|------|------|
| 渠道重要性 | `IMPORTANCE_HIGH` | 确保在所有 OEM 上显式显示 |
| `setOngoing(true)` | 必须 | 通知不可滑动关闭 |
| `setSilent(true)` | **禁止** | 部分 ROM 会将其归类为静默通知而不显示 |
| `setPriority()` | **禁止** | Android 8+ 已废弃 |
| Small Icon | `applicationInfo.icon` | 使用应用自身品牌图标 |
| 前台服务类型 | `remoteMessaging` | Android 14+ 必需 |

---

## 6. 官方插件采用准则

### 6.1 三条铁律

| 优先级 | 规则 |
|--------|------|
| 1 | **官方插件优先**：文件打开用 `tauri-plugin-opener`，触觉反馈用 `tauri-plugin-haptics` |
| 2 | **自定义插件兜底**：Android 特有的前台服务、键盘 Insets、生命周期桥接，官方无覆盖，必须自建插件 |
| 3 | **Raw JNI 仅限一次性调用**：`FLAG_KEEP_SCREEN_ON` 只需一次 `addFlags()`/`clearFlags()`，维持 Rust 侧 JNI 裸调比新增 Kotlin 方法更简洁 |

---

## 7. 血训与不可变共识

### 7.1 插件命令静默拒绝

如果插件命令被调用但没有任何反应（无错误、无返回），**首先检查 `capabilities/default.json` 是否包含 `"vcp-mobile:default"`**。Tauri v2 的能力系统会静默拒绝未授权命令。

### 7.2 POST_NOTIFICATIONS 是通知的前提

Android 13+ 上，没有 `POST_NOTIFICATIONS` 静态声明 + 动态请求，**任何通知在 ColorOS/EMUI/HarmonyOS 上都不会显示**。

### 7.3 Plugin.load() 不可靠

`Plugin.load(webView)` 的调用时机取决于 JNI 注册完成的时机。如果需要在 WebView 初始化后立即执行的逻辑（如返回键拦截），应放在 `MainActivity.onWebViewCreate(webView)` 中，这是 Wry 保证调用的钩子。

### 7.4 PluginHandle.run_mobile_plugin 优先于 Raw JNI

对于 Rust 到 Kotlin 的复杂调用（如启动 Service），优先使用 `PluginHandle.run_mobile_plugin(command, payload)` 而非手动 JNI。前者使用 Tauri 官方桥接通道，类型安全且避免 `loadClass` 等 JNI 反射风险。

---

## 8. 参考链接

| 资源 | URL |
|------|-----|
| Tauri v2 Plugin 开发文档 | https://v2.tauri.app/develop/plugins/ |
| Tauri Android Plugin 详解 | https://v2.tauri.app/develop/plugins/develop-mobile/#android |
| Tauri Plugin Permissions | https://v2.tauri.app/security/permissions/ |
| Android 13 通知权限变更 | https://developer.android.com/develop/ui/views/notifications/notification-permission |
| Android 14 前台服务类型限制 | https://developer.android.com/about/versions/14/changes/fgs-types-required |


---

# 附录 A：Tauri v2 插件生态速查

> **目的**：为 VCP Mobile 后续功能扩展提供插件选型依据，减少重复造轮子。
> **统计口径**：基于 Tauri v2.0+ 官方插件列表与 awesome-tauri 社区生态（2026-05）。

---

## A.1 官方插件总表（按功能分类）

Tauri 官方插件统一托管于 `tauri-apps/plugins-workspace`，Rust crate 前缀为 `tauri-plugin-*`，JS API 包名为 `@tauri-apps/plugin-*`。

### A.1.1 文件 / 存储

| 插件 | Rust 依赖 | JS 包名 | Android | iOS | 说明 |
|------|-----------|---------|---------|-----|------|
| **fs** | `tauri-plugin-fs` | `@tauri-apps/plugin-fs` | ✅ | ✅ | 文件系统读写，支持作用域限制 |
| **dialog** | `tauri-plugin-dialog` | `@tauri-apps/plugin-dialog` | ✅ | ✅ | 原生打开/保存对话框 |
| **store** | `tauri-plugin-store` | `@tauri-apps/plugin-store` | ✅ | ✅ | 持久化键值存储（JSON） |
| **sql** | `tauri-plugin-sql` | `@tauri-apps/plugin-sql` | ✅ | ✅ | 通过 sqlx 操作 SQLite |
| **upload** | `tauri-plugin-upload` | `@tauri-apps/plugin-upload` | ✅ | ✅ | HTTP 文件上传（支持进度） |
| **stronghold** | `tauri-plugin-stronghold` | `@tauri-apps/plugin-stronghold` | ✅ | ✅ | 加密安全存储（IOTA Stronghold） |
| **persisted-scope** | `tauri-plugin-persisted-scope` | — | ❌ | ❌ | 持久化文件系统运行时作用域（Desktop） |

### A.1.2 网络 / 通信

| 插件 | Rust 依赖 | JS 包名 | Android | iOS | 说明 |
|------|-----------|---------|---------|-----|------|
| **http** | `tauri-plugin-http` | `@tauri-apps/plugin-http` | ✅ | ✅ | Rust 实现的 HTTP 客户端（替代 fetch） |
| **websocket** | `tauri-plugin-websocket` | `@tauri-apps/plugin-websocket` | ✅ | ✅ | 基于 Rust 的 WebSocket 客户端 |
| **localhost** | `tauri-plugin-localhost` | — | ✅ | ✅ | 生产环境内嵌 localhost 服务器 |
| **shell** | `tauri-plugin-shell` | `@tauri-apps/plugin-shell` | ✅ | ✅ | 执行系统命令、打开外部程序 |
| **deep-link** | `tauri-plugin-deep-link` | `@tauri-apps/plugin-deep-link` | ✅ | ✅ | 自定义 URL Scheme 协议唤醒 |
| **os** | `tauri-plugin-os` | `@tauri-apps/plugin-os` | ✅ | ✅ | 读取操作系统信息 |
| **process** | `tauri-plugin-process` | `@tauri-apps/plugin-process` | ✅ | ✅ | 进程管理（重启、退出） |

### A.1.3 系统 / 设备 / 传感器

| 插件 | Rust 依赖 | JS 包名 | Android | iOS | 说明 |
|------|-----------|---------|---------|-----|------|
| **geolocation** | `tauri-plugin-geolocation` | `@tauri-apps/plugin-geolocation` | ✅ | ✅ | GPS 定位、持续追踪 |
| **barcode-scanner** | `tauri-plugin-barcode-scanner` | `@tauri-apps/plugin-barcode-scanner` | ✅ | ✅ | 相机扫码（QR/EAN-13 等） |
| **biometric** | `tauri-plugin-biometric` | `@tauri-apps/plugin-biometric` | ✅ | ✅ | 指纹/面容生物识别认证 |
| **nfc** | `tauri-plugin-nfc` | `@tauri-apps/plugin-nfc` | ✅ | ✅ | NFC 标签读写 |
| **haptics** | `tauri-plugin-haptics` | `@tauri-apps/plugin-haptics` | ✅ | ✅ | 触觉反馈与振动 |
| **clipboard-manager** | `tauri-plugin-clipboard-manager` | `@tauri-apps/plugin-clipboard-manager` | ✅ | ✅ | 剪贴板读写 |
| **notification** | `tauri-plugin-notification` | `@tauri-apps/plugin-notification` | ✅ | ❌ | 原生通知（Android 支持，Desktop 支持，iOS 有限） |
| **log** | `tauri-plugin-log` | `@tauri-apps/plugin-log` | ✅ | ✅ | 可配置日志（含移动端文件落盘） |
| **autostart** | `tauri-plugin-autostart` | `@tauri-apps/plugin-autostart` | ❌ | ❌ | 开机自启（Desktop only） |
| **global-shortcut** | `tauri-plugin-global-shortcut` | `@tauri-apps/plugin-global-shortcut` | ❌ | ❌ | 全局快捷键（Desktop only） |
| **single-instance** | `tauri-plugin-single-instance` | — | ❌ | ❌ | 单实例锁定（Desktop only） |
| **positioner** | `tauri-plugin-positioner` | — | ❌ | ❌ | 窗口定位（Desktop only） |
| **window-state** | `tauri-plugin-window-state` | `@tauri-apps/plugin-window-state` | ❌ | ❌ | 窗口状态持久化（Desktop only） |

### A.1.4 更新 / 发布

| 插件 | Rust 依赖 | JS 包名 | Android | iOS | 说明 |
|------|-----------|---------|---------|-----|------|
| **updater** | `tauri-plugin-updater` | `@tauri-apps/plugin-updater` | ❌ | ❌ | 桌面端增量更新（OTA） |

> **注意**：Android 应用更新应通过 Google Play / 自建 OTA 服务器实现，官方 updater 插件目前不支持移动端。

---

## A.2 权限获取类插件专题

以下插件直接或间接涉及 Android 权限请求，选型时应优先评估。

### A.2.1 官方权限相关插件

| 插件 | 涉及权限 | 说明 |
|------|----------|------|
| `geolocation` | `ACCESS_FINE_LOCATION`, `ACCESS_COARSE_LOCATION` | 自动处理运行时权限请求，支持后台定位配置 |
| `biometric` | `USE_BIOMETRIC` | 封装 BiometricPrompt，支持强/弱生物识别策略 |
| `barcode-scanner` | `CAMERA` | 调用相机预览并解码，需相机权限 |
| `nfc` | `NFC` | 需设备支持 NFC 硬件，无需运行时权限（系统级） |
| `notification` | `POST_NOTIFICATIONS`（Android 13+） | 官方插件尚未完全覆盖国产 ROM 通知渠道，需配合自建插件兜底 |
| `haptics` | 无需权限 | `VIBRATE` 为普通权限，无需运行时请求 |
| `clipboard-manager` | 无需权限 | Android 10+ 剪贴板读取受后台限制，前台无需权限 |

### A.2.2 社区 Android 专项插件

以下社区插件针对 Android 特有行为封装，维护状态良好，适合直接采用。

| 插件 | 维护者 | crates/npm | 说明 |
|------|--------|------------|------|
| **tauri-plugin-device-info** | edisdev | `tauri-plugin-device-info` / `tauri-plugin-device-info-api` | 设备/电池/网络/存储/显示器信息，全平台支持，月度下载 ~900 |
| **tauri-plugin-android-fs** | 社区 | `tauri-plugin-android-fs` | 直接访问 Android 文件系统（绕过 Scoped Storage 限制场景） |
| **tauri-plugin-android-battery-optimization** | 社区 | `tauri-plugin-android-battery-optimization` | 检测并申请电池优化白名单（防止后台被杀） |
| **tauri-plugin-keep-screen-on** | cristofa | `tauri-plugin-keep-screen-on` | 禁止屏幕自动休眠（Android/iOS） |
| **tauri-plugin-android-prevent-screen-capture** | aiueo13 | `tauri-plugin-android-prevent-screen-capture` | 防截屏/录屏（设置 FLAG_SECURE） |
| **tauri-plugin-sharesheet** | 社区 | `tauri-plugin-sharesheet` | 调用 Android Sharesheet / iOS Share Pane |
| **tauri-plugin-mobile-sharetarget** | 社区 | `tauri-plugin-mobile-sharetarget` | 处理系统分享 Intent（接收其他应用分享的内容） |
| **tauri-plugin-view** | 社区 | `tauri-plugin-view` | 移动端查看/分享文件（调用系统查看器） |
| **tauri-plugin-clipboard** | 社区 | `tauri-plugin-clipboard` | 增强剪贴板（支持图片/HTML/文件/监听变更） |
| **tauri-plugin-iap** | 社区 | `tauri-plugin-iap` | 应用内购买（Android Google Play / iOS App Store） |

---

## A.3 社区推荐插件精选（通用类）

| 插件 | 用途 | 移动端支持 | 推荐指数 |
|------|------|------------|----------|
| **sentry-tauri** | 错误追踪（JS 错误 + Rust Panic + Native 崩溃） | Android/iOS | ⭐⭐⭐⭐⭐ |
| **tauri-plugin-cache** | 磁盘缓存（TTL、压缩、内存层） | Android/iOS | ⭐⭐⭐⭐ |
| **tauri-plugin-ota** | JS 热更新（基于 manifest） | Android/iOS | ⭐⭐⭐⭐ |
| **tauri-plugin-tracing** | 结构化日志（tracing crate + 火焰图） | Android/iOS | ⭐⭐⭐⭐ |
| **taurpc** | 类型安全 IPC（命令 + 事件） | Android/iOS | ⭐⭐⭐⭐ |
| **tauri-plugin-graphql** | GraphQL 类型安全 IPC | Android/iOS | ⭐⭐⭐ |
| **tauri-plugin-in-app-review** | 应用商店评分弹窗 | Android/iOS | ⭐⭐⭐⭐ |
| **tauri-plugin-theme** | 动态主题切换 | Android/iOS | ⭐⭐⭐ |
| **tauri-plugin-pinia** | Pinia 状态持久化 | Android/iOS | ⭐⭐⭐⭐ |
| **tauri-plugin-system-info** | 系统详细信息（CPU/内存/进程） | Android/iOS | ⭐⭐⭐ |
| **tauri-plugin-network** | 网络信息读取 / 扫描 | Android/iOS | ⭐⭐⭐ |
| **tauri-plugin-serialport** | 串口通信 | Android（OTG） | ⭐⭐⭐ |
| **tauri-plugin-blec** | 蓝牙低功耗（BLE）客户端 | Android/iOS | ⭐⭐⭐⭐ |
| **tauri-plugin-mqtt** | MQTT 客户端 | Android/iOS | ⭐⭐⭐ |
| **tauri-plugin-screenshots** | 屏幕截图 | Desktop | ⭐⭐⭐ |

---

## A.4 选型速查口诀

> 与本文档第 6 节“三条铁律”保持一致：

1. **官方插件优先**：凡是官方已有覆盖（fs、dialog、geolocation、biometric、barcode-scanner、haptics、notification），直接使用 `@tauri-apps/plugin-*`，享受长期维护保障。
2. **社区插件补位**：Android 特有功能（电池优化、防截屏、Sharesheet、设备信息）选用社区成熟插件，评估指标：近 6 个月有提交、crates.io 周下载 > 50、文档完整。
3. **自建插件兜底**：当官方 + 社区均无法满足（如 VCP Mobile 的流式前台保活、键盘 Insets、生命周期桥接），按本文档第 5 节规范自建插件，禁止为了单一简单调用引入整个社区插件。

---

# 附录 B：高版本 Android 权限获取实战手册

> **目的**：为 VCP Mobile 后续自建插件或集成官方插件时，提供 Android 运行时权限请求的完整代码模板与版本适配策略。
> **适用范围**：Android 10（API 29）至 Android 15（API 35）。

---

## B.1 权限模型演进速览

| Android 版本 | API | 关键变更 | 影响范围 |
|--------------|-----|----------|----------|
| 6.0 | 23 | **运行时权限**引入（Dangerous Permissions 需动态请求） | 所有敏感权限 |
| 10 | 29 | **Scoped Storage**；**后台定位**需单独声明 `ACCESS_BACKGROUND_LOCATION` | 存储、定位 |
| 11 | 30 | **一次性权限**（One-time）；权限自动重置（长期未用） | 相机、麦克风、定位 |
| 12 | 31 | **粗略/精确定位**拆分；蓝牙权限细化（`BLUETOOTH_SCAN/CONNECT`） | 定位、蓝牙 |
| 13 | 33 | **通知权限**变为运行时权限（`POST_NOTIFICATIONS`）；**细分媒体权限**（`READ_MEDIA_IMAGES/VIDEO/AUDIO`） | 通知、存储 |
| 14 | 34 | **前台服务类型强制声明**；后台 Activity 启动限制收紧 | 前台服务 |
| 15 | 35 | **部分照片/视频访问**（`READ_MEDIA_VISUAL_USER_SELECTED`）进一步细化 | 存储 |

---

## B.2 现代请求范式（Activity Result API）

Android 官方已废弃 `ActivityCompat.requestPermissions()` + `onRequestPermissionsResult()` 的组合，推荐使用 **Jetpack Activity Result API**，生命周期更安全。

### B.2.1 在 Tauri Plugin Kotlin 侧的标准模板

```kotlin
// 文件：VcpMobilePlugin.kt 或其他自建 Plugin 类

import androidx.activity.result.contract.ActivityResultContracts
import androidx.core.content.ContextCompat
import android.Manifest
import android.content.pm.PackageManager
import android.os.Build
import android.net.Uri
import android.provider.Settings
import app.tauri.plugin.Plugin
import app.tauri.plugin.Invoke
import app.tauri.plugin.Command

@TauriPlugin
class VcpMobilePlugin(private val activity: Activity) : Plugin(activity) {

    // --- 权限 Launcher（必须在 Plugin 初始化前注册） ---
    private val permissionLauncher = activity.registerForActivityResult(
        ActivityResultContracts.RequestMultiplePermissions()
    ) { result ->
        val granted = result.filterValues { it }.keys
        val denied = result.filterValues { !it }.keys
        // 通过 evaluateJavascript 回传结果给前端
        val json = """{"granted":${granted.toJsonArray()}, "denied":${denied.toJsonArray()}}"""
        val script = "window.dispatchEvent(new CustomEvent('vcp-permission-result', { detail: $json }))"
        webView?.evaluateJavascript(script, null)
    }

    // --- 通用权限检查方法 ---
    private fun hasPermissions(vararg permissions: String): Boolean {
        return permissions.all {
            ContextCompat.checkSelfPermission(activity, it) == PackageManager.PERMISSION_GRANTED
        }
    }

    // --- 通用权限请求方法 ---
    private fun requestPermissions(vararg permissions: String) {
        val notGranted = permissions.filter {
            ContextCompat.checkSelfPermission(activity, it) != PackageManager.PERMISSION_GRANTED
        }.toTypedArray()
        if (notGranted.isNotEmpty()) {
            permissionLauncher.launch(notGranted)
        } else {
            // 已全部授权，直接回传成功
            val json = """{"granted":${permissions.toJsonArray()}, "denied":[]}"""
            val script = "window.dispatchEvent(new CustomEvent('vcp-permission-result', { detail: $json }))"
            webView?.evaluateJavascript(script, null)
        }
    }

    // 辅助：Array 转 JSON 字符串
    private fun Array<out String>.toJsonArray(): String {
        return "[" + joinToString(",") { "\"$it\"" } + "]"
    }

    // --- 示例：暴露给 Rust/前端的权限命令 ---
    @Command
    fun checkNotificationPermission(invoke: Invoke) {
        val granted = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
            hasPermissions(Manifest.permission.POST_NOTIFICATIONS)
        } else {
            true // Android 12 及以下通知无需运行时权限
        }
        invoke.resolveObject(mapOf("granted" to granted))
    }

    @Command
    fun requestNotificationPermission(invoke: Invoke) {
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
            requestPermissions(Manifest.permission.POST_NOTIFICATIONS)
        }
        invoke.resolve()
    }
}
```

> **约束**：`registerForActivityResult` 必须在 Activity 状态变为 `CREATED` 之前调用。Tauri Plugin 的初始化时机通常满足此条件，但若出现 `LifecycleOwner stopped` 错误，请将权限请求逻辑延迟到 `MainActivity.onWebViewCreate()` 中初始化。

---

## B.3 分权限实战指南

### B.3.1 通知权限（POST_NOTIFICATIONS）

- **目标版本**：Android 13+（API 33+）
- **静态声明**：
  ```xml
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  ```
- **动态请求**：
  ```kotlin
  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
      if (!hasPermissions(Manifest.permission.POST_NOTIFICATIONS)) {
          requestPermissions(Manifest.permission.POST_NOTIFICATIONS)
      }
  }
  ```
- **国产 ROM 血训**：ColorOS / EMUI / HarmonyOS 上，缺少 `POST_NOTIFICATIONS` 静态声明时，系统设置中的通知开关会变为**灰色不可交互**。即使前台服务通知理论上不受通知权限影响，这些 ROM 也会直接禁用通知渠道，导致 `startForeground()` 成功但通知不显示。

### B.3.2 位置权限（Location）

- **权限组**：
  ```xml
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
  <!-- 后台定位：Android 10+ 需单独声明 -->
  <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
  ```
- **版本分支**：
  ```kotlin
  val locationPermissions = mutableListOf<String>()
  locationPermissions += Manifest.permission.ACCESS_FINE_LOCATION
  locationPermissions += Manifest.permission.ACCESS_COARSE_LOCATION

  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
      // Android 10+ 若需后台定位，必须额外请求
      if (needBackgroundLocation) {
          locationPermissions += Manifest.permission.ACCESS_BACKGROUND_LOCATION
      }
  }

  requestPermissions(*locationPermissions.toTypedArray())
  ```
- **后台定位限制**：Android 10+ 要求 `ACCESS_BACKGROUND_LOCATION` 必须与前台定位权限分开请求，且 Google Play 要求提供合理的使用场景说明。国产 ROM 可能在此基础上增加额外限制（如 MIUI 的"始终允许"需二次弹窗确认）。

### B.3.3 存储与多媒体权限（Storage & Media）

Android 的存储权限经历了从**统一存储权限**到**细分媒体权限**再到**用户选择部分访问**的演进。

- **静态声明（按版本区分）**：
  ```xml
  <!-- Android 12 及以下 -->
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
      android:maxSdkVersion="32" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
      android:maxSdkVersion="29" />

  <!-- Android 13+ 细分媒体权限 -->
  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
  <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
  <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

  <!-- Android 14+ 部分访问（用户选择） -->
  <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

  <!-- 全磁盘访问（特殊权限，需跳转到设置页授权） -->
  <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
      tools:ignore="ScopedStorage" />
  ```
- **动态请求**：
  ```kotlin
  val storagePermissions = when {
      Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE -> {
          // Android 14+：先请求 READ_MEDIA_VISUAL_USER_SELECTED
          arrayOf(Manifest.permission.READ_MEDIA_VISUAL_USER_SELECTED)
      }
      Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU -> {
          // Android 13+：细分权限
          arrayOf(
              Manifest.permission.READ_MEDIA_IMAGES,
              Manifest.permission.READ_MEDIA_VIDEO,
              Manifest.permission.READ_MEDIA_AUDIO
          )
      }
      else -> {
          // Android 12 及以下：传统存储权限
          arrayOf(Manifest.permission.READ_EXTERNAL_STORAGE)
      }
  }
  requestPermissions(*storagePermissions)
  ```
- **Scoped Storage 与 MANAGE_EXTERNAL_STORAGE**：
  - Android 10+ 默认启用 Scoped Storage，应用只能访问自己的目录和公共媒体集。
  - 若需访问非媒体文件（如 PDF、任意路径的文件），使用 `Storage Access Framework`（`ACTION_OPEN_DOCUMENT`）而非申请 `MANAGE_EXTERNAL_STORAGE`。
  - `MANAGE_EXTERNAL_STORAGE` 仅在文件管理器、杀毒软件等特殊场景下允许使用，Google Play 审核会要求提供视频声明。

### B.3.4 相机权限（Camera）

- **静态声明**：
  ```xml
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />
  ```
- **动态请求**：
  ```kotlin
  requestPermissions(Manifest.permission.CAMERA)
  ```

### B.3.5 麦克风 / 录音权限（Record Audio）

- **静态声明**：
  ```xml
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  ```
- **动态请求**：
  ```kotlin
  requestPermissions(Manifest.permission.RECORD_AUDIO)
  ```
- **注意**：Android 11+ 支持一次性权限授予，用户选择"仅本次允许"后，应用退后台即失效。语音消息、实时语音等场景需在前台时保持活跃。

### B.3.6 前台服务权限（Foreground Service）

- **静态声明**：
  ```xml
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <!-- Android 14+ 需声明具体类型 -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
  ```
- **服务声明**：
  ```xml
  <service
      android:name=".service.YourForegroundService"
      android:foregroundServiceType="remoteMessaging"
      android:exported="false" />
  ```
- **Android 14+ 前台服务类型限制**：
  - `dataSync` → 已被废弃，建议使用 `WorkManager` 或 `userInitiatedDataTransfer`
  - `remoteMessaging`：适用于即时通讯类应用的后台消息同步
  - `mediaPlayback` / `mediaProjection` / `microphone` / `camera` / `location` 等按场景选择
  - 启动前台服务前，必须已通过对应权限的动态请求（如 `FOREGROUND_SERVICE_LOCATION` 需先拿到定位权限）。

### B.3.7 生物识别权限（Biometric）

- **静态声明**：
  ```xml
  <uses-permission android:name="android.permission.USE_BIOMETRIC" />
  ```
- **注意**：`USE_FINGERPRINT` 已被废弃，统一使用 `USE_BIOMETRIC`。
- **使用方式**：推荐直接使用 Tauri 官方 `tauri-plugin-biometric`，其内部已封装 `BiometricPrompt`。

### B.3.8 蓝牙权限（Bluetooth）

- **静态声明**：
  ```xml
  <!-- Android 12+ 新权限模型 -->
  <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
      android:usesPermissionFlags="neverForLocation" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

  <!-- Android 11 及以下兼容 -->
  <uses-permission android:name="android.permission.BLUETOOTH"
      android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
      android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
      android:maxSdkVersion="30" />
  ```
- **动态请求（Android 12+）**：
  ```kotlin
  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S) {
      requestPermissions(
          Manifest.permission.BLUETOOTH_SCAN,
          Manifest.permission.BLUETOOTH_CONNECT
      )
  } else {
      requestPermissions(Manifest.permission.ACCESS_FINE_LOCATION)
  }
  ```

---

## B.4 权限拒绝后的用户引导

### B.4.1 判断拒绝状态

```kotlin
when {
    hasPermissions(Manifest.permission.CAMERA) -> {
        // 已授权，执行功能
    }
    activity.shouldShowRequestPermissionRationale(Manifest.permission.CAMERA) -> {
        // 用户之前拒绝了，但未勾选"不再询问"
        // 应显示解释性 UI，说明为什么需要此权限
        showPermissionRationaleDialog(
            permission = Manifest.permission.CAMERA,
            message = "相机权限用于扫描二维码和拍摄头像，拒绝后将无法使用相关功能。",
            onConfirm = { requestPermissions(Manifest.permission.CAMERA) }
        )
    }
    else -> {
        // 用户勾选了"不再询问"或首次请求前
        // 引导用户前往系统设置手动开启
        showGoToSettingsDialog()
    }
}
```

### B.4.2 跳转到应用设置页

```kotlin
val intent = Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS).apply {
    data = Uri.fromParts("package", activity.packageName, null)
    addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
}
activity.startActivity(intent)
```

---

## B.5 Tauri 自定义插件中的权限封装建议

为保持 VCP Mobile 插件架构的一致性，建议将权限逻辑按以下模式封装：

### B.5.1 Plugin 内部工具方法

```kotlin
// 封装在 BasePlugin 或 VcpMobilePlugin 中
object PermissionHelper {
    fun check(invoke: Invoke, vararg permissions: String) { /* ... */ }
    fun request(invoke: Invoke, vararg permissions: String) { /* ... */ }
}
```

### B.5.2 向前端暴露的 API 设计

| Rust Command | Kotlin 方法 | 前端调用 | 说明 |
|--------------|-------------|----------|------|
| `check_permission` | `checkPermission(invoke)` | `invoke('check_permission', { name: 'camera' })` | 检查单一权限状态 |
| `request_permission` | `requestPermission(invoke)` | `invoke('request_permission', { names: ['camera', 'microphone'] })` | 请求权限数组 |
| `open_app_settings` | `openAppSettings(invoke)` | `invoke('open_app_settings')` | 跳转到系统设置 |

### B.5.3 权限结果回传前端

与本文档第 4.3 节 `evaluateJavascript` 事件格式保持一致：

```kotlin
val detail = """
    {
        "permissions": {
            "android.permission.CAMERA": "granted",
            "android.permission.RECORD_AUDIO": "denied"
        },
        "allGranted": false
    }
""".trimIndent()
val script = "window.dispatchEvent(new CustomEvent('vcp-permission-change', { detail: $detail }))"
webView.evaluateJavascript(script, null)
```

前端监听：

```typescript
window.addEventListener('vcp-permission-change', (e: CustomEvent) => {
    const { permissions, allGranted } = e.detail;
    console.log('权限变更:', permissions);
});
```

---

## B.6 参考链接

| 资源 | URL |
|------|-----|
| Android 运行时权限官方指南 | https://developer.android.com/training/permissions/requesting |
| Android 13 通知权限变更 | https://developer.android.com/develop/ui/views/notifications/notification-permission |
| Android 13 细分媒体权限 | https://developer.android.com/about/versions/13/behavior-changes-13#granular-media-permissions |
| Android 14 前台服务类型限制 | https://developer.android.com/about/versions/14/changes/fgs-types-required |
| Android 10 Scoped Storage | https://developer.android.com/training/data-storage#scoped-storage |
| Activity Result API 官方文档 | https://developer.android.com/training/basics/intents/result |
| Jetpack Permissions 示例 | https://github.com/android/permissions-samples |

---

*最后更新：2026-07-04 | VCP Mobile v1.1.3*
